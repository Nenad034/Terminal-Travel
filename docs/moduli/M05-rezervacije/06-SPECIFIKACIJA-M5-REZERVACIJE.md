# Specifikacija modula M5 — Rezervacije i tok prodaje

**Odnosi se na:** `00-MASTER-ARHITEKTURA.md`, poglavlje 4 (M5) i poglavlje 8 (Faza 1)
**Nivo:** Nivo 2 — detaljna specifikacija, dovoljna da AI agent direktno programira po njoj
**Status:** Nacrt za usvajanje
**Verzija:** 1.74 — "Kalendar rezervacija" (M17) prerađen kao Google Calendar stil — Mesec/Nedelja/Dan prikaz + prekidač + "danas" + strelice, plus isti v1 filter-skup kao "Lista rezervacija" (bez datumskih opsega dolaska/odlaska, koje već zadaje sam prikaz), poglavlje 7.4 (novo). NAMERNO bez prevlačenja termina — potvrđeno preko `AskUserQuestion`, ovaj prolaz je čist pregled. `GET /sales/bookings/calendar-summary`/`calendar/:date` dobili aditivan filter-skup (`BookingsService.buildCalendarItemWhere`). Vidi poglavlje 7.4 za pun opis i proveru.
**Verzija:** 1.73 — Ispravka: filter "vrsta usluge" (i cena) nije radio na mock prikazu smeštaja (26.8.2026, prijavio vlasnik uživo: "ne radi filter po tipu usluge"). Uzrok: `AccommodationResultsMock.tsx` (v1.72 dopuna istog dana, mock ekran koji čeka potvrdu izgleda pre prave žice) prikazuje SOPSTVENE hardkodovane hotele — `page.tsx` i dalje računa `boardType`/`priceMin`/`priceMax` filtere kao i ranije (§3.0c.2/§3.0c.3), ali ih je primenjivao SAMO na pravim `results` sa `GET /search`, koje ACCOMMODATION grana uopšte ne prikazuje dok je mock aktivan — filteri iz levog panela nisu imali nijedan efekat na ono što se stvarno vidi na ekranu. Ispravljeno: `page.tsx` prosleđuje `boardType`/`priceMin`/`priceMax` kao props u `AccommodationResultsMock`, koja ih sad primenjuje nad sopstvenim MOCK ponudama (isto pravilo filtriranja koje `page.tsx` već koristi za prave rezultate) pre sortiranja/prikaza; hotel bez nijedne ponude posle filtriranja se ne prikazuje. "Dostupnost" filter namerno NIJE dodat ovde — mock ponude nemaju koncept `availabilityStatus` (sve su podrazumevano dostupne), dodaje se tek kad prava žica stigne.
**Verzija:** 1.72 — Destinacija (država) postaje obavezno polje u vođenoj pretrazi (26.8.2026, na zahtev vlasnika: "treba da piše i koja je destinacija" — kad polje ostane prazno, sažetak/naziv sačuvane pretrage nema šta da prikaže). Potvrđeno preko `AskUserQuestion`: obavezno polje, ne samo kozmetički fallback tekst. `SearchCriteriaPopup.tsx` — samo `destinationCountry` postaje obavezan (M5 spec §3.0c.2 — korak 1 vođene pretrage je uvek zemlja; grad/hotel ostaje opcioni finiji korak, pretraga cele države i dalje moguća), dugme "pretraži" onemogućeno dok polje nije popunjeno, uz vidljivu poruku. Ne menja `page.tsx` `hasQuery`/`GET /search` ugovor (i dalje prihvata direktan URL bez destinacije, ovo je samo UX-nivo validacija u vođenom toku).
**Verzija:** 1.71 — Naziv sačuvane pretrage se generiše sam (26.8.2026, na zahtev vlasnika: "ispravite, naziv sačuvanog filtera treba sam da se generiše iz odabranih filtera"). Popover sa poljem za ručan unos naziva (v1.70) uklonjen — `SearchCriteriaChip.tsx` sad sastavlja naziv od ISTOG sažetka koji već piše na samom chip-u (tip proizvoda · destinacija · datumi · broj osoba, npr. "Smeštaj · Budva, Crna Gora · 2026-08-27 – 2026-09-03 · 2 odr."), jedan klik na "sačuvaj" odmah upisuje bez dodatnog koraka. Kratka "sačuvano" potvrda (2s) zamenjuje dugme posle uspešnog upisa.
**Verzija:** 1.70 — Sačuvane pretrage, max 10 (26.8.2026, na zahtev vlasnika: "omogućite čuvanje filtera pretrage kako bi se vratili po želji (max 10 pretraga) s tim da se svakako proverava cena ponovo").
  - Isti mehanizam kao postojeći "Sačuvani prikazi" za listu rezervacija (v1.98 M17 spec, `SavedViewsSidebarPanel.tsx`, M1 `UserPreference`) — komponenta GENERALIZOVANA (props `preferenceKey`/`baseHref`/`maxItems`/`emptyHint`, podrazumevane vrednosti čuvaju IDENTIČNO ponašanje za `/rezervacije/lista`, bez izmene) da posluži i pretragu, pod sopstvenim ključem `saved_views.rezervacije_pretraga`.
  - Dugme "sačuvaj" dodato u `SearchCriteriaChip.tsx` (pored postojećeg "izmeni", isti mali popover obrazac) — čuva TRENUTNE query parametre pretrage. Provera gornje granice PRE upisa (`existing.length >= 10` → čitljiva poruka, ne tiha odbijena akcija ni brisanje najstarije bez pitanja).
  - **"Svakako proverava cena ponovo"** — već tačno svojstvo mehanizma bez ikakve dodatne izmene: klik na sačuvanu pretragu (Sidebar.tsx, `selected.id === 'pretraga'`) je PRAVA navigacija (`<Link href="/rezervacije/pretraga?...">`) koja ponovo pokreće server komponentu i nov `GET /sales/search` poziv — nikad se ne čuva/prikazuje stara cena iz trenutka čuvanja filtera (samo KRITERIJUMI se pamte, ne rezultati).
  **Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju — `PUT /api/preferences/saved_views.rezervacije_pretraga` upisuje i `GET /api/preferences` vraća sačuvanu pretragu (potvrđeno pravim upisom/čitanjem preko prave `UserPreference` rute, test zapis obrisan posle provere); URL sastavljen iz sačuvanih filtera (`?type=ACCOMMODATION&destinationCity=Budva&...`) vraća HTTP 200 sa strane servera (nov poziv, ne keširan rezultat). Sam klik na dugme "sačuvaj"/popover je klijentska interakcija — potvrđena čitanjem koda, isto poznato ograničenje kao ostatak interakcionih izmena.
**Verzija:** 1.69 — Cena po sobi + čitljivi nazivi usluga + puni detalji u desnom panelu (26.8.2026, na zahtev vlasnika: "kada u pretrazi tražimo ponudu za 1 sobu za 2 odrasle i 2. sobu za 2 odrasle osobe i dete 10 godina... treba da se pojavi pojedinačna cena po sobi za navedeni broj osoba i ukupna cena za obe sobe... za usluge koristite naziv na ovaj način HB - Polupansion... u kartici u desnom panelu treba da pišu detaljne informacije kao i u centralnom panelu").
  - **MOCK model podataka** (`AccommodationResultsMock.tsx`) — `MockOffer.rooms[]` sad je niz POJEDINAČNIH soba (svaka sopstveni `adults`/`children`/`childrenAges`/`price`), umesto ranijeg "broj identičnih soba" zapisa — omogućava tačno vlasnikov primer (soba 1: 2 odrasle; soba 2: 2 odrasle + dete od 10). Banner (grid)/red (list) prikazuje cenu SVAKE sobe pojedinačno + "Ukupno" zbir na dnu (`RoomBreakdown`, deljena komponenta između oba prikaza).
  - **Čitljivi nazivi usluga** — `BOARD_TYPE_LABELS` mapa (BB/HB/FB/AI/UAI → puni naziv), prikazano svuda kao "KOD - Naziv" (npr. "HB - Polupansion"), zamenjuje ranije mešane vrednosti (neke skraćenice, neke pune reči).
  - **`SelectionItem` dopuna** (`SelectionContext.tsx`) — 5 novih OPCIONIH polja (`stars`, `destinationCity`, `destinationCountry`, `boardTypeLabel`, `roomLines[]`) — aditivno, ne menja postojeći `QuoteButton.tsx` (pravi rezultati pretrage) koji ih ne popunjava. Mock klik sada šalje SVE ove podatke.
  - **`RightPanel.tsx` `SelectionRow`** — kad su nova polja prisutna, prikazuje IDENTIČAN nivo detalja kao centralni panel: kategorija, destinacija, datumi, naziv usluge, cena po sobi + ukupno (umesto ranijeg samo naziv/tip/jedna cena).
  **Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju — dati URL renderuje "Noćenje sa doručkom"/"Polupansion" (puni nazivi) i "Ukupno" na svakom od 5 banera (podrazumevano sažeto stanje, po jedna ponuda po hotelu); ostale kombinacije usluga (Pun pansion/All Inclusive/Ultra All Inclusive) postoje u kodu ali se renderuju tek posle klijentskog proširivanja (isto poznato ograničenje curl provere kao i sam accordion). Prikaz u desnom panelu (klik → `SelectionRow` sa detaljima) nije uživo potvrđen — čisto klijentska interakcija, potvrđena čitanjem koda.
**Verzija:** 1.68 — Dorada MOCK grid/list prikaza posle uživo pregleda screenshot-a (26.8.2026, na zahtev vlasnika: "ima mnogo praznog prostora i preklapa se listanje sa tekstom... dovoljno je 2 odrasle (1 soba)... 5 banera u jednom redu... mock slike... manja slika u list view-u u prvom redu, ispod bez slike... klikom odabir ponude i slanje u desni panel").
  - Ikonica "listanje" premeštena iz apsolutnog pozicioniranja (preklapala se sa cenom) u isti red kao cena (`flex justify-between`) — više se ne preklapa ni na jednoj širini kartice.
  - Dve odvojene linije (broj osoba iz pretrage + razbijen prikaz po sobi) spojene u JEDNU (`occupancySummary()`, izračunato iz `roomLines` ponude) — npr. "2 odrasle (1 soba)".
  - Grid prikaz fiksiran na `grid-cols-5` (5 banera u jednom redu, na zahtev), dodata 2 nova mock hotela (ukupno 5) da se to stvarno vidi.
  - Prave mock fotografije (`picsum.photos`, seed po hotelu — stabilne, ne menjaju se pri osvežavanju) umesto praznog placeholder okvira — smanjuje "prazan prostor" utisak.
  - List prikaz: SAMO prvi (najpovoljniji) red po hotelu nosi malu sliku (`h-10 w-14`), dodatni otvoreni redovi ispod bez slike.
  - **Klik za odabir → desni panel** — cela kartica/red je sad dugme koje poziva PRAVI `SelectionContext.addItem()` (isti mehanizam kao `QuoteButton.tsx` u pravim rezultatima pretrage) — desni panel se STVARNO otvara i puni (postojeći `onFirstAdd` u Shell.tsx). Već izabrana ponuda dobija ✓ oznaku i postaje neaktivna (isti "dodato" obrazac kao pravi `QuoteButton`). Napomena zabeležena u kodu: `productId`/`rateLineId` su mock vrednosti — "Napravi ponudu" bi za njih vratilo grešku sa pravog servera (proizvod ne postoji), prihvatljivo dok je ovo mock faza.
  **Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju — dati URL renderuje `grid-cols-5`, prave `picsum.photos` slike, i tačan tekst "2 odrasle (1 soba)" na sve tri postojeće mock kartice (potvrđeno čitanjem stvarnog HTML-a). Sam klik/dodavanje u selekciju je klijentsko ponašanje (React state) — potvrđeno čitanjem koda, isto poznato ograničenje kao ostatak interakcionih izmena.
**Verzija:** 1.67 — Ožičeni specificirani-ali-neizgrađeni filteri vođene pretrage smeštaja + MOCK grid/list prikaz rezultata (26.8.2026, na zahtev vlasnika, posle razrade ekrana kroz razgovor).
  - **`amenity_tags[]` (§3.0c.3) — sad stvaran upitni parametar `GET /search`** — `search-query.dto.ts`/`search.controller.ts`/`search.service.ts`, I-logika (proizvod mora imati SVE tražene tagove u `attributes.amenities[]`, M2 spec §2.3c), isti post-query `.filter()` obrazac kao postojeća 4 tip-specifična filtera (cabinClass/minDriverAge/durationNights/cabinType). Sidebar (`SearchSidebarPanel.tsx`) dobija grupisane checkbox tagove (4 od ~7 grupa iz M2 enuma prikazane kao polazni izbor, proširivo), vidljive SAMO kad je `ACCOMMODATION` aktivan tip.
  - **"Vrsta usluge" (board_type, §3.0c.2 tačka 3) — sad stvaran klijentski filter** u `rezervacije/pretraga/page.tsx`, isti princip kao postojeći "dostupnost" (filtrira već dobijene `offers[]`, ne novi API parametar) — NAMERNO pojednostavljenje u odnosu na spec (statičan skup BB/HB/FB/AI/UAI, ne ograničen na vrednosti koje se stvarno pojavljuju u trenutnim rezultatima — isto pojednostavljenje kao postojeći "dostupnost" select).
  - **Refundabilno/Nerefundabilno (§3.0c.3a) i dalje NIJE ožičeno** — `SearchResultOffer.is_refundable` je specificiran (v1.32) ali nikad implementiran (zahteva izračunavanje iz `CancellationRule` prozora za CONTRACTED ponude, zaseban prolaz) — eksplicitno zabeleženo, ne prećutno izostavljeno.
  - **MOCK — grid/list prikaz rezultata pretrage smeštaja** (`AccommodationResultsMock.tsx`, novi klijentski komponent, hardkodovana 3 hotela) — čeka potvrdu izgleda pre prave žice. Jedan baner (grid)/red (list) po hotelu, prikazuje NAJPOVOLJNIJU varijantu ("cena od"); ikonica u donjem levom uglu banera (grid)/klik na prvi red (list) otkriva OSTALE kombinacije tip-sobe+usluge kao dodatne banere/redove, uokvirene zajedno (accordion — ponovni klik sklapa nazad). Uvek sortirano od najniže ka najvišoj ceni (i hoteli međusobno, i ponude unutar otvorenog hotela). Svaki baner/red obavezno prikazuje: kategoriju hotela, tražene datume, broj odraslih/dece, i broj soba sa kategоrijom/brojem osoba po sobi (`roomLines`, mock strukturа koja NE postoji u pravom `GET /search` odgovoru — nedostajuće polje, zabeleženo kao stavka koju prava žica mora dodati). Prikazuje se SAMO kad je pretraga tačno `type=ACCOMMODATION` (ostalih 8 vrsta pretrage čeka potvrdu istog principa, sa sopstvenim filterima).
  **Provera:** `tsc --noEmit` čist za `apps/api` i `apps/panel`; uživo kroz pravu VLASNIK sesiju — `GET /rezervacije/pretraga?type=ACCOMMODATION&amenityTags=WIFI_FREE&boardType=BB` vraća HTTP 200 bez greške (nema proizvoda sa popunjenim `amenities[]` u dev bazi još, prazan rezultat je očekivan, ne bag); tačan dati URL (`?adults=2&children=0&type=ACCOMMODATION&stayFrom=2026-08-27&stayTo=2026-09-03`) renderuje sva 3 mock hotela u TAČNOM rastućem redosledu cene (Adriatic 299€ → Panorama 389€ → Riviera 456€), potvrđeno čitanjem stvarnog redosleda u HTML-u (ne pretpostavkom). Sam klik/accordion nije uživo proveren (nema pravog browser-a) — potvrđeno čitanjem koda, isto poznato ograničenje kao ostatak interakcionih izmena.
**Verzija:** 1.66 — `productId` filter na `GET /sales/bookings` (26.8.2026, M17 spec "Desni panel — brzi pregled proizvoda" Faza B) — dugme "Aktivne rezervacije" u desnom panelu (kad je pun opis proizvoda otvoren, `ProductContactCard.tsx`) treba link ka rezervacijama TAČNO tog proizvoda, ne po tipu (`productType`, poglavlje 11, već postojao ali filtrira po TIPU, ne po konkretnom proizvodu). Novo, aditivno polje u `itemWhere` (`bookings.service.ts`), isti obrazac kao ostalih 15 filtera — direktno na `BookingItem.productId` (skalarno polje, ne preko `product` relacije kao `productType`/destinacija). "Aktivne" (isključuje `CANCELLED`/`COMPLETED`) se postiže kombinacijom sa postojećim `status` multiselect parametrom (`PENDING_SUPPLIER_CONFIRMATION`+`CONFIRMED`+`MODIFIED`), ne novim poljem. **Provera:** `tsc --noEmit` čist za `apps/api`; uživo — `GET /rezervacije/lista?productId=<pravi-id>` (panel) vraća HTTP 200 bez greške.
**Verzija:** 1.65 — +10/−10 tag okidači za dolaske/odlaske u traci ikonica (26.8.2026, na zahtev vlasnika: "Rezervacije u dolasku od danas u narednih 10 dana ikona... da izgleda ovako +10 (kao tag)... isto to za odlaske... -10... staviti ih u traku koja je stalno vidljiva u filterima, na sredinu"). `BookingsListClient.tsx` — dva nova dugmeta (`sign-in`/`+10` za dolazak, `sign-out`/`-10` za odlazak), centrirana `flex-1 justify-center` u sredini već postojeće stalno vidljive trake (v1.61). Postavljaju/uklanjaju TAČNO ista polja koja `RealFilterBar.tsx` već čita (`stayFrom`/`stayTo`, `returnFrom`/`returnTo`, poglavlje 11) preko prave navigacije (`router.push`) — nema nove filter logike ni novih backend parametara, samo prečica za već postojeći opseg datuma (danas → danas+10). "Aktivno" stanje (i mogućnost ponovnog klika da se ukloni) prepoznaje se po tačnom poklapanju trenutnih query parametara sa izračunatim opsegom. M17 spec v2.27 (dizajn/UI napomena, isti prolaz).
**Verzija:** 1.64 — Traka ikonica tipa proizvoda (poglavlje 3, tačka 3) dobija VIŠESTRUKI izbor (25.8.2026, na zahtev vlasnika, uz snimak ekrana trake ikonica: "omoguciti biranje vise stavki") — ranije je klik na drugu ikonicu ZAMENIO prethodni izbor (`productTypeFilter: string | null`, jedna vrednost); sad je `productTypeFilters: string[]`, klik DODAJE/UKLANJA tu ikonicu iz skupa, prazan skup = bez filtera (svi tipovi), isti princip kao multiselect u `RealFilterBar.tsx` (v1.56). Uzgred ispravlja postojeći bag: "Things to do" nosi TRI tipa (`EXCURSION`/`EVENT`/`TICKET`, `search-product-types.ts`) — ranija logika je filtrirala samo `types[0]` (`EXCURSION`), tiho ignorišući `EVENT`/`TICKET`; nova logika (ne)označava CEO `types` skup te ikonice atomično. `BookingsListClient.tsx`/`RealBookingsTable.tsx`.
**Verzija:** 1.63 — Automatska primena filtera bez klika na "filtriraj" (24.8.2026, na zahtev vlasnika: "da li moze da se podesi da kako se koji filter odabira da se odmah vrsi selekcija u listi rezervacija, kako bi se ubrzale stvari"). `RealFilterBar.tsx` prelazi u klijentsku komponentu (`'use client'`) — forma ostaje ISTI nativan GET `<form>` (§11/v1.54, wire-format nepromenjen), samo se `formRef.current.requestSubmit()` sad poziva automatski:
1. **Delegovan `onChange` na samoj `<form>`** (React sintetički event bubbling, jedno mesto za sve unutrašnje kontrole) — diskretni elementi (checkbox/select/datum) primenjuju ODMAH; tekstualna polja (`type="text"` — Broj/Nosilac/Destinacija/Valuta) čekaju kratku pauzu bez kucanja (`TEXT_DEBOUNCE_MS = 600`) pre submit-a, da se ne pokreće navigacija na svaki taster.
2. **Dugmad za brisanje (v1.62) eksplicitno pozivaju `requestSubmit()`** — ne oslanjaju se na delegovani `onChange` jer njihovo brisanje NE pokreće pravi DOM `'change'` događaj (direktna DOM/React state izmena, ne korisnički unos). `MultiSelectDropdown.tsx` "Obriši izbor" ima dodatnu suptilnost: `setChecked(new Set())` je ASINHRONO (React state) — `requestSubmit()` pozvan odmah posle njega bi pri submit-u video JOŠ neizmenjene DOM čekboksove; ispravljeno direktnom SINHRONOM DOM izmenom (`checkbox.checked = false` nad svim čekboksovima u panelu) PRE `requestSubmit()`, `setChecked` samo drži prikaz/oznaku na dugmetu usklađenim pri sledećem renderu.
3. Dugme "filtriraj" i dalje postoji (nije uklonjeno) — ostaje koristan za tekstualna polja ako korisnik ne želi da čeka debounce pauzu.
**Poznata karakteristika (ne bag):** svaki automatski `requestSubmit()` je STVARNA navigacija (GET forma, ne SPA fetch) — Next.js 14 App Router ne presreće plain `<form>` submit u klijentsku tranziciju bez dodatnog rada, isto ponašanje koje je forma imala i pri ručnom kliku na "filtriraj" od v1.54. Debounce na tekstualnim poljima postoji upravo da ovo ne postane naporno pri kucanju.
**Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju — `GET /rezervacije/lista?status=CONFIRMED` (HTTP 200) i dalje ispravno renderuje ("izabrano (1)" na Status dugmetu), ruta ne pada. Sama automatska primena (debounce tajmer, `requestSubmit()` poziv) je klijentska JS logika koja ne postoji u statičkom SSR HTML izvoru — nije uživo potvrđena kroz pravi klik/kucanje u browseru (isto ograničenje kao ostatak panel UI provera ove faze), logika potvrđena čitanjem koda.
**Verzija:** 1.62 — Poništavanje filtera po pojedinačnom polju (24.8.2026, na zahtev vlasnika: "Omogucite i ponistavanje filtera u pojedinačnim poljima" — dopuna istog dana kao v1.61 raspored). Do sada je postojao samo globalan "obriši filter" (briše SVE odjednom). Tri nove/izmenjene komponente:
1. **`ClearableTextField.tsx`** (novo, `apps/panel/src/components/`) — inline "×" unutar polja, vidljivo SAMO kad polje ima vrednost; klik prazni DOM vrednost (uncontrolled ref) bez gubitka ostatka forme. Zamenjuje plain `<input>` za Broj/Nosilac/Destinacija (grad/država)/Valuta.
2. **`ClearableDateRange.tsx`** (novo) — isti princip za par datuma (Od/Do) koji se tretiraju kao JEDNO logičko polje — jedno dugme prazni oba unosa odjednom. Zamenjuje par `<input type="date">` za Kreirano/Dolazak/Odlazak.
3. **`MultiSelectDropdown.tsx`** — čekboksovi prešli sa necontrolisanih (`defaultChecked`) na CONTROLLED (`checked` prati React state) da dugme "Obriši izbor" unutar panela (vidljivo samo kad ima izabranih) stvarno može da isprazni selekciju (`setChecked(new Set())`), ne samo vizuelno prikaže "0 izabrano" dok DOM elementi ostaju markirani.
"Garancija putovanja" (tri-state select) NAMERNO nije dobio poseban clear — opcija "svejedno" već JESTE njegov "bez filtera" izbor, dodatno dugme bi bilo suvišno.
**Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju — `GET /rezervacije/lista?bookingNumber=TT-2026-1&status=CONFIRMED&createdFrom=2026-06-01` (HTTP 200, tri aktivna filtera) potvrđuje tačno tri "Obriši..." dugmeta u renderovanom HTML-u (po jedno za tekstualno polje, datumski opseg, i multiselect dropdown) i `pr-6` padding klasu primenjenu na polje sa vrednošću (mesto za "×" ikonicu).
**Verzija:** 1.61 — Raspored forme filtera u dva reda, sva polja jednake širine (24.8.2026, na zahtev vlasnika: "polja za pretragu datuma neka idu u drugi red, ostala polja u prvi i neka sva polja budu iste sirine da se rasporede celom sirinom trake filtera"). `RealFilterBar.tsx`: forma je bila jedan `flex flex-wrap` red gde je svako polje zauzimalo samo prirodnu širinu svog sadržaja — zamenjeno sa DVA odvojena `flex` reda (bez `flex-wrap`): prvi red deset polja (Broj/Nosilac/Status/Uplata/Tip nastupanja/Tip proizvoda/Destinacija grad/Destinacija država/Valuta/Garancija putovanja), drugi red tri datumska opsega (Kreirano/Dolazak/Odlazak). `Field` pomoćna komponenta i `MultiSelectDropdown.tsx` dobijaju `flex-1 min-w-0` — svako polje u redu sad deli raspoloživu širinu podjednako sa ostalima u tom redu, umesto da red ostaje neravnomerno zbijen na levoj strani. Dugme "filtriraj"/"obriši filter" premešteno u treći, zaseban red (ne razvlači se kao filter-polja).
**Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju — `GET /rezervacije/lista` (HTTP 200) potvrđuje tačno devet `flex-1` polja (`Field`) u prvom redu i tri u drugom, sve četiri `MultiSelectDropdown` instance takođe sa `flex-1`.
**Verzija:** 1.60 — Multiselect kao dropdown panel sa čekboksovima, ne `<select multiple>` (24.8.2026, na zahtev vlasnika: "ne svidja mi se ovako, napravide dropdown panel za polja i omogucite check box biranje" — poništava vizuelni deo v1.59 od istog dana, wire-format ka API-ju NEPROMENJEN). Nov `MultiSelectDropdown.tsx` (`apps/panel/src/components/`, klijentska komponenta) — dugme ("Status", oznaka "izabrano (N)" kad ima selekcije) otvara panel sa čekboksovima, zatvara se klikom van panela. Čekboksovi su i dalje PRAVI, necontrolisani `<input type="checkbox" name=... value=...>` elementi unutar istog nativnog GET `<form>`-a (v1.54) — pri submit-u i dalje šalju identičan ponovljen parametar (`?status=A&status=B`) kao ranije `<select multiple>`, `GET /sales/bookings` (poglavlje 11/v1.59) ostaje nepromenjen.
**BAG otkriven i ispravljen u istom prolazu:** prvi pokušaj je renderovao čekboksove uslovno (`{open && (...)}`) — kad je panel vizuelno zatvoren, čekboksovi su POTPUNO nestajali iz DOM-a (ne samo vizuelno), pa bi ponovni submit forme (npr. klik "filtriraj" bez otvaranja panela) tiho izgubio već izabranu selekciju (nestao element = nestao parametar pri submit-u). Ispravljeno: čekboksovi se UVEK renderuju, panel se sakriva/prikazuje preko CSS `hidden` klase (CSS display ne utiče na form submit, za razliku od uklanjanja iz DOM-a).
**Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju — `GET /rezervacije/lista?status=CONFIRMED&status=CANCELLED` (HTTP 200) potvrđuje: dugme sa oznakom "izabrano (2)", svih 21 čekboks input-a (5+4+2+10 opcija kroz četiri polja) prisutno u DOM-u nezavisno od otvorenog/zatvorenog stanja panela, tačno 2 markirana `checked`.
**Verzija:** 1.59 — Multiselect za kategoričke filtere na listi rezervacija (24.8.2026, na zahtev vlasnika: "omogucite u svakom polju filtera gde je to moguce multiselect opciju"). Primenjeno na četiri polja koja imaju konačan, mali skup vrednosti — Status, Uplata, Tip nastupanja, Tip proizvoda (`<select multiple>`, Ctrl/Cmd-klik za više opcija, napomenuto u labeli). NAMERNO izostavljeno kod slobodnog teksta (Broj/Nosilac/Destinacija/Valuta — nema konačan skup vrednosti) i tri-state "Garancija putovanja" (ima/nema/svejedno — biranje i "ima" i "nema" istovremeno bi bilo isto što i "svejedno", besmisleno).
1. **`GET /sales/bookings`** (poglavlje 11) — `status`/`paymentStatus`/`tipNastupanja`/`productType` sad prihvataju PONOVLJEN parametar (`?status=CONFIRMED&status=CANCELLED`); `BookingsController` normalizuje `string | string[]` u uvek-niz pre prosleđivanja servisu (`toArray()` helper). `BookingsService.findAll()` primenjuje preko Prisma `{ in: [...] }` umesto tačnog poklapanja — prazan/nedefinisan niz znači "svi" (isti princip kao dosad, samo sad za listu umesto jedne vrednosti).
2. **`RealFilterBar.tsx`** — četiri `<select>` polja dobijaju `multiple` atribut, `defaultValue` postaje niz (novi `toArray()` helper, panel strana — odvojen od istoimenog helpera u kontroleru, ista svrha, drugi sloj). Prazna opcija "svi"/"sve" uklonjena (nema smisla za multi-select — ništa selektovano već znači "svi").
3. **`page.tsx`** — petlja koja gradi query string ka API-ju sad razlikuje `string`/`string[]` vrednost iz `searchParams` (Next.js daje niz kad je parametar ponovljen) — niz se dodaje kroz `URLSearchParams.append()` (ne `.set()`, koji bi ga pogrešno spojio u jedan string).
4. **"Sačuvani prikazi" (v1.56) prošireni na niz** — `SavedView.filters` menja tip iz `Record<string,string>` u `Record<string, string|string[]>` (`SavedViewsSidebarPanel.tsx`, `BookingsListClient.tsx`) — bez ove izmene bi čuvanje prikaza sa više izabranih statusa tiho zadržalo samo POSLEDNJI (stari `filters[key] = value` bi prepisivao pri ponovljenom ključu). `toQueryString()` na oba mesta sad razlikuje niz/string isto kao `page.tsx`.
**Provera:** `tsc --noEmit` čist za `apps/api` i `apps/panel`; uživo kroz pravu VLASNIK sesiju — `GET /sales/bookings?status=CONFIRMED&status=CANCELLED` (HTTP 200) vraća rezervacije SA OBA statusa (potvrđeno na stvarnim podacima iz baze, ne testnim); `GET /rezervacije/lista?status=CONFIRMED&status=CANCELLED` (HTTP 200) potvrđuje `<select name="status" multiple>` sa obe vrednosti markiranom kao izabrane u renderovanom HTML-u.
**Verzija:** 1.58 — Dugme "Sačuvaj pretragu" premešteno u traku ikonica (24.8.2026, na zahtev vlasnika: "stavite dugme Sacuvaj u istu traku"). `BookingsListClient.tsx`: `SaveViewButton` više nije u posebnom redu ispod forme (v1.56/v1.57) — sad je deo iste UVEK-vidljive trake ikonica (tip proizvoda + "demo zvona" + −/+), poravnat uz desnu ivicu odmah pre −/+ dugmeta (`ml-auto` na omotaču dugmeta, `−/+` prati odmah posle u istom flex redu).
**Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju — `GET /rezervacije/lista?status=CONFIRMED` (HTTP 200, aktivan filter u URL-u) potvrđuje dugme "Sačuvaj pretragu" prisutno unutar iste trake (`<div class="ml-auto">` neposredno pre −/+ dugmeta u istom nizu elemenata).
**Verzija:** 1.57 — Sklanjanje/vraćanje filtera na listi rezervacija (24.8.2026, na zahtev vlasnika: "Omogucite i uklanjanje filtera na - i ponovno pojavljivanje na + u listi rezervacija" — dopunjeno istog dana, uz snimak ekrana: "ova traka [ikonica] neka uvek bude vidljiva u desnom kraju stavite - i + kako bi se ostali filteri pojavili ispod ove trake"). `BookingsListClient.tsx`: traka ikonica (tip proizvoda + "demo zvona") ostaje UVEK vidljiva u sticky bloku (v1.55) — dugme `−`/`+` (`filtersCollapsed` state) živi na desnom kraju TE trake, ne u posebnom redu iznad nje (prvi pokušaj istog dana sklanjao je i traku, ispravljeno na vlasnikov predlog). Klik na `−` sklanja formu (`RealFilterBar`) i dugme "Sačuvaj pretragu" ispod trake, klik na `+` ih vraća. Čisto vizuelno sklanjanje prostora — AKTIVNI filteri (URL parametri, `productTypeFilter`, `demoOnly`) ostaju primenjeni na listu dok su sklonjeni, ništa se ne briše (za brisanje postoji zaseban link "obriši filter" unutar same forme).
**Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju — `GET /rezervacije/lista` (HTTP 200) potvrđuje dugme (`ml-auto`, na kraju flex reda trake ikonica) prisutno sa naslovom "Sakrij ostale filtere". Sam klik/toggle (React state) nije uživo potvrđen kroz pravi klik u browseru (isto ograničenje kao ostatak panel UI provera), logika potvrđena čitanjem koda.
**Verzija:** 1.56 — "Sačuvani prikazi" za listu rezervacija, u levom panelu (24.8.2026, na zahtev vlasnika: "sta mislite da filtere za listu rezervacija stavimo u levi panel... Ima dosta praznog prostora" → nakon razmatranja i objašnjenja vlasniku, potvrđeno: "Svidja mi se, krenite"). Dizajn dok. §5b je ovu ideju već predviđao od 18.8.2026 ("Sačuvani prikazi... na bilo kojoj listi korisnik može da sačuva trenutnu kombinaciju filtera pod imenom... pojavljuje se kao dodatna stavka u stablu te sekcije"), ali bez konkretnog modela/endpointa — implementiran je u ovom prolazu, oslonjen na novoizgrađen M1 `UserPreference` (spec v1.8) umesto novog modela.
1. **Dugme "Sačuvaj pretragu"** (`BookingsListClient.tsx`, novo) — čuva TRENUTNE stvarne filtere liste (`RealFilterBar`/`GET /sales/bookings` query parametri, čitani preko `useSearchParams`) pod imenom koje agent unese, kao novu stavku u nizu pod ključem `saved_views.rezervacije_lista` (`PUT /users/me/preferences/:key`, preko panel BFF `/api/preferences/[key]`). Klijentski-samo brzi filteri (tip proizvoda, "demo zvona" — postoje samo u React state-u, ne u URL-u) NAMERNO nisu uključeni — isti obim kao ono što `RealFilterBar` info traka već obećava kao "prave filtere". Dugme se ne prikazuje kad nijedan filter nije aktivan (nema šta da se sačuva).
2. **`SavedViewsSidebarPanel.tsx`** (novo, `apps/panel/src/components/`) — kačen u `Sidebar.tsx` po istom obrascu kao `SearchSidebarPanel` (`selected.id === 'rezervacije-lista'`), popunjava prazan prostor ispod stavke "Lista rezervacija" u levom panelu (tačno mesto koje je vlasnik primetio praznim na snimku ekrana). Čita `GET /users/me/preferences` (preko `/api/preferences`), prikazuje listu sačuvanih prikaza — klik odmah navigira na `/rezervacije/lista?...` sa svim sačuvanim filterima primenjenim (bez ponovnog kucanja), dugme za brisanje po stavci (hover-reveal). Prazno stanje ("Sačuvaj trenutnu pretragu... da je vidiš ovde") umesto praznog prostora bez objašnjenja.
3. **Sinhronizacija dva odvojena dela stabla** — dugme za čuvanje živi u centralnom panelu, spisak u levoj traci (različiti delovi React stabla, bez zajedničkog roditelja koji bi prirodno prosledio state). Rešeno prostim DOM event-om (`tt:saved-views-changed`, `window.dispatchEvent`/`addEventListener`) — čuvanje/brisanje odmah osvežava spisak, bez potrebe za novim Context provajderom radi jedne, retko korišćene sinhronizacije.
**Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju — ceo tok proveren preko panel BFF ruta (ne direktno ka API-ju): `PUT /api/preferences/saved_views.rezervacije_lista` (HTTP 200) upisuje, `GET /api/preferences` (HTTP 200) vraća upravo upisanu vrednost — potvrđuje pravu bazu, ne samo DTO. Dugme "Sačuvaj pretragu" potvrđeno u renderovanom HTML-u SAMO kad je aktivan filter prisutan u URL-u (`?status=...`), odsutno bez filtera. `SavedViewsSidebarPanel` sam po sebi učitava preko `useEffect` (klijentska interakcija posle hidratacije) — nije vidljiv u statičkom SSR snapshot-u preko curl-a, isto ograničenje kao ostatak panel UI provera ove faze; logika potvrđena čitanjem koda i uživo proverenim BFF tokom. Test podaci (`panel-test-1`) obrisani posle provere.
**Verzija:** 1.55 — Filteri na listi rezervacija ostaju vidljivi pri skrolovanju (24.8.2026, na zahtev vlasnika: "Filtere u listi rezervacija fixirajte da budu vidljivi prilikom scrolovanja"). Lista ima DVA odvojena filter-UI-ja od v1.54 — forma `RealFilterBar` (pravi `GET /sales/bookings` upitni parametri) i traka brzih ikonica po tipu proizvoda + "demo zvona" (klijentsko stanje, ranije unutar `RealBookingsTable.tsx`). Dva nezavisna `position: sticky` elementa bi se oba lepila za `top: 0` i preklapala (nema prirodnog CSS mehanizma da se automatski slože jedan ispod drugog bez merenja visine) — rešeno novim `BookingsListClient.tsx`: jedan klijentski omotač drži OBA (formu, prosleđenu kao `children`/`filterBar` prop iz server-komponente `page.tsx`, i traku ikonica, čije je stanje `productTypeFilter`/`demoOnly` premešteno ovamo iz `RealBookingsTable.tsx` — sad kontrolisano spolja preko propova) unutar JEDNOG `sticky top-0 z-20` bloka, sa `border-b`/`bg-panel` da vizuelno prekrije redove tabele koji klize ispod njega.
**Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju — `GET /rezervacije/lista` (HTTP 200) potvrđuje `sticky top-0 z-20` klasu na omotaču koji sadrži i formu i traku ikonica. Sam efekat lepljenja pri skrolovanju (vizuelno ponašanje u browseru) nije uživo potvrđen kroz pravi skrol (isto ograničenje kao ostatak panel UI provera), logika potvrđena čitanjem koda i CSS `position: sticky` ponašanjem.
**Verzija:** 1.54 — "Lista rezervacija" prelazi sa MOCK na PRAVE podatke, poglavlje 13 stavka zatvorena (24.8.2026, na zahtev vlasnika: "krenite" posle potvrđenog v1 skupa filtera — vidi razgovor 23-24.8.2026). Pre koda: pri pripremi je otkriveno da bi prava lista morala da IZGUBI nekoliko funkcija koje mock lista ima (zvonce "Hitno" + kontakt, poslovnica/dodeljeni korisnik, naziv hotela) jer njihovi pravi izvori podataka još ne postoje (M18 alarm signal, M1 pojam poslovnice, `ProductTranslation` join nijedan pravi M5 ekran još ne koristi). Vlasnik je preko `AskUserQuestion` potvrdio: **prava lista + stvarni filteri, uz zadržavanje tih polja VIZUELNO ali BEZ funkcije** (i dalje izvedena mock vrednost, jasno obeležena, ne tvrde da su iz baze).

**`GET /bookings` (poglavlje 11) dobija nove opcione query parametre** — svi kombinuju se I-logikom sa postojećim (`status`/`channel`/`clientAccountId`), isključivo za `INTERNAL_PANEL` kontekst (za B2C/B2B/gost kontekst i dalje važi vlasnička ograda iz poglavlja 6.2, novi parametri se za njih ignorišu):
| Parametar | Tip | Mapira na |
| :---- | :---- | :---- |
| `paymentStatus` | enum `PaymentStatus` | `Booking.payment_status` |
| `tipNastupanja` | enum `TipNastupanja` | `Booking.tip_nastupanja` |
| `buyerName` | string, "sadrži" (case-insensitive) | `Booking.buyer_name` |
| `bookingNumber` | string, "sadrži" | `Booking.booking_number` |
| `currency` | string, tačno poklapanje | `Booking.currency` |
| `createdFrom`/`createdTo` | datum (YYYY-MM-DD) | `Booking.created_at` opseg |
| `stayFrom`/`stayTo` | datum | bar jedna stavka (`BookingItem`) sa `stay_from` u ovom opsegu (dolazak) |
| `returnFrom`/`returnTo` | datum | bar jedna stavka sa `stay_to` u ovom opsegu (odlazak) |
| `productType` | enum `ProductType` | bar jedna stavka čiji `Product.type` odgovara |
| `productId` | string (UUID) | *(dopuna v1.66, 26.8.2026)* bar jedna stavka čiji `BookingItem.product_id` odgovara TAČNO ovom proizvodu (za razliku od `productType` iznad koji filtrira po tipu) — koristi ga desni panel internog panela ("Aktivne rezervacije", M17 spec) |
| `destinationCity`/`destinationCountry` | string, tačno poklapanje | bar jedna stavka čiji `Product.destination_city`/`destination_country` odgovara |
| `hasTravelGuarantee` | `'true'`/`'false'` | postojanje/nepostojanje `Booking.travelGuaranteeRegistration` veze |

Odgovor po stavci (`BookingItem`) sad uključuje ugnježden `product: { destinationCountry, destinationCity, type }` (samo ta tri polja, `select`, ne ceo `Product`) — dovoljno da panel prikaže destinaciju bez dodatnog poziva; naziv proizvoda/hotela NAMERNO izostaje (zahteva `ProductTranslation` join, van obima ovog prolaza — nijedan pravi M5 ekran ga danas ne prikazuje, ni `/rezervacije/[id]`).

**Panel:** `/rezervacije/lista/page.tsx` postaje server komponenta koja čita `searchParams`, gradi query string, zove `GET /sales/bookings` (STVARNO, ne mock), i mapira odgovor u prikazni oblik. Filter forma (`RealFilterBar.tsx`, nov) je obična HTML forma sa `defaultValue`-ima iz `searchParams` — isti obrazac kao već postojeći `/marketing`/`/podrska`/`/b2b/rabati` (`?status=`), NE nova arhitektura kao što je ranije pomenuti "levi panel" iz v1.42 (taj pomen je bio putokaz iz prvog dana ekrana, pre nego što je `FiltersModal` uopšte izgrađen — modal ostaje mesto za dodatne/mock filtere ispod, prava forma ide iznad tabele). `BookingsTable.tsx` zadržava sortiranje/kolonske filtere ali sad radi NAD već filtriranim (server-side) nizom.

**Vizuelno-mock polja bez funkcije** (jasno obeležena, deterministički izvedena kao i do sada preko `deriveEmail`/`derivePhone`/`slugifyName`): zvonce "Hitno" + `UrgentModal` kontakt, `branch`/`assignedUser`, naziv hotela (`hotelName`, izveden iz `destinationCity` teksta — npr. "Hotel u {gradu}" — dok pravi `ProductTranslation` join ne postoji). Njihovi filteri u `FiltersModal`/traci ikona OSTAJU ali rade isključivo nad TRENUTNO učitanom (već server-filtriranom) stranicom — ne šalju se ka `GET /bookings`, jer ne postoje kao pravi upitni parametri. Kod svakog nosi komentar "vizuelno, bez funkcije — čeka pravi izvor" da se ne pomeša sa stvarnom funkcijom pri budućem čitanju.

**Bezbednosna napomena pri implementaciji:** "Hitno" zvonce na demo redovima namerno NIJE crveno/uzbunjujuće kao na staroj mock listi — sivo je, sa tekstom "DEMO" u tooltip-u i u samom sadržaju modula. Razlog: ovo su sad PRAVE rezervacije; crveni alarm koji tvrdi da postoji problem na pravoj rezervaciji, a koji je zapravo izmišljen, nosi drugačiji (veći) rizik zabune od bezopasnih dekorativnih polja poput imena hotela — neko iz tima bi mogao da pomisli da postoji stvaran hitan slučaj. Ostatak "vizuelno mock" polja (kontakt, poslovnica, naziv hotela) zadržan bez izmene tona, u skladu sa vlasnikovom odlukom.

**Provera:** `tsc --noEmit` čist za `apps/api` i `apps/panel`. Uživo kroz PRAV HTTP put (24.8.2026, dopunska provera na zahtev vlasnika) — privremen VLASNIK test nalog sa programski podešenom 2FA (`otplib` generisan TOTP secret preko iste `encryptSecret` funkcije koju koristi `enrollMfa`, kod izračunat preko `authenticator.generate()`), pun tok `POST /iam/auth/login` → `POST /iam/auth/mfa/verify` → `accessToken`, pa stvaran `GET /sales/bookings` sa `Authorization` header-om (ne direktan Prisma/servisni poziv). Rezultati identični ranijoj proveri servisnog sloja, sad potvrđeni kroz kompletan HTTP put (guard-ovi, DTO parsiranje upit parametara, serijalizacija): bez filtera → 28, `paymentStatus=UNPAID` → 4, `hasTravelGuarantee=true` → 1, `destinationCountry=Crna Gora` → 1 (stvaran pogodak, `product` ugnježden u odgovoru stavke potvrđen), `stayFrom=2026-07-01&stayTo=2026-07-31` → 1, `buyerName=a` (sadrži) → 5. Test nalog (uključujući MFA secret/recovery kodove) i privremene skripte obrisani posle provere. `GET /rezervacije/lista` ruta (bez sesije) vraća HTTP 307, ne 500. Klik-interakcije u samom pregledaču (forma filtera, sortiranje, otvaranje modula) i dalje nisu uživo potvrđene kroz pravi klik — logika potvrđena čitanjem koda i `tsc`, isto ograničenje kao ranije faze ovog ekrana.

**Verzija:** 1.53 — Modul "Hitno" prikazuje VIŠE notifikacija po rezervaciji (23.8.2026, na zahtev vlasnika: "jedna rezervacija moze da ima vise notifikacija, kada se klikne na zvono treba da se pojave 1,2,3... onoliko mini modula koliko ima notifikacija"). `MockBookingRow.urgent` menja tip iz pojedinačnog zapisa u niz (`{ reason, target }[] | null`) — svaka stavka zadržava sopstveni `target` (v1.52), pa ista rezervacija može imati i notifikaciju ka dobavljaču I ka klijentu istovremeno (demonstrirano na `TT-2026-100053`: "čeka potvrdu dobavljača" + "nedostaje broj pasoša za bebu", jedna SUPPLIER jedna BUYER). `UrgentModal.tsx` renderuje `NotificationCard` (nov, izdvojena komponenta) za svaku stavku niza, naslov svake kartice "Hitno {i}/{n}" kad ih ima više — jedan zajednički "Zatvori" iznad naslaganih kartica, ne po kartici (klik van zatvara sve). `BookingRecordClient.tsx` (pun zapis) — inline "hitno" oznaka pored broja rezervacije sad renderuje jednu oznaku PO notifikaciji umesto samo prve. **Provera:** `tsc --noEmit` čist za `apps/panel`; ruta i dalje odgovara bez pada servera.

**Verzija:** 1.52 — Modul "Hitno" razlikuje kome se obraća (23.8.2026, na zahtev vlasnika: "treba razlikovati obavestenja koje saljemo klijentima i partnerima, dobavljacima po logici stvari"). Uočeno pri pregledu postojećih 6 mock "hitno" razloga (v1.42): dva su OBAVEZNO o dobavljaču ("Rok za potvrdu dobavljača ističe") — v1.51 je ipak uvek prikazivao kontakt KUPCA, pogrešno za te slučajeve. `MockBookingRow.urgent` dobija `target: 'BUYER' | 'SUPPLIER'`, ručno klasifikovano po logici svakog od 6 postojećih razloga (potvrda dobavljača/dostupnost → SUPPLIER; depozit/ostatak/faktura/ugovor sa klijentom → BUYER — "nalogodavac" u B2B slučaju je i dalje `buyerName`, isti kontakt kao klijent, jer se u modelu ne razdvajaju). Nov `supplierEmail`/`supplierPhone` na `MockBookingRow` (stvaran izvor bi bio `Supplier.contact_email`/`.contact_phone`, M3 spec, model `Supplier`) — izveden iz `supplierName` (jedan kontakt po firmi, isti za sve njene rezervacije, ne po pojedinačnoj rezervaciji kao kod kupca). `UrgentModal.tsx` bira ispravnu sekciju kontakta i ton mejla prema `target` — dobavljaču formalno ("Poštovani, u vezi sa rezervacijom..."), klijentu lično ("Poštovani/a {ime}"). **Provera:** `tsc --noEmit` čist za `apps/panel`; ruta i dalje odgovara bez pada servera; ručno pregledana klasifikacija svih 6 hitnih razloga protiv njihovog teksta.

**Verzija:** 1.51 — Kontakt u modulu "Hitno" (23.8.2026, na zahtev vlasnika: "kada imamo notifikacije kao sto je [zvonce] treba odmah odavde omoguciti slanje mejla i prikazati broj telefona koji treba da se pozove ili posalje poruka"). `MockBookingRow` dobija `buyerEmail`/`buyerPhone` (stvaran izvor bi bio `ClientAccount.email`/`.phone`, M6 spec §2.1 — polje je stvarno, vrednost izmišljena) — izvedeno deterministički iz imena/broja rezervacije kad nije ručno navedeno (`deriveEmail`/`derivePhone` u `mock-data.ts`, isti obrazac kao već postojeće izvođenje `paidAmount`). `UrgentModal.tsx` dobija sekciju "Kontakt" sa brojem telefona + dugmad Pozovi (`tel:`)/SMS (`sms:`)/WhatsApp (`wa.me/<broj>`) i mejlom + dugme "Pošalji mejl" (`mailto:` sa unapred popunjenim naslovom/pozdravom). Namerno linkovi ka spoljnim/OS aplikacijama, NE stvarno slanje kroz M22 API — M22 danas nema sposobnost sastavljanja/slanja proizvoljnog mejla (isti otvoren nedostatak već zabeležen u M15 spec §6.9.3 dopuni v1.30/v1.31 — "Slanje BiTerminalAgent izveštaja mejlom... zahteva prvo dopunu M22 spec-a"); ovo je isti nivo integracije kao postojeći Mejl/WhatsApp/Viber/Telegram klaster u `StatusBar.tsx`. **Provera:** `tsc --noEmit` čist za `apps/panel`; `slugifyName`/`derivePhone` ručno testirani u Node-u (npr. "Đorđe Ilić" → "djordje.ilic@primer.local", "TT-2026-100046" → "+381 66 100 046"); ruta `/rezervacije/lista` i dalje vraća odgovor bez pada servera.

**Verzija:** 1.50 — Sortiranje po koloni na mock listi (23.8.2026, na zahtev vlasnika: "uvedite sortiranje po logici stvari"). `BookingsTable.tsx`: klik na naziv kolone u zaglavlju (strelica pokazuje smer) — prvi klik rastuće, drugi opadajuće, treći isključuje sortiranje, isti troetapni obrazac kao zvonce/urgentOnly toggle ranije u istom fajlu. "Po logici stvari" NIJE azbučni/prirodan redosled za sve kolone: Status i Uplata sortiraju po TOKU rezervacije/naplate (`STATUS_ORDER`: čeka potvrdu dobavljača → izmenjeno → potvrđeno → završeno → otkazano; `PAYMENT_ORDER`: neplaćeno → faktura na čekanju → delimično → plaćeno) tako da rastuće sortiranje prvo pokaže stavke koje traže pažnju — isti princip triaže kao postojeće zvonce/hitno obeležavanje. Datumske kolone hronološki, "Iznos" brojčano, ostale (Broj/Nosilac/Kanal) azbučno (`localeCompare('sr-RS')`). Sortira se nad već filtriranim nizom (`sorted` izveden iz `filtered`), izvoz (`ExportButton`) sad izvozi u prikazanom (sortiranom) redosledu. **Provera:** `tsc --noEmit` čist za `apps/panel`; ruta `/rezervacije/lista` i dalje vraća odgovor bez pada servera.

**Verzija:** 1.49 — "Izmeni" u punom zapisu — stavke (segmenti) po tri postojeća načina unosa + zapis u workflow (23.8.2026, na zahtev vlasnika: "Sta se dogadja kada se klikne na Izmeni?" → "tada bi trebalo za svako segment da se pojavi modul u kom ce se unositi rucno podaci novi ili menjatu stari i za sve to treba da postoji zapis u work flow", razrađeno: "1. Ručno se biraju svi parametri... drzava, destinacija, hotel, vrsta sobe, usluga... rucni unos moze da bude ulazna cena, marza (u% ili iznosu ili i jedno i drugo) izlazna cena. 2. putem api konekcije - samo se unose imena putnika, posle uplate... 3. Iz baze unetih aranzmana rucno ili uz pomoc ai agenata... (za ovo nam trebaju moduli)").
1. **Tri načina unosa NISU nov koncept** — potvrđeno da odgovaraju već postojećem `source_type` modelu (`CONTRACTED`/`API`/`MANUAL`, poglavlje 3.0f, 3.2/4.2), do sada primenjenom samo pri PRAVLJENJU nove Ponude; ovom dopunom se isti model prvi put primenjuje i na IZMENU već potvrđene rezervacije. Nov `MockBookingItem` (mock-data.ts): `sourceType`, opisna polja (država/destinacija/hotel/vrsta sobe/usluga), `baseCost`/`marginPercent`/`marginAmount`/`finalPrice` — tri poslednja polja međusobno povezana (izmena bilo kog preračunava ostala dva), tačno "u % ili iznosu ili i jedno i drugo". `MockBookingRow.items[]` (svaka rezervacija ima 1+ stavki/segmenata) — postojećih 20 mock redova dobija po jednu izvedenu stavku iz ranijih pljosnatih polja (pretpostavljena marža 20% SAMO kad nema drugog izvora, obeleženo u komentaru koda, ne u UI-ju kao stvaran podatak).
2. **CONTRACTED** (ručno iz kataloga) — svi opisni podaci + cena uređivi. **API** — cena/uslovi dolaze od dobavljača, polja cene prikazana ali onemogućena za unos (isto pravilo kao poglavlje 3.0b.4: cena u pretrazi nije konačna garancija dok se ne pozove M4 ponovo); agent menja opisna polja/putnike. **MANUAL** (iz baze aranžmana) — AI-agent asistirano popunjavanje NAMERNO nije povezano u ovom prolazu (traži stvaran M15 modul za izvlačenje sadržaja, poglavlje 3.0f.2, koji postoji za NOVE ponude ali nije ožičen na IZMENU postojeće rezervacije) — vidljivo upozorenje u UI-ju "čeka poseban prolaz", polja se za sada popunjavaju ručno kao CONTRACTED. Nova sekcija "Stavke (segmenti)" u punom zapisu (`BookingItemsEditor.tsx`) — svaka stavka: prikaz + dugme "Izmeni" (inline forma) + "Dodaj stavku" (nov segment, podrazumevano CONTRACTED).
3. **Zapis u workflow** — svako sačuvano polje/dodata stavka odmah dobija unos u tok rezervacije (spojeno sa `buildMockTimeline()` u `BookingRecordClient.tsx`, novi klijentski state `extraLog`), sa čitljivim opisom šta je promenjeno (npr. "izlazna cena: 890,00 EUR → 920,00 EUR"). Vidljivo odmah kroz dugme "Tok rezervacije" u vrhu punog zapisa (premešteno iz onemogućenog stanja).
**Ograničenje, namerno, dosledno "i dalje mock":** izmene stavki žive ISKLJUČIVO u React state-u te sesije/tog otvorenog taba — mock lista nema pravu bazu na koju bi se upisivale, osvežavanje stranice vraća originalne mock vrednosti. Kad lista pređe na pravu bazu, ovaj mehanizam se zamenjuje pravim `PATCH /bookings/:id/items/:itemId` (endpoint još ne postoji, dodaje se u tom prolazu) koji upisuje u `AuditLogEntry` isto kao i ostatak M5 workflow-a (poglavlje 6, "promene statusa se čuvaju kroz postojeći audit log").
**Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju — `GET /rezervacije/lista/TT-2026-100042` (HTTP 200) potvrđuje sekciju "Stavke (segmenti)", dugme "Dodaj stavku", stavku sa `sourceType: CONTRACTED` i poljima marže prisutnim u podacima poslatim klijentskoj komponenti. Stvarno čuvanje izmene/dodavanje stavke/upis u `extraLog` (React state) nije uživo potvrđeno kroz pravi klik u browseru (isto ograničenje kao ostatak ove faze), logika potvrđena čitanjem koda.
**Verzija:** 1.48 — "Pun zapis" rezervacije (mock), otvara se u novom app-tabu (23.8.2026, na zahtev vlasnika: "ne vidim da se klikom na broj rezervacije otvara novi tab sa kompletnom formom rezervacije i svim podacima" — potvrda predloga iz v1.47 changeloga, dat 23.8.2026: "Da gradi po predlogu, s tim sto cemo sigurno imati izmene i dorade"). Zatvara stavku iz `docs/analize/27-BACKLOG-IDEJA-I-PREDLOZI.md` M5 sekcije ("Pun zapis forma za rezervaciju... čeka potvrdu vlasnika") — predlog potvrđen, prvi prolaz implementiran, dalje izmene/dorade očekivane i najavljene.
1. Nov `apps/panel/src/app/(app)/rezervacije/lista/[bookingNumber]/page.tsx` — server komponenta, čita direktno iz `MOCK_BOOKINGS` po broju rezervacije (lista nema stvarne DB ID-jeve, isto ograničenje kao v1.42-v1.47). Sadržaj po dogovorenom predlogu: vrh (broj, status, ikonica tipa aranžmana, crveno upozorenje ako `urgent`, dugmad "Tok rezervacije"/"Izmeni"/"Otkaži" — poslednja dva namerno onemogućena uz `title` objašnjenje, nisu još povezana), sekcija "Nosilac i smeštaj", sekcija "Putnici" (ista `travelerAgeLabel` konvencija kao sažetak u desnom panelu, v1.47), sekcija "Finansije" (ukupno/uplaćeno/dug/status uplate, sa napomenom da je raščlana po stavkama otvorena za kasnije), sekcija "Operativno" (dobavljač/partner/poslovnica/zadužen).
2. `buildMockTimeline()` premešten iz `BookingsTable.tsx` u `mock-data.ts` (deljena funkcija, jedan izvor umesto dve kopije iste logike — koristi je i ikonica toka u listi i dugme "Tok rezervacije" u punom zapisu, novi `FullRecordTimelineButton.tsx`).
3. Otvaranje: (a) klik na broj rezervacije u `BookingsTable.tsx` (`e.stopPropagation()` — ne otvara i sažetak u desnom panelu, to ostaje na klik bilo gde drugde na redu, v1.46), (b) dugme "Otvori pun zapis" u `RightPanel.tsx` `BookingSummary` (kad je sažetak već otvoren). Oba pozivaju `useTabs().openTab('/rezervacije/lista/{broj}', broj)` — isti "namerna radnja uvek otvara nov tab" princip kao ostatak panela (dizajn dok. §5a/§5b), ne `window.open`, nego postojeći app-nivo tab sistem.
**Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju — `GET /rezervacije/lista/TT-2026-100042` (HTTP 200) potvrđuje sve četiri sekcije, putnike, sumu/dug, MOCK upozorenje; `GET /rezervacije/lista/TT-2026-999999` (nepostojeći broj) vraća poštenu poruku "nije pronađena", ne grešku; `GET /rezervacije/lista` potvrđuje 20 klikabilnih dugmadi "Otvori pun zapis rezervacije" (po jedno za svaki red). Stvarno otvaranje NOVOG APP-TABA klikom (React state/`openTab`) nije potvrđeno kroz pravi klik u browseru (isto ograničenje kao ostatak ove faze), logika potvrđena čitanjem koda. I dalje MOCK.
**Verzija:** 1.47 — Uzrast putnika, hitna obaveštenja, brzi filteri po tipu aranžmana, modul "Svi filteri" i Excel izvoz na mock listi (23.8.2026, na zahtev vlasnika, u istom prolazu: "Svuda Prikazati da li je osoba odrasla osoba, dete ili beba (navesti i godine rodjenja za decu i bebe, a za odrasle samo ukoliko je taj podatak unet)." + "U Listi rezervacija Dodati pored one tri linije i zvono za notifikaciju u crvenoj boji. kada se klikne na zvono da se otvori modul u kom pise sta je to sto treba sto pre uraditi. Postaviti filter i za zvonce. Prvi klik filtrira, drugi klik ne filtrira." + "Prva ikona ispred ove dve treba da bude ikona iz pretraga koja u stvari govori o kom turistickom aranzmanu se radi. Omoguciti pretragu i po tome. Staviti sve ikone oz pretrage horizontalno iznad liste rezervacija." + "Dodati iznad liste rezervacija i ikonu za Filtere... i dodatnim filterima (Poslovnica, User, Dobavljač, Partner...)" + "Omoguciti export liste rezervacija u excel ili gogle drive." — vlasnik je eksplicitno naveo da se za sada i dalje radi isključivo sa MOCK podacima: "moracete za sada jos da radite sa Mock podacima jer jios necemo u produkciju").
1. **Uzrast putnika.** `MockBookingRow.travelers[]` (mock-data.ts) proširen sa `ageCategory: 'ADULT'|'CHILD'|'BABY'` i opcionim `birthYear` — `RowSummaryContext`/`RightPanel.tsx` (`BookingSummary`) prikazuje kod svakog putnika "odrasla osoba"/"dete, rođ. GGGG."/"beba, rođ. GGGG.", godina rođenja obavezna za dete/bebu, opciona za odraslu osobu (prati stvarno postojeće `BookingItemGuest`/`ClientAccount` polje uzrasta, samo vrednosti su izmišljene za mock).
2. **Hitna obaveštenja (zvono).** Nov `MockBookingRow.urgent: { reason: string } | null`. U koloni ikonica (proširena sa 1 na do 3 ikonice po redu) crveno zvono se prikazuje SAMO kad `urgent` postoji; klik otvara nov `UrgentModal.tsx` sa razlogom. Toggle filter u novoj traci iznad tabele — prvi klik filtrira samo hitne, drugi klik ukida filter (`urgentOnly` boolean, ne trostruko stanje). Mock napomena u `UrgentModal.tsx`: pravi izvor bi kasnije bila ista M1 `AuditLogEntry`/M18 health-signal logika koja već postoji za operativna upozorenja, ne nov mehanizam.
3. **Ikonica tipa aranžmana + pretraga po tipu.** Nova prva ikonica u koloni (ispred hitne i workflow ikonice) prikazuje tip aranžmana korišćenjem POSTOJEĆEG registra `PRODUCT_ICONS` (`apps/panel/src/lib/search-product-types.ts`, isti niz od 9 ikona koji već koristi M17 pretraga) — uvezeno, ne duplirano. Sve ikone iz `PRODUCT_ICONS` (osim onih bez mapiranog tipa) sad se prikazuju i kao horizontalna traka brzih filtera IZNAD tabele (`productTypeFilter` state, klik postavlja/uklanja filter po `MockBookingRow.productType`).
4. **Modul "Svi filteri".** Novo dugme "Filteri" u traci iznad tabele otvara `FiltersModal.tsx` — DRUGI PRIKAZ istog stanja kao postojeći brzi filteri po koloni (ne paralelan mehanizam; izmena u modulu se odmah odražava u tabeli i obrnuto), plus četiri nova dodatna filtera: Poslovnica (select, `BRANCHES` mock lista), User/zadužen (select, `USERS` mock lista), Dobavljač i Partner (subagent/firma) — oba tekstualna, "pretraživaćemo dalje po potrebi" napomena u samom modulu jer vlasnik je najavio da će dodavati još. `MockBookingRow` proširen sa `branch`/`assignedUser`/`supplierName`/`partnerName`.
5. **Excel izvoz.** Novo dugme "Excel" u traci iznad tabele izvozi TRENUTNO FILTRIRANE redove (ono što je vlasnik stvarno video, ne uvek celu listu) preko novog `POST /api/rezervacije/export` (Next.js Route Handler, `apps/panel/src/app/api/rezervacije/export/route.ts`) koji koristi `exceljs` — ista biblioteka već odobrena u tehničkom steku 23.8.2026 za M15 BiTerminalAgent izveštaje (`report-generator.ts`), ovde samo ponovo iskorišćena, ne nova zavisnost; dodato eksplicitno u `apps/panel/package.json` (bilo je dostupno preko npm-workspaces hoisting-a, sad deklarisano direktno radi jasnoće) i pokrenut `npm install --workspace=apps/panel`. Dugme "Google Drive" NAMERNO trajno onemogućeno (title objašnjenje) — zahteva OAuth/Drive API kredencijale, nova stavka tehničkog steka (Master dokument poglavlje 6) koja čeka posebnu potvrdu vlasnika, nije prećutan propust.
**Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju — `GET /rezervacije/lista` (HTTP 200) HTML potvrđuje: traku "Filteri" dugme prisutno, `ageCategory`/`birthYear`/`urgent`/`productType` polja prisutna u podacima prosleđenim klijentskoj komponenti, ikonica kolona proširena (`w-[64px]`), crvena zvona (`codicon-bell`) prisutna za redove sa `urgent`, dugmad "Excel"/"Google Drive" prisutna. Klik-interakcije (toggle filtera, otvaranje `FiltersModal`/`UrgentModal`, stvaran preuzimanje `.xlsx` fajla) nisu uživo potvrđene kroz pravi klik u browseru (isto ograničenje kao ostatak ove faze), logika potvrđena čitanjem koda. Sve ovo ostaje MOCK — bez veze sa pravim `GET /bookings` (isto ograničenje kao v1.42-v1.46).
**Verzija:** 1.46 (istorijski, videti v1.48 na vrhu) — Destinacija/hotel ispod nosioca + sažetak reda u desnom panelu (23.8.2026, na zahtev vlasnika: "Ispod naziva nosioca, stavite naiv drzave, destinacije, hotela. Omogucite u pretrazi po kolonama da se i po ovim pojmovima pretrazuje" + "Kada otvorimo desni panel i kliknemo na neki red iz liste rezervacija u desnom panelu treba da se prikazu sve najvaznije informacije").
1. `MockBookingRow` (mock-data.ts) proširen sa `country`/`destinationCity`/`hotelName`/`accommodationType`/`travelers[]`/`paidAmount` — prate stvarna M2/M5/M10 polja (`Product.destination_country`/`destination_city`, `Product.name`, `RateLine.board_type`+`ContractPeriod.room_type`, `BookingItemGuest`, `Payment`), izmišljene su samo vrednosti. `BookingsTable.tsx`: destinacija/hotel prikazani kao pod-red ispod naziva nosioca; postojeći filter "Nosilac rezervacije" sad pretražuje i po državi/destinaciji/hotelu (nema posebne nove kolone — polja su pod-red, ne sopstvena kolona, pa isti filter obuhvata sve što se tu vidi).
2. **Prvi stvaran izvor za dizajn dok. §5b "sažetak reda"** — do sad specirano (18.8.2026) ali bez izvora (RightPanel.tsx komentar). Nov `apps/panel/src/components/RowSummaryContext.tsx` (odvojen od `SelectionContext` — različita svrha/oblik), `RightPanel.tsx` sad prikazuje `BookingSummary` kad postoji sažetak i nema aktivne selekcije. Klik na red liste (`BookingsTable.tsx`, ne na ikonicu toka rezervacije — `stopPropagation`) otvara desni panel (ako nije već otvoren, isti "pojavljuje se čim ima šta da pokaže" obrazac kao selekcija) sa: brojem, statusom, nosiocem, destinacijom/hotelom, tipom smeštaja, datumima, spiskom putnika, ukupnim iznosom, uplaćenim iznosom i dugom (`totalPrice - paidAmount`, izračunato na klijentu za mock — pravi izvor bi bio `Payment`/`ClientPaymentSchedule`, M10).
**Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju — `/rezervacije/lista` HTML potvrđuje sva nova polja (destinacija/hotel/putnici/tip smeštaja) prisutna u podacima prosleđenim klijentskoj komponenti. Stvaran klik na red i prikaz u desnom panelu su klijentske React interakcije — nisu uživo potvrđene kroz pravi klik u browseru (isto ograničenje kao ostatak ove faze), logika potvrđena čitanjem koda.
**Verzija:** 1.45 — Valuta ispod iznosa na mock listi (23.8.2026, na zahtev vlasnika: "stavite valutu ispod iznosa"). `BookingsTable.tsx`: kolona "Iznos" poništava raniji prikaz "iznos valuta" u jednom redu — `formatMoney()` razdvojen na `formatAmount()` (samo broj) + valuta u sitnijem tekstu u redu ispod. **Provera:** `tsc --noEmit` čist; uživo kroz pravu VLASNIK sesiju — svih 20 mock redova potvrđeno sa valutom u zasebnom `<div>` ispod iznosa.
**Verzija:** 1.44 — Datumski filteri (Dolazak/Odlazak/Kreirano) + premeštena kolona "Kreirano" na mock listi (23.8.2026, na zahtev vlasnika: "omogucite i pretragu za kolone dolazak, odlazak i kreirano... treba da se ukuca ovako 01/06/2026...10/06/2026" + "Kolonu Kreirano premestite izmedju kolona Broj i Nosilac"). `BookingsTable.tsx`: tri datumske kolone dobijaju isti tip filter-polja kao tekstualne, ali sa opsegom `DD/MM/GGGG...DD/MM/GGGG` (razdvojeno sa `...`) umesto "sadrži" pretrage — `parseDDMMYYYY`/`matchesDateRange` helperi, nepotpun/neispravan unos (dok korisnik kuca) namerno NE filtrira ništa (ne prazni tabelu naglo na svaki taster). Kolona "Kreirano" premeštena sa poslednjeg mesta na mesto odmah posle "Broj" (pre "Nosilac rezervacije"), u header-u i u redovima podataka.
**Verzija:** 1.43 — Pravi `GET /sales/bookings/:id/history` (tok rezervacije) + filteri po koloni na mock listi (23.8.2026, na zahtev vlasnika: "u listi rezervacija pre naziva statusa stavite ikonu... klikom na tu ikonu treba da se otvori modul u kom ce se videti čitav workflow te rezervacije od pocetka do trenutka kada se gleda status sa datumima, vremenima i ko je radio promenu" + "Omogucite pretragu po kolonama. Odmah ispod naziva kolone staviti filter za pretragu").
1. **Nov, STVARAN endpoint** `GET /sales/bookings/:id/history` (`BookingsController`/`BookingsService.history()`) — ne uvodi novi mehanizam, čita postojeći M1 append-only `AuditLogEntry` (poglavlje 11 napomena, već postojeća, ovaj dodatak je prvi koji je stvarno KORISTI za prikaz) filtriran na `resourceType=Booking, resourceId=:id`, hronološki (rastuće), dopunjen čitljivim imenom aktera (`AuditLogEntry.actor_id` → `User.full_name` lookup, pošto audit zapis čuva samo ID). Ista vidljivost/dozvola kao pregled same rezervacije (`M5/booking/VIEW`) — nema novog gate-a, ako smeš da vidiš rezervaciju smeš i njen tok. `AuditLogService` (M1) dobija novu, opštu metodu `findByResource(resourceType, resourceId)` — radi za bilo koji resurs, ne samo Booking.
2. **Panel:** `apps/panel/src/components/BookingTimelineModal.tsx` (nov, deljen) — hronološki prikaz (vremenska linija), poziva se sa pravog ID-ja rezervacije (stvaran `GET .../history` preko nove BFF rute) ili sa `mockEntries` (izmišljen ali realno-oblikovan tok — koristi ga ISKLJUČIVO mock lista ispod, jasno obeležen "MOCK" u naslovu). Ikonica (`three-bars` Codicon — tri kratke horizontalne linije, na zahtev vlasnika) dodata PRE statusa: na pravom ekranu detalja rezervacije (`rezervacije/[id]/page.tsx`, nov `BookingHistoryButton.tsx`) I na mock listi (`rezervacije/lista/BookingsTable.tsx`, po redu).
3. **Filteri po koloni** na mock listi — `BookingsTable.tsx` (nova klijentska komponenta, `page.tsx` ostaje tanak server wrapper) dobija input polje ISPOD svakog naziva kolone (Broj/Nosilac/Kanal/Status/Uplata) — pravo, funkcionalno filtriranje (case-insensitive "sadrži", sve aktivne kolone se AND-uju) nad mock nizom, ne mock samo po sebi. I dalje MOCK podaci (ne `GET /bookings`) — filtriranje je stvarna, radna logika nad izmišljenim redovima, priprema tačan UI obrazac za kad lista dobije prave podatke.

**Provera:** `tsc --noEmit` čist za `apps/api` i `apps/panel`; uživo kroz pravu VLASNIK sesiju — `GET /sales/bookings/:id/history` na stvarnu rezervaciju vraća `200 []` (ta konkretna rezervacija nema audit trag jer je uneta mimo normalnog toka — ispravno prazno stanje, ne greška); `/rezervacije/lista` HTML sadrži tačno 20 `three-bars` ikonica (po jedna po mock redu) i 5 filter polja (po kolona); `/rezervacije/:id` HTML sadrži `three-bars` ikonicu pored statusa. Klik/otvaranje modala i stvaran unos u filter polje su klijentske React interakcije — nisu uživo potvrđene kroz pravi klik u browseru (isto ograničenje kao ostatak ove faze), logika potvrđena čitanjem koda.
**Verzija:** 1.42 — Nov MOCK ekran "Lista rezervacija" (23.8.2026, na zahtev vlasnika: "kreiraj Mock listu rezervacija jedno 20 koje će se prikazivati u centralnom panelu, pa ćemo da vidimo kako ćemo dalje, a filtere tada pojavite u levom panelu") — prvi, namerno mali korak pre nego što se filter-panel (poglavlje 13, novi unos) uopšte specificira. `apps/panel/src/app/(app)/rezervacije/lista/mock-data.ts` — 20 izmišljenih redova, oblik prati STVARAN `Booking`/`BookingItem` Prisma model (bookingNumber/buyerName/buyerType/channel/status/paymentStatus/stayFrom/stayTo/totalPrice/currency/createdAt) da mock ne izmišlja polja koja ne postoje, samo vrednosti u njima su lažne. `page.tsx` renderuje tabelu u centralnom panelu sa vidljivim upozorenjem "MOCK prikaz — izmišljeni podaci, ne dolaze iz baze"; nov nav unos "Lista rezervacija (mock)" (`lib/nav.ts`, grupa "Prodaja") — naziv namerno nosi "(mock)" da se ne pobrka sa gotovom funkcijom. NAMERNO bez poziva `GET /bookings` i bez filtera — to je sledeći, još nedefinisan korak (poglavlje 13). **Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu prijavu — `GET /rezervacije/lista` vraća HTTP 200, svih 20 mock brojeva rezervacija (`TT-2026-100042`…`TT-2026-100061`) prisutno u HTML-u, upozorenje i nav labela potvrđeni. Test nalog obrisan posle provere.

**Verzija:** 1.41 — 4 od 6 tip-specifičnih `GET /search` parametara iz v1.28 KONAČNO ožičeni u kod (22.8.2026, na zahtev vlasnika, otkriveno pri radu na M17 popup pretrazi — v1.28 je opisao ove parametre kao "dobija nove opcione parametre", ali `search-query.dto.ts` ih nikad nije stvarno primio, provereno čitanjem koda pre nego što je popup obećao polja koja ne bi ništa radila). `SearchQueryDto` dobija `cabinClass`/`minDriverAge`/`durationNights`/`cabinType`; `SearchService.search()` filtrira nad `Product.attributes` (M2 spec §2.3 konvencija: `cabin_class` za FLIGHT, `min_driver_age` za TRANSPORT/RENT_A_CAR, `duration_nights`/`cabin_types[].category` za CRUISE) u JS-u nad već dobijenim skupom proizvoda, isti princip kao postojeće SOLD_OUT post-filtriranje — bezbednije od Prisma JSON path operatora nad ugnježdenim nizom (`cabin_types[]`), izbegava dupliranje `where` grananja. `minDriverAge` propušta proizvod bez tog atributa (ne isključuje ono što nije eksplicitno ograničeno). **`origin_city`/`trip_cost` NAMERNO IZOSTALI** — `origin_city` bi filtrirao ugnježđen `attributes.route` objekat čiji tačan oblik ključeva (`route.origin` vs. nešto drugo) M2 spec §2.3 nikad nije precizirao za FLIGHT/TRANSFER/TRANSPORT (samo kaže "strukturirano polazište/odredište" bez imenovanja podključeva) — dodavanje bez potvrđene konvencije bi značilo izmišljanje sheme na licu mesta; `trip_cost` (INSURANCE) M2 spec §2.3 EKSPLICITNO kaže da nije svojstvo `Product`-a nego parametar ponude/kvota — filtriranje PROIZVODA po njemu strukturno nema smisla (svaka polisa bi "odgovarala" svakoj vrednosti, nema šta da se poredi). Oba ostaju otvorena stavka (`docs/analize/27-BACKLOG-IDEJA-I-PREDLOZI.md`, M5 sekcija), čekaju ili potvrdu konvencije za `route` ključeve ili razjašnjenje da li `trip_cost` uopšte treba da bude search-parametar. **Provera:** `tsc --noEmit` čist; 9/9 `search.service.spec.ts` (4 nova testa, po jedan za svaki filter, dva proizvoda koji se razlikuju SAMO u `attributes` da se izoluje da filter bira po pravom polju); uživo `GET /sales/search` sa sva tri nova parametra (`cabinClass`, `durationNights`+`cabinType`, `minDriverAge`) vraća 200 bez validacione greške (prazni rezultati očekivani — nema FLIGHT/CRUISE/TRANSPORT proizvoda u dev bazi da bi se pravi pogodak uživo video, nadoknađeno jediničnim testovima koji ne zavise od sadržaja baze). M17 panel (`SearchCriteriaPopup.tsx`) prikazuje ova polja SAMO kad je izabran odgovarajući tip (v1.80, M17 spec). v1.40 — poglavlje 3.0e.3 implementirano (21.8.2026, na zahtev vlasnika): desni panel (M17) sad stvarno prikazuje selekciju stavki iz pretrage — "Dodaj" na kartici/redu (`QuoteButton.tsx`, preimenovano po ulozi) stavlja stavku u klijentsku selekciju (`SelectionContext.tsx`, novo) umesto da odmah kreira Ponudu; panel se sam otvara čim prva stavka uđe u selekciju (Shell.tsx `onFirstAdd`, isti duh kao "predlog se pojavljuje odmah po dodavanju", §6d); svaka stavka prikazuje cenu, `API`-stavke odbrojavanje isteka (`quoteExpiresAt`), zbir po valuti sa upozorenjem ako selekcija sadrži više valuta; "Napravi ponudu" šalje SVE stavke u jednom `POST /quotes` pozivu (`createQuoteFromSelection`, novi server action) — `CreateQuoteDto.items[]` je već podržavao ovo na API strani, samo panel nikad nije slao više od jedne stavke. **Namerno van obima ove dopune:** deterministička mapa unakrsne prodaje (§3.0e.1 — predlog dopune posle dodavanja), AI pretraga unutar rezultata (§3.0e.2) — obe čekaju sledeći prolaz. **Napomena o ranije prijavljenom nalazu** ("desni panel se sam otvara na katalogu", v1.26 "Otvoreno za dalje"): pri ponovnoj proveri koda nije pronađen nijedan automatski okidač van jednog dugmeta u gornjoj traci; vlasnik je razjasnio (21.8.2026) da je očekivano ponašanje zapravo ovo — kontekstno "izdvajanje" po ekranu koje do sada nije bilo ožičeno ni na jednom ekranu (`RightPanel.tsx` je bio iskren placeholder). Nalaz se smatra razjašnjenim, ne bagom; preostaje isti obrazac za druge ekrane (npr. CRM lista → sažetak gosta) kao poseban naredni korak. **Provera:** `tsc --noEmit` čist u `apps/panel`; uživo kroz pravi login — `GET /rezervacije/pretraga?...` (HTTP 200) sadrži dugme "dodaj" na stvarnim rezultatima. Klik/interakcija (dodavanje u selekciju, auto-otvaranje panela, "Napravi ponudu") nije potvrđena uživo — nema pravog browser-a u ovom okruženju, samo statička/server-render provera.

**Verzija:** 1.39 — poglavlje 13 zatvoreno (21.8.2026, na zahtev vlasnika): `CRUISE` dodat u `ProductType` enum (Prisma migracija `add_cruise_product_type`, `apps/api/prisma/schema.prisma`), primenjena na dev bazu. Zatvara nalaz iz v1.38 — M2 spec je od 17.8.2026 ispravno definisala `CRUISE` (poglavlje 2.1/2.3), samo kod nije bio usklađen. Enum sam po sebi nije bio dovoljan — pronađene i ispravljene dve ručno održavane liste vrednosti koje su i dalje isključivale `CRUISE` posle migracije (obe bi tiho odbijale `type=CRUISE` uprkos važećem enumu): `SearchQueryDto.PRODUCT_TYPES` (poglavlje 11, `search-query.dto.ts` — `@IsIn` validacija za `GET /search`) i `productTypeEnum` u M16 `mcp-server.factory.ts` (isti parametar izložen spoljnim AI agentima preko MCP alata, M16 spec). **Provera:** `tsc --noEmit` čist u `apps/api` i `apps/panel`; API dev server restartovan sa svežim Prisma klijentom (stara `dist/src/main` instanca iz ranije build/dev kolizije — zamka 5.7 — usput identifikovana i ugašena, držala je port 3000 sa zastarelim kodom bez `CRUISE`); uživo kroz pravi login — `GET /rezervacije/pretraga?type=CRUISE&destinationCountry=Grčka` vraća HTTP 200 sa poštenim praznim stanjem "Nema rezultata" (nema `CRUISE` proizvoda u dev bazi), ne 400 validacionu grešku kakva je vraćana pre ispravke.

**Verzija:** 1.38 — poglavlje 13 dopunjeno (19.8.2026): otkriven i upisan nalaz da `CRUISE` (M2 spec poglavlje 2.1/2.3) nikad nije migriran u `schema.prisma` — otkriveno pri implementaciji M17 ikonice "Krstarenja" (M17 spec v1.27), nije ispravljeno u ovom prolazu.

**Verzija:** 1.37 — novo poglavlje 3.1a (19.8.2026, na zahtev vlasnika): deljenje Ponude preko WhatsApp/Viber/Telegram — `Quote.share_token` (isti obrazac kao `Itinerary.share_token`, poglavlje 3.0.5), javan `GET /quotes/shared/:shareToken` (gostu vidljiva cena, bez nabavne cene/marže/dobavljača), dugme "Pošalji ponudu" sastavlja gotov tekst i otvara web-intent link ka izabranoj platformi — jednosmerno, bez bota/naloga, agent ne kuca ništa ručno. v1.36 — četiri nova poglavlja uz Itinerary (18.8.2026, na zahtev vlasnika): 3.0.5 deljiv link ka nacrtu (`share_token`, javan `GET`/`PATCH` preko tokena, gost sme isključivo da uključi/isključi segment, isti obrazac kao M23 `shareToken`); 3.0.6 kloniranje nacrta (`POST /itineraries/:id/clone`, brza varijanta istog putovanja); 3.0.7 deterministička upozorenja o hronološkoj nedoslednosti segmenata (`DATE_OVERLAP`, `MISSING_TRANSFER` — informativna, ne blokiraju konverziju); 3.0.8 PDF izvoz nacrta (`POST /itineraries/:id/generate-pdf`, isti generator kao vaučer/ugovor). v1.35 — poglavlje 3.0.2/3.0.3/3.0.4 dopunjeno (18.8.2026, na zahtev vlasnika): "Individualni paketi" sad znači da preostalih 8 ikona pretrage postaju dugmad za dodavanje segmenta u tekući `Itinerary`, sa uključi/isključi po segmentu (`ItinerarySegment.is_included`, novo) bez brisanja; desni panel prikazuje okvirnu cenu po segmentu i tekući zbir (`preview_final_price` snimljen sa izabranog `SearchResultOffer` — informativno, nikad obavezujuće, uvek ponovo izračunato pri `to-quote` konverziji, poglavlje 3.0.3). Stari "poglavlje 3.0.4 van obima" premešten na 3.0.4a. v1.34 — novo poglavlje 3.0f (18.8.2026, na zahtev vlasnika): treća vrednost `source_type = MANUAL` na `QuoteItem`/`BookingItem` za proizvod van kataloga — `ManualProductEntry` (opisni podaci za ponovni izbor, nikad cena, poglavlje 3.0f.1), hotel sa nalepljenog linka preko M15 (tekst §6.5.6b + nove slike §6.5.6c tog spec-a, poglavlje 3.0f.2), cena po kanalu (interni tim unosi sve, subagent ne unosi cenu uopšte, poglavlje 3.0f.3), pregled osoblja za stavke subagenta po istom obrascu kao M7 "Gejt B" — status "upit" dok se ne odobri (poglavlje 3.0f.4), potvrda bez poziva M3/M4 (poglavlje 3.0f.5). `POST /quotes/:id/confirm` sad odbija dok ijedna stavka čeka pregled osoblja (poglavlje 4, novi korak 0). Nove dozvole `M5/manual-item/CREATE`/`REVIEW` (poglavlje 10), novi endpoint-i (poglavlje 11). v1.33 — novo poglavlje 4.4 (18.8.2026, na zahtev vlasnika): UI tok "Ponuda → Rezervacija" u M17/M7 — redosled ekrana (tiha provera cene → kupac → gosti → tip_nastupanja samo u graničnom slučaju → status ugovora, ne blokira INTERNAL_PANEL/PHONE → plaćanje informativno → potvrda → rezultat u novom tabu, potvrđeno kao namerna radnja); oblik forme (jedna duga forma naspram stepper-a) postaje korisnički izbor čuvan u novom M1 `UserPreference` (M1 spec §3.9, dopuna istim prolazom). v1.32 — novo poglavlje 3.0c.3a (18.8.2026, na zahtev vlasnika): dva vrlo vidljiva, uvek otvorena brza filtera — Refundabilno/Nerefundabilno (novo `SearchResultOffer.is_refundable`, poglavlje 3.0b.2 — izračunato za `CONTRACTED` iz `CancellationRule` prozora, `null`/nepoznato za `API` dok se ne potvrdi po provajderu) i Odmah potvrda/Upit (postojeći `availability_status`). Oba klijentska filtera, isti princip kao "vrsta usluge". v1.31 — novo poglavlje 3.0b.4 (18.8.2026, na zahtev vlasnika): svaka kartica/red rezultata pretrage dobija radnju "Info" koja na zahtev korisnika (M17/M7, ne M8) otvara desni panel sa M23 sadržajem o proizvodu/destinaciji (`GET /knowledge/articles?productId=`/`?destinationCountry=&destinationCity=`, novi opcioni filteri, dopuna M23 spec poglavlja 8 istim prolazom); bez pogotka prikazuje prazno stanje, ne izmišlja sadržaj. v1.30 — poglavlje 13 dopunjeno (17.8.2026): nalepljen link sa tuđeg sajta — bezbedan deo (tumačenje URL-a, pokretanje naše pretrage) specificiran u M15 spec poglavlju 6.5.6a; poređenje sa stvarnom tuđom cenom (scraping) namerno ostaje otvoreno pitanje, čeka pravnika, nije specirano. v1.29 — poglavlje 3.0e (novo, 17.8.2026, isti dan kao v1.28): deterministička mapa unakrsne prodaje (predlog dopuna odmah po dodavanju stavke u selekciju), AI pretraga unutar rezultata (isti M15 omnisearch, sa kontekstom trenutnih rezultata — ne nova pretraga), odbrojavanje isteka cene/upozorenje na više valuta u selekciji. Letovi (poglavlje 3.0d.1) dobili pun skup filtera istražen naspram Google Flights (presedanja/kompanija/cena/vreme/trajanje/prtljag/obližnji aerodromi) uz eksplicitnu napomenu da GDS-oblik multi-segment rezultata čeka M4 avio adapter. AI prepoznavanje više namera u jednoj rečenici namerno odloženo (poglavlje 3.0e.4) dok osnovni tok ne bude uživo proveren. v1.28 — poglavlje 3.0d (novo, 17.8.2026, na zahtev vlasnika, isti dan kao v1.27): vođena pretraga za preostalih 8 vrsta proizvoda (letovi, transferi, rent-a-car, things-to-do, individualni paketi, grupni paketi, krstarenja, putno osiguranje) — istraženo naspram Google Flights/Skyscanner/Booking.com Cars i Cruises/Viator/InsureMyTrip. `GET /search` dobija nove opcione parametre (`origin_city`/`cabin_class`/`driver_age`/`duration_nights`/`cabin_type`/`trip_cost`, poglavlje 11). Individualni paketi potvrđeni kao postojeći `Itinerary` tok, ne nov tip; "Grupna putovanja" i "Grupni paketi" potvrđeni kao ista stavka. v1.27 — poglavlje 3.0c (novo, 17.8.2026, na zahtev vlasnika): vođena pretraga smeštaja — `GET /search/countries`/`GET /search/destinations` (kaskadni geografski koraci sa prečicom na ime hotela), `GET /search` dobija `amenity_tags[]` parametar (M2 poglavlje 2.3c), AI kontekstualna pretraga na dnu ekrana preko postojećeg M15 omnisearch-a (bez novog mehanizma). Potvrđuje da `occupancy.room_config[]` (poglavlje 3.2a) već tačno odgovara vlasnikovom opisu unosa soba/gostiju/uzrasta — nema izmene tog dela modela. v1.26 — bag ispravljen pri implementaciji M17 (avgust 2026): `GET /search` (poglavlje 11) nikad nije stvarno radio za `channel=INTERNAL_PANEL` — `SearchQueryDto.channel` je bio tipiziran kao `VisibleChannel` (samo `B2C_SITE`/`B2B_PORTAL`/`MOBILE`, M2 spec §2.1), pa je interni panel dobijao `400` pri svakom pokušaju; čak i da je vrednost bila prihvaćena, `Product.visible_channels` strukturno ne može sadržati `INTERNAL_PANEL` (taj enum ostaje uži, M2 spec §2.1/§5.1 — kontroliše samo javne/samoposlužne kanale), pa bi filter `visibleChannels: { has: 'INTERNAL_PANEL' }` uvek vratio nula proizvoda. Ispravljeno: `search-query.dto.ts` sad prihvata `SearchChannel = VisibleChannel | 'INTERNAL_PANEL'`; `search.service.ts` preskače `visibleChannels` filter kad je `channel = INTERNAL_PANEL` (tim vidi svaki `ACTIVE` proizvod, u skladu sa M2 spec §5.1 — to polje kontroliše samo gde se proizvod PRIKAZUJE gostima/subagentima, ne interni pristup). Pošto je `GET /search` namerno bez guard-a (v1.22, javna M8 pretraga), `SearchController` sad ručno proverava JWT + `M5/booking/VIEW` SAMO za `channel=INTERNAL_PANEL` (anonimni pozivi sa `B2C_SITE`/`B2B_PORTAL`/`MOBILE` ostaju nepromenjeni, i dalje bez guard-a) — sprečava da ova vrednost postane rupa za anonimne pozive. Otkriveno jer je M5 Faza 1 izlazni kriterijum ("tim može ručno da pretraži") formalno postojao od Faze 1, ali stvarno nikad nije bio proveren uživo dok M17 nije prvi put pozvao ovaj put. v1.25 — M9 implementiran (avgust 2026, deo za vodiče na terenu): dodato `PATCH /bookings/items/:itemId/assign-guide` (poglavlje 11, zahteva `M5/booking/MODIFY`) — postavlja/uklanja `BookingItem.assigned_guide_id` (polje je postojalo u šemi od ranije, dodato unapred pri specifikaciji M9, ali dosad nije imalo endpoint za izmenu); `M9 FieldStaffService` čita ovo polje in-process preko Prisma (isti obrazac kao M14 `relatedBooking`), ne poziva M5 API. v1.24 — M16 implementiran (avgust 2026): `resolveApiContext` (bookings.service.ts) i `QuotesService.create` dobili granu za `User.account_type = AI_AGENT` (M1 šema — polje postojalo od ranije, nikad ožičeno) — isto B2C maskiranje kao Gost, `ownProfileId` je direktno `ClientAccount.id` (bez posrednika, jednostavnije od SUBAGENT_CONTACT grane); dodata vrednost `MCP_AGENT` u `M5Channel` enum (poglavlje 11) da rezervacije preko MCP kanala budu vidljivo razdvojene u izveštajima (M13) od ostalih kanala; `MCP_AGENT` dodat i u `SELF_SERVICE_CHANNELS` (poglavlje 4.0a, `tip-nastupanja.ts`) — otkriveno pri ručnoj verifikaciji da je bez ovoga clickwrap provera (`contract_terms_accepted`, poglavlje 3.1) tiho preskočena za MCP rezervacije, jer `isSelfServiceChannel()` nije prepoznavala novu vrednost enuma; M16 `create_quote` alat sada zahteva `contract_terms_accepted=true` u svakom pozivu (isti zahtev kao B2C sajt). **Prateći bezbednosni nalaz, otkriven pri M16 verifikaciji:** `BookingsService.confirmQuote()` je od uvek vraćao NEMASKIRAN prikaz rezervacije (hardkodovano `this.findOne(booking.id, 'INTERNAL_PANEL')`, bez obzira ko poziva) — pogađalo je svaki samouslužni kanal koji odmah čita povratnu vrednost potvrde (M8 bankovni prenos, sada i M16 `confirm_booking`), ne samo M16. Ispravljeno na `this.findOne(booking.id, actor.userId)` — ista ispravna B2C/B2B/INTERNAL_PANEL grana kao `findOne`/`findAll` (poglavlje 6.2). v1.23 — M7 implementiran (avgust 2026): `ComplianceStubsService.checkCreditLimitIfSubagent`/`isActiveSubagentWithinCreditLimit` (poglavlje 4 korak 1b, poglavlje 6.3) više nisu no-op stub, pozivaju stvaran M7 `SubagentsService` in-process preko `SubagentStubService` (m5-rezervacije/common) — isti obrazac kao v1.18 (M11); `QuotesService.create` prepoznaje `Subagent` zapis za `Quote.client_account_id` i primenjuje `effective_commission_percentage` (M7 poglavlje 5) umesto M6 lojalnosti kad zapis postoji; `BookingsService.resolveApiContext` sada ispravno mapira `User.linked_profile_id` (Subagent.id za `SUBAGENT_CONTACT`) na pravi `ClientAccount.id` pre poređenja ownership-a (ranije nedostajalo, otkriveno pri implementaciji M7) — zatvara poslednje dve stavke izlaznog kriterijuma (poglavlje 12) koje su čekale M7; v1.22 — dopuna (avgust 2026, priprema za M8): `GET /search` (poglavlje 11) postaje javan (uklonjen `JwtAuthGuard`) — ranija verzija je zahtevala prijavu, neusklađeno sa M8 zahtevom za anonimnu pretragu; v1.21 — dopuna (avgust 2026, priprema za implementaciju M8): poglavlje 6.2 dobija eksplicitan mehanizam izbora masking konteksta (`GET /bookings/:id` čita `account_type` iz JWT-a na serveru, ne iz klijentskog parametra) — dosadašnja implementacija je uvek vraćala `INTERNAL_PANEL` prikaz, ovim se zatvara stavka izlaznog kriterijuma (poglavlje 12, linija o M7/M8/M9-gost kontekstu); v1.20 — dopuna (avgust 2026, otkriveno pri implementaciji M6): `BookingStatus.COMPLETED` je postojao u enumu od početka ali nikad nije bio definisan mehanizam prelaza u njega — dodato poglavlje 6.1a (periodičan posao prevodi `CONFIRMED`/`MODIFIED` rezervaciju u `COMPLETED` kad prođe poslednji `stay_to` od neotkazanih stavki, emituje `booking.completed`) i dopunjena lista događaja u poglavlju 9 — M6 poglavlje 4.3 (anketa posle putovanja) zavisi od ovog prelaza; v1.19 — M20 implementiran (avgust 2026): dodato `Booking.contract_terms_accepted_at` (poglavlje 4.1, kopija istog polja sa `Quote` u trenutku potvrde — `Booking` nema `quote_id` referencu nazad, pa se prenosi samo ono što je M20 stvarno potrebno, isti obrazac kao v1.17 buyer polja); `ClientContractStubService.hasGeneratedContract` (poglavlje 6, dodatni uslov za vaučer) više nije no-op stub, poziva stvaran `ClientContractsService` in-process — zatvara stavku izlaznog kriterijuma; v1.18 — M11 implementiran (avgust 2026): `ComplianceStubsService.checkTravelGuaranteeUtilization` (poglavlje 4, korak 1a) više nije no-op stub, poziva stvaran `TravelGuaranteeService` in-process (isti obrazac cross-modularnog poziva kao M10 PaymentsService → M5 BookingsService) — zatvara stavku izlaznog kriterijuma; v1.17 — dodato `Booking.buyer_name`/`buyer_type`/`buyer_tax_id` (poglavlje 4.1, avgust 2026) — otkriveno pri implementaciji M10: M10 spec §1.1 pretpostavlja da `client_account_id` nosi minimalno naziv/tip/PIB kupca, ali to polje je uvek bio goli FK bez tih podataka; dopuna dodaje tri nova polja koja se unose u `POST /quotes/:id/confirm` (ne čekaju M6 Nalogodavac) i koja M10 koristi za SEF/ESIR izbor tipa fiskalnog dokumenta; v1.16 — dodato `POST /supplier-manifests/prepare-batch` (poglavlje 8.4/11, na zahtev vlasnika, avgust 2026) — proširuje ad-hoc pripremu iz v1.15 sa jedne rezervacije na (a) ručno izabranu listu rezervacija (checkbox u M17, isključivo — ignoriše ostale filtere kad je prosleđeno) ili (b) kombinaciju filtera koji se logički I-uju: opseg kreiranja rezervacije, opseg boravka (preklapanje), opseg dolaska, opseg odlaska, status rezervacije — bar jedan mora biti prisutan; isti princip automatskog grupisanja po dobavljaču kao v1.15. Namerno NE uvodi prirodno-jezički AI interfejs niti bilo kakvo AI-inicirano slanje — priprema nacrta ostaje nivo "Autonomno" (kao i do sada), slanje ostaje isključivo ljudski klik, nepromenjeno iz poglavlja 8.4; v1.15 — dodato `POST /bookings/:id/prepare-supplier-manifests` (poglavlje 8.4/11, na zahtev vlasnika, avgust 2026) — ad-hoc priprema najave za JEDNU rezervaciju odmah, bez čekanja na periodični posao; ako rezervacija ima stavke od više različitih dobavljača, priprema se po jedan DRAFT `SupplierManifest` za svakog, automatski grupisano — potvrđeno da je ovo ponašanje već postojalo za periodično agregiranje (poglavlje 8.1, `SupplierManifest` je uvek vezan za tačno jednog dobavljača), dopuna dodaje samo ad-hoc okidač po rezervaciji; v1.14 — dopuna `QuoteItem`/`BookingItem` (poglavlje 3.2/4.2), na osnovu nalaza pri implementaciji (avgust 2026): `unit_count` (broj rezervisanih jedinica/soba, iz `occupancy.room_config.length`) — bez ovog polja, otkazivanje višesobne rezervacije je oslobađalo nazad u M3 samo jednu sobu umesto svih, jer `BookingItem` do sada nije pamtio koliko je jedinica stvarno rezervisano; i `cancellation_policy_snapshot` (snimak M4 `AvailabilityQuote.cancellationPolicy` u trenutku građenja stavke, isti strukturirani oblik kao M3 `CancellationRule`) — omogućava da se procenat povraćaja pri otkazivanju API stavke računa deterministički (poglavlje 6), na isti način kao za CONTRACTED stavke, umesto da ostane trajno prazan; v1.13 dodato `Quote`/`Booking.referral_tracking_code` (M12 poglavlje 3a, avgust 2026) — M5 samo prenosi sirov kod iz M8, bez validacije, zatvara deo M12↔M13 integracione praznine iz backlog-a; v1.12 na zahtev vlasnika (avgust 2026), definisan oblik odgovora `GET /search` (poglavlje 3.0b, `SearchResultProduct`/`SearchResultOffer`) i eksplicitan prelaz izabranog rezultata u `Quote`/`ItinerarySegment`, uključujući proveru isteka `quote_expires_at` pre kreiranja stavke ponude — do sada su bili definisani samo ulazni parametri pretrage; v1.11 na zahtev vlasnika, rešava problem #11: `SupplierManifest` i novi `SupplierChangeNotice` (izmena/storno) dobijaju `reference_code` i idu isključivo kroz jedinstveno M22 sanduče za dobavljače; automatsko poklapanje pristigle potvrde po referentnom kodu (poglavlje 8.8) — potvrda i dalje uvek zahteva ljudski klik, ni pri visokoj pouzdanosti; v1.10 na osnovu analize stvarnih cenovnika više dobavljača: dodata formula za `base_cost` sobe sa mešovitim uzrastom gostiju, koristeći M3 `RateLine.price_basis`/`age_pricing[]` (poglavlje 3.2b); v1.9 — `occupancy.room_config` (poglavlje 3.2a) postao strukturiran niz po sobi (`room_type_code`, `adults`, `children`, `children_ages`) umesto nedefinisanog bloba, uz pravilo slaganja sa zbirnim `adults`/`children`; v1.8 — `GET /search` `type` parametar postao niz (multi-select), podržava tri nova M2 tipa (`TRANSPORT`/`TICKET`/`EVENT`, poglavlje 11) — dopunjuje M2 poglavlje 2.3; v1.7 (retroaktivno zabeleženo, nije imalo sopstveni changelog unos u trenutku kad je dodato) — poglavlja 8.6 (najava dobavljaču kao formalni koncept) i 8.7 (`SupplierAnnouncementRule`), zatvaraju probleme #2/#3 sa liste; v1.6 — rešeni nalazi iz `VALIDACIJA-WORKFLOW-B2C.md`/`VALIDACIJA-WORKFLOW-B2B.md` (avgust 2026, na zahtev vlasnika): dodato `Quote.contract_terms_accepted*` (poglavlje 3.1), automatsko izvođenje `tip_nastupanja` za samouslužne kanale (poglavlje 4.0a), redosled provere garancije/kreditnog limita (poglavlje 4, korak 1), eksplicitni parametri `GET /search` (poglavlje 11), sistemski izuzetak izdavanja vaučera za subagenta unutar odobrenog kredita (poglavlje 6.3); v1.5 dodato pravilo skrivanja identiteta dobavljača od B2C/B2B kanala (poglavlje 6.2), na zahtev vlasnika (avgust 2026), dopunjuje M2 poglavlje 5.1; v1.4 dodato opciono sastavljanje putovanja pre Ponude, za kompleksna višedestinacijska putovanja (poglavlje 3.0), poređenjem sa Travel Compositor portfolio modelom (istraživanje 2.8.2026, vidi Dodatak A Master dokumenta); v1.3 dodati podsetnici/alarmi posle potvrde rezervacije (poglavlje 6.1: neplaćena rezervacija sa izdatim vaučerom, otvorena potvrda dobavljača po stavci, vaučer koji nedostaje uprkos punoj uplati); v1.2 dodala izbor jezika operativne liste za dobavljača (poglavlje 8.3); v1.1 dodala konvenciju celobrojnih novčanih iznosa (poglavlje 2) — v1.1/v1.2 poređenjem sa PrimeTravel analizom (`22-ANALIZA-PRIMETRAVEL-NALAZI.md`)
**Zavisi od:** M1, M2, M3, M4; od avgusta 2026 i M22 (poglavlje 8.8, jedinstveno sanduče za potvrde dobavljača)

---

## 1. Svrha i obim modula

M5 vodi gosta/agenta kroz tok **Search → Ponuda → Potvrda → Upravljanje rezervacijom**, bez obzira da li proizvod dolazi iz M3 (ugovoren) ili M4 (API). M5 je jedino mesto gde se pravi konačna prodajna cena (nabavna cena + marža) i jedino mesto koje sme da zatraži rezervaciju kapaciteta kod M3 ili M4.

M5 takođe izlaže **kalendarski pregled rezervacija po datumu** — dolasci, odlasci i stavke u toku za izabrani dan (poglavlje 7) — i generiše/šalje **operativne liste ka dobavljačima** (rooming liste, spiskovi putnika i sl., za `CONTRACTED` stavke), suprotan smer komunikacije od vaučera koji ide gostu (poglavlje 6), detaljno u poglavlju 8.

Van obima: naplata i fiskalizacija (M10, Faza 2), garancija putovanja (M11, Faza 2), CRM istorija gosta (M6, Faza 3) — M5 samo emituje događaje koje ti moduli kasnije koriste (poglavlje 9).

---

## 2. Model marže (mark-up)

Potvrđeno: potreban je fleksibilan sistem koji podržava procenat, fiksan iznos, i kombinaciju oba, sa podrazumevanom vrednošću po dobavljaču/provajderu koja se može override-ovati na finijem nivou.

**Konvencija za novčane vrednosti:** svi novčani iznosi (`base_cost`, `final_price`, `fixed_amount`, `total_price`, itd.) čuvaju se kao `integer` u najmanjoj jedinici valute (para/cent), nikad kao `decimal`/float — ista konvencija kao M3 (poglavlje 2) i M10 (poglavlje 3.2, kanonski izvor pravila). `percentage` ostaje `decimal` jer nije novčani iznos.

### 2.1 `MarkupRule`
| Polje | Tip | Napomena |
| :---- | :---- | :---- |
| id | UUID (PK) | |
| scope_type | enum: `M3_SUPPLIER`, `M3_CONTRACT`, `M3_CONTRACT_PERIOD`, `M4_PROVIDER`, `M2_PRODUCT` | nivo na koji se pravilo odnosi |
| scope_id | UUID | referenca ka entitetu tog nivoa |
| percentage | decimal, nullable | npr. `15.00` = 15% |
| fixed_amount | integer, nullable | dodaje se posle procenta, u najmanjoj jedinici valute (poglavlje 2) |
| fixed_amount_currency | string, nullable | |
| active_from / active_to | date, nullable | omogućava vremenski ograničene kampanje marže |
| created_by / created_at / updated_at | UUID / timestamp | |

**Formula (fiksan redosled, da bude deterministički i proverljiv):**
`finalna_cena = round(nabavna_cena * (1 + percentage / 100)) + fixed_amount`
Ako `percentage` nije postavljen, tretira se kao 0. Ako `fixed_amount` nije postavljen, tretira se kao 0. Bar jedno od dva mora biti postavljeno da bi pravilo bilo validno. Pošto su `nabavna_cena`/`fixed_amount`/`finalna_cena` celi brojevi (poglavlje 2, konvencija), `round()` ovde zaokružuje na najbližu celu jedinicu najmanje valute (cent/para) — izbegava se float aritmetika kroz ceo lanac izračuna.

### 2.2 Razrešavanje pravila (najspecifičnije pobeđuje)
Za proizvod iz M3 (ugovoren): `M2_PRODUCT` → `M3_CONTRACT_PERIOD` → `M3_CONTRACT` → `M3_SUPPLIER` (podrazumevano).
Za proizvod iz M4 (API): `M2_PRODUCT` → `M4_PROVIDER` (podrazumevano).

**Ograda:** sistem ne dozvoljava da `Contract` (M3) ili `ProviderConfig` (M4) pređe u status `ACTIVE` dok njegov dobavljač/provajder nema bar jedno podrazumevano `MarkupRule` — sprečava slučajnu prodaju bez marže.

**Dopuna (uneta u M6 specifikaciji, kad je taj modul specificiran):** posle primene `MarkupRule`, ako `Quote.client_account_id` postoji, M5 poziva M6 `GET /loyalty-status/:clientAccountId` i primenjuje popust nivoa lojalnosti kao poslednji korak: `konačna_cena_za_gosta = final_price * (1 - discount_percentage / 100)`. Ovo ne menja logiku marže iznad, samo dodaje korak posle nje.

---

## 3.0 Sastavljanje putovanja — Itinerary (opcioni korak pre Ponude, dopuna avgust 2026 — poređenjem sa Travel Compositor portfolio modelom)

Za jednostavne rezervacije (jedan hotel, jedan transfer) tok direktno kreće od Ponude (poglavlje 3). Za **kompleksna višedestinacijska putovanja** — differentiator eksplicitno naveden u Master dokumentu poglavlje 1.1 kao razlog da gost ostane kod agencije umesto generičkog AI — M5 uvodi opcioni korak sastavljanja pre Ponude, tako da redosled i kombinacija destinacija ne moraju biti unapred poznati da bi se pravila konkretna ponuda.

### 3.0.1 `Itinerary`
| Polje | Tip | Napomena |
| :---- | :---- | :---- |
| id | UUID (PK) | |
| client_account_id | UUID, nullable | isto kao `Quote` (poglavlje 3.1) — nullable dok se gost ne identifikuje |
| channel | enum (isto kao `Quote`) | |
| status | enum: `DRAFT`, `CONVERTED`, `ABANDONED` | `CONVERTED` kad postane `Quote` (poglavlje 3.0.3) |
| title | string, nullable | radni naziv, npr. "Italija + Grčka, 14 dana" — samo interni, ne prevodi se (M2 poglavlje 2.2 se ovde ne primenjuje) |
| created_by | UUID, nullable | user_id ako agent sastavlja; null ako gost sam sastavlja na sajtu |
| share_token | string, nullable, unique | dopuna 18.8.2026, poglavlje 3.0.5 — generiše se na zahtev preko `POST /itineraries/:id/share` |
| document_url | string, nullable | dopuna 18.8.2026, poglavlje 3.0.8 — PDF nacrta, isto skladište kao vaučer/ugovor |
| created_at / updated_at | timestamp | |

### 3.0.2 `ItinerarySegment`
| Polje | Tip | Napomena |
| :---- | :---- | :---- |
| id | UUID (PK) | |
| itinerary_id | UUID (FK) | |
| sequence_order | integer | redosled segmenta u putovanju — korisnik menja raspored; tačan UI (drag & drop ili sl.) van obima ove specifikacije |
| product_id | UUID (FK → M2 Product), nullable | popunjeno kad je segment vezan za konkretan proizvod (hotel, transfer, izlet); nullable dok se destinacija samo okvirno bira, pre konkretne pretrage |
| destination_country / destination_city | string, nullable | kopija radi prikaza dok `product_id` nije popunjen |
| stay_from / stay_to | date, nullable | |
| notes | text, nullable | slobodna napomena agenta/gosta o segmentu |
| is_included | boolean, default `true` | dopuna 18.8.2026, poglavlje 3.0.4a — segment ostaje u itineraru ali se isključuje iz zbira/konverzije bez brisanja (npr. poređenje "sa osiguranjem/bez") |
| preview_base_cost / preview_final_price / preview_final_price_currency | integer / integer / string, nullable | dopuna 18.8.2026, poglavlje 3.0.4a — **informativna** procena cene, snimljena iz `SearchResultOffer` u trenutku kad je `product_id` izabran; nikad obavezujuća, ponovo se računa pri konverziji (poglavlje 3.0.3) |
| preview_source_type | enum: `CONTRACTED`, `API`, nullable | isto poreklo kao izabrani `SearchResultOffer` (poglavlje 3.0b.2), radi prikaza |

### 3.0.3 Konverzija u Ponudu
`POST /itineraries/:id/to-quote` prevodi svaki `ItinerarySegment` sa `is_included = true` i popunjenim `product_id` u `QuoteItem` (poglavlje 3.2), po istim pravilima cene/marže kao svaka druga stavka (poglavlje 2) — **cena se ovde računa iznova, ne prepisuje se `preview_final_price`** (isti princip kao ponovna provera isteka `quote_expires_at` za API stavke, poglavlje 3.0b.3 korak 5 — procena tokom sastavljanja nikad nije obavezujuća). `Itinerary` sam po sebi ne rezerviše niti proverava dostupnost, isti princip kao što `Quote` sam po sebi ne zaključava kapacitet (poglavlje 3). Segmenti sa `is_included = false` se **tiho** preskaču (namerno isključeni, ne greška) — segmenti sa `is_included = true` ali bez popunjenog `product_id` (samo okvirno izabrana destinacija) se preskaču uz jasno upozorenje korisniku pre konverzije. Pri uspešnoj konverziji, `Itinerary.status` prelazi u `CONVERTED`; nastala `Quote` čuva referencu nazad radi sledljivosti (novo polje `Quote.itinerary_id`, nullable, dopuna poglavlja 3.1).

### 3.0.4 Sastavljanje kao uključivanje/isključivanje segmenata (dopuna, 18.8.2026, na zahtev vlasnika)

Kad je "Individualni paketi" aktivan (§3.0d.5), svaka od preostalih 8 ikona pretrage (dizajn dokument §5b) postaje **dugme za dodavanje segmenta tog tipa** u tekući `Itinerary`, ne samo ulazna tačka za potpuno nezavisnu pretragu — agent može krenuti od bilo koje (npr. Letovi ili Smeštaj prvi), pa dodavati/uklanjati ostale (Transfer, Things to do...) po potrebi. Svaki dodat segment ostaje toggle-abilan (`is_included`, poglavlje 3.0.2) — isključivanje ne briše segment, samo ga privremeno izostavlja iz zbira i konverzije, korisno za brzo poređenje "sa ovim/bez ovog".

**Desni panel** (dizajn dokument §5b) prikazuje, dok se sastavlja:
- **Cenu po segmentu** — `preview_final_price` svakog uključenog segmenta koji ima izabran proizvod, ažurira se čim segment dobije/promeni `product_id`.
- **Zbir trenutnog stanja** — suma `preview_final_price` svih `is_included = true` segmenata sa popunjenim proizvodom, uživo, isti princip kao postojeći zbir selekcije van itinerara (poglavlje 3.0e.3) — uključujući isto upozorenje ako segmenti nose više valuta.
- Obe vrednosti su vidno označene kao **procena** (npr. "~", ili natpis "okvirno") — stvarna cena je ona iz koraka konverzije (poglavlje 3.0.3), ne ova.

### 3.0.4a Van obima ove dopune
Vizuelni prikaz sastavljanja (mapa, drag & drop redosled) je dizajnersko pitanje van obima ove specifikacije, isto obrazloženje kao izgled kalendara (poglavlje 13). Automatski predlozi AI agenta za popunu "praznina" u itineraru (npr. transfer između dva segmenta, ili predlog trećeg segmenta na osnovu prva dva) je prirodan kandidat za M15 kad taj sloj bude uveden za M5 — ne uvodi se ovde, u skladu sa principom #4 Master dokumenta (determinizam pre autonomije: `Itinerary` sam ostaje čist podatak dok ne postoji stabilan M15 okvir koji bi ga dopunjavao).

### 3.0.5 Deljiv link ka nacrtu (dopuna, 18.8.2026, na zahtev vlasnika)

Isti obrazac kao već postojeći M23 `shareToken` mehanizam (javna, samo-za-čitanje-plus-jedna-radnja stranica, bez naloga) — differentiator iz Master dokumenta poglavlja 1.1 (kompleksno višedestinacijsko putovanje kao razlog da gost ostane kod agencije) zaslužuje sopstvenu vidljivost pre nego što postane obavezujuća Ponuda.

- **`Itinerary.share_token`** (string, nullable, unique) — generiše se na zahtev (`POST /itineraries/:id/share`, zahteva `M5/itinerary/EDIT`), ne automatski pri kreiranju; poziv je idempotentan (vraća postojeći token ako već postoji).
- **`GET /itineraries/shared/:shareToken`** (javan, bez guard-a, isti princip kao M23 §9) — vraća segmente sa `preview_final_price` (nikad `base_cost`/maržu — isto pravilo skrivanja kao poglavlje 6.2), jasno obeleženo kao okvirna procena (poglavlje 3.0.4).
- **`PATCH /itineraries/shared/:shareToken/segments/:segmentId`** (javan, bez guard-a) — gost sme **isključivo** da promeni `is_included` na postojećem segmentu (poglavlje 3.0.2); ne sme da doda nov segment, ne menja datume/proizvod/cenu. Pošto `Itinerary` sam po sebi ne drži kapacitet niti obavezuje (poglavlje 3.0), ovo je bezbedno bez autentikacije — isti nivo rizika kao gost koji menja filtere u sopstvenoj pretrazi.
- Agent vidi izmene gosta čim se vrati na `Itinerary` u M17/M7 (obična `GET /itineraries/:id`, isti podatak) — nema posebne sinhronizacije, jedan zapis.
- Token se ne poništava automatski pri konverziji u Ponudu — stranica posle konverzije prikazuje da je nacrt već pretvoren u Ponudu (nema više šta da se menja preko linka).

### 3.0.6 Kloniranje nacrta (dopuna, 18.8.2026, na zahtev vlasnika)

`POST /itineraries/:id/clone` (zahteva `M5/itinerary/CREATE`) — kreira nov `Itinerary` (`status = DRAFT`) sa kopijom svih `ItinerarySegment` zapisa izvornog (uključujući `is_included`/`preview_*` polja kao polaznu tačku — osvežavaju se čim se segment ponovo dotakne). `title` nove kopije dobija sufiks (npr. "Italija + Grčka, 14 dana (kopija)"), korisnik ga slobodno menja. Namena: agent brzo pravi varijantu istog putovanja (npr. "budžetska" naspram "premium" verzija) bez ponovnog sastavljanja od nule — dve varijante se prirodno porede kroz već postojeću mogućnost dva desna panela jedan pored drugog (dizajn dokument poglavlje 5b).

### 3.0.7 Upozorenje na hronološku nedoslednost segmenata (dopuna, 18.8.2026, na zahtev vlasnika)

Čisto informativno, nikad blokirajuće — isti nivo kao postojeće upozorenje o više valuta u selekciji (poglavlje 3.0e.3). Deterministička provera (princip #4 Master dokumenta, ne AI procena), izračunata pri svakom `GET /itineraries/:id` nad segmentima sa `is_included = true` i popunjenim datumima:

- **`DATE_OVERLAP`** — dva segmenta sa različitim `destination_city` imaju preklapajuće `stay_from`/`stay_to` opsege (isti princip strogog preseka kao M3 poglavlje 2.3b) — gost ne može fizički biti na dva mesta istovremeno.
- **`MISSING_TRANSFER`** — dva uzastopna segmenta (po `sequence_order`) sa različitim `destination_city` nemaju treći segment tipa `TRANSFER`/`FLIGHT` čiji datum pada između njihovih — heuristika, ne tvrdo pravilo (agent možda namerno ne prodaje prevoz), prikazuje se kao blag predlog ("Dodati transfer između ova dva segmenta?"), ne greška.

Oba upozorenja prikazuju se u desnom panelu uz listu segmenata (poglavlje 3.0.4), korisnik ih slobodno ignoriše — ne sprečavaju konverziju u Ponudu (poglavlje 3.0.3).

### 3.0.8 PDF izvoz nacrta (dopuna, 18.8.2026, na zahtev vlasnika)

`POST /itineraries/:id/generate-pdf` (zahteva `M5/itinerary/VIEW`) — generiše dokument sa spiskom uključenih segmenata i okvirnim cenama (isto pravilo skrivanja nabavne cene/marže kao deljiv link, poglavlje 3.0.5), popunjava `Itinerary.document_url` (string, nullable, dopuna poglavlja 3.0.1 — isto EU cloud skladište kao vaučer/ugovor, poglavlje 6). Namena: agent koji radi sa gostom uživo u kancelariji odmah štampa/šalje trenutni nacrt, bez čekanja da postane Ponuda. Deli isti generator dokumenta kao vaučer/ugovor (M20), ne nov mehanizam.

---

## 3.0b Rezultati pretrage — struktura `SearchResult` i prelaz u `Quote`/`Itinerary` (dopuna, avgust 2026, na zahtev vlasnika)

`GET /search` (poglavlje 11) objedinjuje M2 katalog + M3 ugovorenu dostupnost + M4 uživo, ali oblik onoga što taj poziv vraća do sada nije bio definisan — samo ulazni parametri. Ovo dopunjuje tu definiciju i objašnjava kako se izabrani rezultat prenosi dalje u `Quote` (poglavlje 3) ili `ItinerarySegment` (poglavlje 3.0.2).

### 3.0b.1 `SearchResultProduct` — jedan proizvod u rezultatima
| Polje | Tip | Napomena |
| :---- | :---- | :---- |
| product_id | UUID (FK → M2 Product) | |
| type | enum | isto kao `M2 Product.type` |
| source_type | enum: `CONTRACTED`, `API` | |
| name | string | iz `M2 ProductTranslation` za traženi jezik, sa istim fallback-om kao M2 poglavlje 2.2 |
| destination_country / destination_city | string | |
| thumbnail | object, nullable | `{url, category}` — prva slika iz `M2 Product.media[]` po `order`, prioritet kategoriji `EXTERIOR` pa `ROOM` (M2 poglavlje 2.3a); `null` ako proizvod nema slika |
| short_description | string, nullable | kratak izvod iz `ProductTranslation` |
| offers[] | niz `SearchResultOffer` | jedna ili više ponuda unutar istog proizvoda (npr. različit tip sobe/`board_type`) — vidi 3.0b.2 |

### 3.0b.2 `SearchResultOffer` — jedna konkretna kombinacija cene/dostupnosti
| Polje | Tip | Napomena |
| :---- | :---- | :---- |
| room_type_code / room_type_name | string, nullable | popunjeno samo za `ACCOMMODATION`/`PACKAGE` sa smeštajem (M2 poglavlje 2.3a); `null` za tipove bez sobe (`TRANSPORT`/`TICKET`/`EVENT`/samostalno `INSURANCE`) |
| board_type | string, nullable | iz `M3 RateLine.board_type` (`CONTRACTED`) ili normalizovano iz M4 odgovora (`API`) |
| price_basis | enum, nullable | `M3 RateLine.price_basis` (`CONTRACTED`, poglavlje 2.4 M3); za `API` se ponuda uvek tretira kao ukupna cena (ekvivalent `PER_ROOM_PER_NIGHT`) — M4 adapteri ne razlikuju osnovicu |
| final_price / final_price_currency | integer / string | `CONTRACTED` — rezultat formule iz poglavlja 2.1/3.2b; `API` — marža primenjena na `M4 AvailabilityQuote.priceAmount`; u najmanjoj jedinici valute (poglavlje 2) |
| availability_status | enum: `AVAILABLE`, `ON_REQUEST`, `SOLD_OUT` | `CONTRACTED` `FIXED`/`CHARTER`/`FIXED_LEASE`: `SOLD_OUT` kad je `total_capacity - units_sold` manje od broja traženih soba (dužina `room_config[]`, ili 1 ako nije poslat), inače `AVAILABLE` (M3 poglavlje 2.3); `CONTRACTED` `ON_REQUEST`: uvek `ON_REQUEST` (M3 poglavlje 2.3) — sistem ne garantuje kapacitet, potvrda ide ručno/kroz API dobavljača pre nego što se gostu potvrdi; `API`: `AVAILABLE` kad `M4 AvailabilityQuote.availableUnits > 0`, inače `SOLD_OUT`. **`SOLD_OUT` ponude se ne vraćaju u rezultatima** — filtriraju se pre odgovora (standardno ponašanje pretrage); pun uvid u zauzet alotman i dalje je dostupan direktno kroz M3 za tim kome je ta informacija potrebna |
| rate_line_id | UUID, nullable | `CONTRACTED` — FK → M3 `RateLine`, nosi se dalje u `POST /quotes` (poglavlje 3.2) |
| provider_quote_reference | string, nullable | `API` — iz `M4 AvailabilityQuote.externalId`, nosi se dalje u `POST /quotes` |
| quote_expires_at | timestamp, nullable | `API` — iz `M4 AvailabilityQuote.quoteExpiresAt`; `null` za `CONTRACTED` (cena ne ističe u tom smislu, samo se ponovo proverava pri potvrdi, poglavlje 4) |
| cancellation_policy_summary | string, nullable | `CONTRACTED` — agregirano iz niza `M3 CancellationRule` te `ContractPeriod` (npr. "30 dana: 100%, 15 dana: 50%, 0 dana: 0%"); `API` — direktno iz `M4 AvailabilityQuote.cancellationPolicy`, prikazano kako stigne od dobavljača |
| is_refundable | boolean, nullable | dopuna 18.8.2026, na zahtev vlasnika — omogućava filter "Refundabilno"/"Nerefundabilno" (poglavlje 3.0c.3). `CONTRACTED`: izračunato — `true` ako bar jedan primenjiv `M3 CancellationRule` prozor za tu `ContractPeriod` ima `refund_percentage > 0`, `false` ako su svi prozori `0%` (potpuno nerefundabilna tarifa). `API`: **`null` (nepoznato)**, ne pretpostavlja se iz slobodnog teksta `cancellation_policy_summary` — većina M4 provajdera ne izlaže ovo kao strukturisano polje (potvrditi po provajderu pre nego što se ikad postavi na `true`/`false`); `null` stavke se prikazuju u oba filtera dok se pouzdanost ne potvrdi (ne skrivaju se tiho) — otvoreno u `docs/analize/27-BACKLOG-IDEJA-I-PREDLOZI.md` |

### 3.0b.3 Od rezultata pretrage do `Quote`/`ItinerarySegment`
1. Korisnik (agent u M17 ili gost u M8/M9) bira jedan `SearchResultOffer` unutar jednog `SearchResultProduct`.
2. **Direktno u ponudu** (bez sastavljanja putovanja): `POST /quotes` prima `product_id`, `rate_line_id` ili `provider_quote_reference` (zavisno od `source_type`), `stay_from`/`stay_to`, `occupancy` (isti oblik kao poglavlje 3.2a) — ova polja se prepisuju iz izabranog `SearchResultOffer`/upita pretrage, korisnik ih ne unosi ponovo ručno.
3. **Kroz sastavljanje putovanja** (poglavlje 3.0): isti podaci popunjavaju `ItinerarySegment.product_id` (poglavlje 3.0.2); konverzija u `Quote` (poglavlje 3.0.3) ih dalje prenosi na isti način.
4. **Provera isteka pre kreiranja stavke ponude:** ako je `source_type = API` i `quote_expires_at` izabranog `SearchResultOffer` je već prošao u trenutku poziva `POST /quotes` (korisnik je "sedeo" na rezultatima pretrage), kreiranje te `QuoteItem` se odbija sa jasnom porukom koja traži ponovnu pretragu — sistem nikad tiho ne kreira stavku sa cenom koja više nije garantovana od M4. Ovo je odvojena provera od one pri potvrdi rezervacije (poglavlje 4) — ta druga se izvršava kasnije, nezavisno, čak i ako je ova ovde prošla.
5. `base_cost`/`final_price` se pri kreiranju `QuoteItem` ne preuzima slepo iz `SearchResultOffer.final_price` — računa se ponovo po formuli iz poglavlja 2.1/3.2b (`CONTRACTED`) ili ponovnim čitanjem `AvailabilityQuote` preko M4 (`API`, svaki put pri `POST /quotes`, iz bezbednosnih razloga dok se operativno ne pokaže da je to preskupo u broju poziva ka dobavljaču — tad se uvodi prag, kao posebna dopuna). Cena prikazana u rezultatima pretrage je uvek najbolja procena u tom trenutku, ne konačna garancija — konačna garancija je tek `Quote`/`Booking` potvrda (princip #4, determinizam pre autonomije, poglavlje 3 Master dokumenta).

### 3.0b.4 Veza sa bazom znanja (M23) — informacije o smeštaju/destinaciji na zahtev (dopuna, 18.8.2026, na zahtev vlasnika)

Svaka kartica/red rezultata (poglavlje 6d dizajn dokumenta) nosi radnju **"Info"** koja, na zahtev korisnika (nikad automatski — ne opterećuje ekran dodatnim pozivima dok se ne traži), otvara desni panel (`29-DIZAJN-SISTEM-UI.md` poglavlje 5b, "izdvajanje" — isti obrazac kao AI razgovor ili istorija izmena, samo drugi izvor) sa M23 sadržajem vezanim za taj `SearchResultProduct`:

- **O proizvodu** — `GET /knowledge/articles?productId=<product_id>` (M23 spec poglavlje 8, novi opcioni filter, dopuna ovim istim prolazom) — traži `Article` sa `subject_type = PRODUCT` i tim `product_id`.
- **O destinaciji** — `GET /knowledge/articles?destinationCountry=<...>&destinationCity=<...>` — traži `Article` sa `subject_type = DESTINATION` (ili `COUNTRY` ako grad nema poseban članak, isti fallback princip kao jezik — grad → zemlja) za `destination_country`/`destination_city` tog rezultata.
- Oba poziva traže isključivo `status = PUBLISHED` (M23 spec poglavlje 3.1 — ista publika, interni tim i subagenti, koja već čita M23 direktno).
- **Nema pogotka:** desni panel prikazuje jasno prazno stanje ("Nema još sadržaja baze znanja o ovoj destinaciji/smeštaju") — ne izmišlja sadržaj, ne poziva AI da odgovori uživo u ovom toku. Korisnik sa `M23/article/EDIT` pravom dobija link ka postojećem M17 "Znanje" ekranu da pokrene istraživanje — ne nov, paralelan mehanizam za pisanje članaka.
- **Dostupno gde je M23 direktno dostupan** — M17 (interni tim) i M7 (subagenti), u skladu sa M23 spec poglavljem 1.3 (gost na M8 ne pretražuje bazu direktno, samo prima deljen link) — na M8 rezultatima pretrage ova radnja se ne prikazuje u ovom prolazu.

---

## 3.0c Vođena pretraga smeštaja — geografski koraci, tag-filteri, AI kontekst (dopuna, avgust 2026, na zahtev vlasnika)

Ovo poglavlje dopunjuje `GET /search` (poglavlje 11) sa oblikom koji omogućava **vođenu**, korak-po-korak pretragu smeštaja u M17/M7 (isti obrazac interfejsa, `29-DIZAJN-SISTEM-UI.md` poglavlje 7) — polazna tačka za `ACCOMMODATION`, proširiva na ostale `Product.type` kad dođu na red. Sam raspored/izgled ekrana (levi/centralni/desni panel) je dizajn-dokument pitanje (poglavlje 5b tog dokumenta); ovo poglavlje definiše **podatke** koje taj ekran poziva, ne piksele.

### 3.0c.1 Popunjenost (occupancy) — nema promene modela, samo potvrda

Broj soba / odraslih po sobi / dece po sobi / uzrast svakog deteta koje vlasnik traži kao vođen unos već je tačno `occupancy.room_config[]` (poglavlje 3.2a) — `{room_type_code?, adults, children, children_ages[]}` po sobi. UI ovo prikuplja korak po korak (jedna soba u trenutku, semantički vođeno pitanjima), ali šalje isti postojeći oblik — nema izmene API ugovora.

### 3.0c.2 Geografski koraci — zemlja → destinacija/hotel → smeštaj → vrsta usluge

Vlasnikova odluka (17.8.2026): vođeni koraci strogim redosledom, ali svaki korak radi na kucanje (autocomplete), ne samo padajući meni; drugi korak prepoznaje i ime hotela kao prečicu.

1. **`GET /search/countries?q=`** — vraća `{code, name}[]`, ograničeno na zemlje u kojima postoji bar jedan `ACTIVE` proizvod vidljiv pozivaocu (`visible_channels`/`INTERNAL_PANEL` izuzetak, isto pravilo kao poglavlje 11 `GET /search`) — polje **prazno dok se ne otkuca ništa ili ne otvori padajući meni**, sistem ne nudi zemlje bez ijednog proizvoda (princip "aplikacija semantički navodi korisnika", ne nudi ćorsokak).
2. **`GET /search/destinations?country_code=&q=`** — obavezan `country_code` iz koraka 1. Vraća **mešovitu** listu, svaka stavka označena tipom da UI zna kako da prikaže zagradu:
   - `{type: 'DESTINATION', city, country_name}` — grad/mesto sa bar jednim proizvodom u toj zemlji; UI prikazuje `Budva (Crna Gora)`.
   - `{type: 'PRODUCT', product_id, name, city, country_name}` — proizvod (hotel) čiji naziv (iz `ProductTranslation`, poglavlje 2.2 M2) odgovara upisanom tekstu; UI prikazuje `Hotel Avala Resort (Crna Gora, Budva)`. Ovo je prečica iz vlasnikovog prvog opisa — kucanje imena hotela u istom polju gde se inače bira destinacija, bez posebnog trećeg polja.
   - Izbor `DESTINATION` stavke postavlja `destination_city` filter i **automatski** poziva `GET /search` (poglavlje 11) sa `destination_country`+`destination_city` — rezultati (smeštaji) se pojavljuju bez dodatnog koraka korisnika, tačno kako je vlasnik opisao ("automatski se pojave smeštaji").
   - Izbor `PRODUCT` stavke preskače listu rezultata i vodi direktno na taj proizvod (isti ekran kao klik na jedan rezultat pretrage).
3. **Vrsta usluge** (`board_type`, već postoji na `SearchResultOffer.board_type`, poglavlje 3.0b.2) — filtrira se **unutar** već dobijenih rezultata koraka 2, ne kao poseban API poziv; UI prikazuje samo vrednosti `board_type` koje se stvarno pojavljuju u trenutnim rezultatima (nikad prazna opcija koja ništa ne vraća).

### 3.0c.3 Tag-filteri — `amenities[]` (M2 poglavlje 2.3c)

`GET /search` (poglavlje 11) prima novi opcioni parametar `amenity_tags[]` (niz `AmenityTag` vrednosti, M2 poglavlje 2.3c) — proizvod se vraća samo ako njegov `attributes.amenities[]` sadrži **sve** tražene tagove (I-logika, ne ILI — korisnik koji je izabrao "bazen" i "kućni ljubimci dozvoljeni" očekuje oboje). Predlog samih tagova (koji se nude dok korisnik kuca 3+ slova) je **statična, poznata lista** iz `AmenityTag` enuma — ne zahteva novi endpoint, filtrira se na klijentu nad fiksnim spiskom vrednosti koje backend već poznaje (isti princip kao svaki drugi zatvoren enum, npr. `ProductType` na front-endu M8).

### 3.0c.3a Brzi filteri — refundabilno/nerefundabilno, odmah potvrda/upit (dopuna, 18.8.2026, na zahtev vlasnika)

Dva para filtera koji ostaju **vrlo vidljivi**, ne sklopljeni unutar grupe kao ostali tagovi (dizajn dokument poglavlje 6d — pinovani na vrhu sekcije "Filteri", ne unutar sklopivih pod-sekcija):

- **Odmah potvrda / Upit** — filtrira po već postojećem `SearchResultOffer.availability_status` (poglavlje 3.0b.2): "Odmah potvrda" = `AVAILABLE`, "Upit" = `ON_REQUEST`. Filtrira se **na klijentu** nad već dobijenim rezultatima, isti princip kao "vrsta usluge" (poglavlje 3.0c.2, tačka 3) — polje već postoji u odgovoru, nema potrebe za novim parametrom `GET /search`.
- **Refundabilno / Nerefundabilno** — filtrira po novom `SearchResultOffer.is_refundable` (poglavlje 3.0b.2). Isto klijentsko filtriranje. Pošto `is_refundable` može biti `null` (nepoznato, tipično za `API` izvor dok se ne potvrdi po provajderu), stavke sa `null` se **prikazuju u oba filtera** dok se ne sakriju svesnom odlukom — nikad se ne pretpostavlja vrednost da bi filter "radio čisto".

Oba filtera su nezavisna jedan od drugog i od ostatka sekcije "Filteri" (I-logika sa svim ostalim aktivnim filterima, isto kao amenity tagovi).

### 3.0c.4 AI kontekstualna pretraga na dnu ekrana

Nije nov mehanizam — isti `POST /ai-orchestration/omnisearch` (M15 poglavlje 6.5/9) koji već pokriva komandnu paletu (`29-DIZAJN-SISTEM-UI.md` poglavlje 4) i AI-razgovor obrazac (isti dokument poglavlje 6c). Ekran vođene pretrage ovde samo prosleđuje trenutni kontekst (izabrana zemlja/destinacija/occupancy ako postoje) kao deo `channel`/kontekst polja poziva — omogućava upit kao "nešto na moru do 500m od plaže za porodicu sa dvoje dece" da se protumači sa već poznatim delom konteksta, ne od nule. Nikad ne izvršava pretragu/rezervaciju sam (isto ograničenje kao poglavlje 6.5.4 M15) — vraća predlog/rezultat koji korisnik i dalje pregleda kao svaki drugi rezultat pretrage.

---

## 3.0d Vođena pretraga — ostalih 8 vrsta turističkih proizvoda (dopuna, 17.8.2026, na zahtev vlasnika)

Dopunjuje poglavlje 3.0c (Smeštaj) istim principom — svaka stavka u levom navigatoru (`29-DIZAJN-SISTEM-UI.md` poglavlje 5b) odgovara jednoj vođenoj pretrazi. Istraženo naspram Google Flights/Skyscanner, Booking.com Cars/Cruises, Viator/GetYourGuide, InsureMyTrip/Squaremouth (avgust 2026). Svih 8 koristi isti postojeći `GET /search` (poglavlje 11) sa `type` filtrirano na odgovarajući `Product.type`, osim gde je eksplicitno drugačije navedeno.

### 3.0d.1 Letovi (`FLIGHT`)
Tip putovanja **određuje samo UI tok, ne novi API parametar**: jednosmerno = jedan poziv `GET /search`; povratno = dva poziva (odlazak/povratak), rezultati uparuju se na klijentu; multi-city (uklj. "otvoren povratak" — let u grad A, povratak iz grada B, kao specijalan slučaj sa samo dve noge gde druga noga ne polazi iz odredišta prve) = niz poziva, po jedan po nozi puta — svaki `FLIGHT` proizvod (poglavlje 2.1 M2) ostaje jedna noga na jedan datum (M2 poglavlje 2.3, dopuna 17.8.2026). Parametri po pozivu: `destination_country`/`destination_city` (polazište se rešava kao posebna vrednost `origin_city`, novo opciono polje `GET /search`, dopuna 17.8.2026 — do sada `GET /search` nije imalo pojam polazišta, samo odredišta), `stay_from` (datum leta), `occupancy.adults`/`children`/`children_ages` (bez `room_config`, nema smisla za let), `cabin_class` (novi opcioni parametar, filtrira `SearchResultOffer` na traženu klasu).

**Filteri, dopuna 17.8.2026 (istraženo naspram Google Flights)**, primenjuju se nad već dobijenim rezultatima poziva iznad, isti mehanizam kao `amenity_tags[]` (poglavlje 3.0c.3) — ne menjaju sam poziv `GET /search`, filtriraju prikaz: broj presedanja (direktno/1/2+), izbor aerodroma presedanja, maksimalno vreme čekanja na presedanju, avio-kompanija (uključi/isključi), gornja granica cene, opseg vremena poletanja/sletanja (posebno po nozi), gornja granica ukupnog trajanja puta, broj uključenog ručnog/predatog prtljaga, obližnji aerodromi kao alternativa. Sortiranje: "Najjeftiniji" (cena) ili "Najbolji" (kombinacija cene/trajanja/presedanja/vremena, isti princip kao Google Flights).

**Tehnička napomena, ne odluka:** filteri "presedanja"/"aerodrom presedanja" pretpostavljaju da jedan rezultat pretrage može biti kombinacija više segmenata spojenih u jednu cenu (GDS oblik) — to je drugačiji oblik od `SearchResultProduct`/`SearchResultOffer` (poglavlje 3.0b) koji danas nosi tačno jedan `Product` po rezultatu. Ovaj dokument namerno **ne** izmišlja GDS oblik odgovora unapred (CLAUDE.md — ne izmišljati tehničke detalje eksternih sistema) — polja/filteri iznad su ugovor koji UI/M17 ekran može da očekuje; tačan oblik `SearchResultOffer` za multi-segment let (verovatno nov, poseban od postojećeg) definiše se kad M4 stvarno dobije avio/GDS adapter (M4 backlog, "implementiraju se kasnije").

### 3.0d.2 Transferi (`TRANSFER`)
`destination_country`/`destination_city` (odredište), `origin_city` (isto novo polje kao 3.0d.1 — polazna tačka, npr. aerodrom), `stay_from` (datum), `occupancy.adults`/`children` (broj putnika, bez `room_config`). Jednosmeran/povratni transfer je UI odluka (dva odvojena `SearchResultOffer` izbora), ne poseban parametar.

### 3.0d.3 Rent-a-car (`TRANSPORT`, `transport_mode=RENT_A_CAR`)
Sopstvena ikonica u levom navigatoru (vlasnikova odluka, dizajn dokument poglavlje 5b) iako ostaje isti `Product.type` u bazi. Parametri: `origin_city` (mesto preuzimanja), `destination_city` (mesto vraćanja — **različito od `origin_city` znači one-way rental**, dozvoljeno, ne greška), `stay_from`/`stay_to` (datum+vreme preuzimanja/vraćanja — polje već postoji, ovde nosi drugo značenje nego kod smeštaja, isto kao što `TICKET`/`EVENT` već rade po poglavlju 11), `driver_age` (novi opcioni parametar — filtrira ponude koje zahtevaju doplatu/ograničenje za mlade vozače, iz `attributes.min_driver_age`, M2 poglavlje 2.3).

### 3.0d.4 Things to do — izleti, koncerti, utakmice (`EXCURSION` + `EVENT` + `TICKET`)
**Jedan ekran, tri tipa u pozadini** (vlasnikova odluka — UI spajanje, ne spajanje modela): `GET /search?type=EXCURSION&type=EVENT&type=TICKET` (već podržan multi-select, poglavlje 11) sa `destination_country`/`destination_city`, `stay_from`/`stay_to` (opseg — kod `EVENT` filtrira da li `event_datetime` pada u opseg, kod `TICKET` da li `valid_from`/`valid_to` preklapa opseg, kod `EXCURSION` datum polaska; poglavlje 11 već definiše ovo ponašanje po tipu), `occupancy.adults`/`children` (broj učesnika). Kategorija (kulturno/avantura/hrana/sport/koncert...) čita se iz `attributes.category` (već postoji na sva tri tipa, M2 poglavlje 2.3) — filtrira se kao tag, isti mehanizam kao `amenity_tags[]` (poglavlje 3.0c.3), ali poseban skup vrednosti po tipu (nije `AmenityTag`).

### 3.0d.5 Individualni paketi (hotel + let + transfer + ...)
**Nije `Product.type` niti novi ekran pretrage** — ovo je ulazna tačka u postojeći `Itinerary` tok (poglavlje 3.0). Ikonica u levom navigatoru otvara nov `POST /itineraries` (prazan nacrt) umesto da poziva `GET /search` direktno; korisnik zatim unutar tog toka otvara pretragu za svaki segment (Smeštaj, Letovi, Transferi...) kao i inače, svaki izbor postaje `ItinerarySegment.product_id` (poglavlje 3.0.2).

### 3.0d.6 Grupni paketi — tačno određen datum (`PACKAGE`)
Vlasnikova odluka (17.8.2026): "Grupna putovanja" i "Grupni paketi" su ista stavka. `destination_country`/`destination_city`, `occupancy.adults`/`children`. **Datum polaska se bira sa liste ponuđenih termina, ne slobodan opseg** — `stay_from` parametar za `PACKAGE` vraća samo proizvode čiji `M3 ContractPeriod` (`allotment_mode IN (FIXED, CHARTER)`) sadrži taj tačan datum kao polazak (novo ponašanje, dopuna 17.8.2026 — do sada `PACKAGE` nije imao poseban tretman od `ACCOMMODATION` u pretrazi); UI prikazuje dostupne datume kao izbor (npr. iz liste `ContractPeriod.stay_from` vrednosti za taj proizvod), ne kalendar sa slobodnim unosom.

### 3.0d.7 Krstarenja (`CRUISE`)
`destination_country` (ili region — `attributes.itinerary_ports[]` sadrži zemlje svake luke, pretraga po regionu je pretraga po bilo kojoj luci u tom regionu, ne novo polje), `stay_from` (datum/mesec polaska), `occupancy.adults`/`children`, `duration_nights` (novi opcioni parametar, opseg trajanja), `cabin_type` (novi opcioni parametar — filtrira `SearchResultOffer` na `cabin_types[].category`, isti mehanizam kao `cabin_class` za letove).

### 3.0d.8 Putno osiguranje (`INSURANCE`)
Jedini tip gde pretraga **ne** liči na ostale — nema `destination_country`/`destination_city` u smislu "gde je proizvod", jer polisa nije vezana za lokaciju nego pokriva putovanje. Parametri `GET /search?type=INSURANCE`: `destination_country` (odredište koje se osigurava — filtrira polise čiji `attributes.coverage_regions[]`, novo polje M2, pokriva tu zemlju/region), `stay_from`/`stay_to` (trajanje putovanja), `occupancy.adults`/`children`/`children_ages` (broj i uzrast putnika — cena police po uzrastu ide kroz M3 `age_pricing[]`, isti mehanizam kao smeštaj, poglavlje 3.2b), `trip_cost` (novi opcioni parametar — vrednost putovanja, relevantno samo ako polisa ima nivoe pokrića vezane za vrednost, poslovno pravilo za M3 markup/rate izvan obima ove dopune).

### 3.0d.9 Van obima ove dopune
Tačan skup `attributes.category` vrednosti za `EXCURSION`/`EVENT`/`TICKET` (poglavlje 3.0d.4) — dorađuje se pri stvarnoj izradi ekrana, isti princip kao `AmenityTag` (M2 poglavlje 2.3c). `coverage_regions[]` na `INSURANCE.attributes` (poglavlje 3.0d.8) — pomenuto kao neophodno polje, ne razrađeno strukturno ovde (M2 spec dopuna kad M2 dođe na red za osiguranje). Tačan prag/logika za date-range vs. fiksni-datum izbor u UI (poglavlje 3.0d.6) je ekranska odluka, ne API pitanje.

---

## 3.0e Unakrsna prodaja, pretraga unutar rezultata, i AI kao brz put do ponude (dopuna, 17.8.2026, na zahtev vlasnika)

### 3.0e.1 Unakrsna prodaja — deterministička mapa, ne AI poziv

Kad korisnik doda jednu stavku (bilo koji od 9 tipova iz poglavlja 3.0c/3.0d) u tekuću selekciju (desni panel, `29-DIZAJN-SISTEM-UI.md` poglavlje 5b), sistem **odmah** (vlasnikova odluka — ne čeka kraj pretrage) predlaže logične dopune iz fiksne mape — determinističko poslovno pravilo, ne poziv jezičkom modelu (isti princip kao svaka druga odluka u projektu gde je determinizam dovoljan — npr. M18 nedeljni pregled, poglavlje 4 tog dokumenta):

| Dodata stavka | Predlaže se |
| :---- | :---- |
| `ACCOMMODATION` | Transfer, Things to do, Putno osiguranje; Let — **samo** ako je `destination_country` različit od pretpostavljene zemlje porekla putnika (heuristika: `Product.destination_country` različit od zemlje agencije/RS — grubo polazište, dorađuje se ako se pokaže netačno) |
| `FLIGHT` | Smeštaj na odredištu, Transfer, Rent-a-car, Putno osiguranje |
| `TRANSFER` | Smeštaj — samo ako selekcija još nema nijedan `ACCOMMODATION`/`PACKAGE` |
| `TRANSPORT` (`RENT_A_CAR`) | Smeštaj, Putno osiguranje |
| `CRUISE` | Let (do luke polaska), Transfer, Putno osiguranje |
| `EXCURSION`/`EVENT`/`TICKET` | Smeštaj, Transfer |
| `PACKAGE` | (ništa — paket već sadrži multi-destinacijski sadržaj, poglavlje 2.3 M2) |
| `INSURANCE` | (ništa — poslednji, ne prvi dodatak) |

Prihvaćen predlog otvara odgovarajuću vođenu pretragu **predpopunjenu** poznatim podacima (destinacija, datumi, `occupancy`) iz stavke koja je upravo dodata — korisnik ne unosi iste podatke dvaput. Odbijen predlog se ne ponavlja za istu selekciju (ne gnjavi korisnika ponovnim istim pitanjem).

### 3.0e.2 Pretraga unutar rezultata — AI kao filter, ne nova pretraga

Kad `GET /search` vrati veći broj rezultata, korisnik može da precizira slobodnim jezikom ("jeftinije", "bliže centru", "bez bazena za decu") kroz isti `POST /ai-orchestration/omnisearch` (M15 poglavlje 6.5/9) korišćen za komandnu paletu — poziv nosi dodatni kontekst: trenutni `SearchResultProduct[]` skup i već primenjene filtere/tagove (poglavlje 3.0c.3/3.0d), ne prazan upit. Odgovor je **osvežena, filtrirana lista** nad istim rezultatima (isti princip kao "traži unutar ovoga", ne nova pretraga od nule) — nikad ne dodaje stavku u selekciju sam, korisnik i dalje bira iz prikazane liste (M15 poglavlje 6.5.4, nepromenjeno).

### 3.0e.3 Odbrojavanje isteka i valuta u selekciji

Desni panel (dizajn dokument poglavlje 5b) prikazuje `quote_expires_at` (već postoji, poglavlje 3.0b.2) kao vidljivo odbrojavanje za svaku stavku iz `source_type = API` izvora — korisnik vidi da stavka ističe pre nego što pokuša da kreira ponudu, ne posle. Ako selekcija sadrži stavke u više valuta (`final_price_currency`, poglavlje 3.0b.2), prikazuje se upozorenje uz zbir po valuti — sama konverzija ostaje M10 posao pri fakturisanju, ovo je samo vidljivost pre nego što se dođe do tog koraka.

### 3.0e.4 Namerno van obima ove dopune

**AI prepoznavanje više namera u jednoj rečenici** ("hotel u Budvi plus transfer sa aerodroma" → dve predpopunjene pretrage odjednom) — vlasnikova odluka (17.8.2026): prvo se grade i uživo proveravaju pojedinačne vođene pretrage iz poglavlja 3.0c/3.0d, multi-namera NLU je poseban naredni korak kad osnovni tok već radi pouzdano (isti postepeni princip kao ostatak M15 — determinizam/jednostavno pre autonomije/složenog). Kalendar cena/fleksibilni datumi (Google Flights stila) — zahteva infrastrukturu (cene unapred za više datuma) van obima ove dopune, razmatra se kao V2. Poređenje 2-3 stavke jedna pored druge — prirodno se uklapa u već postojeću mogućnost dva desna panela jedan pored drugog (dizajn dokument poglavlje 5b), ali tačan UI ostaje ekranska odluka, ne ovde.

---

## 3.0f Ručne (`MANUAL`) stavke — proizvod koji ne postoji u katalogu (dopuna, 18.8.2026, na zahtev vlasnika)

Ceo tok do sada (3.0-3.0e) pretpostavlja da proizvod postoji u M2 katalogu i dolazi preko M3 (ugovoreno) ili M4 (uživo API). Za jednokratan dogovor sa dobavljačem koji nikad neće ući u katalog (npr. hotel van naše mreže, tražen za jednog konkretnog gosta), uvodi se treća vrednost `source_type = MANUAL` na `QuoteItem`/`BookingItem` (poglavlja 3.2/4.2) — paralelan put koji zaobilazi `GET /search` i M3/M4 u potpunosti, dostupan sa istih 9 ikona tipa proizvoda (dizajn dokument poglavlje 5b) kroz novu radnju "Ručna stavka".

### 3.0f.1 `ManualProductEntry` — opisni podaci, bez cene

Ono što se pamti za ponovni izbor (vlasnikova odluka 18.8.2026) — **isključivo opisni podaci, nikad cena** (cena stara koliko i konkretan dogovor, ponovno korišćenje stare cene bi bilo opasno):

| Polje | Tip | Napomena |
| :---- | :---- | :---- |
| id | UUID (PK) | |
| product_type | enum (isti kao M2 `Product.type`, poglavlje 2.1) | |
| name | string | ručno kucano, ili predpopunjeno iz koraka 3.0f.2 za hotel |
| description | text, nullable | isto |
| country_id / destination_id | UUID, nullable (FK → M2 postojeća geografska tabela) | birano iz **postojeće** M2 baze zemalja/destinacija, isti padajući meniji kao vođena pretraga smeštaja (poglavlje 3.0c.2) — ništa novo ovde |
| source_url | string, nullable | nalepljen link (tipično sajt hotela), poglavlje 3.0f.2 |
| image_urls | niz string, nullable | interne (Terminal storage) putanje slika preuzetih preko poglavlja 3.0f.2 — nikad direktan hotlink na tuđ sajt |
| extracted_via_ai | boolean | `true` ako su naziv/opis/slike došli preko M15 mehanizma (poglavlje 3.0f.2), `false` ako je sve ručno kucano — koristi se za obeležavanje autora radnje (dizajn dokument poglavlje 6a) |
| created_by | UUID (FK → M1 User) | |
| created_at / last_used_at | timestamp | `last_used_at` se osvežava svaki put kad se zapis ponovo izabere za novu stavku — lista za ponovni izbor (poglavlje 3.0f.4) je sortirana po ovome |

Svaka ručna stavka **automatski** kreira ili ažurira jedan `ManualProductEntry` (ne zahteva dodatnu odluku agenta) — sledeći put kad se isti proizvod pojavi, agent bira iz liste umesto ponovnog kucanja/lepljenja linka.

### 3.0f.2 Hotel sa nalepljenog linka — tekst i slike preko M15

Kad je `product_type = ACCOMMODATION` i agent nalepi link sajta hotela (umesto ručnog kucanja naziva/opisa):

- **Tekst** (naziv, adresa, opis) — koristi već specificiran M15 opšti mehanizam pretrage interneta uz odobrenje čoveka (M15 poglavlje 6.5.6b), bez izmene tog poglavlja.
- **Slike** (dopuna, 18.8.2026, na zahtev vlasnika) — nova, uže ovlašćena dopuna M15 (poglavlje 6.5.6c, novo) — dozvoljena **isključivo** kad je link eksplicitno nalepljen od strane čoveka kao autoritativan izvor za konkretan proizvod koji se unosi (ne za opštu pretragu iz poglavlja 6.5.6b, koja ostaje isključivo tekst).
- Agent uvek pregleda/ispravlja izvučen tekst i **bira** koje ponuđene slike zadržava (ne slepo uvozi sve sa stranice) pre čuvanja `ManualProductEntry` — isti obrazac kao svaki drugi AI predlog koji čeka potvrdu (dizajn dokument poglavlje 6c).

### 3.0f.3 Cena — ko je unosi zavisi od kanala

- **`INTERNAL_PANEL`/`PHONE`** (interni tim) — agent unosi `base_cost`, `final_price` direktno u istoj formi gde bira `ManualProductEntry` (poglavlje 4.4 dopunjeno niže); `manual_review_status = NOT_REQUIRED`, jer je agent već ručno dogovorio uslove sa dobavljačem pre unosa.
- **`B2B_PORTAL`** (subagent) — subagent **ne unosi cenu uopšte** (vlasnikova odluka 18.8.2026 — subagent radi na proviziji, ne na marži, i ne postavlja naše nabavne uslove) — stavka se kreira samo sa opisnim podacima (poglavlje 3.0f.1) i traženim datumima/`occupancy`, `base_cost`/`final_price` ostaju `null` do pregleda osoblja. `manual_review_status = AWAITING_STAFF_REVIEW` — takva Ponuda ima **status upita**, ne aktivne rezervacije (vlasnikova formulacija, 18.8.2026), dok osoblje ne odluči.

`markup_rule_id` ostaje `null` za `MANUAL` stavke u oba slučaja — nema pravila, cena je uvek direktan unos, ne formula iz poglavlja 2.1.

### 3.0f.4 Pregled osoblja (isti obrazac kao M7 "Gejt B")

Kad `Quote.channel = B2B_PORTAL` sadrži bar jednu `MANUAL` stavku sa `manual_review_status = AWAITING_STAFF_REVIEW`:

1. Zahtev se pojavljuje u M15 Agent Inbox (M15 poglavlje 6) — isti mehanizam, isti krug ovlašćenih (Vlasnik, Direktor, Sales Manager — isti krug kao M7 spec poglavlje 2.0.4c korak 4, "Gejt B"), **nikad AI agent**.
2. Osoblje unosi `base_cost`/`final_price` (agent/subagent ih nikad nije uneo, poglavlje 3.0f.3) i bira `tip_nastupanja` ručno (poglavlje 4.0a nema kandidata za `MANUAL` izvor — nema `Contract`/`ProviderConfig` iz kojeg bi se izveo, isti izuzetak kao granični slučaj u poglavlju 4.0a koraku 3).
3. `POST /quotes/:id/items/:itemId/manual-review` (poglavlje 11) upisuje `manual_reviewed_at`/`manual_reviewed_by` i `manual_review_decision` (`APPROVED`/`REJECTED`, uz `rejection_reason` za odbijanje) — zahteva `M5/manual-item/REVIEW`.
4. **`POST /quotes/:id/confirm` (poglavlje 4) odbija potvrdu** ako bilo koja stavka Ponude ima `manual_review_status = AWAITING_STAFF_REVIEW` — jasna poruka ("čeka pregled osoblja"), isti princip kao provera kreditnog limita pre bilo kog poziva ka M3/M4 (poglavlje 4, korak 1). Tek kad je svaka `MANUAL` stavka `APPROVED` (ili `NOT_REQUIRED`), potvrda može da se pozove — u tom trenutku Ponuda prestaje da bude "upit" i postaje potvrdiva rezervacija, tačno kako je vlasnik opisao (18.8.2026).

### 3.0f.5 Potvrda — bez poziva M3/M4

`BookingItem` sa `source_type = MANUAL` (poglavlje 4.2) prelazi direktno u `CONFIRMED` pri potvrdi Ponude (poglavlje 4, korak 4) — nema poziva ka M3/M4 jer nema šta da se rezerviše kod njih, dobavljač je već ručno potvrđen (interni tim) ili odobren pri pregledu osoblja (subagent). `cancellation_policy_summary` je slobodan tekst koji agent/osoblje kuca ručno (nema `CancellationRule`); procenat povraćaja pri otkazivanju je ručna odluka tima u tom trenutku, ne izračunata vrednost — nema šta da se čita iz M3/M4 snimka.

### 3.0f.6 Dopune modela (`QuoteItem`/`BookingItem`, poglavlja 3.2/4.2)

| Polje | Tip | Napomena |
| :---- | :---- | :---- |
| `source_type` | enum: `CONTRACTED`, `API`, `MANUAL` | proširen enum (bilo `CONTRACTED`/`API`) |
| `product_id` | UUID, **nullable** (FK → M2 Product) | postaje nullable — `MANUAL` stavke nemaju M2 zapis |
| `manual_product_entry_id` | UUID, nullable (FK → `ManualProductEntry`) | popunjeno isključivo za `MANUAL` stavke |
| `manual_review_status` | enum: `NOT_REQUIRED`, `AWAITING_STAFF_REVIEW`, `APPROVED`, `REJECTED`, default `NOT_REQUIRED` | pravilo dodele: poglavlje 3.0f.3 |
| `manual_reviewed_at` / `manual_reviewed_by` | timestamp / UUID (FK → M1 User), nullable | |
| `manual_review_decision` / `rejection_reason` | isto kao M7 `SubagentBookingRequest` (M7 spec poglavlje 2.0.4b) — isti obrazac imena polja | |

---

## 3. Model podataka — Ponuda (Quote)

Ponuda je **neobavezujuća** kalkulacija — ne drži (ne "zaključava") kapacitet kod M3/M4. Kapacitet se proverava i rezerviše tek u koraku Potvrde (poglavlje 4). Ovo je namerna odluka: zaključavanje kapaciteta tokom razgledanja ponude uvodi složenost (isticanje rezervacije, oslobađanje) koja nije neophodna — standardna praksa u turizmu je da se dostupnost garantuje tek pri potvrdi, uz jasnu poruku gostu ako je nešto u međuvremenu prodato.

### 3.1 `Quote`
| Polje | Tip | Napomena |
| :---- | :---- | :---- |
| id | UUID (PK) | |
| client_account_id | UUID, nullable | referenca ka M6 Nalogodavac — nullable dok se gost ne identifikuje (anonimno razgledanje) |
| channel | enum: `B2C_SITE`, `B2B_PORTAL`, `MOBILE`, `INTERNAL_PANEL`, `PHONE` | |
| status | enum: `DRAFT`, `EXPIRED`, `CONVERTED` | |
| expires_at | timestamp | najkraći `quoteExpiresAt` među stavkama (M4) ili podrazumevanih 30 min za čisto ugovorene stavke |
| itinerary_id | UUID (FK → Itinerary), nullable | popunjeno samo ako je Ponuda nastala konverzijom iz sastavljenog putovanja (poglavlje 3.0.3) — nullable za direktne ponude jedne/nekoliko stavki |
| contract_terms_accepted | boolean, default false | dopuna avgust 2026, rešava nalaz iz `VALIDACIJA-WORKFLOW-B2C.md` — privremeno čuva clickwrap pristanak (M20 poglavlje 3.2) **pre** nego što `Booking`/`ClientContract` postoje; obavezno `true` pre prelaska na plaćanje za `B2C_SITE`/`B2B_PORTAL`/`MOBILE` kanale (M8 poglavlje 3, korak 4) |
| contract_terms_accepted_at | timestamp, nullable | kad je pristanak zabeležen; prenosi se na `ClientContract.accepted_at`/`accepted_method = ELECTRONIC_CLICKWRAP` čim taj zapis nastane (M20 poglavlje 3.1) — vidi poglavlje 4.1 niže |
| created_by | UUID, nullable | user_id ako je kreirao agent; null ako gost sam kreira na sajtu |
| referral_tracking_code | string, nullable | dopuna avgust 2026 (M12 poglavlje 3a) — sirov kod prosleđen iz M8 (`?ref=` parametar), **bez validacije protiv M12** u ovom trenutku; M5 samo prenosi string dalje na `Booking` pri potvrdi. Razrešavanje ka stvarnom sadržaju radi M13, ne M5 |
| share_token | string, nullable, unique | dopuna 19.8.2026 (poglavlje 3.1a) — generiše se na zahtev, isti obrazac kao `Itinerary.share_token` (poglavlje 3.0.5) |
| created_at | timestamp | |

### 3.1a Deljenje ponude preko WhatsApp/Viber/Telegram (dopuna, 19.8.2026, na zahtev vlasnika)

**Namerno jednosmerno, bez nove poslovne integracije** — agent šalje gotov tekst + link ka ponudi, otvarajući spoljnu aplikaciju koju već koristi (isti princip koji je vlasnik opisao — lični WhatsApp/Viber nalog na službenom telefonu, korišćen svakodnevno za goste/partnere/kolege). Ovo **nije** dvosmeran kanal: nema bota, nema primanja odgovora nazad u Terminal, nema mapiranja spoljnog naloga na `ClientAccount`/`Subagent`/`Supplier` — gost odgovara istim putem kojim inače komunicira sa agencijom (telefon, dalje ručno). Razlika u odnosu na već postojeći backlog predlog (17.8.2026, "web-intent linkovi") je što sad postoji **stvaran link** kome tekst vodi, ne samo ideja.

1. **`POST /quotes/:id/share`** (zahteva `M5/quote/VIEW` — isti krug kao pregled ponude) — generiše/vraća `share_token`, idempotentno, isti obrazac kao `POST /itineraries/:id/share` (poglavlje 3.0.5).
2. **`GET /quotes/shared/:shareToken`** — **javan, bez guard-a** — vraća stavke ponude sa `final_price` (gostu vidljiva cena), **nikad** `base_cost`/`markup_rule_id`/identitet dobavljača (isto pravilo skrivanja kao poglavlje 6.2). Prikazuje se kao jednostavna, čitljiva stranica (isti princip kao M23 `shareToken` javne stranice) — gost otvara link i vidi ponudu, bez naloga.
3. **Radnja "Pošalji ponudu"** (M17/M7, dugme na ekranu Ponude) otvara meni sa tri opcije, svaka sastavlja gotov tekst (npr. "Vaša ponuda za Budvu, 12-19.8: 450€. Detalji: [link]") i otvara odgovarajući spoljni "web-intent" URL — **agent ne kuca ništa ručno**, samo bira platformu i (za WhatsApp) opciono kome:
   - **WhatsApp** — `https://wa.me/<telefon>?text=<tekst>` ako `Quote.client_account_id` ima telefon u M6 zapisu, inače `https://api.whatsapp.com/send?text=<tekst>` (agent bira kontakt unutar WhatsApp-a).
   - **Viber** — `viber://forward?text=<tekst>` — otvara Viber, agent bira kontakt unutar aplikacije (Viber ne podržava direktan broj u ovom URI obrascu).
   - **Telegram** — `https://t.me/share/url?url=<link>&text=<tekst>` — otvara Telegram, agent bira kontakt.
4. **Ništa od ovoga ne generiše `AuditLogEntry` poseban za "poslato" stanje** — otvaranje spoljne aplikacije se ne može programski potvrditi (Terminal ne zna da li je poruka stvarno poslata unutar WhatsApp-a/Vibera/Telegrama) — sam klik na "Pošalji ponudu" (generisanje linka) se beleži, ne stvarna isporuka.
5. **Van obima ove dopune** — isti mehanizam za `Booking`/vaučer (slanje potvrđene rezervacije istim putem) je prirodno proširenje, ali nije traženo sada — upisano u backlog kao sledeći korak istim obrascem, ne ovde.

### 3.2 `QuoteItem`
| Polje | Tip | Napomena |
| :---- | :---- | :---- |
| id | UUID (PK) | |
| quote_id | UUID (FK) | |
| product_id | UUID, nullable (FK → M2 Product) | dopuna 18.8.2026 (poglavlje 3.0f.6) — nullable jer `MANUAL` stavke nemaju M2 zapis |
| source_type | enum: `CONTRACTED`, `API`, `MANUAL` | dopuna 18.8.2026 — treća vrednost, poglavlje 3.0f |
| manual_product_entry_id / manual_review_status / manual_reviewed_at / manual_reviewed_by / manual_review_decision / rejection_reason | vidi poglavlje 3.0f.6 | popunjeno isključivo za `MANUAL` stavke |
| stay_from / stay_to | date | |
| occupancy | JSONB | `{adults, children, room_config}` — `room_config` strukturiran, vidi poglavlje 3.2a |
| base_cost / base_cost_currency | integer / string | iz M3 RateLine ili M4 AvailabilityQuote, u najmanjoj jedinici valute (poglavlje 2) |
| rate_line_id | UUID, nullable (FK → M3 RateLine) | za `CONTRACTED` stavke — koja konkretna cenovna kombinacija (usluga/`board_type`) je izabrana; nullable za `API` stavke. `room_type` se ne duplira ovde — dobija se preko `ContractPeriod.room_type` (roditelj izabranog `RateLine`, vidi M3 §2.3/2.4) |
| markup_rule_id | UUID (FK → MarkupRule) | koje je pravilo primenjeno — čuva se radi sledljivosti čak i ako se pravilo kasnije promeni |
| final_price / final_price_currency | integer / string | rezultat formule iz 2.1, u najmanjoj jedinici valute |
| provider_quote_reference | string, nullable | za API stavke, radi ponovne provere pred potvrdu |
| unit_count | integer, default 1 | dopuna v1.14 — broj rezervisanih jedinica (soba), izveden iz `occupancy.room_config.length` u trenutku građenja stavke (`QuoteItemBuilderService`); prenosi se nepromenjen na `BookingItem` pri potvrdi (poglavlje 4) i pri izmeni (poglavlje 6) — koristi se da se pri potvrdi rezerviše, a pri otkazivanju/izmeni oslobodi TAČAN broj jedinica u M3, ne uvek pretpostavljena 1 |
| cancellation_policy_snapshot | JSON, nullable | dopuna v1.14 — samo za API (M4) stavke: snimak `M4 AvailabilityQuote.cancellationPolicy` (niz `{days_before_stay, refund_percentage}`, isti oblik kao M3 `CancellationRule`) u trenutku građenja stavke; `null` za CONTRACTED stavke, koje pri otkazivanju i dalje čitaju M3 `CancellationRule` uživo preko `rate_line_id`. Bez ovog snimka, procenat povraćaja za API stavke ne bi mogao da se izračuna deterministički pri otkazivanju (M4 se ne poziva ponovo u tom trenutku, a polisa u međuvremenu može da se promeni) |

### 3.2a `occupancy.room_config` — struktura (dopuna, avgust 2026, na zahtev vlasnika)

Do sada nedefinisan JSON blob — sad je **niz objekata**, jedan po traženoj sobi (npr. porodica koja treba 2 sobe je niz sa dva elementa):

| Polje | Tip | Napomena |
| :---- | :---- | :---- |
| room_type_code | string, nullable | referencira M2 `Product.attributes.room_types[].code` (M2 poglavlje 2.3a) ako je gost/agent već izabrao konkretan tip sobe; `null` dok se samo traži dostupnost bez izbora |
| adults | integer | |
| children | integer | |
| children_ages | niz integer, nullable | uzrast svakog deteta (uključujući bebe) — svrstava se u kategoriju prema M2 `room_types[].age_policy[]` (M2 poglavlje 2.3b), vidi validaciju kapaciteta niže |

**Pravilo slaganja:** zbir `adults`/`children` preko svih stavki `room_config[]` mora odgovarati `occupancy.adults`/`occupancy.children` na nivou cele stavke — `occupancy.adults`/`children` ostaju jednostavan zbirni broj (koriste ga i tipovi bez sobe, npr. `TRANSPORT`/`TICKET`/`EVENT`, M5 poglavlje 11), `room_config[]` je detaljan raspored samo za tipove sa smeštajem (`ACCOMMODATION`, `PACKAGE` koji uključuje smeštaj). Neusklađen zbir se odbija pri kreiranju `Quote` (`POST /quotes`) sa jasnom porukom, ne tiho ispravlja.

Ako `room_config` nije poslat za tip koji zahteva smeštaj, tretira se kao jedna soba sa svim gostima zajedno (`[{room_type_code: null, adults, children, children_ages: null}]`) — nazadnokompatibilno ponašanje, ne obavezuje pozivaoca da uvek eksplicitno razlaže po sobama.

**Validacija kapaciteta po uzrastu (dopuna, avgust 2026, na zahtev vlasnika):** kad je `room_type_code` poznat, M5 za svaki uzrast iz `children_ages[]` pronalazi odgovarajuću kategoriju u M2 `room_types[].age_policy[]` (po `age_from`/`age_to` te sobe — M2 poglavlje 2.3b) i broji protiv `capacity_adults`/`capacity_children` **samo kategorije sa `counts_toward_capacity = true`**. Gost čija kategorija ima `counts_toward_capacity = false` (tipično beba u krevetcu) se i dalje evidentira na stavci (radi krevetca i eventualne kasnije cene iz M3), ali se ne računa protiv formalnog kapaciteta sobe niti odbija `Quote` zbog toga. Broj gostiju u pojedinačnoj kategoriji koji prelazi `age_policy[].max_count` te kategorije (npr. druga beba u sobi koja dozvoljava samo jednu) se odbija sa jasnom porukom, nezavisno od ukupnog kapaciteta.

### 3.2b Računanje `base_cost` za sobu sa `age_pricing[]` (dopuna, avgust 2026, na zahtev vlasnika)

Kad `rate_line_id` upućuje na `RateLine` koji ima `price_basis`/`age_pricing[]` (M3 poglavlje 2.4/2.4a), `base_cost` jedne sobe za jedno noćenje se računa ovako, deterministički, bez AI procene (princip #4 Master dokumenta):

1. **Svrstaj svakog gosta u kategoriju.** Odrasli su `ADULT`; svaki uzrast iz `children_ages[]` se svrstava po `M2 room_types[].age_policy[]` (isti mehanizam kao validacija kapaciteta iznad). Gosti unutar iste kategorije dobijaju redni broj (`occupant_index`) po redosledu u nizu — prvi, drugi, itd.
2. **Osnovna popunjenost.** `RateLine.occupancy` (M3 poglavlje 2.4) opisuje na koju popunjenost se `RateLine.price` odnosi (npr. "dvokrevetna" = 2 `ADULT` gosta). Ti gosti se ne obračunavaju posebno kroz `age_pricing[]` — već su pokriveni cenom iz koraka 3.
3. **Osnovna cena.**
   - Ako `price_basis = PER_ROOM_PER_NIGHT` — `RateLine.price` se uzima **jednom**, za celu sobu.
   - Ako `price_basis = PER_PERSON_PER_NIGHT` — `RateLine.price` se množi brojem `ADULT` gostiju pokrivenih osnovnom popunjenošću.
4. **Svaki gost iznad osnovne popunjenosti** (bilo koje kategorije — dodatni `ADULT`, `CHILD`, `TEEN`, `INFANT`) se dodaje pojedinačno preko `M3 RateLine.age_pricing[]`: pronađi red po pravilu razrešavanja iz M3 poglavlja 2.4a (kategorija + `occupant_index` + `min_adults_present`), primeni `pricing_mode` (`PERCENTAGE_OF_BASE_PRICE` → `round(RateLine.price * percentage / 100)`, ili `FLAT_PRICE_PER_NIGHT` → `flat_price`), i saberi.
5. **Krevetac.** Ako soba ima gosta kategorije sa `requires_crib = true` (M2 poglavlje 2.3b) i `RateLine.crib_fee_per_night` je postavljen, dodaj taj iznos jednom po traženom krevetcu.
6. **Zbir po noći × broj noćenja** (`stay_to - stay_from`) = `base_cost` te sobe. Za više soba u `room_config[]`, `base_cost` cele `QuoteItem` je zbir po sobama.

Ako korak 4 ne pronađe odgovarajući `age_pricing[]` red ni za jednog gosta (M3 poglavlje 2.4a, ograda), kreiranje `Quote` se odbija — ne postoji podrazumevana pretpostavka o ceni. Rezultat ovog izračuna je `base_cost` na koji se dalje primenjuje `MarkupRule` (poglavlje 2.1) — ništa se ovde ne menja u toj formuli, samo se preciznije određuje ulaz u nju za sobe sa mešovitim uzrastom gostiju.

---

## 4.0a Određivanje `Booking.tip_nastupanja` (dopuna, avgust 2026 — rešava nalaz #1 iz `VALIDACIJA-WORKFLOW-B2C.md`/`VALIDACIJA-WORKFLOW-B2B.md`)

M10 poglavlje 4.1 kaže da `tip_nastupanja` "bira prodajni tim/agent pri potvrdi rezervacije" — ovo tačno važi za `INTERNAL_PANEL` i `PHONE` kanale (M17, poziv), gde postoji ljudski nalog koji svesno bira. Za samouslužne kanale (`B2C_SITE`, `MOBILE`, `B2B_PORTAL`) **nema ljudskog naloga u toku** — vrednost se mora izvesti automatski, deterministički, bez AI procene (princip #4 Master dokumenta):

1. **Sve stavke iz iste ponude moraju se slagati.** Za svaku `QuoteItem`, izvedi kandidat vrednost: `CONTRACTED` stavka → `M3 Contract.default_tip_nastupanja` (M3 poglavlje 2.2a) preko `Product.source_contract_id`; `API` stavka → `M4 ProviderConfig.default_tip_nastupanja` (M4 poglavlje 3.1) preko `Product.source_provider`.
2. Ako se svi kandidati slažu, ta vrednost postaje `Booking.tip_nastupanja`.
3. Ako se kandidati **ne slažu** (npr. ponuda kombinuje proizvod gde je agencija organizator sa proizvodom gde je posrednik), samouslužni kanal **ne sme sam da potvrdi rezervaciju** — sistem vraća jasnu grešku i traži da se ponuda razdvoji na zasebne rezervacije, ili da tim preuzme potvrdu ručno kroz M17 (gde ljudski nalog svesno bira jednu vrednost za celu rezervaciju, svestan da to ne odgovara doslovno sastavu stavki — retka, granična situacija).
4. **`MANUAL` stavke nemaju kandidata** (dopuna 18.8.2026, poglavlje 3.0f) — nema `Contract`/`ProviderConfig` iz kojeg bi se vrednost izvela. Tretira se kao granični slučaj iz koraka 3: vrednost bira ručno interni tim — agent pri kreiranju (`INTERNAL_PANEL`/`PHONE`) ili osoblje pri pregledu (poglavlje 3.0f.4, za `B2B_PORTAL` stavke koje čekaju odobrenje).
4. Za `INTERNAL_PANEL`/`PHONE` kanale, ponašanje iz koraka 1–3 služi samo kao **podrazumevana, unapred popunjena vrednost** — ljudski nalog je uvek slobodan da je eksplicitno promeni pre potvrde (M3 poglavlje 2.2a), isti princip kao svaki drugi podrazumevani unos u sistemu.

`Booking.tip_nastupanja` ostaje **nepromenljivo posle kreiranja rezervacije**, bez obzira na to da li je vrednost automatski izvedena ili ručno izabrana (nepromenjeno pravilo iz M10 poglavlja 4.1).

---

## 4. Potvrda rezervacije (Quote → Booking)

Korak po korak:

0. **Provera pregleda osoblja** (dopuna 18.8.2026, poglavlje 3.0f.4) — ako bilo koja `QuoteItem` ima `manual_review_status = AWAITING_STAFF_REVIEW`, potvrda se odbija sa jasnom porukom ("čeka pregled osoblja"), pre bilo koje druge provere.
1. Proveri da `Quote.status = DRAFT` i da nije istekla. Ako je istekla, **ponovo izračunaj cenu/dostupnost** (nova pitanja ka M3/M4) pre nastavka — nikad se ne potvrđuje na osnovu zastarele cene. Odredi `Booking.tip_nastupanja` po pravilu iz poglavlja 4.0a. Redosled provera pre bilo kog poziva ka M3/M4, **uvek u ovom fiksnom redosledu** (dopuna avgust 2026 — rešava nalaz o nedefinisanom redosledu iz `VALIDACIJA-WORKFLOW-B2B.md`): **(a)** ako je `tip_nastupanja = ORGANIZATOR`, pozovi M11 `GET /travel-guarantee/utilization` (M11 poglavlje 4.2) — prekoračenje limita garancije odbija potvrdu; **(b)** ako `Quote.client_account_id` pripada Subagentu (M7 poglavlje 2.1 — provera postojanja `Subagent` zapisa za taj `client_account_id`, ne provera `ClientAccount.account_type`, jer obično pravno lice koje nije registrovan subagent ne prolazi kroz ovu proveru), proveri kreditni limit (M7 poglavlje 4) — prekoračenje odbija potvrdu. Razlog za ovaj redosled: zakonska ograda (garancija) je stroža i nezavisna od toga ko kupuje, pa se proverava prva; poslovna ograda (kredit) proverava se druga, samo ako je prva prošla.
2. Za svaku `QuoteItem`:
   - Ako `CONTRACTED`: pozovi M3 `/contracts/:id/periods/:periodId/reserve`. Uspeh → `item_status = CONFIRMED`. Ako je period `ON_REQUEST` → `item_status = PENDING_SUPPLIER_CONFIRMATION`. Neuspeh (nema kapaciteta) → stavka pada.
   - Ako `API`: pozovi M4 `/internal/providers/:code/bookings` sa jedinstvenim `idempotency_key`. Mapiraj `BookingConfirmation.status` u `item_status`.
   - Ako `MANUAL` (dopuna 18.8.2026, poglavlje 3.0f.5): nema poziva ka M3/M4 — `item_status = CONFIRMED` direktno (dobavljač je već ručno potvrđen ili odobren pri pregledu osoblja, koraci pre ovog poziva).
3. **Sve ili ništa:** ako bilo koja stavka padne (nema kapaciteta, provajder odbije), sve već uspešno rezervisane stavke iz ovog pokušaja se **odmah oslobađaju** (M3 release / M4 `cancelBooking`) — sistem nikad ne ostavlja polovično rezervisanu rezervaciju. Gost/agent dobija jasnu poruku koja stavka nije uspela, uz ponudu da prilagodi izbor.
4. Ako sve stavke uspeju, kreira se `Booking` sa statusom: `CONFIRMED` ako su sve stavke `CONFIRMED`; `PENDING_SUPPLIER_CONFIRMATION` ako je bar jedna stavka u tom stanju (rezervacija prelazi u `CONFIRMED` tek kad se i poslednja stavka potvrdi — ručno ili preko M4 povratnog poziva). `buyer_name`/`buyer_type`/`buyer_tax_id` (dopuna v1.17) se unose u ovom pozivu (`POST /quotes/:id/confirm`) i snimaju direktno na `Booking` — obavezni podaci, provereni pre kreiranja.
5. Emituje se događaj (poglavlje 9).

### 4.1 `Booking`
| Polje | Tip | Napomena |
| :---- | :---- | :---- |
| id | UUID (PK) | |
| booking_number | string, unique | čitljiva oznaka za gosta (npr. `TT-2027-000482`) |
| client_account_id | UUID (FK → M6) | ko plaća |
| buyer_name | string | dopuna v1.17 (avgust 2026) — unosi se u `POST /quotes/:id/confirm` (poglavlje 11), ne čeka M6; potrebno M10 fiskalizaciji (SEF/ESIR, M10 spec §1.1/§2) |
| buyer_type | enum: `FIZICKO_LICE`, `PRAVNO_LICE` | dopuna v1.17 — određuje da li M10 izdaje SEF e-fakturu (pravno lice) ili ESIR račun (fizičko lice) |
| buyer_tax_id | string, nullable | dopuna v1.17 — obavezno kad je `buyer_type = PRAVNO_LICE` (PIB), validirano i u DTO-u i u servisu |
| channel | enum (isto kao Quote) | |
| tip_nastupanja | enum: `ORGANIZATOR`, `POSREDNIK` | dodato u M10 specifikaciji, poglavlje 4.1 — određuje se po pravilu iz poglavlja 4.0a (automatski za samouslužne kanale, ručno biran uz podrazumevanu vrednost za `INTERNAL_PANEL`/`PHONE`), **nepromenljivo posle kreiranja rezervacije**; određuje PDV tretman (M10) i tip klijentskog ugovora (M20) |
| status | enum: `PENDING_SUPPLIER_CONFIRMATION`, `CONFIRMED`, `MODIFIED`, `CANCELLED`, `COMPLETED` | |
| payment_status | enum: `UNPAID`, `PARTIALLY_PAID`, `PAID`, `INVOICE_PENDING` | **potvrđeno: potvrda rezervacije ne zavisi od statusa plaćanja** — B2B kredit i avansno plaćanje su podržani od starta |
| total_price / currency | integer / string | zbir `final_price` svih stavki, u najmanjoj jedinici valute |
| voucher_url | string, nullable | generiše se kad su ispunjeni uslovi iz poglavlja 6 (puna uplata, ili odobren izuzetak) |
| voucher_override_approved_by | UUID, nullable (FK → M1 User) | popunjeno samo ako je vaučer izdat bez pune uplate — vidi poglavlje 6 |
| voucher_override_reason | text, nullable | obrazloženje izuzetka, unosi ga odobravalac |
| voucher_override_at | timestamp, nullable | |
| contract_terms_accepted_at | timestamp, nullable | dopuna v1.19 (avgust 2026) — kopija `Quote.contract_terms_accepted_at` u trenutku potvrde (samo kad je `Quote.contract_terms_accepted = true`); `Booking` nema `quote_id` referencu nazad, pa se ovo polje kopira direktno jer je M20 stvarno potrebno (poglavlje 3.1/3.2, ne-null znači ugovor se automatski prevodi u `ACCEPTED`) |
| created_at / confirmed_at / cancelled_at | timestamp | |
| created_by | UUID | user ili "GOST_SELF" |
| referral_tracking_code | string, nullable | dopuna avgust 2026 (M12 poglavlje 3a) — kopirano iz `Quote.referral_tracking_code` pri potvrdi, isti obrazac kao `channel`/`client_account_id`; M13 ga razrešava ka `ContentPiece` pri izgradnji projekcije |

### 4.2 `BookingItem`
| Polje | Tip | Napomena |
| :---- | :---- | :---- |
| id | UUID (PK) | |
| booking_id | UUID (FK) | |
| product_id | UUID, nullable (FK → M2) | dopuna 18.8.2026 — nullable za `MANUAL` (poglavlje 3.0f) |
| source_type | enum: `CONTRACTED`, `API`, `MANUAL` | dopuna 18.8.2026 — treća vrednost, poglavlje 3.0f |
| manual_product_entry_id | UUID, nullable (FK → `ManualProductEntry`) | kopira se iz `QuoteItem` pri potvrdi, samo za `MANUAL` |
| supplier_reference | string, nullable | za CONTRACTED: `ContractPeriod.id`; za API: `providerBookingReference` iz M4; za MANUAL: prazno ili slobodan tekst ako dobavljač da sopstvenu potvrdu (poglavlje 3.0f.5) |
| stay_from / stay_to | date | |
| base_cost / rate_line_id / markup_rule_id / final_price | (isto kao QuoteItem) | prenosi se iz ponude u trenutku potvrde |
| item_status | enum: `CONFIRMED`, `PENDING_SUPPLIER_CONFIRMATION`, `CANCELLED` | |
| cancellation_refund_percentage | integer, nullable | popunjava se pri otkazivanju, iz M3 `CancellationRule` (preko `rate_line_id`) za CONTRACTED, ili iz `cancellation_policy_snapshot` (dopuna v1.14, ista deterministička formula — najspecifičniji `days_before_stay` koji je `<= dana do stay_from` pobeđuje) za API |
| unit_count | integer, default 1 | dopuna v1.14 — kopira se iz `QuoteItem.unit_count` pri potvrdi (poglavlje 4) ili pri izmeni (poglavlje 6); koristi se pri otkazivanju/izmeni da se oslobodi tačan broj jedinica nazad u M3, ne uvek pretpostavljena 1 (ranija greška — ispravljeno v1.14) |
| cancellation_policy_snapshot | JSON, nullable | dopuna v1.14 — kopira se iz `QuoteItem.cancellation_policy_snapshot` pri potvrdi/izmeni; vidi poglavlje 3.2 za tačan oblik |
| assigned_guide_id | UUID, nullable (FK → M1 User, uloga `VODIC`) | dodato pri specifikaciji M9 — dodeljuje interni panel (M17), koristi ga M9 za filtriranje itinerara vodiča na terenu |
| duplicate_conflict_item_id | UUID, nullable (FK → `BookingItem`) | dodato u poglavlju 6.4 — popunjava se proverom duplikata pri otkazivanju, referenca na konfliktnu stavku koja je pronađena |
| duplicate_check_overridden_by / duplicate_check_overridden_at | UUID (FK → M1 User) / timestamp, oba nullable | dodato u poglavlju 6.4 — popunjava se samo ako je operater eksplicitno potvrdio otkazivanje uprkos pronađenom duplikatu |
| announced_at | timestamp, nullable | dodato u poglavlju 8.6 — trenutak kad je stavka formalno najavljena dobavljaču; za `CONTRACTED` kopira se iz `SupplierManifest.sent_at` liste koja je sadrži, za `API` popunjava se automatski u trenutku potvrde (poglavlje 8.6) |
| supplier_confirmed_at / supplier_confirmed_by | timestamp / UUID (FK → M1 User), oba nullable | dodato u poglavlju 8.6 — potvrda da je dobavljač primio/prihvatio najavu; za `CONTRACTED` ručni unos (dobavljači nemaju API), za `API` popunjava se automatski |

### 4.3 `BookingItemGuest` (spojna tabela)
`booking_item_id`, `guest_profile_id` (FK → buduće M6 Gost) — više gostiju po stavci (npr. porodica u jednoj sobi).

**Napomena o istoriji statusa:** promene statusa rezervacije se ne čuvaju u posebnoj tabeli — koristi se `AuditLogEntry` iz M1 (modul `M5`, `resource_type = Booking`), u skladu sa principom #5 iz poglavlja 3 (jedan mehanizam za trag izmena, ne dupliranje).

### 4.4 UI tok "Ponuda → Rezervacija" u M17/M7 (dopuna, 18.8.2026, na zahtev vlasnika)

Redosled ekrana koji agent (M17) ili subagent (M7) prolazi od finalne Ponude do potvrđene Rezervacije, sve podaci već definisani gore u ovom poglavlju — ovo samo propisuje redosled/prikaz, ne nove podatke:

1. **Tiha provera pre prikaza forme** — sistem ponovo proveri cenu/dostupnost za `API`-sourced stavke čiji `quote_expires_at` ističe (poglavlje 3.0b.3, korak 5); ako se cena promenila, agent se upozorava pre nego što bilo šta unese.
2. **Podaci o kupcu** (`buyer_name`/`buyer_type`/`buyer_tax_id`) — predpopunjeno iz `Quote.client_account_id` ako postoji (M6), inače agent pretražuje/kreira nalog inline.
3. **Podaci o gostima po stavci** (`BookingItemGuest`, poglavlje 4.3) — imena/uzrast po sobi, delimično već poznato iz `occupancy.room_config`.
4. **`tip_nastupanja`** (poglavlje 4.0a) — prikazuje se agentu na izbor **samo** u graničnom slučaju kad se stavke ne slažu automatski; inače se ne prikazuje, vrednost je već određena.
5. **Status ugovora sa klijentom (M20)** — za `INTERNAL_PANEL`/`PHONE` kanal `contract_terms_accepted` **nije** uslov za nastavak (to važi isključivo za samouslužne kanale, poglavlje 3.1) — prikazuje se kao informativan status, ne blokirajući checkbox.
6. **Plaćanje** — prikaz `Booking.payment_status` kao informativno polje (poglavlje 5); ne blokira `POST /quotes/:id/confirm` osim gde ovaj dokument ili M10 to eksplicitno traže.
7. **Potvrda** — dugme "Potvrdi rezervaciju" poziva `POST /quotes/:id/confirm` (poglavlje 11); rezultat po stavci (`CONFIRMED` vs `PENDING_SUPPLIER_CONFIRMATION`, poglavlje 4) prikazan pojedinačno po redu, ne samo jedan zbirni status.
8. **Rezultat** — nastala `Booking` se otvara u **novom tabu** (potvrđeno 18.8.2026 — namerna radnja, isti princip kao dizajn dokument §5a "prelazak na pun zapis je uvek nov tab"), tab Ponude ostaje otvoren nepromenjen za poređenje/istoriju.

**Oblik forme (koraci 2-6) — korisnički izbor, ne fiksna odluka dokumenta** (na zahtev vlasnika 18.8.2026): agent bira između **jedne duge forme** (sve sekcije vidljive odjednom, brže za rutinski rad) i **stepper-a** (korak po korak sa Nazad/Dalje, vodi kroz proces eksplicitno) — izbor se čuva kao `UserPreference.key = "booking_form_layout"` (M1 spec poglavlje 3.9), dostupan u ličnim podešavanjima (dizajn dokument §5d). Podrazumevana vrednost za novog korisnika: jedna duga forma. Oba oblika prikazuju isti skup polja istim redosledom — razlika je isključivo u tempu prikaza, ne u sadržaju.

---

## 5. Plaćanje — namerno van obima detalja

`Booking.payment_status` postoji kao polje, ali **sama naplata, fiskalizacija i knjiženje su van obima M5** — to je M10 (Faza 2). M5 izlaže `PATCH /bookings/:id/payment-status`, koji poziva isključivo M10 kad se plaćanje obradi; M5 sam nikad ne inicira transfer novca (u skladu sa "Nikad autonomno" iz poglavlja 7).

---

## 6. Upravljanje rezervacijom nakon potvrde

- **Izmena** (datum, broj gostiju): tretira se interno kao otkazivanje pogođene stavke + nova provera dostupnosti/cene za novi zahtev, prikazano gostu kao jedna radnja "izmeni rezervaciju". Razlika u ceni (doplata ili povraćaj) se beleži, stvarna naplata/povraćaj opet ide kroz M10.
- **Otkazivanje:** za svaku stavku koja se otkazuje, `cancellation_refund_percentage` se izračunava iz M3 `CancellationRule` (po broju dana do `stay_from`) ili M4 `cancellationPolicy`; kapacitet se oslobađa nazad (M3 `units_sold` se umanjuje, ili M4 `cancelBooking`). Booking prelazi u `CANCELLED` samo ako se otkazuju sve stavke; ako se otkazuje samo deo, booking ostaje `MODIFIED` sa preostalim aktivnim stavkama.
- **Vaučer:** generiše se PDF sa detaljima rezervacije, čuva se u EU cloud skladištu, referenca u `voucher_url`. **Podrazumevano se generiše tek kad je `Booking.status = CONFIRMED` I `payment_status = PAID`** (puna uplata) — sistem ne generiše vaučer za `UNPAID`, `PARTIALLY_PAID` ili `INVOICE_PENDING` bez izričitog odobrenja. Pošto uplata (posebno B2B kredit/avans) često stiže posle same potvrde rezervacije, sistem proverava ovaj uslov ne samo pri potvrdi nego i pri svakoj promeni `payment_status` (`PATCH /bookings/:id/payment-status`, poglavlje 5) — čim stavka pređe u `PAID`, vaučer se generiše automatski ako do tada nije već izdat.

  **Izuzetak — izdavanje bez pune uplate:** Vlasnik ili Direktor mogu ručno odobriti izdavanje vaučera bez uplate ili sa delimičnom uplatom. Ovo je uvek eksplicitna ljudska radnja — nivo **"Nikad autonomno"** iz poglavlja 7 Master dokumenta (AI agent zadužen za M5 nikad sam ne pokreće ovo odobrenje, isto obrazloženje kao izuzeci od finansijskih ograda u M3/M11) — beleži se u `voucher_override_approved_by`/`voucher_override_reason`/`voucher_override_at` (poglavlje 4.1) i vidljivo je u M1 audit logu.

  **Dodatni uslov za `tip_nastupanja = ORGANIZATOR`** (dopuna M20 specifikacije, poglavlje 3.3): vaučer se ne generiše — ni automatski ni preko izuzetka iznad — dok `ClientContract` (M20) ne postoji bar u statusu `GENERATED`. Ugovor sa klijentom mora postojati pre nego što gost dobije vaučer.

### 6.1 Praćenje posle potvrde — podsetnici i alarmi

Van same potvrde i vaučera, M5 periodičnim poslom prati tri situacije i upozorava tim (interni panel + email, isti obrazac kao M11 poglavlje 2.2) — sve na nivou **"Autonomno"** iz poglavlja 7 Master dokumenta: čisto informativno, ne menja niti blokira stanje rezervacije.

- **Neplaćena rezervacija sa izdatim vaučerom (izuzetak iznad):** dok god `voucher_override_approved_by` nije prazno i `payment_status != PAID` i `Booking.status != CANCELLED`, sistem svakodnevno podseća tim da uplata nije završena. Podsetnik prestaje čim `payment_status` pređe u `PAID` (redovan generiše se vaučer bez izuzetka od tog trenutka) ili se rezervacija otkaže.
- **Otvorena potvrda dobavljača, po stavci:** `BookingItem.item_status = PENDING_SUPPLIER_CONFIRMATION` (poglavlje 4) prati se **po stavci, ne po celoj rezervaciji** — kod rezervacije sa više proizvoda od različitih dobavljača (npr. hotel + transfer), svaka stavka se nezavisno prati prema sopstvenom dobavljaču, razrešenom preko `BookingItem.product_id` → M3 `Supplier` (isto razrešavanje kao poglavlje 8.1). Stavka koja ostane u ovom statusu duže od praga (podrazumevano 48h, konfigurabilno po tipu proizvoda) generiše upozorenje za tu stavku — potvrda ostalih stavki iste rezervacije kod drugih dobavljača ne utiče na ovaj alarm i obrnuto.
- **Vaučer nedostaje uprkos punoj uplati:** ako `payment_status = PAID` a `voucher_url` je i dalje prazno (van override toka iznad, koji bi ga već popunio), to je znak da automatsko generisanje iz poglavlja 6 nije uspelo — sistem odmah generiše upozorenje; ovo je greška u sistemu i ne sme tiho da prođe.
- **Nenajavljena stavka pred boravak (dopuna, avgust 2026 — problem #2):** `CONTRACTED` stavka sa `item_status = CONFIRMED` čiji `stay_from` pada u narednih 7 dana (podrazumevano, konfigurabilno), a `announced_at` je i dalje prazno — znači da dobavljač još ne zna ko dolazi. Ovo je hitniji alarm od ostalih u ovoj listi jer direktno preti check-inu.
- **Najava bez potvrde dobavljača (dopuna, avgust 2026 — problem #2):** `announced_at` popunjeno duže od 5 dana (podrazumevano, konfigurabilno), a `supplier_confirmed_at` i dalje prazno — informativni podsetnik timu da proveri kod dobavljača, niži prioritet od prethodnog.

Ovi alarmi se prikazuju u internom panelu (M17 Agent Inbox) i ne uvode novu dozvolu — vidljivi su svima sa `M5/booking/VIEW` nad tom rezervacijom, uz kopiju na email Vlasniku/Direktoru za treću i četvrtu stavku (sistemska greška, odnosno rizik od propuštenog check-ina).

### 6.1a Prelaz u `COMPLETED` (dopuna, avgust 2026, otkriveno pri implementaciji M6)

`BookingStatus.COMPLETED` (poglavlje 4.1) je postojao u enumu od prve verzije ove specifikacije, ali nikad nije bio definisan mehanizam koji ga stvarno postavlja — M6 poglavlje 4.3 (anketa posle putovanja) je prvi modul kome je taj prelaz stvarno potreban, pa se dopunjuje ovde.

**Pravilo:** isti dnevni periodičan posao kao poglavlje 6.1 (ista "Autonomno" klasifikacija — mehanička posledica proteklog vremena, ne poslovna odluka) proverava sve `Booking` u statusu `CONFIRMED` ili `MODIFIED` čije su **sve** neotkazane stavke (`BookingItem.item_status != CANCELLED`) prošle svoj `stay_to` (u prošlosti u odnosu na trenutak provere). Takva rezervacija prelazi u `status = COMPLETED` i emituje se događaj `booking.completed` (poglavlje 9).

Rezervacija kod koje su **sve** stavke otkazane nikad ne dobija ovaj prelaz — takva rezervacija je već u `status = CANCELLED` (poglavlje 6), do kog dolazi drugim putem, ne kroz ovaj posao.

### 6.2 Identitet dobavljača se nikad ne izlaže B2C/B2B kanalima (dopuna, avgust 2026, na zahtev vlasnika)

Isto pravilo kao M2 poglavlje 5.1, primenjeno na M5: vaučer (poglavlje 6), pregled rezervacije koji vidi gost (M8/M9) ili B2B subagent (M7), i svaki M5 API odgovor ka tim kanalima **nikad** ne sadrže `BookingItem.supplier_reference` niti bilo koje polje iz M3 `Supplier`/`Contract` do kog bi se moglo doći preko `BookingItem.product_id` → M2 `Product.source_contract_id`. Vidljivi ostaju proizvod, datumi, cena za gosta/subagenta i status — ono što je već prirodan sadržaj vaučera/pregleda.

**Izuzetak — interni kanal:** M17 (interni radni panel), pregled kalendara (poglavlje 7) i operativne liste ka dobavljaču (poglavlje 8) i dalje pun pristup imaju — poglavlje 8 uostalom *postoji* zato što dobavljač mora da dobije spisak, to ostaje nepromenjeno. Ograničenje važi isključivo za ono što vidi gost (M8/M9) ili B2B subagent (M7).

**Sprovođenje:** isti princip kao M2 poglavlje 5.1 — polje se ne sme naći u payload-u ka gost/B2B kontekstu, ne oslanjati se na to da front-end jednostavno ne prikaže.

**Mehanizam izbora konteksta (dopuna, avgust 2026, priprema za M8).** `GET /bookings/:id` bira masking kontekst automatski na serveru, ne iz parametra koji klijent šalje. Pošto access token namerno ne nosi ništa o identitetu/pravima osim `user_id`/`session_id` (M1 spec §3.7 — "ništa o pravima"), servis pri svakom pozivu učita `User.account_type` iz baze po `userId` iz tokena (ista "uvek uživo, nikad iz tokena" filozofija kao M1 §3.6 RBAC provera) i na osnovu toga bira kontekst: `account_type = GUEST` → `B2C_SITE` (M8, kasnije M9-gost); `account_type = SUBAGENT_CONTACT` → B2B kontekst (M7); svaki drugi `account_type` (interno osoblje) → `INTERNAL_PANEL`, pun prikaz. Ovim se sprečava da bilo koji pozivalac sam sebi zatraži pun prikaz slanjem drugačijeg parametra — jedini ulaz je identitet iz već proverenog tokena.

**Vlasništvo, ne samo maskiranje (otkriveno pri implementaciji, avgust 2026).** `PermissionsGuard` proverava samo da li pozivalac ima `M5/booking/VIEW` dozvolu za modul/resurs/akciju — ne i da li je konkretna rezervacija njegova. Bez dodatne provere, gost sa dodeljenom `M5/booking/VIEW` dozvolom (potrebnom za "Moje rezervacije" na M8) mogao bi da pročita TUĐU rezervaciju prostom izmenom `:id` u URL-u, ili tuđu listu rezervacija slanjem tuđeg `clientAccountId` query parametra na `GET /bookings`. Zato `GET /bookings/:id` i `GET /bookings` za ne-`INTERNAL_PANEL` kontekst **nameću** `client_account_id = ownClientAccountId` (izveden iz `User.linked_profile_id`, ne iz klijentskog parametra) — pokušaj čitanja tuđe rezervacije vraća 404 (ne otkriva ni da rezervacija postoji), a `clientAccountId` query parametar se za taj kontekst ignoriše.

### 6.3 Sistemski izuzetak izdavanja vaučera — subagent unutar odobrenog kredita (dopuna, avgust 2026 — rešava nalaz iz `VALIDACIJA-WORKFLOW-B2B.md`)

Poglavlje 6 zahteva `payment_status = PAID` pre automatskog izdavanja vaučera, sa ručnim izuzetkom ograničenim na Vlasnika/Direktora (`voucher_override_*`). Ovo pravilo je pisano sa B2C rizikom na umu (spreči izdavanje dokumenta bez ikakve naplate) — ali B2B prodaja na kredit (M7 poglavlje 4) je **redovan, ugovoren način poslovanja** sa subagentom, ne izuzetak, pa bi zahtevanje ručnog odobrenja za svaku takvu rezervaciju opterećivalo Vlasnika/Direktora bez stvarne dodatne bezbednosti — kreditni rizik je već proveren i ograničen pri potvrdi rezervacije (poglavlje 4, korak 1b).

**Pravilo:** Za `Booking` gde `client_account_id` pripada `Subagent`-u sa `status = ACTIVE` (M7 poglavlje 2.1), vaučer se generiše automatski čim `Booking.status = CONFIRMED`, **nezavisno od `payment_status`** — pod uslovom da je rezervacija uopšte uspela da prođe proveru kreditnog limita iz poglavlja 4 (što znači da je `current_outstanding_balance` posle ove rezervacije i dalje unutar `credit_limit`). Ovo je nivo **"Autonomno"** iz poglavlja 7 Master dokumenta — mehanička posledica već izvršene ljudske/sistemske kontrole rizika (odobrenje subagenta u `ACTIVE`, poglavlje 9 M7 specifikacije; kreditni limit koji je postavio Vlasnik/Direktor), ne nova autonomna finansijska odluka.

`voucher_override_*` polja (poglavlje 6) ostaju rezervisana za **izuzetke van ovog pravila** — npr. rezervacija koja bi prekoračila kreditni limit, ili poseban jednokratni dogovor van standardnog B2B odnosa; u tim slučajevima i dalje je obavezno ručno odobrenje Vlasnika/Direktora, nepromenjeno.

**Napomena:** ovo ne menja tok naplate niti fiskalizaciju — `payment_status` i dalje tačno odražava stvarno stanje uplate (M10), samo se izdavanje vaučera više ne uslovljava punom uplatom za ovu specifičnu, već rizično-proverenu kategoriju rezervacija.

### 6.4 Provera duplikata pre otkazivanja (dopuna, avgust 2026 — rešava problem #10 iz `Problemi koje zelimo da resimo ovom aplikacijom.md`, vidi `24-GAP-ANALIZA-PROBLEMI-VS-ARHITEKTURA.md` poglavlje 10)

**Poznat slučaj iz prakse:** isti gost je imao dve odvojene rezervacije za isti hotel/termin/uslugu — jednu direktnu, jednu preko B2B subagenta (M7) — svaku kao poseban `Booking`. Operater nije primetio da imena gostiju upućuju na istu osobu i otkazao je onu koja kod nas nije bila uplaćena, misleći da je duplikat. Pošto dobavljač (hotel) rezervacije prati po imenu i prezimenu gosta, a ne po internom ID-ju našeg sistema, dobavljač je posledično otkazao i onu drugu, ispravnu i uplaćenu rezervaciju.

**Pravilo:** pre nego što se otkazivanje stavke iz poglavlja 6 ("Otkazivanje") izvrši, sistem proverava da li postoji **druga aktivna** `BookingItem` (`item_status = CONFIRMED` ili `PENDING_SUPPLIER_CONFIRMATION`, u bilo kom `Booking`-u, **nezavisno od kanala ili `client_account_id`**) koja upućuje na mogući duplikat:

- isti `product_id` (isti objekat kod istog dobavljača),
- preklapajući `stay_from`/`stay_to`,
- podudarno ime gosta preko `BookingItemGuest` → M6 `GuestProfile.first_name`/`last_name` — **deterministički fuzzy-match** (normalizacija dijakritika/velikih-malih slova + prag sličnosti niske cene računanja, npr. Levenshtein), ne AI/LLM poziv po stavci, u skladu sa principom #4 Master dokumenta ("determinizam pre autonomije").

Ako provera pronađe podudaranje, otkazivanje se **ne izvršava tiho** — operater dobija eksplicitno upozorenje pre potvrde storna, sa prikazom konfliktne stavke (broj rezervacije, kanal, ime gosta, `payment_status`). Otkazivanje se nastavlja tek posle eksplicitne dodatne potvrde operatera; ta potvrda popunjava `duplicate_conflict_item_id`, `duplicate_check_overridden_by` i `duplicate_check_overridden_at` (poglavlje 4.2) i ostaje vidljiva u `AuditLogEntry` (isti mehanizam kao poglavlje 4.3).

Nivo autonomije: **"Predloži pa čovek odobri"** (Master dokument poglavlje 7) — sistem ne blokira otkazivanje automatski (mogu postojati legitimni razlozi za dve odvojene rezervacije iste osobe — npr. dve različite grupe putnika koje slučajno dele ime i prezime), samo traži svesnu potvrdu umesto da propusti storno bez upozorenja. Provera se ne ograničava na posebnu dozvolu — važi za svakog ko ima pravo da otkaže stavku (`M5/booking/CANCEL`, poglavlje 10).

**Primena na subagentski kanal (M7):** pošto B2B subagenti otkazuju rezervacije kroz isti `POST /bookings/:id/cancel` tok (bilo direktno kroz portal, bilo preko internog panela u njihovo ime), provera duplikata iz ovog poglavlja se automatski primenjuje i tamo — nije potreban poseban mehanizam u M7 specifikaciji, samo referenca (vidi `12-SPECIFIKACIJA-M7-B2B-SUBAGENTI.md`).

**Otvoreno za dalje:** ova provera rešava trenutak storna, ali ne i uzrok — da isti fizički gost stoji iza dva različita `ClientAccount`-a (direktan gost i klijent subagenta) bez povezanog profila u M6. Dugoročno rešenje (predlog spajanja/povezivanja gostiju u M6 kad se otkrije podudaranje identiteta) ostaje otvoreno pitanje za M6 specifikaciju.

---

## 7. Kalendar rezervacija (pregled po datumu)

Operativni prikaz za tim: klikom na datum u kalendaru vidi se koje stavke rezervacije (`BookingItem`) tog dana **dolaze**, **odlaze**, ili su **u toku** — standardan obrazac iz hotelskih/PMS sistema (Arrivals / Departures / Stayovers), primenjen ovde na sve tipove proizvoda iz M2, ne samo hotel. Ovo nije nov entitet ni nova baza — čist izveden upit nad postojećim `BookingItem.stay_from`/`stay_to`, u skladu sa principom "jedan izvor istine" (isto obrazloženje kao M3 poglavlje 3).

### 7.1 Klasifikacija po danu D (deterministička)
| Kategorija | Uslov | Napomena |
| :---- | :---- | :---- |
| **Dolazi** (arrival) | `stay_from = D` i `stay_to > D` | check-in / početak boravka/ture tog dana |
| **Odlazi** (departure) | `stay_to = D` i `stay_from < D` | check-out / kraj boravka/ture tog dana |
| **U toku** (stayover) | `stay_from < D < stay_to` | gost/putnik je prisutan taj dan, ni ne dolazi ni ne odlazi |
| **Jednodnevno** | `stay_from = D` i `stay_to = D` | npr. izlet ili transfer u istom danu — posebna, četvrta grupa, **ne** broji se ni kao dolazak ni kao odlazak da se brojevi ne dupliraju |

Obuhvataju se samo aktivne stavke: `item_status` = `CONFIRMED` ili `PENDING_SUPPLIER_CONFIRMATION`; `CANCELLED` stavke se isključuju — isti filter koji već važi za ostale preglede rezervacija.

### 7.2 Dva nivoa prikaza
- **Mesečni/nedeljni pregled** — samo brojevi po danu (koliko dolazi/odlazi/u toku/jednodnevno), da se kalendar iscrta brzo bez učitavanja svake pojedinačne stavke.
- **Dnevni detalj** (klik na datum) — puna lista stavki razvrstana u četiri grupe iz poglavlja 7.1, sa osnovnim podacima (gost/putnik, proizvod, dobavljač ili kanal, status).

### 7.3 Dozvole i vidljivost
Kalendar je samo drugačiji prikaz `BookingItem` podataka koje korisnik već sme da vidi — **ne uvodi novu dozvolu**, koristi `M5/booking/VIEW` (poglavlje 10). Posledica: Prodajni agent čiji je `M5/booking/VIEW` podrazumevano ograničen na sopstvene klijente vidi kalendar filtriran na isti način; Vlasnik/Direktor/Sales Manager vide sve.

### 7.4 M17 prikaz kao Google Calendar + filteri Liste (dopuna, 27.8.2026, na zahtev vlasnika)

Vlasnikov zahtev: "Kalendar rezervacija treba napraviti kao Google kalendar sa svim funkcijama prilagođenim potrebama TT-a. Dodati filtere koji postoje u Listi rezervacija." Obim potvrđen preko `AskUserQuestion` pre početka rada:

- **Tri prikaza + prekidač** (Mesec/Nedelja/Dan, dugme "danas", strelice prethodno/sledeće) — `apps/panel/src/app/(app)/rezervacije/kalendar/`. Mesečni prikaz je puna 6×7 nedeljna mreža (uključuje krajeve susednih meseci, zasenčeno) umesto ranijeg prikaza koji je počinjao praznim ćelijama. Nedelja prikazuje 7 kolona sa spiskom termina po danu. Dan prikazuje punu listu iz poglavlja 7.1 (Dolasci/Odlasci/U toku/Jednodnevno) kao GLAVNI sadržaj ekrana (ranije: panel ispod mesečnog grida).
  - **Namerno BEZ satnice u Nedelja/Dan prikazu** — `BookingItem.stay_from`/`stay_to` su DATUMI, ne trenuci (nema polja za vreme dolaska/odlaska u modelu podataka), pa ovi prikazi grupišu termine PO DANU, ne po satu kao pravi Google Calendar sastanak. Ako se ikad doda stvaran čas dolaska/odlaska (nov zahtev, van ovog obima), satnica postaje moguća kao naredna dopuna.
  - **Klik na dan (u Mesec ili Nedelja prikazu) prebacuje na Dan prikaz za taj datum** — zamenjuje raniji obrazac ("panel se otvara ispod grida na istoj stranici").
- **NAMERNO BEZ prevlačenja termina** — potvrđeno preko `AskUserQuestion` sa vlasnikom: ovaj prolaz je čist pregled. Promena datuma rezervacije i dalje ide isključivo kroz postojeći tok izmene rezervacije (`PATCH` tok, poglavlje 5), ne prevlačenjem u kalendaru — prevlačenje bi zahtevalo punu ponovnu proveru kapaciteta/cene (M3/M4) pri svakoj izmeni, potvrdu pre snimanja, i verovatno zaseban prolaz specifikacije; zabeleženo kao mogući budući zahtev u `docs/analize/27-BACKLOG-IDEJA-I-PREDLOZI.md`, ne izgrađeno sada.
- **Filteri identični "Listi rezervacija"** (poglavlje 11, `GET /sales/bookings` v1 skup), **BEZ** dolazak/odlazak datumskih opsega (`stayFrom`/`stayTo`/`returnFrom`/`returnTo`) — sam prikaz (mesec/nedelja/dan koji se gleda) već zadaje taj opseg, drugi, zaseban datumski filter bi bio konfuzan/redundantan. Ostaje: status, uplata, tip nastupanja, ime nosioca, broj rezervacije, valuta, tip proizvoda, destinacija (grad/država), garancija putovanja, kreirano od/do.
  - `GET /sales/bookings/calendar-summary` i `GET /sales/bookings/calendar/:date` (poglavlje 11) prošireni istim v1 filter-skupom (`BookingsService.buildCalendarItemWhere`, deljena privatna metoda) — aditivno, svi parametri opcioni, postojeći pozivaoci bez filtera (M15 omnisearch `list_bookings_by_date` alat) rade nepromenjeno.
- **Nema izmene dozvola/API ugovora van gorenavedenog** — i dalje `M5/booking/VIEW`, isti princip kao poglavlje 7.3.

**Provera:** `tsc --noEmit` čist za `apps/api` i `apps/panel`; uživo preko prave VLASNIK sesije — sva tri prikaza (`?view=month/week/day`) vraćaju HTTP 200 sa stvarnim sadržajem (ne praznim/error stanjem), mesečna mreža ispravno počinje ponedeljkom nedelje koja sadrži 1. u mesecu (avgust 2026 testiran: 1.8. je subota, mreža počinje 27.7.), dan-prikaz ispravno prikazuje "četvrtak" za 27.8.2026 (potvrđeno spoljnim `date` pozivom), navigacija (danas/strelice/prekidač prikaza) generiše tačne `?view=&date=` kombinacije za sve tri granularnosti (dan ±1, nedelja ±7, mesec ±1 normalizovano na 1.), filter sa parametrima (`status=CONFIRMED&productType=ACCOMMODATION`) i dalje vraća HTTP 200 sa ispravnim sadržajem.

---

## 8. Operativne liste za dobavljače

Odvojeno od vaučera koji dobija gost (poglavlje 6), agencija mora dobavljaču — hotelu, prevozniku, osiguravaču — da pošalje **operativni spisak gostiju/putnika** za konkretan period boravka ili polazak (rooming lista hotelu, spisak putnika prevozniku, spisak osiguranika osiguravaču). Ovo je dokument koji ide **od agencije ka dobavljaču**, obrnuto od vaučera, i primenjuje se isključivo na `CONTRACTED` stavke (poglavlje 4.2) — kod `API` stavki (M4) podaci o gostu već putuju do dobavljača kroz samu API rezervaciju u trenutku potvrde, pa poseban operativni dokument nije potreban.

### 8.1 `SupplierManifest` — operativna lista
| Polje | Tip | Napomena |
| :---- | :---- | :---- |
| id | UUID (PK) | |
| supplier_id | UUID (FK → M3 Supplier) | rešava se preko `BookingItem.product_id` → M2 `Product.source_contract_id` → M3 `Contract.supplier_id`, u trenutku generisanja |
| contract_period_id | UUID, nullable (FK → M3 ContractPeriod) | period na koji se lista odnosi, kad su sve stavke iz istog perioda (uobičajen slučaj); nullable ako lista objedinjuje više perioda istog dobavljača za isti opseg datuma |
| supplier_type_snapshot | enum (isto kao M3 `Supplier.type`) | kopira se u trenutku generisanja — određuje format liste (poglavlje 8.3); ne menja se retroaktivno ako se `Supplier.type` kasnije izmeni |
| language | enum: `SR`, `EN`, podrazumevano `SR` | bira korisnik pri generisanju nacrta (poglavlje 8.3) — ne menja se retroaktivno na već poslatoj listi, isti princip kao `supplier_type_snapshot` |
| period_from / period_to | date | opseg boravka/polaska koji lista pokriva |
| status | enum: `DRAFT`, `SENT`, `SUPERSEDED` | `DRAFT` — generisana, nije poslata; `SENT` — poslata dobavljaču; `SUPERSEDED` — zamenjena novijom verzijom (poglavlje 8.5) |
| document_url | string, nullable | PDF, generiše se pri prelasku u `DRAFT`, isto EU cloud skladište kao vaučer |
| generated_at / generated_by | timestamp / UUID | `generated_by` može biti `AI_AGENT_M5` (poglavlje 8.4) |
| sent_at / sent_by | timestamp / UUID, nullable | `sent_by` mora biti stvaran korisnik — slanje nikad nije autonomno (poglavlje 8.4) |
| sent_to_email | string, nullable | kopija `Supplier.contact_email` u trenutku slanja, radi traga čak i ako se kontakt kasnije promeni |
| reference_code | string, unique, nullable | dopuna avgust 2026 (poglavlje 8.8) — jedinstvena referenca (npr. `TT-000423`) upisana u naslov mejla pri slanju, radi automatskog poklapanja sa potvrdom dobavljača; generiše se pri prelasku u `DRAFT` |
| confirmation_email_thread_id | UUID, nullable (FK → M22 `EmailThread`) | dopuna avgust 2026 (poglavlje 8.8) — popunjeno kad je nit prepiske o ovoj listi prepoznata (po `reference_code` ili ručno) |
| supersedes_manifest_id | UUID, nullable (FK → SupplierManifest) | ako je ovo revizija ranije poslate liste |

### 8.2 `SupplierManifestItem` — spojna tabela
`supplier_manifest_id` (FK), `booking_item_id` (FK → `BookingItem`), `included_at` (timestamp). Jedan `BookingItem` može tokom vremena biti na više listi (npr. original pa revizija), ali sme biti na najviše jednoj listi koja nije `SUPERSEDED` u datom trenutku — ograda na nivou aplikacije.

### 8.3 Format po tipu dobavljača
| `Supplier.type` | Sadržaj liste |
| :---- | :---- |
| `HOTEL` | Raspored po jedinicama (svaki `BookingItem` = jedna prodata soba/jedinica): imena gostiju iz `BookingItemGuest` → M6 Gost, `stay_from`/`stay_to`, broj gostiju po jedinici, tip sobe (`ContractPeriod.room_type`, iz M3) i usluga (`RateLine.board_type` preko `BookingItem.rate_line_id`) |
| `PREVOZNIK` | Spisak putnika: ime, broj putovnice/lične karte (kad postoji u M6 profilu gosta), datum/vreme polaska |
| `OSIGURAVAC` | Spisak osiguranika sa datumima pokrića (`stay_from`/`stay_to`) |
| `DRUGO` | Generički spisak — ime i period, bez dodatnih pretpostavki |

**Ograda:** cena (`base_cost`, `final_price`, marža) se **nikad** ne uključuje u listu ka dobavljaču — to je interni podatak agencije; dobavljač već zna svoju ugovorenu cenu iz sopstvenog `RateLine` (M3).

**Jezik dokumenta:** operativna lista se generiše na srpskom ili engleskom (`SupplierManifest.language`, poglavlje 8.1) — dovoljno za sada; ne mora pratiti punih 8 jezika M2 kataloga, pošto je ovo interni operativni dokument ka dobavljaču, ne B2C sadržaj. Korisnik bira jezik pri generisanju nacrta; izabrani jezik ostaje nepromenjen i na revizijama (poglavlje 8.5). Dodato poređenjem sa PrimeTravel rooming-listom (SR/EN prekidač), vidi `22-ANALIZA-PRIMETRAVEL-NALAZI.md`.

### 8.4 Generisanje i slanje — uloga AI agenta
Agent zadužen za M5 sme **automatski da pripremi nacrt** (`status = DRAFT`) — npr. periodičan posao koji za svaki `ContractPeriod` čiji `stay_from` pada u narednih N dana agregira potvrđene (`item_status = CONFIRMED`) stavke po dobavljaču — ovo je čisto informativna priprema, nivo **"Autonomno"** iz poglavlja 7 Master dokumenta. **Slanje dobavljaču** (prelazak u `status = SENT`) zahteva ljudsku potvrdu, nivo **"Predloži pa čovek odobri"** — isti princip kao slanje ponuda B2B partnerima, i isto obrazloženje kao u M3 poglavlju 4 (agent nikad sam ne šalje potvrdu dobavljaču). Od avgusta 2026 (poglavlje 8.8), slanje ide isključivo kroz jedinstveno M22 sanduče za dobavljače — ne sa ličnog naloga zaposlenog koji klikne "pošalji".

**Ad-hoc priprema po rezervaciji (dopuna, avgust 2026, na zahtev vlasnika, v1.15):** pored periodičnog posla iznad, operater može da zatraži pripremu odmah za JEDNU konkretnu rezervaciju (`POST /bookings/:id/prepare-supplier-manifests`, poglavlje 11) — npr. čim je rezervacija potvrđena, bez čekanja na sledeći prolaz periodičnog posla. Ovo koristi ISTI princip grupisanja kao periodično agregiranje: `SupplierManifest` je uvek vezan za tačno jednog dobavljača (poglavlje 8.1), pa ako rezervacija sadrži `CONTRACTED`/`CONFIRMED` stavke od više različitih dobavljača (npr. hotel + transfer od dva različita dobavljača), akcija automatski pripremi po jedan odvojen DRAFT nacrt za svakog — operater ne mora ručno da zna koji su dobavljači uključeni niti da poziva akciju više puta. Isti nivo autonomije kao iznad (**"Autonomno"** za pripremu nacrta), slanje ostaje nepromenjeno (ljudski klik, `POST /supplier-manifests/:id/send`). Stavke koje su već na nekoj ne-`SUPERSEDED` listi se preskaču (isti filter kao periodično agregiranje) — sprečava duplo najavljivanje iste stavke.

**Ad-hoc priprema za VIŠE rezervacija — kombinovani filteri (dopuna, avgust 2026, na zahtev vlasnika, v1.16):** proširenje prethodnog za slučaj kad operater bira više rezervacija odjednom (`POST /supplier-manifests/prepare-batch`, poglavlje 11). Dva režima:

- **Ručni izbor** (`booking_ids[]`) — operater u M17 čekira nekoliko konkretnih rezervacija (checkbox lista) i traži pripremu za tačno te. Isključiv: kad je prosleđen, svi filteri ispod se ignorišu (nema mešanja "izaberi ove ID-jeve I još filtriraj po datumu" — zbunjujuća kombinacija, ručni izbor je već konačan).
- **Filteri** (kad `booking_ids[]` nije prosleđen) — bar JEDAN od sledećih mora biti prisutan (prazan poziv bez ijednog kriterijuma bi zahvatio SVE nenajavljene stavke ikad postojale, namerno onemogućeno kao previše širok/rizičan podrazumevani obim); prisutni filteri se **kombinuju logičkim I** kad ih je prosleđeno više odjednom:
  - `created_from`/`created_to` — rezervacija **napravljena** u opsegu (`Booking.created_at`), npr. "sve rezervacije napravljene ove nedelje".
  - `stay_from`/`stay_to` — stavka čiji **boravak preklapa** opseg (isti obrazac preklapanja kao `period_from`/`period_to` u periodičnom agregiranju iznad).
  - `arrival_from`/`arrival_to` — stavke čiji je **dolazak** (`stay_from`, ne preklapanje) u opsegu, npr. "ko stiže sledeće nedelje".
  - `departure_from`/`departure_to` — stavke čiji je **odlazak** (`stay_to`) u opsegu.
  - `booking_status[]` — samo rezervacije sa datim statusom/statusima (`Booking.status`, npr. samo `CONFIRMED`).

  Napomena: `created_from`/`created_to` je opseg **kreiranja** rezervacije ("uhvati sve što je nedavno prodato i još nenajavljeno") — namerno odvojeno od preostala tri vremenska filtera koji gledaju **boravak** ("šta se dešava u datom terminu", operativni ugao gledanja hotela/prevoznika).

Oba načina koriste identičnu logiku grupisanja po dobavljaču kao ad-hoc priprema po jednoj rezervaciji — ako izabrane rezervacije (ili opseg) sadrže stavke od N različitih dobavljača, nastaje N odvojenih DRAFT nacrta. Ako isti dobavljač ima stavke iz više različitih `ContractPeriod` (npr. dva različita termina), `contract_period_id` na nastalom nacrtu ostaje `null` (isto pravilo kao poglavlje 8.1 — "nullable ako lista objedinjuje više perioda").

**Namerno van obima ove dopune — prirodno-jezički AI interfejs.** Vlasnik je u diskusiji pomenuo mogućnost da se ovo zatraži prirodnim jezikom ("AI agente, grupiši rezervacije od...do i pošalji nenajavljene"). Osnovna sposobnost (grupisanje po dobavljaču, obim po listi/opsegu) je upravo ono što ova dopuna uvodi — dostupna je i AI agentu kao i svakom drugom pozivaocu za PRIPREMU nacrta (nivo **"Autonomno"**, nepromenjeno). Ono što se **ne uvodi**, ni ovde ni bilo gde u M5: mogućnost da AI agent (ili bilo šta osim eksplicitnog ljudskog klika) izvrši `POST /supplier-manifests/:id/send` — to ostaje **"Predloži pa čovek odobri"**, nepromenjeno iz izvornog teksta poglavlja 8.4, bez obzira ko/šta je pripremu pokrenuo. Sâm prirodno-jezički interfejs ka agentu (parsiranje slobodnog teksta u ovaj poziv) je van obima M5 dok se ne specifikuje M15 sloj za ovaj modul (isto obrazloženje kao poglavlje 3.0.4).

### 8.5 Izmene posle slanja
Ako se stavka koja je već na poslatoj listi (`status = SENT`) izmeni ili otkaže (poglavlje 6):
1. Postojeća lista se označava kao `SUPERSEDED`.
2. Priprema se nova `DRAFT` sa `supersedes_manifest_id` ka prethodnoj, sa trenutno važećim stavkama.
3. Nova lista se ne šalje automatski — isti princip kao 8.4: čovek pregleda i šalje reviziju.

### 8.6 "Najava" kao formalni koncept (dopuna, avgust 2026 — rešava problem #2 iz `Problemi koje zelimo da resimo ovom aplikacijom.md`, vidi `24-GAP-ANALIZA-PROBLEMI-VS-ARHITEKTURA.md` poglavlje 2)

Do sada je "da li je dobavljač najavljen/potvrdio" postojalo samo posredno — preko `item_status = PENDING_SUPPLIER_CONFIRMATION` (koje prati potvrdu dostupnosti pri rezervaciji, poglavlje 4) i posredno preko `SupplierManifest.status = SENT` (koje prati da li je operativna lista poslata, poglavlje 8.1). Ovo su dva različita koraka istog toka koja se dosad nisu eksplicitno pratila na nivou pojedinačne stavke: **(a)** dostupnost potvrđena od dobavljača pri samoj rezervaciji, **(b)** dobavljač formalno obavešten *ko* dolazi (najava/rooming lista).

**Pravilo:** `BookingItem.announced_at` (poglavlje 4.2) formalizuje korak (b):
- Za `CONTRACTED` stavke: popunjava se automatski čim stavka uđe u `SupplierManifest` koji pređe u `status = SENT` (kopira se `SupplierManifest.sent_at`). Ako lista kasnije postane `SUPERSEDED` i stavka se pojavi na novoj poslatoj listi (poglavlje 8.5), `announced_at` se ažurira na vreme slanja te nove liste.
- Za `API` stavke: popunjava se automatski u trenutku kad `item_status` pređe u `CONFIRMED` (poglavlje 4, korak 2) — najava je već sadržana u samom API pozivu ka dobavljaču (isto obrazloženje kao uvod poglavlja 8, "poseban operativni dokument nije potreban").

`BookingItem.supplier_confirmed_at`/`supplier_confirmed_by` prati korak potvrde od strane dobavljača da je najava primljena:
- Za `CONTRACTED` stavke: ručni unos — dobavljači tipično nemaju API, potvrda stiže mejlom/telefonom, zaposleni je unosi u M17 panelu (dugme "Označi kao potvrđeno od dobavljača" na stavci ili na celoj poslatoj listi odjednom, popunjava sve stavke te liste).
- Za `API` stavke: popunjava se automatski, isti trenutak kao `announced_at` (M4 `BookingConfirmation` već predstavlja i najavu i potvrdu dobavljača u jednom koraku).

Alarmi za oba koraka (nenajavljena stavka pred boravak, najava bez potvrde) definisani su u poglavlju 6.1.

### 8.7 Konfigurabilno automatsko pripremanje najave po dobavljaču (dopuna, avgust 2026 — rešava problem #3, vidi GAP-analiza poglavlje 3)

Poglavlje 8.4 danas priprema `DRAFT` nacrt po fiksnom pravilu (N dana pre `stay_from`, isto za sve dobavljače) i uvek zahteva ljudski klik za slanje. Vlasnik traži da trenutak *pripreme* nacrta bude podesiv po dobavljaču (npr. tek posle naplaćene akontacije za jednog dobavljača, odmah po potvrdi za drugog) — **slanje i dalje ostaje isključivo ljudska radnja, ovo se ne menja** (potvrđeno sa vlasnikom — vidi diskusiju pri specifikaciji ovog poglavlja); menja se samo kada je nacrt spreman i istaknut za odobrenje, ne ko ga šalje.

#### `SupplierAnnouncementRule`
| Polje | Tip | Napomena |
| :---- | :---- | :---- |
| id | UUID (PK) | |
| supplier_id | UUID, nullable (FK → M3 Supplier) | `NULL` = podrazumevano pravilo, važi za dobavljače bez sopstvenog pravila |
| trigger_condition | enum: `DAYS_BEFORE_STAY`, `ON_CONFIRMATION`, `AFTER_DEPOSIT_PAID`, `AFTER_FULL_PAYMENT` | `DAYS_BEFORE_STAY` — postojeće ponašanje iz 8.4 (koristi `days_before_stay`); `ON_CONFIRMATION` — čim `item_status = CONFIRMED`, nezavisno od uplate; `AFTER_DEPOSIT_PAID` — čim `Booking.payment_status` pređe u `PARTIALLY_PAID` ili `PAID`; `AFTER_FULL_PAYMENT` — tek kad `payment_status = PAID` |
| days_before_stay | integer, nullable | koristi se samo kad `trigger_condition = DAYS_BEFORE_STAY` |
| created_by / created_at / updated_by / updated_at | UUID / timestamp | isti obrazac kao `MarkupRule` (poglavlje 2.1) |

**Razrešavanje pravila:** najspecifičnije pobeđuje — isti princip kao `MarkupRule` (poglavlje 2.2). Ako za `Supplier` postoji sopstveni `SupplierAnnouncementRule`, koristi se on; inače se koristi podrazumevano pravilo (`supplier_id IS NULL`). Ako podrazumevano pravilo ne postoji, važi `trigger_condition = DAYS_BEFORE_STAY` sa vrednošću iz poglavlja 6.1 (7 dana) kao ugrađeni fallback — nema regresije za dobavljače koji danas nemaju posebno pravilo.

**Tok:** periodičan posao (poglavlje 8.4) za svaku `CONTRACTED` stavku sa `item_status = CONFIRMED` i praznim `announced_at` proverava da li je uslov iz važećeg `SupplierAnnouncementRule` ispunjen. Kad jeste, nacrt (`SupplierManifest.status = DRAFT`) se priprema odmah (ako već ne postoji za taj period/dobavljača) i **ističe se kao prioritetan** u M17 Agent Inbox — vidljivo odvojeno od običnih nacrta pripremljenih rutinski. Ovo ostaje nivo **"Autonomno"** za pripremu (čisto informativno, isto obrazloženje kao postojeći 8.4), a slanje ostaje **"Predloži pa čovek odobri"** — nepromenjeno.

**Dozvole:** `M5/supplier-announcement-rule/VIEW`, `EDIT` — Vlasnik, Direktor (isti krug kao `markup-rule`, poglavlje 10 — pravila koja utiču na odnos sa dobavljačem su osetljiva).

### 8.8 Jedinstveno sanduče za dobavljače i automatsko poklapanje potvrde (dopuna, avgust 2026 — rešava problem #11 iz `Problemi koje zelimo da resimo ovom aplikacijom.md`, vidi `24-GAP-ANALIZA-PROBLEMI-VS-ARHITEKTURA.md` poglavlje 11)

Do sada je potvrda dobavljača (`supplier_confirmed_at`/`by`, poglavlje 8.6) bila isključivo ručan unos zaposlenog, bez veze sa stvarnom mejl porukom koja je stigla — i slanje najave je išlo sa ličnog naloga zaposlenog koji klikne "pošalji". Vlasnik je opisao stvaran problem iz prakse: kad svaki zaposleni šalje sa svog ličnog mejla, potvrda dobavljača stiže na taj isti lični mejl, i nema jednog mesta gde tim vidi ceo tok "najavljeno → potvrđeno" niti lako uočava šta nije potvrđeno. Potvrđeno sa vlasnikom (avgust 2026): jedna zajednička adresa za sve dobavljače (ne po timu/regionu), koja pokriva **i slanje i prijem**; potvrda dobavljača i dalje **uvek** zahteva klik zaposlenog, čak i kad AI prepozna poklapanje sa visokom pouzdanošću — ovo pravilo se ne menja ovom dopunom, samo se ubrzava i pojednostavljuje.

**Jedinstveno sanduče:** postoji tačno jedan M22 `Mailbox` (poglavlje 2.1 te specifikacije, `mailbox_type = SHARED`) kroz koji ide **svaka** komunikacija ka dobavljačima vezana za konkretnu rezervaciju — nova najava (`SupplierManifest`, poglavlje 8.1), izmena, i storno (`SupplierChangeNotice`, niže). Ovo sanduče je sistemska konfiguracija (jedan red, ne po dobavljaču/timu) — koji tačno korisnici imaju `REPLY` pristup određuje se kroz postojeći M22 `MailboxAccess` mehanizam (poglavlje 2.2 M22), ne ovde.

**Referentni kod:** svaki `SupplierManifest` i svaki `SupplierChangeNotice` (niže) dobija `reference_code` pri kreiranju nacrta — format `TT-NNNNNN` (šestocifren, sekvencijalan, jedinstven kroz oba tipa). Pri slanju, kod se upisuje na početak naslova mejla u fiksnom obliku `[REF: TT-NNNNNN] ...` — kratak, bez specijalnih znakova, da preživi i kad dobavljač odgovori kroz "Reply" (thread se čuva) i kad pokrene nov mejl ručno prekucavajući naslov.

#### `SupplierChangeNotice` — najava izmene ili storna (novo, avgust 2026)

Za razliku od nove rezervacije (pokriva je `SupplierManifest`), izmena i storno pojedinačne stavke ne čekaju sledeću rutinsku listu — dobavljač mora **eksplicitno** biti obavešten, ne da sam zaključi iz toga što je gost nestao sa naredne revidirane liste (poglavlje 8.5). Ovo je i direktna pouka iz problema #10 (poglavlje 6.4) — nejasna/prećutna komunikacija ka dobavljaču pri stornu je tačno ono što je izazvalo pravi incident iz prakse.

| Polje | Tip | Napomena |
| :---- | :---- | :---- |
| id | UUID (PK) | |
| booking_item_id | UUID (FK → BookingItem) | |
| notice_type | enum: `MODIFICATION`, `CANCELLATION` | |
| reference_code | string, unique | isti format/svrha kao `SupplierManifest.reference_code` iznad |
| status | enum: `DRAFT`, `SENT` | isti princip kao `SupplierManifest.status`, bez `SUPERSEDED` — jedna izmena/storno je jedna poruka |
| sent_at / sent_by | timestamp / UUID, nullable | `sent_by` mora biti stvaran korisnik — isto pravilo kao poglavlje 8.4, slanje nikad autonomno |
| supplier_confirmed_at / supplier_confirmed_by | timestamp / UUID, nullable | popunjava se **isključivo** ljudskim klikom (vidi niže), nikad automatski, bez obzira na pouzdanost prepoznavanja |
| confirmation_email_thread_id | UUID, nullable (FK → M22 `EmailThread`) | |
| created_at | timestamp | |

Priprema `DRAFT` je nivo **"Autonomno"** (čisto informativna priprema teksta, ista logika kao poglavlje 8.4) — okidač je promena `item_status` na `MODIFIED`/`CANCELLED` (poglavlje 6). Slanje ostaje **"Predloži pa čovek odobri"**.

**Automatsko poklapanje potvrde (AI agent, M22):** kad u jedinstveno sanduče stigne `INBOUND` poruka, M22 (poglavlje 3.1a te specifikacije) traži `[REF: TT-NNNNNN]` obrazac u naslovu/telu i, ako pronađe poklapanje sa postojećim `SupplierManifest.reference_code`/`SupplierChangeNotice.reference_code`, **predlaže** vezu (popunjava `confirmation_email_thread_id` kao predlog, ne konačno). Ako referenca nije pronađena (dobavljač pokrenuo nov mejl bez nje), pada na fuzzy-match po imenu dobavljača/gosta/datumima (isti obrazac kao poglavlje 6.4 i M10 poglavlje 8.6.3) kao slabiji predlog. **U oba slučaja**, konačno postavljanje `supplier_confirmed_at`/`by` zahteva eksplicitan klik zaposlenog na predloženu vezu (dugme "Potvrdi kao potvrdu dobavljača" u M17, uz vidljiv izvor: dokument je pronašao referencu tačno, ili je predlog samo po sličnosti) — nivo **"Predloži pa čovek odobri"**, potvrđeno sa vlasnikom da se ovo ne menja ni pri visokoj pouzdanosti.

---

## 9. Događaji (Event Bus) koje M5 emituje

`booking.confirmed`, `booking.pending_supplier_confirmation`, `booking.modified`, `booking.cancelled`, `booking.completed` (poglavlje 6.1a — M6 post-trip anketa je jedini pretplatnik za sada) — buduci moduli (M6 istorija gosta i post-trip anketa, M10 fakturisanje, M11 CIS registracija garancije putovanja poglavlje 2.3, M12 marketing, M20 generisanje/revizija ugovora sa klijentom) se pretplaćuju na ove događaje kad dođu na red; M5 ih ne poziva direktno (princip #2, poglavlje 3).

---

## 10. Dozvole (registruju se u M1 katalog dozvola)

| Dozvola | Podrazumevana dodela po ulozi |
| :---- | :---- |
| `M5/itinerary/CREATE`, `VIEW`, `EDIT` | Vlasnik, Direktor, Sales Manager, Prodajni agent; Gost (samo sopstveni, preko sajta) |
| `M5/quote/CREATE`, `VIEW` | Vlasnik, Direktor, Sales Manager, Prodajni agent; Gost (samo sopstvene, preko sajta) |
| `M5/booking/CREATE` (potvrda) | Vlasnik, Direktor, Sales Manager, Prodajni agent; Gost (samostalna rezervacija na sajtu) |
| `M5/booking/VIEW` | Vlasnik, Direktor, Sales Manager (sve); Prodajni agent (podrazumevano samo sopstveni klijenti — širi se pojedinačnim izuzetkom iz M1 ako treba); Gost (samo sopstvene) — **koristi i kalendar rezervacija, poglavlje 7.3** |
| `M5/booking/MODIFY`, `CANCEL` | Vlasnik, Direktor, Sales Manager, Prodajni agent (sopstveni klijenti); Gost (sopstvena rezervacija, u skladu sa pravilima otkazivanja) |
| `M5/markup-rule/VIEW`, `EDIT` | Vlasnik, Direktor — cenovna politika je osetljiva, ne deli se šire podrazumevano |
| `M5/supplier-announcement-rule/VIEW`, `EDIT` | Vlasnik, Direktor — vidi poglavlje 8.7 |
| `M5/voucher/OVERRIDE_ISSUE` (izdavanje bez pune uplate) | Vlasnik, Direktor — **nikad AI agent, nikad Sales Manager/Prodajni agent**, u skladu sa poglavljem 6 (finansijski rizik) |
| `M5/supplier-manifest/VIEW` | Vlasnik, Direktor, Sales Manager, Prodajni agent |
| `M5/supplier-manifest/CREATE` (nacrt) | Vlasnik, Direktor, Sales Manager, Prodajni agent; i AI agent zadužen za M5 (poglavlje 8.4) |
| `M5/supplier-manifest/SEND` | Vlasnik, Direktor, Sales Manager — **nikad AI agent**, u skladu sa poglavljem 8.4 |
| `M5/supplier-change-notice/CREATE`, `SEND` | Isti krug kao `supplier-manifest/CREATE`/`SEND` (poglavlje 8.8) — **slanje nikad AI agent** |
| `M5/supplier-confirmation/CONFIRM` (ljudski klik na predloženu vezu sa mejlom, poglavlje 8.8) | Vlasnik, Direktor, Sales Manager, Prodajni agent — **nikad AI agent**, isti krug kao `supplier-manifest/VIEW` |
| `M5/manual-item/CREATE` (dopuna 18.8.2026, poglavlje 3.0f) | Vlasnik, Direktor, Sales Manager, Prodajni agent (`INTERNAL_PANEL`/`PHONE`); Subagent (`B2B_PORTAL`, bez cenovnih polja — poglavlje 3.0f.3) |
| `M5/manual-item/REVIEW` (dopuna 18.8.2026, poglavlje 3.0f.4 — "Gejt B" za ručne stavke) | Vlasnik, Direktor, Sales Manager — **nikad AI agent**, isti krug kao M7 spec poglavlje 2.0.4c korak 4 |

---

## 11. API ugovor (REST, OpenAPI) — ključni endpoint-i

**Pristup `GET /search` (ispravka, avgust 2026, priprema za M8).** Ranija implementacija je zahtevala `JwtAuthGuard` ("dostupna svakom autentifikovanom pozivaocu") — ovo je bilo neusklađeno sa M8 poglavljem 3, korak 1 ("Pretraga — anonimna, bez potrebe za nalogom"). `GET /search` je **javan, bez guard-a**, isti princip kao M2 `PublicProductsController` (nema osetljivog podatka koji bi guard štitio — identitet dobavljača se i ovde nikad ne izlaže, poglavlje 6.2/M2 poglavlje 5.1).

Prefiks: `/api/v1/sales`

| Endpoint | Metod | Opis |
| :---- | :---- | :---- |
| `/itineraries` | GET / POST | lista / kreiranje novog sastavljanja putovanja (poglavlje 3.0.1) |
| `/itineraries/:id` | GET / PATCH | pregled / izmena (dodavanje, brisanje, preslagivanje segmenata, poglavlje 3.0.2) |
| `/itineraries/:id/to-quote` | POST | konverzija u Ponudu (poglavlje 3.0.3), vraća kreiranu `Quote` |
| `/itineraries/:id/share` | POST | dopuna 18.8.2026 (poglavlje 3.0.5), zahteva `M5/itinerary/EDIT`; generiše/vraća `share_token`, idempotentno |
| `/itineraries/shared/:shareToken` | GET | dopuna 18.8.2026 (poglavlje 3.0.5), **javan, bez guard-a** — segmenti sa okvirnom cenom, bez nabavne cene/marže |
| `/itineraries/shared/:shareToken/segments/:segmentId` | PATCH | dopuna 18.8.2026 (poglavlje 3.0.5), **javan, bez guard-a** — isključivo `{isIncluded: boolean}` |
| `/itineraries/:id/clone` | POST | dopuna 18.8.2026 (poglavlje 3.0.6), zahteva `M5/itinerary/CREATE`; vraća nov `Itinerary` sa kopiranim segmentima |
| `/itineraries/:id/generate-pdf` | POST | dopuna 18.8.2026 (poglavlje 3.0.8), zahteva `M5/itinerary/VIEW`; popunjava `document_url` |
| `/search` | GET | objedinjena pretraga (M2 katalog + M3 dostupnost + M4 uživo), vraća niz `SearchResultProduct` sa ugnježdenim `offers[]` (`SearchResultOffer`), sa već primenjenom maržom — struktura odgovora definisana u poglavlju 3.0b. Parametri (dopuna avgust 2026, rešava nalaz iz `VALIDACIJA-WORKFLOV-B2C.md`, dopunjeno avgust 2026 za multi-select i nova tri M2 tipa): `type` (**niz** enum vrednosti, isti skup kao M2 `Product.type` — dopunjeno `TRANSPORT`/`TICKET`/`EVENT`, M2 poglavlje 2.1 — opciono, bez njega pretražuju se svi tipovi; sa više vrednosti rezultat je unija svih traženih tipova, isti princip kao filter po više vrednosti bilo gde drugde u sistemu), `destination_country`/`destination_city` (opciono), `stay_from`/`stay_to` (obavezno za `ACCOMMODATION`/`TRANSFER`/`EXCURSION`/`PACKAGE`/`TRANSPORT`; za `TICKET`/`EVENT` opciono i menja značenje — filtrira da li proizvod pada u traženi period, ne proverava dostupnost kao kod smeštaja; neprimenjivo za samostalnu `INSURANCE`), `occupancy` (JSON, `{adults, children, room_config}`, isti oblik i pravilo slaganja kao `QuoteItem.occupancy` — poglavlje 3.2a — `room_config` primenjivo samo na `ACCOMMODATION`/`PACKAGE` sa smeštajem, `adults`/`children` čita se kao broj putnika/mesta i za `TRANSPORT`/`TICKET`/`EVENT` gde je primenjivo, obavezno kad `type` podrazumeva smeštaj), `channel` (obavezno — filtrira po `Product.visible_channels`, isti enum kao poglavlje 3.1), `amenity_tags[]` (opciono, dopuna avgust 2026 — poglavlje 3.0c.3; niz `AmenityTag` vrednosti, M2 poglavlje 2.3c; proizvod se vraća samo ako sadrži SVE tražene tagove). Dopuna 17.8.2026 (poglavlje 3.0d, ostalih 8 vrsta proizvoda): `origin_city` (opciono — polazna tačka za `FLIGHT`/`TRANSFER`/rent-a-car, do sada `GET /search` nije imalo pojam polazišta), `cabin_class` (opciono — filtrira `SearchResultOffer` za `FLIGHT`), `driver_age` (opciono — rent-a-car), `duration_nights` (opciono — `CRUISE`), `cabin_type` (opciono — filtrira `SearchResultOffer` za `CRUISE`), `trip_cost` (opciono — `INSURANCE`). Kad `type` sadrži `PACKAGE`, pretraga za tu stavku vraća gotove pakete iz M2 (`source_type = CONTRACTED` ili `API` sa `Product.type = PACKAGE`) — za custom višedestinacijsko sastavljanje van gotovog paketa koristi se `Itinerary` tok (poglavlje 3.0), ne ovaj endpoint direktno. |
| `/search/countries` | GET | dopuna (poglavlje 3.0c.2, avgust 2026) `?q=` — zemlje sa bar jednim `ACTIVE`/vidljivim proizvodom, za prvi korak vođene pretrage smeštaja |
| `/search/destinations` | GET | dopuna (poglavlje 3.0c.2, avgust 2026) `?country_code=&q=` — mešovita lista destinacija i proizvoda (prečica na hotel po imenu) unutar izabrane zemlje |
| `/quotes` | POST | kreira ponudu od izabranih proizvoda/datuma/gostiju |
| `/quotes/:id` | GET | pregled ponude, uključujući da li je istekla |
| `/quotes/:id/confirm` | POST | pokreće tok iz poglavlja 4, vraća kreiranu `Booking` ili grešku po stavci; odbija ako bilo koja stavka čeka pregled osoblja (poglavlje 3.0f.4, korak 4) |
| `/quotes/:id/share` | POST | dopuna 19.8.2026 (poglavlje 3.1a), zahteva `M5/quote/VIEW`; generiše/vraća `share_token`, idempotentno |
| `/quotes/shared/:shareToken` | GET | dopuna 19.8.2026 (poglavlje 3.1a), **javan, bez guard-a** — stavke sa gostu vidljivom cenom, bez nabavne cene/marže/identiteta dobavljača |
| `/quotes/:id/items/manual` | POST | dopuna 18.8.2026 (poglavlje 3.0f), zahteva `M5/manual-item/CREATE`; kreira `QuoteItem` sa `source_type = MANUAL` — telo uključuje `manual_product_entry_id` (postojeći) ili podatke za nov unos (poglavlje 3.0f.1), `stay_from`/`stay_to`/`occupancy`; cenovna polja obavezna za `INTERNAL_PANEL`/`PHONE`, odbijena (moraju ostati prazna) za `B2B_PORTAL` (poglavlje 3.0f.3) |
| `/quotes/:id/items/:itemId/manual-review` | POST | dopuna 18.8.2026 (poglavlje 3.0f.4), zahteva `M5/manual-item/REVIEW`; telo: `decision` (`APPROVED`/`REJECTED`), `base_cost`/`final_price`/`tip_nastupanja` (obavezni uz `APPROVED`), `rejection_reason` (obavezan uz `REJECTED`) |
| `/manual-product-entries` | GET | dopuna 18.8.2026 (poglavlje 3.0f.1), `?product_type=&q=` — lista za ponovni izbor, sortirano po `last_used_at` opadajuće |
| `/manual-product-entries/from-url` | POST | dopuna 18.8.2026 (poglavlje 3.0f.2), zahteva `M5/manual-item/CREATE`; telo `{url}` — pokreće M15 mehanizam (poglavlje 6.5.6b tekst + 6.5.6c slike tog spec-a), vraća predlog (naziv/opis/slike) na pregled agentu, **ne čuva** dok agent eksplicitno ne potvrdi (isto ponašanje kao svaki drugi AI predlog koji čeka odobrenje) |
| `/bookings` | GET | lista, filtrirano po statusu/kanalu/klijentu (prava pristupa iz poglavlja 10) |
| `/bookings/:id` | GET | detalji rezervacije |
| `/bookings/:id/modify` | POST | izmena datuma/gostiju |
| `/bookings/:id/cancel` | POST | otkazivanje (celo ili po stavci); ako provera duplikata (poglavlje 6.4) pronađe konflikt, vraća upozorenje sa detaljima konfliktne stavke umesto da izvrši storno — poziv se ponavlja sa `confirm_duplicate_override: true` da bi se otkazivanje ipak izvršilo |
| `/bookings/:id/payment-status` | PATCH | poziva isključivo M10; ako novi status prelazi u `PAID`, automatski proverava i pokreće generisanje vaučera (poglavlje 6) |
| `/bookings/:id/voucher/override` | POST | zahteva `M5/voucher/OVERRIDE_ISSUE`; generiše vaučer bez obzira na `payment_status`, popunjava `voucher_override_*` polja (poglavlje 4.1/6) |
| `/bookings/items/:itemId/assign-guide` | PATCH | dopuna (M9 spec §4, avgust 2026), zahteva `M5/booking/MODIFY`; postavlja/uklanja `BookingItem.assigned_guide_id` (`{ assignedGuideId: "..." \| null }`) — dodeljuje interni panel (M17, još ne postoji kao implementacija, ova ruta je zamena dok M17 ne dođe na red); upisuje audit log `booking_item.guide_assigned` |
| `/bookings/:id/prepare-supplier-manifests` | POST | dopuna v1.15, zahteva `M5/supplier-manifest/CREATE`; ad-hoc priprema DRAFT `SupplierManifest` za SVE dobavljače ove rezervacije odjednom (poglavlje 8.4), po jedan nacrt po dobavljaču ako ih ima više — vraća niz kreiranih nacrta |
| `/supplier-manifests/prepare-batch` | POST | dopuna v1.16, zahteva `M5/supplier-manifest/CREATE`; isto kao gore, ali obim je `booking_ids[]` (checkbox izbor, isključivo) ILI kombinacija filtera — `created_from`/`created_to`, `stay_from`/`stay_to`, `arrival_from`/`arrival_to`, `departure_from`/`departure_to`, `booking_status[]` — bar jedan filter mora biti prisutan kad `booking_ids[]` nije poslat (poglavlje 8.4) |
| `/markup-rules` | GET / POST / PATCH | upravljanje pravilima marže |
| `/bookings/calendar-summary` | GET | `?from=&to=` (npr. opseg meseca) — vraća niz `{date, arrivals_count, departures_count, stayovers_count, single_day_count}` po danu, za brzo iscrtavanje meseca bez učitavanja pojedinačnih stavki (poglavlje 7.2) |
| `/bookings/calendar/:date` | GET | pun spisak `BookingItem` razvrstan u dolazi/odlazi/u toku/jednodnevno za taj dan (poglavlje 7.1), sa istim pravima pristupa kao `/bookings` |
| `/supplier-manifests` | GET / POST | lista postojećih / generisanje nacrta (agregacija potvrđenih stavki po dobavljaču + periodu, poglavlje 8.4; `POST` prima i `language`, poglavlje 8.3) |
| `/supplier-manifests/:id` | GET | detalji, uključujući stavke i lanac revizija (`supersedes_manifest_id`) |
| `/supplier-manifests/:id/send` | POST | zahteva `M5/supplier-manifest/SEND`; menja status u `SENT`, šalje dokument na `sent_to_email`, popunjava `sent_at`/`sent_by`, i (poglavlje 8.6) popunjava `announced_at` na svakoj obuhvaćenoj `BookingItem` |
| `/supplier-manifests/:id/confirm-supplier` | POST | zahteva `M5/supplier-manifest/SEND`; ručni unos potvrde dobavljača (poglavlje 8.6) — popunjava `supplier_confirmed_at`/`supplier_confirmed_by` na svim `BookingItem` sa te liste |
| `/supplier-announcement-rules` | GET / POST / PATCH | upravljanje pravilima iz poglavlja 8.7, zahteva `M5/supplier-announcement-rule/VIEW` ili `EDIT` |

---

## 12. Izlazni kriterijum (Faza 1 — izlazni kriterijum cele faze, poglavlje 8)

- [ ] Tim može kroz interni panel da pretraži, dobije ponudu i potvrdi rezervaciju hotela — i iz M3 (ugovoreno) i preko M4 (Travelgate).
- [ ] Marža se ispravno primenjuje po hijerarhiji iz poglavlja 2, sa dokazivim izračunom (ista ulazna cena uvek daje istu izlaznu cenu).
- [ ] Rezervacija sa više stavki gde jedna stavka ne uspe ne ostavlja "napola" rezervaciju — sve već rezervisane stavke se oslobađaju.
- [ ] Rezervacija može biti `CONFIRMED` sa `payment_status = UNPAID` ili `INVOICE_PENDING`, bez greške.
- [ ] Vaučer se ne generiše automatski dok `payment_status != PAID`; prelazak u `PAID` (i pre i posle potvrde) automatski generiše vaučer bez ručne akcije.
- [ ] `POST /bookings/:id/voucher/override` ispravno generiše vaučer bez pune uplate samo za Vlasnika/Direktora, upisuje `voucher_override_*` polja i vidljiv je u audit logu; ista radnja je odbijena za sve ostale uloge i za AI agenta.
- [ ] Otkazivanje ispravno računa procenat povraćaja iz `CancellationRule` i oslobađa kapacitet nazad u M3.
- [ ] Svaka promena statusa rezervacije vidljiva je u M1 audit logu.
- [ ] Klikom na datum u kalendaru tim vidi stavke tog dana ispravno razvrstane u dolazi/odlazi/u toku/jednodnevno (poglavlje 7.1), bez duplog brojanja i bez otkazanih stavki.
- [ ] Mesečni pregled kalendara prikazuje tačne brojeve po danu bez učitavanja pune liste stavki (poglavlje 7.2).
- [ ] Nijedno novčano polje (`base_cost`, `final_price`, `total_price`, `fixed_amount`) nije tipa `decimal`/float — provereno da su sva `integer` u najmanjoj jedinici valute (poglavlje 2).
- [ ] Moguće je generisati nacrt operativne liste za dobavljača agregacijom potvrđenih `CONTRACTED` stavki po `ContractPeriod`, i ručno je poslati — poslata lista dobija status `SENT` sa zapisom ko je poslao i kada.
- [ ] Test (dopuna v1.15): `POST /bookings/:id/prepare-supplier-manifests` za rezervaciju sa `CONTRACTED` stavkama od DVA različita dobavljača pravi TAČNO dva DRAFT nacrta, svaki sadrži isključivo stavke svog dobavljača — nijedan zajednički nacrt koji meša dobavljače.
- [ ] Test (dopuna v1.16): `POST /supplier-manifests/prepare-batch` odbija poziv bez `booking_ids[]` i bez ijednog filtera; sa `booking_ids[]` prosleđenim ignoriše ostale filtere (uzima stavke SAMO iz tih rezervacija); svaki od `created_from`/`created_to`, `stay_from`/`stay_to`, `arrival_from`/`arrival_to`, `departure_from`/`departure_to`, `booking_status[]` filtrira po ispravnom polju (`Booking.created_at`, preklapanje `stay_from`/`stay_to`, `stay_from` unutar opsega, `stay_to` unutar opsega, `Booking.status`); dva ili više filtera prosleđena istovremeno se kombinuju logičkim I, ne OR. U svim slučajevima rezultat je grupisan po dobavljaču, isto kao pojedinačna priprema (v1.15).
- [ ] Izmena ili otkazivanje stavke koja je već na poslatoj listi automatski priprema revidiran nacrt (`SUPERSEDED` + novi `DRAFT`), nikad tiho ne menja već poslat dokument.
- [ ] Cena (nabavna, prodajna, marža) se nikad ne pojavljuje u dokumentu poslatom dobavljaču.
- [ ] Operativna lista se može generisati na srpskom ili engleskom, i izabrani jezik ostaje nepromenjen na već poslatoj listi i njenim revizijama.
- [ ] Rezervacija sa izdatim vaučerom bez pune uplate (izuzetak, poglavlje 6) generiše dnevni podsetnik timu dok se ne naplati do kraja ili ne otkaže (poglavlje 6.1).
- [ ] Stavka rezervacije u statusu `PENDING_SUPPLIER_CONFIRMATION` duže od praga generiše upozorenje nezavisno po dobavljaču — rezervacija sa stavkama od dva dobavljača ispravno prijavljuje samo onu stavku čiji dobavljač kasni (poglavlje 6.1).
- [ ] Rezervacija sa `payment_status = PAID` a bez generisanog vaučera odmah generiše vidljivo upozorenje (poglavlje 6.1).
- [ ] Test: vaučer i odgovor `/bookings/:id` preko M7/M8/M9-gost konteksta ne sadrže `BookingItem.supplier_reference` niti bilo šta iz M3 `Supplier`/`Contract` (poglavlje 6.2); isti poziv preko M17 (interni kontekst) ta polja ispravno vraća.
- [ ] `Booking.tip_nastupanja` se ispravno automatski izvodi iz `default_tip_nastupanja` (M3/M4) za `B2C_SITE`/`B2B_PORTAL`/`MOBILE` kanale; ponuda sa nesaglasnim kandidatima se odbija sa jasnom porukom umesto da tiho izabere jednu vrednost (poglavlje 4.0a).
- [ ] Test (dopuna 18.8.2026, poglavlje 3.0f): `POST /quotes/:id/items/manual` sa `channel=B2B_PORTAL` odbija zahtev koji sadrži bilo koje cenovno polje; ista ruta sa `channel=INTERNAL_PANEL`/`PHONE` zahteva `base_cost`/`final_price`.
- [ ] Test: `POST /quotes/:id/confirm` odbija potvrdu čim bar jedna stavka ima `manual_review_status = AWAITING_STAFF_REVIEW`, sa jasnom porukom, pre bilo kog poziva ka M3/M4; posle `POST /quotes/:id/items/:itemId/manual-review` sa `decision=APPROVED` (uz upisanu cenu) ista Ponuda se uspešno potvrđuje.
- [ ] Test: `BookingItem` sa `source_type = MANUAL` prelazi direktno u `item_status = CONFIRMED` pri potvrdi, bez ijednog poziva ka M3/M4 (mock provere da servisi tih modula nisu pozvani za tu stavku).
- [ ] Test: `POST /manual-product-entries/from-url` sa linkom hotela vraća predlog (naziv/opis/slike) ali ne upisuje `ManualProductEntry` dok agent eksplicitno ne potvrdi zasebnim pozivom — odbijen/izmenjen predlog se ne čuva neizmenjen.
- [x] Provera garancije putovanja (M11) se uvek izvršava pre provere kreditnog limita (M7), nikad obrnuto (poglavlje 4, korak 1) — M11 implementiran avgust 2026 (in-process poziv `ComplianceStubsService` → `TravelGuaranteeService`), testirano jedinično da se `checkTravelGuaranteeUtilization` poziva samo za `ORGANIZATOR` i pre `checkCreditLimitIfSubagent`; M7 implementiran avgust 2026 (in-process poziv preko `SubagentStubService` → M7 `SubagentsService`), testirano e2e (`test/m7-exit-criteria.e2e-spec.ts`, §4) da se kreditni limit proverava pre bilo kakve rezervacije kod M3/M4.
- [ ] `Quote.contract_terms_accepted` mora biti `true` pre nego što se dozvoli prelazak na plaćanje za samouslužne kanale; `Booking` kreiran bez ovog uslova (za te kanale) se ne dozvoljava.
- [x] Subagent sa `status = ACTIVE` i rezervacijom unutar kreditnog limita dobija vaučer automatski čim `Booking.status = CONFIRMED`, bez obzira na `payment_status`, bez ručnog override-a (poglavlje 6.3); rezervacija koja prekorači limit i dalje zahteva ručni override — implementirano avgust 2026 (`ComplianceStubsService.isActiveSubagentWithinCreditLimit` → M7).
- [ ] `GET /search` ispravno filtrira po `type` (nizu vrednosti — test: dva tipa istovremeno vraćaju uniju rezultata, ne presek), `destination_country`/`destination_city`, `stay_from`/`stay_to`, `occupancy`, `channel`; `type` koji sadrži `PACKAGE` vraća gotove pakete iz M2 za tu stavku.
- [ ] `GET /search` ispravno vraća `TRANSPORT`/`TICKET`/`EVENT` proizvode kad su traženi, sa atributima iz M2 poglavlja 2.3.
- [ ] `occupancy.room_config` sa više soba (npr. 2 sobe, različit broj gostiju po sobi) se ispravno čuva i čita kao niz; zbir po sobama koji se ne slaže sa `occupancy.adults`/`children` se odbija pri kreiranju `Quote`.
- [ ] Test: beba čiji uzrast (iz `children_ages[]`) pada u M2 `age_policy[]` kategoriju sa `counts_toward_capacity = false` ne izaziva odbijanje `Quote` zbog kapaciteta, dok drugo dete/gost koji prelazi kategoriju sa `counts_toward_capacity = true` iznad `capacity_children` biva odbijen (M2 poglavlje 2.3b).
- [ ] Test: `base_cost` sobe sa 2 odrasla + 1 dete se ispravno računa za `RateLine` sa `price_basis = PER_ROOM_PER_NIGHT` i posebno za `price_basis = PER_PERSON_PER_NIGHT`, oba sa istim ulaznim gostima ali različitim očekivanim zbirom (poglavlje 3.2b).
- [ ] Test: gost čija kategorija nema odgovarajući `M3 age_pricing[]` red odbija kreiranje `Quote` sa jasnom porukom, ne pretpostavlja cenu (poglavlje 3.2b).
- [ ] Moguće je sastaviti `Itinerary` sa više segmenata u više destinacija, promeniti im redosled, i konvertovati ga u `Quote` — svaki segment sa popunjenim `product_id` postaje `QuoteItem` sa ispravno primenjenom cenom/maržom (poglavlje 3.0.3).
- [ ] Test (dopuna 18.8.2026, poglavlje 3.0.4): segment sa `is_included = false` se ne pojavljuje u zbiru desnog panela i tiho se preskače pri `to-quote` konverziji (bez upozorenja); isti segment vraćen na `is_included = true` ponovo ulazi u zbir i konverziju.
- [ ] Test: `preview_final_price` snimljen na `ItinerarySegment` se NIKAD ne prepisuje direktno u `QuoteItem.final_price` pri konverziji — cena se uvek računa iznova (mock provera da se `MarkupRule`/M3/M4 stvarno pozivaju pri `to-quote`, ne samo kopira postojeća vrednost).
- [ ] Test (dopuna 18.8.2026, poglavlje 3.0.5): odgovor `GET /itineraries/shared/:shareToken` ne sadrži `base_cost`/`markup_rule_id` niti bilo šta iz M3 `Contract`/`Supplier` ni na jednom segmentu; `PATCH` na taj isti link uspeva samo za `isIncluded`, i odbija svaki drugi pokušaj izmene polja (400, ne tiho ignoriše).
- [ ] Test (dopuna 19.8.2026, poglavlje 3.1a): odgovor `GET /quotes/shared/:shareToken` sadrži `final_price` ali ne `base_cost`/`markup_rule_id`/ništa iz M3 `Contract`/`Supplier`; `POST /quotes/:id/share` je idempotentan (dva uzastopna poziva vraćaju isti token).
- [ ] Test: `POST /itineraries/:id/clone` proizvodi nov `Itinerary` sa identičnim brojem segmenata i istim `is_included`/datumima kao original, ali sopstvenim `id` i `share_token = null` (ne nasleđuje tuđ token).
- [ ] Test (poglavlje 3.0.7): itinerar sa dva segmenta u različitim gradovima čiji se datumi preklapaju vraća `DATE_OVERLAP` upozorenje; isti itinerar sa dodatim `TRANSFER` segmentom između njih više ne vraća `MISSING_TRANSFER`; nijedno od upozorenja ne sprečava `to-quote` konverziju.
- [ ] Segment bez popunjenog `product_id` se ne konvertuje tiho — korisnik dobija jasno upozorenje pre konverzije koji segmenti su preskočeni.
- [ ] `Quote.itinerary_id` ispravno referencira izvorni `Itinerary` kad Ponuda nastane konverzijom, i ostaje `null` za direktne ponude.
- [ ] `SupplierManifest` i `SupplierChangeNotice` dobijaju jedinstven `reference_code` pri kreiranju nacrta; slanje ide isključivo kroz jedinstveno M22 sanduče (poglavlje 8.8), sa `[REF: TT-NNNNNN]` u naslovu.
- [ ] Test: `item_status` prelazi u `MODIFIED`/`CANCELLED` ispravno pokreće pripremu `SupplierChangeNotice` nacrta (poglavlje 8.8), odvojeno od revizije `SupplierManifest` (poglavlje 8.5).
- [ ] Test: dolazna poruka u jedinstveno sanduče sa tačnim `[REF: TT-NNNNNN]` u naslovu se predlaže kao poklapanje sa odgovarajućim `SupplierManifest`/`SupplierChangeNotice`, ali `supplier_confirmed_at`/`by` ostaje prazno dok zaposleni eksplicitno ne klikne potvrdu — provereno da AI agent nema pristup akciji koja to polje popunjava.
- [ ] Test: dolazna poruka bez prepoznate reference pada na fuzzy-match predlog (ime dobavljača/gosta/datumi), i dalje samo kao predlog, nikad automatski upis.
- [ ] `GET /search` vraća `SearchResultProduct[]` sa ugnježdenim `offers[]` (poglavlje 3.0b); `CONTRACTED` ponuda sa `total_capacity - units_sold` manje od traženog broja soba se ne pojavljuje u rezultatima; `ON_REQUEST` period se pojavljuje sa `availability_status = ON_REQUEST`, ne kao potvrđeno dostupan.
- [ ] Test: `API`-sourced ponuda sa `availableUnits = 0` (M4) se ne pojavljuje u rezultatima; ponuda sa `availableUnits > 0` nosi ispravan `quote_expires_at` iz `M4 AvailabilityQuote`.
- [ ] Test: `POST /quotes` za `API`-sourced stavku sa već isteklim `quote_expires_at` (iz izabranog `SearchResultOffer`) se odbija sa jasnom porukom, bez kreiranja `QuoteItem`; ista stavka sa `quote_expires_at` koji još nije prošao se prihvata i cena se ponovo pribavlja od M4 pre upisa (poglavlje 3.0b.3, korak 5).
- [ ] Test: izbor `SearchResultOffer` (i za `CONTRACTED` i za `API`) ispravno popunjava `product_id`/`rate_line_id` ili `provider_quote_reference`/`stay_from`/`stay_to`/`occupancy` u `POST /quotes` bez ručnog ponovnog unosa; isto važi za popunu `ItinerarySegment.product_id` kroz tok sastavljanja putovanja (poglavlje 3.0).
- [ ] `Quote.referral_tracking_code` (kad popunjeno) se ispravno kopira na `Booking.referral_tracking_code` pri potvrdi; M5 ga ni u jednom trenutku ne validira niti odbija rezervaciju zbog nepostojećeg/pogrešnog koda (M12 poglavlje 3a).

---

## 13. Otvoreno za dalje

- ~~"Lista rezervacija" ekran (M17) — pravi filter-panel~~ **Rešeno (24.8.2026, v1.54)** — `GET /bookings` dobio pun v1 skup pravih filtera (status uplate, tip nastupanja, ime kupca, broj, valuta, opseg kreiranja/dolaska/odlaska, tip proizvoda, destinacija, garancija putovanja), `/rezervacije/lista` sad zove stvaran endpoint. Ostaje otvoreno (namerno van ovog prolaza, PrimeTravel polja bez pravog izvora u Terminal-u): **Poslovnica/Odeljenje/Projekat/dodeljeni korisnik** (M1 nema taj pojam), **"dobavljač isplaćen"** (M10 obaveza prema dobavljaču nije povezana sa listom rezervacija), **naziv hotela** kao pravi prikaz (zahteva `ProductTranslation` join, nijedan pravi M5 ekran ga danas nema), i **"Hitno" upozorenja** kao stvaran signal (nema još pravog izvora — M18 alarm signal je najbliži kandidat kad dođe na red).
- ~~`CRUISE` specificiran (M2 spec poglavlje 2.1/2.3) ali nikad migriran u šemu.~~ **Rešeno (21.8.2026, na zahtev vlasnika)**: Prisma migracija `add_cruise_product_type` dodaje `CRUISE` u `ProductType` enum (`apps/api/prisma/schema.prisma`), primenjena na dev bazu. M17 ikonica "Krstarenja" (v1.28) otključana.
- ~~Tačan prag/format za avans (deo unapred, ostatak kasnije).~~ **Rešeno u M10 specifikaciji** (poglavlje 5.4, avgust 2026): globalna agencijska politika (`PaymentTermsConfig`) određuje procenat akontacije i rokove; M10 `ClientPaymentSchedule` prati ispunjenje po rezervaciji.
- ~~Da li B2B kreditni limit (M7) treba da blokira potvrdu rezervacije kad se pređe limit.~~ **Rešeno u M7 specifikaciji**: da — kad `Quote.client_account_id` pripada Subagentu, M5 proverava kreditni limit **pre** pokretanja toka potvrde (pre bilo kog poziva ka M3/M4); prekoračenje odbija potvrdu bez rezervisanja kapaciteta. Isto tako, cena za subagenta koristi proviziju (M7) umesto popusta lojalnosti (M6) kao poslednji korak u tok cene.
- Format vaučera (sadržaj, izgled) — definiše se kad se dođe do stvarne izrade, van obima ove specifikacije.
- Tačan izgled/template operativne liste po tipu dobavljača (poglavlje 8.3) — isto obrazloženje kao za vaučer, van obima ove specifikacije.
- Da li slanje operativne liste ide isključivo email prilogom (PDF) ili se razmatra i strukturisan API kanal ka većim hotelskim lancima/prevoznicima — otvoreno dok ne postoji konkretan zahtev dobavljača za tim.
- Da li `API` (M4) stavke ikad zahtevaju sličan operativni dokument (npr. provajder ne prosleđuje kompletne podatke dalje ka stvarnom dobavljaču) — trenutna pretpostavka (poglavlje 8) je da API konekcija sama nosi te podatke, revidira se ako se u praksi pokaže suprotno.
- ~~Tačan izgled kalendara (mesečni grid vs. nedeljni vs. lista)~~ — **rešeno 27.8.2026 (v1.74)**: Mesec/Nedelja/Dan prikaz + filteri Liste, poglavlje 7.4. I dalje otvoreno, namerno van ovog prolaza (potvrđeno preko `AskUserQuestion`): prevlačenje termina koje STVARNO menja datum rezervacije u bazi (sad je čist pregled, promena ide kroz postojeći tok izmene) — veći poduhvat, zahteva ponovnu proveru kapaciteta/cene (M3/M4) i verovatno zaseban prolaz specifikacije pre koda.
- Vizuelni prikaz/UI za sastavljanje putovanja (poglavlje 3.0.4a) — dizajnersko pitanje van obima.
- AI predlozi za popunu praznina u itineraru (poglavlje 3.0.4a) — otvoreno dok M15 ne dođe na red za M5.
- Da li `Itinerary` treba i sopstveni rok isteka (`ABANDONED` po vremenu neaktivnosti, slično `Quote.expires_at`) ili ostaje bez roka dok ga korisnik ne konvertuje ili ručno ne napusti — otvoreno, nije kritično jer `Itinerary` ne drži kapacitet niti cenu.
- ~~Tačna semantika `stay_from`/`stay_to`/`occupancy` za `TRANSPORT`/`TICKET`/`EVENT`~~ — **delimično rešeno 17.8.2026** (poglavlje 3.0d): rent-a-car (`TRANSPORT/RENT_A_CAR`) dobio `origin_city`/`destination_city` za preuzimanje/vraćanje (različite vrednosti = one-way rental) i `driver_age`; `EXCURSION`/`EVENT`/`TICKET` dobili zajednički tretman kroz "Things to do" (poglavlje 3.0d.4). I dalje otvoreno: tačan skup `attributes.category` vrednosti za ta tri tipa (isti princip kao `AmenityTag`, M2 poglavlje 2.3c) — dorađuje se pri stvarnoj izradi ekrana.
- **Novo (17.8.2026) — `INSURANCE.attributes.coverage_regions[]`** (poglavlje 3.0d.8) pomenuto kao neophodno za pretragu po odredištu, ali struktura (lista zemalja? region-enum poput "Šengen"/"Svet bez SAD"?) nije razrađena — čeka M2 spec dopunu kad putno osiguranje stvarno dođe na red za implementaciju.
- **Novo (17.8.2026) — datum polaska za `PACKAGE` sa liste ponuđenih termina, ne slobodan opseg** (poglavlje 3.0d.6) — tačan UI (kalendar sa markiranim danima vs. padajuća lista datuma) je dizajnersko pitanje van obima ove specifikacije, isto obrazloženje kao ostatak kalendarskih UI pitanja iznad.
- **Novo (avgust 2026) — prag za ponovnu proveru `API` cene pri `POST /quotes`** (poglavlje 3.0b.3, korak 5). Trenutno pravilo je najstrože moguće (uvek ponovo pitati M4, bez obzira koliko je vremena prošlo od pretrage) — ostavljeno namerno konzervativno dok se ne vidi u praksi da li broj/cena poziva ka M4 (Travelgate) to opravdava. Ako se pokaže preskupo, uvodi se prag (npr. "ne proveravaj ponovo ako je manje od N sekundi prošlo od pretrage") — zahteva potvrdu vlasnika pre uvođenja, jer svaki takav prag nosi rizik prikazivanja cene koja više nije tačna.
- **Novo (avgust 2026, na zahtev vlasnika) — automatski podsetnik gostu o roku za potvrdu/uplatu opcije kod dobavljača.** Kad dobavljač drži stavku rezervacije "na opciju" (`item_status = PENDING_SUPPLIER_CONFIRMATION`, poglavlje 4) sa konkretnim rokom posle kog dobavljač sam automatski otkazuje ako agencija do tada ne izda vaučer/ne potvrdi, sistem treba automatski da pošalje **gostu** (ne samo internom timu kao u poglavlju 6.1) podsetnik pre tog roka — analogno automatskim email obaveštenjima koje sami dobavljači šalju agenciji (primer iz prakse: "Reservations pending confirmation" — hotel, check-in/check-out datum, referenca dobavljača, ime nosioca rezervacije, tačan rok otkazivanja, veb-referenca). Ovo je nov zahtev, van postojećeg obima 6.1, i pre implementacije zahteva dalju razradu i potvrdu vlasnika oko: (a) novog polja za rok opcije na `BookingItem` — ovaj rok je rok **koji je dao dobavljač**, različit je od praga 48h iz poglavlja 6.1 (koji je interni prag za upozorenje tima, ne stvarni rok posle kog dobavljač otkazuje); (b) kanala/mehanizma slanja transakcionih (ne-marketinških) email obaveštenja gostu — ni ova specifikacija ni M6 (poglavlje 4.1, koji pokriva samo marketinške/informativne poruke uz `marketing_consent`) trenutno ne definišu takav kanal, pa ovo verovatno zahteva dopunu i M5 i M6; (c) tačnog praga koliko pre roka se podsetnik šalje i da li se ponavlja. Vidi i `24-GAP-ANALIZA-PROBLEMI-VS-ARHITEKTURA.md` Dodatak B.
- **Zakon o zaštiti potrošača — povećanje cene posle potvrde rezervacije** (istraživanje, avgust 2026; upisano ovde 19.8.2026 posle nalaza da je stavka do sada postojala samo u backlog indeksu, ne i u izvornoj specifikaciji) — trenutno ne postoji nikakav koncept izmene cene posle `booking.confirmed`. Zakon (prema istraženim izvorima, potrebna potvrda pravnika za tačne brojeve) navodno dozvoljava povećanje samo za gorivo/takse/kurs, ograničeno na 8% ukupne cene, uz pisano obaveštenje gostu najmanje 20 dana pre polaska. Realna pravna izloženost dok se ovo ne implementira ili svesno ne odluči da agencija cene nikad ne menja posle potvrde. Detalji: `docs/analize/26-PRAVNA-I-KNJIGOVODSTVENA-OTVORENA-PITANJA.md` stavka B9.
- **Zakon o zaštiti potrošača — "opravdan razlog" izuzetak pri otkazivanju od strane gosta** (isto istraživanje) — trenutni `cancellation_refund_percentage` (poglavlje 6) preuzima dobavljačevu politiku otkazivanja bez razdvajanja od onoga što firma zakonski duguje gostu, i ne postoji izuzetak za iznenadnu bolest/smrt u porodici/višu silu (gost bi tad trebalo da dobije nazad sve osim stvarnih troškova, ne standardnu naknadu po danima). Zahteva potvrdu pravnika pre spec dopune. Detalji: `26-PRAVNA-I-KNJIGOVODSTVENA-OTVORENA-PITANJA.md` stavka B10.
- **Zakon o zaštiti potrošača — minimalan broj učesnika** (isto istraživanje) — `min_participants` (M2) postoji samo kao opisno polje za `EXCURSION` proizvode, nema procesa automatskog otkazivanja/punog povraćaja kad se minimum ne skupi, niti rokova obaveštenja (20/7/2 dana zavisno od dužine putovanja) iz zakona. Trenutno van obima za `PACKAGE`/`ACCOMMODATION` proizvode. Detalji: `26-PRAVNA-I-KNJIGOVODSTVENA-OTVORENA-PITANJA.md` stavka B11.
- **Novo (17.8.2026, na zahtev vlasnika) — poređenje sa STVARNOM cenom sa tuđeg sajta (scraping).** Bezbedan deo ideje (tumačenje SAMO string-a URL-a koji korisnik nalepi, pokretanje naše pretrage sa istim parametrima, bez ijednog poziva ka tuđem serveru) specificiran u M15 spec poglavlju 6.5.6a. Puna verzija — čitanje stvarne cene sa tuđe stranice radi prikaza pored naše — namerno NIJE specirana ovde: uslovi korišćenja skoro svakog OTA/agencijskog sajta zabranjuju automatsko čitanje njihovih stranica (pravni rizik), a cena se često učitava dinamički/menja po sesiji-geolokaciji (tehnički nepouzdano čak i da je pravno čisto). Zahteva potvrdu pravnika pre bilo kakvog razmatranja implementacije — isti standard kao ostale pravno osetljive teme u ovom dokumentu.
