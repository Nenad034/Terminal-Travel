# Specifikacija modula M17 — Interni radni panel

**Odnosi se na:** `00-MASTER-ARHITEKTURA.md`, poglavlje 4 (M17), poglavlje 5 (referentna arhitektura) i poglavlje 8 (Faza 0/1 — panel se pretpostavlja gotovim od tada)
**Nivo:** Nivo 2 — detaljna specifikacija, dovoljna da AI agent direktno programira po njoj
**Status:** Nacrt za usvajanje

**Verzija:** 2.42 — Ekran rezervacije (`/rezervacije/[id]`) prelazi sa jedne duge stranice na **kartice dosijea** (1.9.2026, na zahtev vlasnika, posle poređenja sa PrimeTravel formom — `docs/analize/22-ANALIZA-PRIMETRAVEL-NALAZI.md` §14, M5 spec §4.5/§4.6): Pregled / Finansije / Komunikacija / Dokumenti / Beleške. Stanje kartice živi u URL-u (`?tab=`), ne u klijentskom state-u — stranica ostaje server-komponenta, pojedinačna kartica se može podeliti linkom, i **podaci se dohvataju samo za otvorenu karticu** (nema poziva M10/M6 dok se gleda Pregled). Finansije/Komunikacija/Dokumenti su čist primer kompozicije iz poglavlja 2 — nula nove poslovne logike, samo `GET /finance/payments?bookingId=`, `GET /crm/communication-log?clientAccountId=` i već postojeći M20/M10 pozivi; svaka kartica se prikazuje isključivo uz dozvolu matičnog modula (bez dozvole kartice nema, ne prikazuje se prazna). Vaučer i M20/M10 kartice premeštene sa Pregleda u Dokumente (bile bi duplirane). Beleške su jedina kartica sa novim podatkom (`BookingNote`, M5 §4.6) — append-only, sa `ActorLabel` obeležavanjem autora (§3.1). Poznato ograničenje, vidljivo označeno na samom ekranu: kartica Komunikacija prikazuje prepisku sa NALOGODAVCEM, ne sa rezervacijom, jer M6 `CommunicationLog` nema `booking_id` (dodavanje te veze je u backlog indeksu, M6 sekcija).
**Verzija:** 2.41 — Upozorenje na neusklađene datume u selekciji (29.8.2026), panel-strana napomena — `RightPanel.tsx` "Napravi ponudu" sad prvo proverava (`@/lib/date-mismatch.ts`) da li se termin PREVOZ (let/transfer) stavke uopšte poklapa sa periodom BORAVAK (hotel/paket) stavki u selekciji; kad ne, prikazuje upozorenje sa spiskom neusklađenih stavki i traži eksplicitnu potvrdu ("da, termini su namerno različiti") pre ponovnog slanja `POST /quotes` sa `dateMismatchAcknowledged: true`. Detalji (API strana, audit log) u M5 spec §3.0e.3a/v1.81.
**Verzija:** 2.40 — Grupne pretrage (29.8.2026), panel-strana napomena — `SearchCriteriaChip.tsx` dobija dugme "dodaj u grupu" (sesijsko stanje, `GroupSearchBuilderContext.tsx`, montiran u `Shell.tsx` da preživi promenu query stringa) i "sačuvaj grupu" kad ima bar 2 pretrage u izgradnji; nov `SavedGroupSearchesSidebarPanel.tsx` u levom panelu (pored postojećih pojedinačnih sačuvanih pretraga) — klik na grupu otvara SVAKU njenu pretragu u sopstvenom tabu (`TabsContext.openTab`). Detalji (M1 `UserPreference` ključ, max 10 grupa/6 pretraga po grupi) u M5 spec v1.82.
**Verzija:** 2.39 — Ispravka Ctrl+K komandne palete: unos teksta gasio direktno poklapanje po nazivu sekcije (28.8.2026, prijavio vlasnik uživo — "ne radi pretraga u ovom panelu"). Uzrok: `CommandPalette.tsx` unosom teksta u potpunosti sakriva statičnu, već ispravno filtriranu navigaciju (`navResults`) i prelazi isključivo na AI (omnisearch) panel; za kratke upite (≤12 znakova, bez "?") AI korak ne zove ni pretragu entiteta ni jezički model i vraća prazan odgovor, a fallback na navigaciju je do sada postojao samo za `active:false`/grešku, ne i za ovaj "uspešan ali prazan" slučaj — pa je čak i doslovno poklapanje (npr. upis "rezervacije" → sekcija "Pretraga i rezervacije") bilo potpuno nevidljivo. Ispravka: sekcija "Sekcije panela" (poklapanja iz `navResults`) se sad prikazuje UVEK kad postoji poklapanje, iznad AI panela, bez obzira na AI ishod — AI panel zadržava sopstvenu poruku (odgovor/greška/neaktivno/nema rezultata) ispod. Server-strana (`omnisearch.service.ts`) nepromenjena — provereno uživo (curl sa pravom sesijom) da API i dalje ispravno vraća prazan AI rezultat za kratak upit, bag je bio isključivo u klijentskom prikazu.
**Verzija:** 2.38 — Panel ekran za unos tipa sobe (28.8.2026, na zahtev vlasnika: "koja polja sve taj panel treba da ima"). Detaljno u M2 spec §2.3a/§2.3b/v1.14 — ovde samo panel-strana napomena: `apps/panel/src/app/(app)/katalog/[id]/RoomTypesEditor.tsx` (nov), lista tipova soba na ekranu proizvoda + modal za dodavanje/izmenu (kreveti/tip kreveta/kapacitet/uzrasna politika/karakteristike); server akcija `saveRoomTypes` (`katalog/actions.ts`) čuva preko `PATCH /catalog/products/:id`. Prvi primer u kodbazi obrasca "lista objekata + modal za dodavanje/izmenu" — nema ranijeg da se prati, uspostavljen ovim prolazom.
**Verzija:** 2.37 — (retroaktivno zabeleženo, propušteno u trenutku dodavanja) — v1.79 M5 spec ispravka (brz period Dan/Nedelja/Mesec premešten iz `RealFilterBar` forme u uvek vidljivu traku ikonica `BookingsListClient.tsx`) je panel-strana izmena bez sopstvenog M17 changelog unosa; detalji ostaju isključivo u M5 spec v1.79 (isti sadržaj, ne duplira se ovde).
**Verzija:** 2.36 — Ispravka rasporeda filtera u Kalendaru + brz period Dan/Nedelja/Mesec i na "Lista rezervacija" (27.8.2026, na zahtev vlasnika, uz snimak ekrana: "pogledajte polja pretrage nije sve lepo stalo, ispravite. Pregled na dnevnom mesečnom i nedeljnom nivou staviti i u listu rezervacija"). Detaljno u M5 spec §7.4/v1.77 — ovde samo panel-strana napomena: `PeriodQuickFilter.tsx` (nov, `rezervacije/lista/`) dodaje isti Dan/Nedelja/Mesec prekidač kao Kalendar, primenjen kao brz filter nad postojećim `stayFrom`/`stayTo` poljima (tabela ostaje ista, potvrđeno preko `AskUserQuestion` — ne novi način prikaza). Deljena datumska aritmetika izdvojena u `@/lib/calendar-date.ts`.
**Verzija:** 2.35 — Sumarni izveštaj u desnom panelu za "Kalendar rezervacija" (27.8.2026, na zahtev vlasnika: "kada kliknemo na stavku kalendara u desnom panelu treba da se pojavi sumarni izveštaj koliko rezervacija, statusi, koje destinacije, koliko osoba, koliko soba, da li ima i koliko rezervacija sa alertima. možete i vi dodati nešto"):
  - **`RowSummaryContext.tsx`** — `RowSummary` postaje unija: postojeći oblik preimenovan u `BookingRowSummary` (`kind: 'booking'`), nov `CalendarDaySummary` (`kind: 'calendar-day'`) sa `bookingCount`/`itemCount`/`statusCounts`/`destinationCounts`/`productTypeCounts`/`totalGuests`/`totalRooms`/`valueByCurrency`/`supplierPendingCount`/`unpaidCount`. Isti kontekst, isti "klik zameni prethodni sažetak" mehanizam kao pre — sad samo dva različita oblika sadržaja.
  - **`rezervacije/kalendar/build-day-summary.ts`** (nov) — agregira već fetch-ovan `DayDetail` (Dan prikaz) u `CalendarDaySummary`. Rezervacije se broje po JEDINSTVENOM `bookingId` (dve stavke iste rezervacije istog dana se ne broje dvaput), uplata takođe (svojstvo rezervacije, ne stavke).
  - **`rezervacije/kalendar/RegisterDaySummary.tsx`** (nov, client adapter — isti obrazac kao `RegisterTab.tsx`) — gura sažetak u kontekst čim se Dan prikaz montira/promeni datum.
  - **`RightPanel.tsx`** — grana prikaz po `summary.kind` (`BookingSummary` za `'booking'`, nov `CalendarDaySummaryCard` za `'calendar-day'`) — statistika u 4 pločice (rezervacija/stavki/osoba/soba), crveno uokviren blok upozorenja SAMO kad ima nešto da se prijavi (čeka potvrdu dobavljača / nije naplaćeno), raščlanjenja po statusu/destinaciji/tipu proizvoda, ukupna vrednost po valuti.
  - **`GET /bookings/calendar/:date` prošireno** (M5 spec v1.76) da nosi dovoljno podataka za ovu agregaciju (destinacija/tip proizvoda/broj soba/cena/status uplate po stavci) — API ugovor promenjen na M5 strani, ovde samo potrošen.
  **Provera:** `tsc --noEmit` čist za `apps/api` i `apps/panel`; SSR (`curl`) potvrđuje da Dan prikaz i dalje vraća HTTP 200 sa mock zapisima nepromenjeno posle ove dopune — sam prikaz u desnom panelu je klijentsko stanje (React Context), potvrđeno čitanjem koda, isto poznato ograničenje kao svaka druga interakciona izmena u ovom dokumentu (nema pravog browsera u ovom okruženju) — vizuelnu potvrdu daje vlasnik uživo.
**Verzija:** 2.34 — "Kalendar rezervacija" prerađen kao Google Calendar stil (27.8.2026, na zahtev vlasnika: "napraviti kao Google kalendar sa svim funkcijama prilagođenim potrebama TT-a, dodati filtere koji postoje u Listi rezervacija"). Detaljno u M5 spec §7.4 (v1.74) — ovde samo panel-strana napomena:
  - Novi fajlovi pod `apps/panel/src/app/(app)/rezervacije/kalendar/`: `calendar-utils.ts` (čista datumska aritmetika, bez vremena — nema sata dolaska/odlaska u modelu), `CalendarHeader.tsx` (prekidač Mesec/Nedelja/Dan + danas + strelice, sve preko `TabLink`/`navigateInTab`, isti obrazac kao zamka 9.2), `CalendarFilterBar.tsx` (isti filter-skup kao `RealFilterBar.tsx` sa `rezervacije/lista`, obična GET forma — prava navigacija, ne App Router "meko" navigiranje), `MonthGrid.tsx`/`WeekGrid.tsx`/`DayAgenda.tsx` (tri prikaza + deljena lista termina po danu).
  - Klik na dan u Mesec/Nedelja prikazu sad prebacuje na Dan prikaz za taj datum (ranije: panel se otvarao ispod istog grida) — bliže stvarnom Google Calendar obrascu.
  - NAMERNO bez prevlačenja termina — potvrđeno preko `AskUserQuestion` sa vlasnikom pre početka rada (dve odvojene odluke: obim prikaza Mesec+Nedelja+Dan, i "samo pregled, bez prevlačenja" umesto stvarne promene datuma rezervacije).
  **Provera:** `tsc --noEmit` čist za `apps/panel` i `apps/api`; uživo kroz pravu VLASNIK sesiju (dev-login) — sva tri prikaza (`?view=month/week/day`) HTTP 200 sa stvarnim sadržajem (mesečna mreža ispravno počinje ponedeljkom, dan-prikaz ispravno prikazuje "četvrtak" za 27.8.2026), navigacija (danas/strelice/prekidač) generiše tačne `?view=&date=` kombinacije, filter (`status=CONFIRMED&productType=ACCOMMODATION`) i dalje HTTP 200 sa ispravnim sadržajem.
**Verzija:** 2.33 — Ispravka "klik na 'Pretraga i rezervacije' u levom panelu ne radi" (27.8.2026, na zahtev vlasnika, uz snimak ekrana: "kada kliknem na Prodaja i rezervacije ništa se ne događa. Moram da kliknem na neku drugu ikonu u levoj traci pa da se vratim nazad, tek tada se u levom panelu [pojavi] pretraga"). Vlasnik je potvrdio (preko `AskUserQuestion`) da je adresa u browseru menjala pri kliku, ali sadržaj ostajao neosvežen — nepouzdano "meko" App Router navigiranje preko golog `<Link href>`, isti simptom kao ranije popravljena greška u `TabsContext.tsx` ("openTab je ranije SAMO upisivao zapis..."). `Sidebar.tsx` — stavke unutar grupe (npr. "Pretraga i rezervacije") menjaju se sa `<Link href={item.href}>` na `<button onClick={() => openTab(item.href, item.label)}>`, koristeći isti, već proveren `TabsContext.openTab()` mehanizam (eksplicitan `router.push` + sinhrono upisivanje/aktiviranje taba) koji već pouzdano radi svuda drugde u aplikaciji (AI chat prečice, meni "Poruke", klik na obaveštenje). **Provera:** `tsc --noEmit` čist za `apps/panel`; sama nepouzdanost je bila klijentska/interakciona (nije vidljiva iz koda niti reprodukovana u ovom okruženju bez pravog browsera) — vizuelnu potvrdu da je klik sad pouzdan daje vlasnik uživo.
**Verzija:** 2.32 — Ručno premeštanje tabova prevlačenjem, horizontalno (26.8.2026, na zahtev vlasnika: "omogućite ručno menjanje pozicije tabova u centralnom panelu, horizontalno"). Nativan HTML5 drag-and-drop (bez nove biblioteke — poglavlje 6 Master dokumenta nema DnD paket u steku, nativan API dovoljan za prostu linearnu listu):
  - **`TabsContext.tsx`** — nova `reorderTabs(draggedId, targetId)`: pronalazi obe pozicije u `tabs[]`, `splice`-uje `draggedId` na mesto `targetId` (isti obrazac kao standardan browser/VS Code drag). Menja samo redosled — ne dira aktivan tab ni putanju.
  - **`TabBar.tsx`** — svaki tab (`<Link>`) postaje `draggable`; `onDragStart`/`onDragEnd` prati `draggedId`, `onDragOver`/`onDragLeave` prati `dragOverId` (vizuelni indikator — deblja `border-accent-strong` ivica na tabu preko kog se trenutno prevlači), `onDrop` poziva `reorderTabs`. Prevučen tab dobija `opacity-40` dok se vuče. `onClick` na `<Link>` je zaštićen `if (draggedId) { e.preventDefault(); return; }` — sprečava da browser posle `drop`-a odmah ispali i `click` na istom elementu i nepotrebno navigira.
  **Provera:** `tsc --noEmit` čist za `apps/panel`. Sam drag-and-drop je client-side interakcija bez servera — potvrđeno čitanjem koda (isto poznato ograničenje "nema pravog browsera u ovom okruženju" kao svaka druga klijentska interakcija u ovom dokumentu); vizuelnu potvrdu daje vlasnik uživo.
**Verzija:** 2.31 — "Desni panel — brzi pregled proizvoda", Faza B: prava veza na katalog + kategorija/destinacija u kartici (26.8.2026, na zahtev vlasnika, posle uživo pregleda Faze A mock-a istog dana: "ovo je dobro, dodajte samo da se ispod naziva hotela napiše država i destinacija i da pored naziva hotela bude napisana kategorija hotela 5*", zatim "kreni na Fazu B"). Zamenjuje mock iz ranijeg prolaza istog dana stvarnom žicom:
  - **`ProductPreviewContext.tsx`** (nov) — LRU istorija max 3 proizvoda (potvrđeno preko `AskUserQuestion`: "max 3 taba" = istorija brzih pregleda u desnom panelu, ne opšti limit tabova). `showPreview({productId, name})` poziva se klikom na naziv proizvoda u rezultatima pretrage (`ProductPreviewButton.tsx`, `rezervacije/pretraga/page.tsx`).
  - **`/api/catalog/products/:id/preview`** (nova BFF ruta) — tanak proxy ka internom `GET /catalog/products/:id` (M2/product/VIEW dozvola), trimovan oblik (naziv, opis, grad/država, `stars`, `amenities`, `contact` iz `attributes` — M2 spec §2.3d dopuna, `photos`).
  - **`ProductPreviewCard.tsx`** — sad učitava PRAVE podatke po aktivnom tabu (kešira po `productId`, ne ponavlja fetch pri prebacivanju), kategorija (`X*`) pored naziva + država/destinacija ispod (obrnut redosled od ranijeg mock-a: država pa grad, na zahtev vlasnika). Dugme "Prikaži pun opis" sad stvarno otvara `openTab(/katalog/:id/pregled, naziv)`.
  - **`/katalog/[id]/pregled`** (nova ruta) — pun prikaz proizvoda u centralnom panelu (galerija grupisana iz `media[]`, opis, sadržaji, tipovi soba) — odvojena od postojeće `/katalog/[id]` FORME ZA IZMENU.
  - **`ProductContactCard.tsx`** (nov) — kad je `/katalog/:id/pregled` aktivan tab, desni panel prikazuje kontakt (ili "kontakt nije unet") + dugme "Aktivne rezervacije" (`openTab` sa novim `productId` filterom + `status` isključuje CANCELLED/COMPLETED, M5 spec v1.66). Prikazuje se PO RUTI (`PRODUCT_PREGLED_RE` regex na `pathname`, `RightPanel.tsx`), ne po `moduleId` — `katalog` pripada grupi `katalog-nabavka`, RAZLIČITOJ od `prodaja` gde je pregled pokrenut (nav.ts), pa provera po modulu ne bi pogodila ovu rutu.
  - **Prioritet u `RightPanel.tsx` "prodaja" grani** (nepromenjen plan): selekcija (ponuda) > brzi pregled (`ProductPreviewContext` neprazan) > sažetak reda > prazno stanje.
  - Uzgredna dopuna u istom razgovoru (vlasnik uočio da AI ne zna ništa o mock hotelu, dijagnostikovano kao STVARAN nedostatak nezavisan od mock-a — nijedan postojeći AI alat nije vraćao pune detalje proizvoda) — nov AI alat `get_product_details`, M15 spec v1.44.
  **Provera:** `tsc --noEmit` čist za `apps/panel` i `apps/api`; uživo kroz pravu VLASNIK sesiju protiv stvarne baze — `/api/catalog/products/:id/preview` za pravi hotel ("Hotel M13 Test...") vraća tačan naziv/opis/stars=4; `/katalog/:id/pregled` (HTTP 200) renderuje isti naziv/opis u HTML-u; `/rezervacije/lista?productId=...` (HTTP 200, nova filter grana ne baca grešku); pravi `POST /api/omnisearch` upit "šta mi možete reći o hotelu [naziv]" ispravno pozvao `get_product_details` i vratio tačnu kategoriju/destinaciju uz pošteno priznanje da sadržaji/kontakt nisu uneti. Sam klik/prebacivanje tabova u desnom panelu je klijentsko ponašanje — potvrđeno čitanjem koda, isto poznato ograničenje kao ostatak interakcionih izmena u ovom dokumentu.
**Verzija:** 2.30 — Ispravka "klik na Home dok je traka skupljena je ćutljiv" (26.8.2026, na zahtev vlasnika: "omogućite da kad zatvorimo levi panel i kliknemo na ikonu Home, opet se otvori. sada to nije tako").
  - `ActivityBar.tsx` — Home je (v2.25) potpuno izuzet iz "klik na već-aktivnu grupu skuplja/širi" prečice (namerno, druga svrha — Home uvek treba da navigira, ne da nudi skupi/proširi kao Audit log/API/MCP). Taj potpun izuzetak je slučajno uklonio i legitiman slučaj: dok je traka SKUPLJENA, klik na Home nikad nije dirao `collapsed` stanje, pa se traka nije otvarala. Ispravljeno — Home i dalje uvek navigira (nema `e.preventDefault()`), ali DODATNO poziva `onToggleCollapse()` kad je `collapsed === true`.
  **Provera:** `tsc --noEmit` čist za `apps/panel`. Sam klik/expand tok je čisto klijentsko ponašanje — potvrđeno čitanjem koda, isto poznato ograničenje kao ostatak interakcionih izmena u ovom dokumentu.
**Verzija:** 2.29 — Logo +15% + ispravka "treba više klikova da se otvori skupljena leva traka" (26.8.2026, dva zahteva vlasnika istog dana).
  - **Logo +15%** — "kada se skupi levi panel previše se smanjuje logo, uvećajte generalno logo za 15%" — `TopBar.tsx` ikonica 20px→23px (prošireno)/16px→18.4px (skupljeno stanje bez natpisa), natpis 14px→16.1px.
  - **Skupljena traka — bag više klikova** — pravi uzrok: `ActivityBar.tsx` klik na grupu koja NIJE aktivna zove samo `onSelectGroup` (menja `activeGroupId`); dok je `sidebarCollapsed=true`, `Sidebar.tsx` se uopšte ne montira (`if (collapsed) return null`, v1.94 dizajn), pa taj prvi klik nema NIKAKAV vidljiv efekat. Tek DRUGI klik (sad na već-aktivnu grupu) pogađa granu koja stvarno zove `onToggleCollapse`. Ispravljeno u `Shell.tsx` — `onSelectGroup` prosleđen `ActivityBar`-u sad, uz `setActiveGroupId`, odmah zove i `setCollapsed(false)` ako je traka skupljena — jedan klik bira grupu I širi traku.
  **Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju — `GET /` (HTTP 200) potvrđuje nove klase `h-[23px] w-[23px]`/`text-[16.1px]` u renderovanom HTML-u (prošireno stanje, podrazumevano na serveru). Skupljeno stanje ikonice (18.4px) i sam klik/expand tok su čisto klijentsko ponašanje (localStorage + React state) — potvrđeno čitanjem koda, ne curl-om, isto poznato ograničenje kao ostatak vizuelnih/interakcionih izmena u ovom dokumentu.
**Verzija:** 2.28 — Novi logotip (ikonica + wordmark malim slovima) (26.8.2026, na zahtev vlasnika, uz sopstveni Gemini-generisan koncept ikonice i primer fonta za natpis).
  - **Ikonica** — `apps/panel/public/brand/terminal-travel-icon-v2.svg`, novi mask-izvor (isti CSS `mask-image`/`bg-accent` mehanizam kao ranija PNG verzija, v2.24) sa 4 polja (kvadrat-obris gore levo, tri linije gore desno, terminal `>_` dole levo, tri linije dole desno) — nadahnuto vlasnikovim konceptom, namerno NIJE identična kopija bilo kog postojećeg tuđeg znaka (razjašnjeno u razgovoru: vlasnik je sam napravio koncept preko Gemini-ja, poslata "sabre" slika je bila primer FONTA, ne logo za kopiranje). `terminal-travel-icon.png` (v2.08) ostaje na disku neiskorišćen (nije obrisan — moglo bi zatrebati za drugu namenu, npr. favicon).
  - **Wordmark** — natpis menja se sa "TTerminal TTravel" (v2.24) na malim slovima "terminal travel", u novom `font-brand` (Chakra Petch, `tailwind.config.ts` + `@import` u `globals.css`) — čvrst, geometrijski Google Font najbliži utisku sa slike koju je vlasnik poslao kao primer fonta. `sans`/`mono` stek (poglavlje 6 Master dokumenta, tehnički stek) ostaje nepromenjen svuda drugde — `font-brand` postoji isključivo za ovaj natpis u `TopBar.tsx`.
  **Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju — `GET /` (HTTP 200) potvrđuje `terminal-travel-icon-v2.svg` (2× u markup-u, mask-image + fallback), `font-brand` klasu i tačan tekst "terminal travel" malim slovima u renderovanom HTML-u; `GET /brand/terminal-travel-icon-v2.svg` vraća HTTP 200. Stvaran vizuelni izgled fonta/ikonice u pregledaču nije uživo potvrđen — isto poznato ograničenje kao ostatak vizuelnih izmena u ovom dokumentu (nema pravog browser-a u ovom okruženju).
**Verzija:** 2.27 — `HomeSidebarPanel` dorada (kartice + jasniji naziv izvora) + +10/−10 tag okidači u listi rezervacija (26.8.2026, dva odvojena zahteva vlasnika istog dana, posle uživo pregleda v2.26).
  - **`HomeSidebarPanel.tsx`** — "Nije mi jasno napisano hoću da piše Agent Inbox" — red je ranije pisao samo "N stavki čeka odobrenje" bez imena izvora; sad "Agent Inbox: N na čekanju". "Stavite linkove u neke lepe kartice kao što je to u VS Code" — svaki red (sažetak i brzi linkovi) postaje kartica (`rounded-lg border border-border bg-panel p-2`, ikonica u obojenoj značci pored teksta) umesto ravne linije — isti "kartica" vizuelni jezik kao VS Code Welcome stranica, ista osnova koja se već koristi za kartice drugde u panelu (`RightPanel.tsx` `SelectionRow`/`CollectedItemRow`), ne nov obrazac.
  - **`BookingsListClient.tsx` (M5 lista rezervacija)** — dva nova "tag" okidača u traci ikonica koja je već stalno vidljiva (v1.61): **"+10"** (dolasci od danas u narednih 10 dana) i **"-10"** (odlasci od danas u narednih 10 dana), centrirani na sredini trake (`flex-1 justify-center` srednji blok, `ml-auto` desnog bloka uklonjen jer više nije potreban — prirodno gura levi/desni blok na ivice). Postavljaju/uklanjaju ISTA dva polja koja `RealFilterBar` već razume (`stayFrom`/`stayTo` za dolazak preko `sign-in` ikonice, `returnFrom`/`returnTo` za odlazak preko `sign-out` ikonice, M5 spec §11) preko prave navigacije — nema nove filter logike na serveru, samo prečica za već postojeći opseg datuma. Klik dok je tag već aktivan (query parametri se tačno poklapaju sa izračunatim opsegom danas→danas+10) uklanja ta dva parametra, isti "toggle" princip kao demo zvona/tip proizvoda dugmad na istoj traci.
  **Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju — `GET /` (HTTP 200) potvrđuje "Agent Inbox:" tekst i 5 kartica sa tačnom CSS klasom u renderovanom HTML-u; `GET /rezervacije/lista` (HTTP 200) potvrđuje `codicon-sign-in`/`codicon-sign-out` i tekst ">+10<"/">-10<" u traci. Sam klik/toggle na tagove je klijentska navigacija (`router.push`), potvrđena čitanjem koda — isto poznato ograničenje curl provere kao ostala čisto klijentska ponašanja u ovom dokumentu.
**Verzija:** 2.26 — `HomeSidebarPanel` — sažetak + brzi linkovi za Početnu (26.8.2026, na zahtev vlasnika, uz snimak ekrana GitLens-ove "Get Started" table kao primer: "Osmisliti šta možemo ovde da prikazujemo a što je važno za prikaz poslovanja"). Popunjava prazan prostor levog panela ispod naslova "Početna" (isti mehanizam kao `SearchSidebarPanel.tsx`/`SavedViewsSidebarPanel.tsx`, `Sidebar.tsx` bira po `selected.id === 'pocetna'`). Vlasnikova odluka preko `AskUserQuestion`: "i sažetak i brzi linkovi" (ne samo jedno od dvoje). Dva bloka:
  - **Sažetak** — ISTI izvori podataka koje glavni dashboard (`app/(app)/page.tsx`, M17 spec §5) već učitava (M3 rokovi povrata alotmana, M11 garancija putovanja, M1 bezbednosna upozorenja, M15 Agent Inbox) — ništa novo se ne agregira, samo se postojeći brojevi sažimaju za levu traku. Nova BFF ruta `GET /api/home-summary` (`HomeSidebarPanel.tsx` je klijentska komponenta, ne može direktno da pozove `getMe()`/`apiFetch` server-only helpere kao stranica) ponavlja ISTU permission-gated logiku kao `page.tsx` (polje `null` kad korisnik nema dozvolu za taj izvor, ne 0 — razlika između "nema upozorenja" i "nema pristup"). Redovi se prikazuju samo kad je broj > 0 (osim garancije putovanja, koja se prikazuje uvek kad je dostupna, isti princip kao dashboard kartica), svaki vodi na isti ekran kao odgovarajuća dashboard kartica.
  - **Brzi linkovi** — kratak, kuriran spisak najčešće korišćenih ekrana (Pretraga i rezervacije, Lista rezervacija, Katalog proizvoda, CRM, Podrška), filtriran kroz već postojeću `items` (vidljivost po dozvoli, isti princip kao ostatak `Sidebar.tsx`).
  **Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju — `GET /api/home-summary` vraća stvarne brojeve (`expiringReleasesCount: 1, guaranteeStatus: "ACTIVE", agentInboxTotal: 211`), `GET /` (HTTP 200) potvrđuje "Sažetak"/"Brzi linkovi" naslove i svih 5 brzih linkova u renderovanom HTML-u leve trake.
**Verzija:** 2.25 — Ispravka pravog uzroka "Home dugme ništa ne radi" / "Pretraga se ne prikaže odmah" (26.8.2026, prijavio vlasnik uživo: "Kada se klikne na dugme home u levom panelu ništa se ne događa... nije mi ni logično klikanje, ne odgovara uvek na klik"; "Link za Pretraga i rezervacije takođe ne reaguje odmah, moram da kliknem na neku drugu ikoni iz menija pa da se vratim").
  - **Pravi uzrok (zajednički za oba prijavljena simptoma):** `activeGroupId` (`Shell.tsx`) se DOSAD menjao ISKLJUČIVO ručnim klikom na ActivityBar/TopBar ikonicu grupe (`onSelectGroup`) — svaka DRUGA navigacija koja menja stvarnu putanju (klik na već otvoren tab u `TabBar.tsx`, `CommandPalette`, klik na obaveštenje, link iz AI chat-a, ili prost `<Link>` klik na single-item ActivityBar stavku poput same Home ikonice) NIKAD nije ažurirala `activeGroupId` — ostajao je "zaleđen" na grupi iz koje je korisnik POSLEDNJI PUT ručno kliknuo ikonicu, bez obzira gde ga je navigacija stvarno odvela u međuvremenu.
  - **Simptom 1 (Home "ne radi"):** Home je "single-item" ActivityBar stavka (`ActivityBar.tsx`) — klik na nju dok je `activeGroupId` slučajno i dalje "pocetna" (npr. posle navigacije na drugo mesto preko `CommandPalette`-a ili klika na obaveštenje, koji ne ažuriraju `activeGroupId`) se tumači kao "klik na već aktivnu ikonicu" i SAMO skuplja/širi levu traku (postojeća "ponovni klik na aktivnu single-item ikonicu = skupi/proširi" prečica, korisna za Audit log/API konekcije/MCP) umesto da stvarno vodi na Početnu — izgledalo je kao da dugme ništa ne radi.
  - **Simptom 2 (Pretraga se ne prikaže odmah):** `Sidebar.tsx` bira KOJU grupu da prikaže (i time da li se `selected` uopšte poklopi sa trenutnom putanjom) isključivo preko `activeGroupId`. Dok je on zaleđen na pogrešnoj (staroj) grupi, `/rezervacije/pretraga` se stvarno otvara u centralnom panelu, ali `Sidebar` i dalje računa `sectionItems`/`selected` za PRETHODNU grupu — ne poklapa se sa trenutnom putanjom, pa `SearchSidebarPanel` ne može da se prikaže dok se `activeGroupId` ne "odblokira" SLEDEĆIM ručnim klikom na neku ActivityBar ikonicu (tačno ponašanje koje je vlasnik opisao).
  - **Ispravka:** `Shell.tsx` — nov `useEffect` sinhronizuje `activeGroupId` sa `groupForHref(pathname)` na SVAKU promenu stvarne putanje, ne samo na ručne klikove (ručno "pregledanje" druge grupe preko ActivityBar/TopBar ikonice, bez navigacije, i dalje radi nepromenjeno — taj efekat se ne okida dok se putanja stvarno ne promeni). `ActivityBar.tsx` — Home stavka dodatno izuzeta iz "ponovni klik na aktivnu ikonicu = skupi/proširi" prečice (jedina single-item stavka čiji je posao da UVEK pouzdano vodi na Početnu, ne da nudi skupi/proširi kad je već tu — razlika u nameni od Audit log/API konekcija/MCP, koji taj izuzetak zadržavaju).
  **Provera:** `tsc --noEmit` čist za `apps/panel`; ovo je čisto klijentska React-state sinhronizacija (redosled navigacija kroz TabBar/CommandPalette/ActivityBar) koja se ne može reprodukovati preko curl-a — potvrđeno čitanjem koda: uzrok je precizno lociran (jedino `onSelectGroup` je ranije ažurirao `activeGroupId`, sad ga ažurira i sama promena putanje), oba prijavljena simptoma imaju IDENTIČAN koren, ispravka ne dira nijedan drugi postojeći put (ručno pregledanje grupe bez navigacije ostaje nepromenjeno).
**Verzija:** 2.24 — Naslov grupe VELIKIM SLOVIMA + linije vođenja (indent guide) + logo u zlatnoj boji aplikacije + uklonjena pozadina uvećanog logotipa (26.8.2026, tri odvojena zahteva vlasnika istog dana).
  - **`Sidebar.tsx`** — naslov grupe (npr. "PRODAJA") dobija `uppercase` (čisto CSS `text-transform`, isti obrazac kao VS Code naslovi sekcija bočne trake) i uvećanje za JOŠ 10% u odnosu na v2.23 (`text-[13.2px]` → `text-[14.52px]`, na zahtev "uvecajte ih za jos 10%"). Nova tanka vertikalna linija (`bg-ink-faint/30`, apsolutno pozicionirana `left-5`/20px, poravnata sa horizontalnim centrom ikonica stavki ispod) proteže se od dna naslova do dna poslednje stavke — "Povezite naziv modula sa stavkama modula onim linijama kao kada se u VS Code ispisuje tekst" (indent/tree guide linija, isti vizuelni princip kao VS Code Explorer stablo, bez horizontalnih grana koje ni sam VS Code Explorer nema).
  - **`TopBar.tsx`, logo boja** — poništava prethodni "Zalazak" preliv (v2.08, tri fiksne boje iz `apps/web` palete, namerno NEZAVISNE od teme panela). Vlasnik: "Logo Terminal Travel neka bude u zlatnoj boji aplikacije" — `--accent` (#8A8A5E, "maslinasto-zlatna", `globals.css`) JE zvanična zlatna boja OVE aplikacije (panel, "Horizont" tema, ista vrednost u oba moda, već WCAG proverena) — logotip (ikonica preko mask-a i tekst) sad koristi `bg-accent`/`text-accent` umesto sopstvene fiksne palete, prati temu panela ubuduće (svesna izmena ranije "fiksno bez obzira na temu" odluke).
  - **`TopBar.tsx`, naziv** — "Dodajte jos jedno po jedno T neka piše TTerminal TTravel" — vidljiv tekst logotipa postaje **"TTerminal TTravel"** (po jedno dodatno "T" na početak svake reči). `aria-label` dugmeta namerno OSTAJE "Terminal Travel" (stvaran naziv agencije za čitače ekrana) — stilizovan wordmark je vizuelna odluka, ne menja stvaran identitet za pristupačnost.
  - **`TopBar.tsx`, pozadina uvećanog logotipa uklonjena** — "kada se klikne na uvecanje loga uklonite pozadinu jer zahvata ikonu za Home i ne vidi se cela". Neprozirna `bg-bar` (uvedena 24.8.2026 baš da spreči providno preklapanje sa tabom "Početna" ispod) je sama postala problem: uvećana 2×, taj neprozirni pravougaonik je fizički prekrivao dugme "Početna" pored logotipa. `bg-bar`/`px-1.5`/`py-1`/`shadow-lg` uklonjeni iz uvećanog stanja — ostaje samo `scale-[2]` transform, providno preklapanje (ako se vizuelno ponovo primeti) je manji problem od potpuno nevidljivog dugmeta, eksplicitna vlasnikova odluka.
  **Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju — `GET /` (HTTP 200) potvrđuje `TTerminal TTravel` i `text-accent` klasu u renderovanom HTML-u, stara `#e8a63c` paleta više se ne pojavljuje nigde na strani. Linija vođenja i uklanjanje pozadine na klik su vizuelne/klijentske promene potvrđene čitanjem koda (linija vođenja i naslov grupe dele isto poznato ograničenje kao v2.23 — grana se ne renderuje na SSR-u nijedne prave rute).
**Verzija:** 2.23 — Uklonjena duplirana ikonica ispred naziva grupe u levoj traci (26.8.2026, na zahtev vlasnika, uz snimak ekrana: "u svakoj stavci menija imate po dve iste ikone"). `Sidebar.tsx` — kad nijedna sekcija grupe nije aktivno otvorena (npr. "Prodaja" naslov iznad "Pretraga i rezervacije"/"Kalendar rezervacija"/"Lista rezervacija"), naslov je nosio `activeGroup.icon` koja je IDENTIČNA ikonici te iste grupe u `ActivityBar.tsx` levo od cele trake (§4a grupisanje) — vizuelno se ista ikona ponavljala dva puta zaredom. Ikonica uklonjena iz `Sidebar.tsx` naslova (ActivityBar-ova ostaje, jedini nosilac identiteta grupe); naziv grupe uvećan 10% (`text-xs` 12px → `text-[13.2px]`) i podebljan (`font-medium` → `font-bold`) da i dalje jasno nosi hijerarhiju bez ikonice koja je ranije pomagala da se odvoji od stavki ispod. **Provera:** `tsc --noEmit` čist za `apps/panel`; ovo grananje (`!selected` u `Sidebar.tsx`) se prikazuje samo kad korisnik klikne strelicu nazad unutar već otvorene sekcije (klijentsko `forceShowList` stanje) — nijedna prava ruta ga ne renderuje na početnom SSR-u, pa je uživo curl provera ovde nemoguća po prirodi te grane (isto poznato ograničenje kao ostala čisto klijentska ponašanja u ovom dokumentu); potvrđeno čitanjem koda da je izmena sintaksno i logički ispravna (uklonjen `<Icon>` element, klase ažurirane, `Icon` import i dalje korišćen na ostalim mestima u fajlu).
**Verzija:** 2.22 — Ispravka: linije liste "ulaze" u prostor desnog panela u "overlay" prikazu (25.8.2026, prijavio vlasnik uživo, uz snimak ekrana: "linije ulaze u prostor desnok panela"). Uzrok: traka za ručno prevlačenje širine (`ResizablePane.tsx`, `handle` div, širina 6px) nema pozadinsku boju (samo `border-transparent`, providna dok se ne pređe mišem) — u "overlay" prikazu se centralni sadržaj NAMERNO ne sužava (panel samo PRELAZI preko liste, §6c.0), pa lista i dalje pune širine renderuje ISPOD cele fiksirane trake panela, uključujući i tih 6px providne trake — kroz nju su se videle horizontalne linije redova tabele ispod. U "push" prikazu se isto nije primećivalo jer je tamo lista stvarno sužena, nema šta providno da otkrije. Ispravljeno dodavanjem `bg-panel-2` na traku (isti "hrom" ton kao Sidebar/RightPanel, dizajn dok. princip iz v2.x — sad neprovidna u sva tri postojeća poziva `ResizablePane`, ne samo u desnom panelu).
**Verzija:** 2.21 — Ispravka: desni panel u "overlay" prikazu se nije protezao do donje ivice (25.8.2026, prijavio vlasnik uživo, uz snimak ekrana — "desni panel se ne prostire do donje ivice"). Uzrok: `ResizablePane.tsx` (koren komponente, `className="flex flex-shrink-0"`) nema eksplicitnu visinu — u "push" prikazu (Shell.tsx) to nije smetalo jer je roditelj `flex flex-1 overflow-hidden` (flex red sa podrazumevanim `align-items: stretch`) automatski razvlačio dete na punu visinu; u "overlay" prikazu je roditelj `fixed ... top-[43px] bottom-[38px]` (Shell.tsx v2.15) BLOK element bez `display: flex`, pa se dete NIJE automatski razvlačilo — `ResizablePane`/`RightPanel` su se skupili na visinu sopstvenog sadržaja, ostavljajući prazan (neobojen) prostor ispod, do donje granice fiksnog omotača. Ispravljeno dodavanjem `h-full` direktno na koren `ResizablePane.tsx` — bezuslovno tačno u sva tri postojeća poziva (bočna traka, desni panel push, desni panel overlay), jer svaki od njihovih roditelja u lancu ima stvarno definisanu visinu (`h-screen` ili `top`+`bottom` fiksni omotač).
**Verzija:** 2.20 — Ispravka "Invalid Date" u sažetku reda (25.8.2026, prijavio vlasnik uživo, uz snimak ekrana — red rezervacije bez ijedne stavke, npr. TT-M6-E2E-...-mjlipe8avah, prikazivao je "Dolazak: Invalid Date"/"Odlazak: Invalid Date"). Uzrok: `RealBookingsTable.tsx` (`decorate()`) računa `stayFrom`/`stayTo` iz stavki rezervacije — kad ih nema (`items.length === 0`), vrednost je `null`, koja se pri slanju u `RowSummaryContext` pretvara u prazan string (`b.stayFrom ?? ''`); `RightPanel.tsx` je zatim bezuslovno pozivao `new Date('').toLocaleDateString(...)`, a `new Date('')` je UVEK "Invalid Date", ne prazan datum. Ispravljeno: oba reda ("Dolazak"/"Odlazak") se sad prikazuju SAMO kad postoji stvaran datum — isti obrazac kao ostala opciona polja sažetka (hotelName, accommodationType...).
**Verzija:** 2.19 — Prilog dokumenta/slike u AI chat + lepljenje slike (25.8.2026, na zahtev vlasnika: "kada se klikne na + omogucite unos nekog fajla koji zelimo da unesemo kao kontekst i da razgovaramo o njemu", dopunjeno "slika takodje i kao fajl i copy-paste"; puni tehnički obrazac u M15 spec v1.43). "+" meni (`AiChatBox.tsx`) dobija treću stavku "Priloži fajl ili sliku" — skriven `<input type=file>`, dokument ide na server (`extract-file.service.ts`, novi `POST .../extract-file`) da se izvuče tekst, slika ostaje u pregledaču (base64, Claude Vision). Glavno polje za unos dobija `onPaste` — Ctrl+V slike iz clipboard-a ide istim IMAGE putem kao dugme. Greška pri prilogu (nepodržan tip/prevelik fajl) prikazana kao mala traka iznad reda za unos, zatvara se ručno ili se piše preko sledeće poruke.
**Verzija:** 2.18 — Tri dopune, isti dan kao v2.17, na zahtev vlasnika/uživo nalazi: (1) auto-kontekst taba i "#" na stavci menija (AiChatBox.tsx) sad postaju FILTERED_LIST umesto RECORD kad ruta ima pravi pogled u `filterable-views.ts` (`filterableViewForPath` mapiranje: `/rezervacije/lista`→`bookings`, `/crm`→`crm`, `/marketing`→`marketing`, `/nadzor`→`health_signals`) — ispravlja uživo nalaz gde je pitanje o broju rezervacija dobilo pogrešno obrazloženje "ne vidim sadržaj ekrana"; (2) `AiChatBox` u Fokus tabu (`fokus=true`) ograničen na `w-[70%] mx-auto`; (3) `TabBar.tsx` — naziv taba dobija `flex-1`, dugme za zatvaranje uvek na desnoj ivici.
**Verzija:** 2.17 — Dve dopune, isti dan kao v2.16, na zahtev vlasnika: (1) ikonica "#" (v2.16) sad postoji i na pojedinačnim stavkama unutar svake grupe u popup-u "Otvori modul", ne samo na nazivu grupe; (2) `AiContextProvider.onFirstAdd` (Shell.tsx) ne otvara desni panel dok je korisnik na `/ai-asistent` (Fokus tab) — provera `pathname !== '/ai-asistent'` pre poziva `openRightPanelForCurrentModule`.
**Verzija:** 2.16 — Dve dopune, isti dan kao v2.15, na zahtev vlasnika uz snimke ekrana: (1) `<input>` polje za unos u AiChatBox.tsx dobija `border-b` (jedna tanka donja linija, Material-stil); (2) popup "Otvori modul" (v2.14) — svaki red naziva grupe dobija ikonicu "#" na desnom kraju, klik zove `useAiContext().addRecord('Modul: <naziv>')`, popup ostaje otvoren (razlika od klika na stavku unutar grupe, koji navigira i zatvara popup).
**Verzija:** 2.15 — Četiri dopune desnog panela/AI chat-a, isti dan kao v2.14, na zahtev vlasnika: (1) linija koja deli AI sekciju od sadržaja iznad je sad ručno prevlačiva (`cursor-row-resize`, procenat visine panela, opseg 15–80%, pamti se u `localStorage`), prikazana samo kad su obe sekcije otvorene; (2) dugme "Fokus" (otvara `/ai-asistent`) premešteno iz reda za unos AiChatBox.tsx u gornji desni ugao zaglavlja AI sekcije (RightPanel.tsx), pored ikonice za sklapanje; (3) "push" i "overlay" prikaz desnog panela sad dele ISTU širinu preko istog `ResizablePane` (`storageKey="tt-panel-right-width"`, podrazumevano podignuto 320px→420px) — overlay više nema sopstvenu fiksnu širinu, "push" prestaje biti uža podrazumevana varijanta; (4) mikrofon premešten sa pre polja za unos na neposredno pre strelice "Pošalji" (posle polja).
**Verzija:** 2.14 — Popup "Otvori modul" (v2.13) grupisan po `NAV_GROUPS` umesto ravne liste (25.8.2026, isti dan, uživo nalaz uz snimak ekrana: "boldirajte osnovne stavke i neka budu bez linka. Otvaraju se samo podlinkovi") — naziv grupe podebljan, bez linka; klikaju se samo pojedinačne stavke unutar grupe. Prazne grupe se ne prikazuju.
**Verzija:** 2.13 — Ispravka + dve dopune poglavlja iz v2.12, isti dan, na osnovu uživo probe (vidi dizajn dok. §6c.0/§6c.0a/§6c.0b): (1) red za unos je pogrešno ostao pri VRHU umesto pri dnu (poruke se renderovale u uslovnom bloku bez `flex-1` rezervisanog prostora kad je prazan) — ispravljeno, blok poruka je sad uvek montiran; (2) istorija razgovora sad raste odozdo nagore (`flex-col-reverse`); (3) 4 brze prečice ispod polja UKLONJENE, zamenjene novom ikonicom "Otvori modul" (`/api/nav-items`, nova ruta — ista ulogom filtrirana lista kao Sidebar/CommandPalette preko `visibleNavItems`, potrebna jer AI chat u Fokus tabu nema pristup Shell.tsx server-side `items` props-ima) koja u popup-u NAGORE nudi SVE module; (4) meniji koji se otvaraju iz reda za unos (`+`, "Otvori modul") sad rastu nagore, ne nadole; (5) strelica "Pošalji" dobija fiksan preliv u bojama loga (inline SVG, isti gradijent kao TopBar logo — Codicon glif ne podržava pouzdano `background-clip: text`); (6) desni panel — svaka od dve naslagane sekcije (sažetak/podsetnik, AI chat) dobija sopstvenu ikonicu za sklapanje na visinu 0, oslobađa prostor drugoj sekciji.
**Verzija:** 2.12 — AI chat napušta plutajući prozor, postaje stalan deo desnog panela (25.8.2026, na zahtev vlasnika — vidi dizajn dok. §6c.0). Naslagan ISPOD postojećeg sadržaja desnog panela (sažetak reda/podsetnik, §5.5 ranije), ne tabovi — otvaranje/zatvaranje desnog panela sad kontroliše i AI chat (raniji zaseban prekidač "AI chat" u "Customize Layout" meniju uklonjen — "Desni panel" stavka pokriva oboje). Desni panel se više ne uklanja iz DOM-a pri zatvaranju (`ResizablePane collapsed`, isti mehanizam kao bočna traka) — AI istorija razgovora se ne gubi. Dva nova mehanizma: `UserPreference` ključ `right_panel_display_mode` (`push`/`overlay` — sužava sadržaj naspram plutajuće prelazi preko njega, prekidač u zaglavlju panela) i novi tab tip "Fokus" (ruta `/ai-asistent`, ikonica u zaglavlju dokovanog chat-a otvara AI chat preko celog centralnog prostora, isti sistem tabova kao ostatak panela — zatvaranje vraća na dokovan prikaz). `CommandPalette` (§4) ostaje nepromenjen — AI chat ne dobija duplikat spiska menija (vlasnikova eksplicitna odluka). **Provera:** `tsc --noEmit` čist za `apps/panel`; dev server (`next dev`) rekompajlirao bez grešaka, `/rezervacije/lista` i novi `/ai-asistent` vraćaju očekivan 307 (preusmerenje na prijavu) bez 500. **Puna uživo provera kroz pravu prijavu (klik na dugmad, izgled dokovanog panela, push/overlay prekidač, Fokus tab) NIJE urađena u ovom prolazu** — preporučeno pre nego što se ova stavka smatra potpuno zatvorenom (CLAUDE.md, "build prolazi a ekran je pokvaren" zamka).
**Verzija:** 2.11 — Ikonica "Dodaj u AI kontekst" po redu/kartici, generalizovano kroz sve module (25.8.2026, na zahtev vlasnika — vidi dizajn dokument §6c.1a, M15 spec §6.5.4.3). Zamenjuje razmatran desni klik (nema dodirni ekvivalent, nije vidljiv bez znanja da postoji) — stalno prisutna, hover-otkrivena ikonica (`symbol-number`) u prvoj koloni svakog reda/svake kartice, deljena komponenta bez posebnog rada po modulu. Klik dodaje zapis kao NOV čip u AI chat kontekstu (`AiChatBox.tsx`) — ne zamenjuje postojeći, do 8 istovremeno, omogućava poređenje više zapisa u istom pitanju. Ekrani sa listom/filterom (poglavlje 6d dizajn dokumenta) dodatno dobijaju dugme "Dodaj filtrirani prikaz u AI kontekst" (najviše jedan takav čip odjednom) — pokriva i trenutan filter i već postojeće sačuvane prikaze (§5.5 ispod, M1 §3.9 `UserPreference`). Prvi prolaz implementacije: `/rezervacije/lista` (M5) i `/crm` (M6) — generalizacija na ostale liste je isti kod, dodaje se postepeno kako svaki ekran dobije stvaran red/karticu (nema šta da se doda tamo gde još ne postoji lista).
**Verzija:** 2.10 — Desni panel po modulu + generičko prevlačenje (25.8.2026, na zahtev vlasnika: "Desni panel treba da bude uključen samo u modulu gde smo ga uključili. Kada pređemo na drugi modul desni panel se zatvara... u drugim modulima omogućite da ono što želimo da prenesemo iz centralnog modula u desni panel to uradimo prevlačenjem"). Tri odluke potvrđene preko `AskUserQuestion`: (1) panel se pri promeni modula SAKRIVA, ne prazni — sadržaj po modulu se pamti i vraća pri povratku; (2) prikupljena stavka van M5 pretrage je za sada ČISTO podsetnik bez akcije (akcije po modulu dolaze postepeno, isti obrazac kao ostatak projekta); (3) prvi modul sa prevlačenjem je M2 Katalog proizvoda. "Modul" = postojeća `NAV_GROUP` granica iz §4a (`nav.ts`) — ISTI nivo koji već grupiše `pretraga`/`kalendar`/`rezervacije-lista` pod "Prodaja" u gornjoj traci, ne pojedinačan tab niti pojedinačna `NAV_ITEM` stavka; "Katalog i nabavka" grupa (M2+M3) deli jednu policu podsetnika, isto obrazloženje.
  - **Vidljivost po modulu**: `Shell.tsx` — `rightPanelOpenModules: Set<moduleGroupId>` zamenjuje raniji jedan globalni `rightPanelOpen: boolean`. Trenutni modul se računa iz STVARNE putanje otvorenog taba (`groupForHref(pathname)`, već postojeća `nav.ts` funkcija), NE iz `activeGroupId` (to je odvojeno UI stanje — koja je ikonica gornje trake "razvijena" za pregled bočne trake, menja se SAMO ručnim klikom, može se razlikovati od modula stvarno otvorenog taba). Panel je vidljiv kad `rightPanelOpenModules.has(trenutniModul)`. X dugme u zaglavlju (`RightPanel.tsx`) uklanja SAMO trenutni modul iz seta (sakriva, ne prazni sadržaj — SelectionContext/RowSummaryContext/nova PanelCollectionContext ostaju netaknuti). Automatsko otvaranje (M5 selekcija, sažetak reda, novo — prva prevučena stavka) dodaje trenutni modul u set, isti "pojavljuje se prema potrebi" princip kao ranije (§5b dizajn dok.), sad po modulu.
  - **`PanelCollectionContext.tsx`** (nov) — generička "policа podsetnika" po modulu, odvojena od `SelectionContext.tsx` (M5 selekcija i dalje ima SVOJU, posebnu svrhu — kreiranje ponude, nepromenjeno). Oblik stavke: `{key, moduleId, label, subtitle?, href?}` — namerno minimalan (bez cene/valute/datuma kao M5 `SelectionItem`, jer generička stavka NEMA poslovnu akciju u ovom prolazu, samo prikaz+link). `addItem` dedupe-uje po `key`, poziva `onFirstAdd(moduleId)` na prvi unos za taj modul (isti obrazac kao `SelectionProvider`). `removeItem`/`removeItems` (masovno, za više označenih)/`clearModule` (sve odjednom) — tri načina brisanja koje je vlasnik tražio ("obrišemo svaku stavku posebno ili odaberemo više njih pa obrišemo ili sve obrišemo jednim klikom"). Montiran jednom, globalno u `Shell.tsx` (kao `SelectionProvider`) — podaci za SVE module postoje istovremeno u memoriji, samo se prikazuje slice trenutnog modula, čisto klijentsko stanje (ne preživljava osvežavanje stranice, isti princip kao M5 selekcija).
  - **Prevlačenje (M2 Katalog)** — `TabLink.tsx` dobija opcioni `dragPayload` prop; kad je prisutan, kartica postaje `draggable`, `onDragStart` upisuje JSON u `dataTransfer` pod sopstvenim MIME tipom (`application/x-tt-panel-item`, izbegava sudar sa eventualnim tuđim drag izvorima). `katalog/page.tsx` prosleđuje `dragPayload` na svaku karticu proizvoda (`key: "katalog:" + p.id`, `moduleId: "katalog-nabavka"`, naziv/tip/destinacija kao `label`/`subtitle`, `href` ka punom zapisu). `RightPanel.tsx` dobija zonu za spuštanje (`onDragOver` sprečava podrazumevano, `onDrop` čita `dataTransfer` i poziva `addItem`) — vidljiva SAMO kad trenutni modul NIJE "Prodaja" (M5 zadržava svoju postojeću selekciju/sažetak prikaz nepromenjeno).
  - **`RightPanel.tsx`** grana prikaz po trenutnom modulu: `moduleId === 'prodaja'` → nepromenjen M5 tok (selekcija/sažetak/"Napravi ponudu"); svaki drugi modul → nova generička lista (`PanelCollectionContext` slice za taj modul) sa čekboksom po stavci (masovna selekcija), pojedinačnim X, "Obriši izabrano" (kad ima označenih) i "Obriši sve" dugmetom, prazno stanje poziva na prevlačenje.
  **Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju — prevlačenje kartice iz Kataloga u desni panel dodaje stavku i otvara panel; prelazak na Rezervacije sakriva panel (bez brisanja); povratak na Katalog panel se ponovo pojavljuje sa istom stavkom; X na stavci/"Obriši izabrano"/"Obriši sve" rade nezavisno.
**Verzija:** 2.09 — Uvećanje plutajućeg AI chat-a na visinu ekrana (25.8.2026, na zahtev vlasnika: "omogucite da se ai agent po zelji poveca visinu na visinu ekrana na kom se prikazuje"). Novo dugme u zaglavlju plutajućeg prozora (`Shell.tsx`, pored X dugmeta, ikonica `screen-full`/`screen-normal`), prekida `chatMaximized` stanje. Menja se ISKLJUČIVO visina — širina ostaje `560px` kao inače (v1.82). Tehnika: prozor je `position: fixed`; kad je uvećan, klase se menjaju sa `max-h-[70vh]` (auto-visina, sažima se na sadržaj) na `top-4 bottom-[38px]` (i `top` i `bottom` istovremeno postavljeni na fiksnom elementu — CSS tad sam računa visinu da popuni razmak, prava/definisana visina, za razliku od `max-height` koji NIJE "definisana" visina za dalje procentualno nasleđivanje). `AiChatBox.tsx` dobija nov opcioni `maximized` prop — SAMO kad je `true` menja se `h-full`/`flex-1 min-h-0` na korenu i na listi tura (istorija razgovora), da bi razgovor stvarno iskoristio dodatnu visinu (u suprotnom bi ostao zaleđen na staroj `max-h-64` granici bez obzira na spoljnu visinu prozora); kad prop nedostaje/`false`, ponašanje je BIT ZA BIT identično ranijem (`max-h-64`), nema rizika za podrazumevani mali prozor. Horizontalna pozicija (`chatPos.left`, ako je prozor prevučen) se poštuje i u uvećanom stanju — samo se vertikalna komponenta privremeno zanemaruje, vraća se na prethodnu pri umanjenju. **Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju — `GET /` (HTTP 200) potvrđuje dugme (`title="Uvećaj na visinu ekrana"`, ikonica `codicon-screen-full`) prisutno u renderovanom HTML-u zaglavlja chata. Vizuelna promena stanja (klik → `chatMaximized`/CSS klase/`screen-normal` ikonica) je čisto klijentska React interakcija, potvrđena čitanjem koda (isti obrazac kao ranije dokumentovana ograničenja curl provere za klijentsko ponašanje), ne uživo klikom kroz automatizovan pregledač.
**Verzija:** 2.08 — Logo (ikonica + slova) u bojama "Zalazak" palete (24.8.2026, na zahtev vlasnika: "Logo neka bude u bojama zalaska sunca"). Poništava plavo-tirkiznu boju uzorkovanu direktno iz PNG slike (v2.04). `TopBar.tsx`: umesto izmišljanja nove "sunset" boje, korišćena je VEĆ POSTOJEĆA, WCAG AA proverena paleta sajta (M8, "Zalazak", `apps/web/src/app/globals.css`) — tri najzasićenija tokena tamnog moda te palete čitaju se kao horizont→sumrak preliv: `--accent` `#E8A63C` (amber) → `--danger` `#E2685A` (koralno-crvena) → `--plum` `#A99BD8` (šljiva). Fiksno, ne prati temu panela "Horizont" (isti princip kao boja slova iz v2.04 — brend mark ostaje isti bez obzira na temu).
**Tehnika ikonice:** PNG (`terminal-travel-icon.png`) nema sopstvenu boju u prikazu — zamenjen `<img>` sa `<span>` čija providnost slike (`mask-image`/`-webkit-mask-image`, `mask-size: contain`) postaje OBLIK, dok CSS `linear-gradient` iza maske daje stvaran preliv. Precizniji pristup od CSS `filter: hue-rotate` (koji ne bi mogao da pogodi DVA nezavisna ciljna tona istovremeno iz dva izvorna tona bez izobličenja). `alt` tekst premešten sa `<img>`-a na `aria-label="Terminal Travel"` na `<button>`-u (pristupačnost očuvana i u uskom stanju bočne trake, kad se slovni naslov ne prikazuje).
**Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju — `GET /` (HTTP 200) potvrđuje sve tri boje (`e8a63c`/`e2685a`/`a99bd8`) i `mask-image:url(/brand/terminal-travel-icon.png)` prisutne u renderovanom HTML-u, na ikonici i na naslovu.
**Verzija:** 2.07 — Ispravka bag-a: dupliranje taba pri prvom otvaranju stranice (24.8.2026, na zahtev vlasnika: "Kliknuo sam sada na Katalog proizvoda i pojavila su se dva ista taba Katalog proizvoda. Proverite to"). Uzrok: React 18 Strict Mode (`reactStrictMode: true`, `next.config.js`, samo u `next dev`) namerno DVAPUT poziva svaki `useEffect` pri mount-u pre ijednog re-render-a, da bi otkrio efekte koji nisu idempotentni. `useRegisterTab` (`TabsContext.tsx`) poziva `openTab(pathname, label)` iz efekta bez čišćenja — oba dvostruka poziva su čitala ISTU zatvorenu (stale) `tabs` promenljivu iz render-a PRE mount-a (React još nije stigao da primeni ni jedan `setTabs` između njih), pa je provera "da li tab već postoji" (`tabs.find(...)`) i drugi put vratila "ne postoji" — oba poziva su dodala PO JEDAN nov tab za istu putanju. Isti obrazac je latentno postojao i u `navigateInTab`/`closeTab`/`markDirty`/`closeAllTabs` (nisu do sada prijavljeni jer se ređe pozivaju u brzoj sukcesiji, ali isti uzrok).
Ispravka: nov `tabsRef` (`useRef`, sinhron, mimo React state batch-a) postaje jedini izvor istine za PROVERE unutar svih funkcija koje menjaju listu tabova — piše se SINHRONO kroz novi `commitTabs()` helper u istom trenutku kad se poziva `setTabs`, pa drugi poziv u istom sinhronom dvostrukom pozivu (Strict Mode) odmah vidi tab koji je prvi upravo dodao. `tabs` više NIJE u `useCallback` zavisnostima `openTab`-a (nepotrebno, čita se `tabsRef.current`).
**Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju — `GET /katalog` (HTTP 200), ruta i dalje odgovara bez pada servera. Sama logika dedupe-a je React state ponašanje vidljivo tek kroz pravi klik u dev-modu preglеdača (isto ograničenje kao ostatak panel UI provera ove faze) — potvrđeno čitanjem koda i poznatim React 18 Strict Mode dvostruko-pozivnim ponašanjem efekata, ne curl-om.
**Verzija:** 2.06 — Uvećan logo se seče na vrhu ekrana + uklonjena uokvirujuća kutija (24.8.2026, na zahtev vlasnika, uz drugi snimak ekrana istog dana: "ne deluje lepo sece se u vrhu, uklonite kutiju koja uokviruje"). Uzrok sečenja: `origin-left` (Tailwind: "left center") rasta uvećanog loga PODJEDNAKO gore/dole od vertikalne sredine — header sedi na samom vrhu stranice (`y=0`), pa je gornja polovina rasta bila fizički odsečena ivicom prozora pregledača (ne CSS `overflow`, koji je već bio uklonjen u v2.03 — nema šta dalje da se "otključa", prostor gore stvarno ne postoji). Ispravljeno: `origin-top-left` — rast ide isključivo NADOLE, u prostor koji postoji (iznad centralnog panela). `ring-1 ring-border` (uokvirujuća kutija iz v2.05) uklonjen — ostaje samo `shadow-lg` za odvajanje od pozadine, bez vidljivog okvira.
**Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju — `GET /` (HTTP 200) potvrđuje `origin-top-left` klasu prisutnu, `ring-1 ring-border` klasu odsutnu iz izvora stranice.
**Verzija:** 2.05 — Ispravka preklapanja uvećanog logotipa sa prvim tabom (24.8.2026, na zahtev vlasnika, uz snimak ekrana: "malo se ovde preklapa kada se uveca logo"). Uzrok: `terminal-travel-icon.png` ima providnu pozadinu — uvećan logo (`scale-[2]`) je providno prelazio PREKO taba "Početna" umesto da ga vizuelno prekrije. Ispravljeno: dugme oko loga dobija `bg-bar` (ista boja kao traka — u NEuvećanom stanju vizuelno ništa se ne menja) + `shadow-lg`/`ring-1 ring-border` SAMO kad je uvećan (`logoZoomed`) — sad izgleda kao namerna "iskačuća" značka preko taba, ne kao providno preklapanje teksta.
**Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju — `GET /` (HTTP 200) potvrđuje osnovne klase (`scale-100`) u početnom, neuvećanom stanju nepromenjene; klase za uvećano stanje (`bg-bar`/`shadow-lg`/`ring-1`) potvrđene čitanjem koda (uslovne, ne pojavljuju se u HTML-u dok logo nije uvećan preko klika, isto ograničenje kao ostatak ovih provera).
**Verzija:** 2.04 — Slova "Terminal Travel" uz logo: uvećavaju se zajedno sa ikonicom + boja loga, 20% tamnija (24.8.2026, na zahtev vlasnika: "Mislio sam i na slova Terminal Travel. Slova neka budu u boji loga samo za 20% tamnija" — dopuna v2.03 istog dana). `TopBar.tsx`: `<span>` naslova premešten UNUTAR istog `<button>` kao logo (ranije bio van, sledio je uvećanje samo vizuelno kroz `overflow-hidden` roditelja, sad je stvaran deo istog `scale-[2]` toggle-a). Boja slova uzorkovana DIREKTNO iz `terminal-travel-icon.png` (Python/Pillow, piksel-po-piksel na levoj/desnoj ivici loga, ne pogađanje) — logo je plavo→tirkizni preliv, krajnja plava `#1CABE5`, krajnja tirkizna `#52DFB4`; svaki RGB kanal pomnožen sa 0.8 ("20% tamnija") daje `#1689B7`/`#42B290`. `bg-gradient-to-r from-[#1689b7] to-[#42b290] bg-clip-text text-transparent` — isti preliv kao logo, samo tamniji, umesto izmišljene jednobojne boje koja bi zanemarila da logo NIJE jednobojan.
**Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju — `GET /` (HTTP 200) HTML potvrđuje klase `from-[#1689b7] to-[#42b290] bg-clip-text ... text-transparent` na naslovu, unutar istog `<button>` kao logo.
**Verzija:** 2.03 — Logo u gornjoj traci se uvećava na klik (24.8.2026, na zahtev vlasnika: "Omogucite da se logo na jedan klik uveca duplo, a na drugi klik da se vrati nazad"). `TopBar.tsx`: logo (`<img>`) obavijen u `<button>`, klik toggluje `logoZoomed` stanje — `scale-100`/`scale-[2]` (`transform`, `origin-left`, `transition-transform`), isti troetapni klik-toggle obrazac kao zvonce/urgentOnly filter (M5 mock lista). Roditeljski `overflow-hidden` uklonjen sa spacer `<div>`-a (naslov "Terminal Travel" i dalje sam sebi ograničava tekst preko Tailwind `truncate` klase, ne treba mu roditeljski overflow) — bez ove izmene bi uvećan logo bio isečen na granici spacer-a umesto da vizuelno "izađe" preko trake tabova; `z-20` na uvećanom logotipu obezbeđuje da se iscrtava iznad `TabBar`-a, ne ispod njega.
**Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju — `GET /` (HTTP 200) potvrđuje dugme oko logotipa sa `scale-100` klasom u početnom stanju. Sam klik/toggle (React state) nije uživo potvrđen kroz pravi klik u browseru (isto ograničenje kao ostatak panel UI provera), logika potvrđena čitanjem koda.
**Verzija:** 2.02 — Dashboard upozorenja dobijaju prave linkove + ispravljen "Invalid Date" (23.8.2026, na zahtev vlasnika, uz priloženi snimak ekrana: "ovo treba da ima linkove ka stavkama na koje obavestava"). `apps/panel/src/app/(app)/page.tsx` (§5 dashboard):
1. **Dva "Invalid Date" nalaza otkrivena pri istom prolazu** (bila su vidljiva na snimku ekrana, nisu pretpostavljena) — polja koja stranica čita nikad nisu postojala u API odgovoru: `AuditLogEntry.createdAt` (stvarno polje je `timestamp` — `/audit-log/page.tsx` je istu grešku već davno ispravio, ovaj ekran ju je jedini još imao) i `ExpiringRelease.releaseDeadline` (API vraća sirov `ContractPeriod`, nema tog polja — rok se sad računa `stayFrom` minus `releaseDaysBefore` dana, isto što bi računao svaki drugi potrošač tog endpoint-a).
2. **`Card` komponenta je primala `href` prop koji NIKAD nije bio iskorišćen** (dead prop, otkriveno pri ovom prolazu) — naslov karte se sad renderuje kao link kad `href` postoji (npr. "M11 — garancija putovanja" → `/compliance`).
3. **Linkovi po stavci, ne samo po karti** — "M3 — rokovi povrata alotmana" vodi na `/ugovori#contract-{id}` (nov HTML `id` atribut po redu u `ugovori/page.tsx` — nema posebnog ekrana za pojedinačan ugovor, anchor je jedini realan način da se pogodi tačan red bez gradnje nove stranice); "M1 — bezbednosna upozorenja" vodi na `/audit-log#audit-{id}` (isti obrazac, nov `id` u `audit-log/page.tsx`); "Agent Inbox" stavke linkuju na postojeći ekran tog modula SAMO kad on stvarno postoji — M7 → `/b2b/rabati?status=DRAFT`, M12 → `/marketing?status=PENDING_APPROVAL`, M14 → `/podrska`; M3 (uvoz cenovnika) i M5 (operativne liste dobavljaču) NAMERNO ostaju bez linka jer još nemaju sopstveni panel ekran (samo AI-draft prikaz unutar M22 mejl niti) — nije izmišljena putanja koja ne postoji.
**Provera:** `tsc --noEmit` čist za `apps/panel`; direktno upitano preko Prisma-e (isti upit kao `ContractPeriodsService.expiringReleases`/`AuditLogService.find`) — `stayFrom`/`releaseDaysBefore`/`timestamp` potvrđeno validni datumi u bazi, potvrđuje da je uzrok bio isključivo pogrešno ime polja u panel kodu, ne prazna/neispravna baza; ruta `/` i dalje odgovara bez pada servera.

**Verzija:** 2.01 — Tok rezervacije (ikonica + modal) i filteri po koloni na mock listi rezervacija (23.8.2026, na zahtev vlasnika) — puni detalj u M5 spec v1.43 (`BookingTimelineModal.tsx`, `BookingsTable.tsx`, nov `GET /sales/bookings/:id/history`). Takođe: mock demo-podaci upisani u dev bazu za ekran "API konekcije" (v2.00) na zahtev vlasnika ("Stavite neki mock podatak... da vidim vizuelno kako izgleda") — TRI `ProviderConfig` reda (`TRAVELGATE`/`WEBHOTELIER` `ACTIVE`, `SOLVEX` `INACTIVE` — realno, prati stvaran M4 spec §9 nalaz o nevažećim Solvex kredencijalima) preko STVARNOG `POST /integrations/providers`, plus ručno upisani `ProviderHealthSnapshot`/`ProviderCallLog` redovi (nema javnog endpoint-a za direktan upis snapshot-a — to inače radi isključivo periodičan posao, M18 spec §2.3) da bi se videla sva tri stanja (`ONLINE`/`UNSTABLE`/`NEMA PODATKA`). **Ovo su demo podaci u lokalnoj razvojnoj bazi, ne trajna funkcija koda** — obrisati kad prestanu da služe pregledu (nisu deo seed skripte, neće se ponovo pojaviti posle `prisma migrate reset`).
**Verzija:** 2.00 — Nov ekran "API konekcije" u Administracija grupi (23.8.2026, na zahtev vlasnika: "Kada se klikne na settings dugme u levom panelu treba da se pojave sve live api konekcije sa nazivom statusom i health check statusom"). Prvi panel prikaz koji spaja DVA već postojeća, do sad odvojena endpoint-a — nijedan backend modul nije menjan, samo prezentacioni sloj:
- M4 `GET /integrations/providers` (`ProviderConfigsController`, M4 spec §6/§7) — naziv (`displayName`), kategorija, konfiguracioni status (`ACTIVE`/`INACTIVE`), `circuitState` (M4 spec §4.1).
- M18 `GET /ops/provider-health` (`ProviderHealthController`, M18 spec §2.3/§9) — health-check status (`ONLINE`/`UNSTABLE`/`OFFLINE`), prosečna latencija, uptime%, broj grešaka u poslednjem satu, sve na 15-minutnom periodičnom poslu.

Nov ekran `apps/panel/src/app/(app)/integracije/page.tsx` (server komponenta, isti obrazac kao `apps/panel/src/app/(app)/mcp/page.tsx` — `getMe()`/`hasPermission()`/`apiFetch()`, bez klijentskog BFF sloja) spaja dva odgovora po `providerCode` u jednu listu. Novi `NavItem` (`nav.ts`, id `integracije`, ikonica `pulse`, `phase: 4`) dodat u `NAV_GROUPS` grupu `administracija` (pored `korisnici`/`audit-log`/`mcp`) — klik na "settings" (`ActivityBar.tsx` "Administracija" ikonica) sad prikazuje ovaj ekran u spisku bočne trake, tačno kako je traženo. Vidljivost isključivo preko `M18/provider-health/VIEW` dozvole (Vlasnik/Direktor, M18 spec) — ako korisnik ima i `M4/provider-config/VIEW`, dobija pun prikaz (naziv/kategorija/konfiguracioni status); ako nema, ekran se ne ruši nego i dalje prikazuje health-check podatke koje SME da vidi, sa kodom provajdera umesto naziva (odbrana u dubinu, isti princip kao svaki drugi prikaz preko više dozvola u ovom dokumentu). Provajder bez health-check zapisa u prozoru od 15 minuta (nema poziva) dobija "NEMA PODATKA" oznaku umesto da tiho izostane iz liste.

**Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju — `GET /integrations/providers` i `GET /ops/provider-health` oba vraćaju 200 sa praznim nizom (dev baza nema seed-ovane `ProviderConfig` zapise — prazan ekran je prazna baza, ne pokvaren kod, zamka 7.x ovog dokumenta), `/integracije` ruta vraća 200 i renderuje ispravno "Nema konfigurisanih provajdera." bez greške. Spajanje/merge logika i oba stanja dozvola (sa/bez M4 pristupa) potvrđeni čitanjem koda — pravi podaci sa realnim statusima nisu uživo potvrđeni jer dev baza trenutno nema nijedan konfigurisan provajder da se testira.
**Verzija:** 1.99 — Ispravka "trake su najsvetlije umesto najtamnije" (pravi uzrok: zastareo Tailwind build) + fiksna širina tabova (23.8.2026, uživo — "ja vidim da je boja traka sada najsvetlija, ispravite to" + "Sirina tabova treba da bude ista za svaki tab... 20 karaktera... tri tacke... misem preko taba da se pojavi ceo tekst").
1. **Uzrok bar-boje NIJE bio pogrešna vrednost** (dizajn dok. §2.0e vrednosti su bile ispravno numerički poređane, `--bar` #F0F1F2 tamnije od `--panel` #FAFAFD) — `.bg-bar` klasa NIJE POSTOJALA u iskompajliranom CSS-u panel dev servera (`.next/static/css/app/layout.css`), potvrđeno grep-om PRE restart-a. `tailwind.config.ts` je izmenjen (nova `bar` boja) dok je dev server (pokrenut ranije u sesiji) i dalje radio sa starim, keširanim Tailwind build-om — Next.js dev server ne detektuje pouzdano nove `theme.colors` unose u `tailwind.config.ts` bez restarta. Element bez ijedne primenjene `bg-*` klase pada nazad na pozadinu roditelja (`--bg`, koji je namerno JEDNAK `--panel`, najsvetlijoj nijansi) — otud "trake su najsvetlije", tačno suprotno od namere, iako je sam kod bio ispravan. Rešeno: obrisan `.next` keš, dev server pokrenut iznova; potvrđeno grep-om POSLE restarta da `.bg-bar { background-color: var(--bar); }` sad stvarno postoji u iskompajliranom CSS-u. **Nova zamka za `docs/analize/33-ZAMKE-I-OBAVEZNE-PROVERE.md`** (upisati u istom prolazu) — izmena `tailwind.config.ts` (nova boja/token) zahteva restart dev servera, ne samo hot-reload; provera "klasa postoji u HTML-u" NIJE dovoljna (to samo znači da je React string ispravan), mora se proveriti da CSS PRAVILO za tu klasu stvarno postoji u `.next/static/css/.../*.css`.
2. **Fiksna širina tabova** — `TabBar.tsx`: `max-w-[200px]` (svaki tab RAZLIČITE širine do tog maksimuma) zamenjeno sa `w-[20ch]` (FIKSNA širina, ~20 karaktera — CSS `ch` jedinica, širina cifre "0" u trenutnom fontu, radi i za proporcionalan font ne samo monospace). `truncate` (već postojeće, `overflow-hidden`+`text-overflow:ellipsis`+`whitespace-nowrap`) sad ima stvaran efekat na duže nazive. Pun naziv dostupan preko native `title` atributa (hover tooltip) — nema potrebe za sopstvenim JS tooltip mehanizmom, browser to već radi.
**Provera:** `tsc --noEmit` čist za `apps/panel`; `.bg-bar` i `.w-\[20ch\]` potvrđeni grep-om u iskompajliranom CSS-u POSLE restarta dev servera (ne samo u HTML className stringu, stvarna CSS pravila).
**Verzija:** 1.98 — Vidljiva linija između podeljenih panela + "+" prilaganje konteksta modula (23.8.2026, na zahtev vlasnika: "stavite liniju izmedju dva terminala" + "dodajte u chat terminala i + [dugme] za dodavanje konteksta klikom na odredjeni modul"). (1) Razdelnik između dva panela (v1.97) pojačan sa `bg-ink-faint/40` na 1px (praktično nevidljivo) na `bg-ink-faint` puna boja — jasno vidljiva granica; odmah zatim stanjen za 30% na zahtev vlasnika (2px→1.4px, `w-[1.4px]`). (2) Nov "+" ispred `$` u redu za unos svakog panela (isti obrazac kao AiChatBox.tsx §6c.1/6c.2 dizajn dok. — `[Kontekst: X] pitanje` tekstualni oblik, portal ka `document.body` iz istog razloga, panel ima `overflow-hidden`), NAMERNO uže od AiChatBox verzije: samo ručan izbor JEDNOG modula iz `NAV_ITEMS` (klik postavlja kontekst, prikazuje se kao uklonjiva pilula iznad reda za unos i kao "kontekst: X" ispod pitanja u istoriji) — bez automatskog prilaganja trenutnog taba/sadržaja ekrana kao kod AiChatBox-a, jer `BiTerminalAgent` radi isključivo preko strukturiranih alata (§6.9.3/§6.9.6), ne preko sirovog teksta ekrana. Meni se otvara NAGORE (`transform: translateY(-100%)`), razlog: dugme je pri dnu panela (red za unos), za razliku od AiChatBox-a gde je pri vrhu. **Provera:** `tsc --noEmit` čist za `apps/panel`; stvaran klik/vizuelni izgled u browseru nije uživo potvrđen — isto ograničenje kao ostatak ove faze.
**Verzija:** 1.97 — Podeljen terminal (23.8.2026, na zahtev vlasnika: "omogucite podelu terminala u dva dela i da oba budu isto operativna da mogu dve razlicite stavri da radim u isto vreme"). Isti obrazac kao VS Code "Split Terminal". `TerminalPanel.tsx` refaktorisan: sav sadržaj jednog terminala (razgovor, unos, `send()`, `hideRow()`, kopiranje) izdvojen u nov `TerminalPane` (interni, ne izvezen iz fajla) — svaka instanca ima POTPUNO nezavisno stanje (sopstveni `turns`/`input`), nijedna promenljiva se ne deli između dve otvorene instance. Novo dugme u zaglavlju (`split-horizontal` Codicon, pored dugmeta za zatvaranje) prebacuje između jednog i dva panela jedan pored drugog (tanka `border` linija razdvaja), stanje se pamti (`localStorage`, `tt-panel-terminal-split`, isti privremeni obrazac kao visina/layout vidljivost). Visina panela ostaje ZAJEDNIČKA za oba (deljenje je horizontalno, jedna ručka za prevlačenje). Kad je podeljen, svaki panel dobija sopstveno dugme "Kopiraj ceo razgovor" (mala traka u svom zaglavlju); kad NIJE podeljen, jedno zajedničko dugme ostaje u glavnom zaglavlju panela (isto mesto kao v1.93), sad preko `ref`/`useImperativeHandle` (`TerminalPaneHandle`) umesto direktnog čitanja stanja — sprečava suptilnu grešku zastarelog teksta koji bi se kopirao da je ostalo direktno čitanje `turns` kroz `ref` u trenutku renderovanja roditelja. **Provera:** `tsc --noEmit` čist za `apps/panel`; `split-horizontal` Codicon potvrđen u instaliranom setu (`@vscode/codicons`); stvaran klik/vizuelni raspored dva panela jedan pored drugog u browseru nije uživo potvrđen (terminal panel se montira samo kad je otvoren preko "Customize Layout", isto ograničenje kao ostatak ove faze) — potvrđeno čitanjem refaktorisanog koda da su `turns`/`input`/`send` u potpunosti izolovani po instanci.
**Verzija:** 1.96 — Konačna ispravka v1.94/v1.95: širina se STVARNO MERI, ne pogađa (23.8.2026, uživo, drugi snimak ekrana istog dana — "i dalje nije dobra pozicija prvog taba"). Binarni pokušaj v1.95 (255px/31px po tome da li je traka "puna" ili ne) je i dalje bio netačan — leva kolona ima previše promenljivih stanja (kolabovano/prošireno/ručno prevučeno 180-420px/sakriveno preko "Customize Layout") da bi ijedna unapred pretpostavljena vrednost uvek bila tačna. `Shell.tsx` sad meri STVARNU renderovanu širinu preko `ResizeObserver` nad `<div ref={leftColumnRef}>` koji obavija `ActivityBar`+`ResizablePane(Sidebar)`, prosleđuje tačan broj piksela (`leftColumnWidth`) kroz `TopBar` prop (zamenjuje raniji `sidebarWide` bulean). `TopBar.tsx` računa razmak kao `leftColumnWidth - 12` (12px = header-ov `px-2`+`gap-1`, nepromenjena v1.65 formula) i prikazuje naziv "Terminal Travel" samo ako ima dovoljno mesta (`spacerWidth >= 100`), inače samo manju ikonicu. Radi tačno u SVAKOM stanju, uključujući uživo prevlačenje granice bočne trake — više nema mesta za "pogodi pa proveri" ciklus. **Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju (curl + dev-login) — početni server-render i dalje daje očekivanih `255px` (podrazumevana pretpostavka pre prvog merenja, ista vrednost kao v1.94/v1.95, hidratacija bezbedna isti obrazac kao `sidebarCollapsed`); uživo merenje/reagovanje na promenu širine je klijentska `ResizeObserver` logika, nije proverljivo preko curl-a — potvrđeno čitanjem koda, stvaran vizuelni ishod u browseru (da li tab STVARNO stoji na ivici u svakom stanju) čeka sledeću potvrdu vlasnika.
**Verzija:** 1.95 — Ispravka v1.94: tabovi prate STVARNU širinu leve kolone, kolabovana traka nestaje potpuno (23.8.2026, uživo, uz snimak ekrana — "i dalje prvi tab stoji gde ne treba" + "kada uvlacimu levi panel, treba da ostane samo leva traka ne i ova druga kolona").
1. **Tabovi prate širinu leve kolone.** Uzrok prve prijave: `w-[255px]` razmak u `TopBar.tsx` (v1.94) je pretpostavljao PROŠIRENU bočnu traku — kad je traka kolabovana/sakrivena, stvarna leva kolona je uska (43px, samo `ActivityBar`), pa je tab traka ostajala pomerena udesno od stvarne ivice centralnog panela. `Shell.tsx` sad računa `sidebarWide = layoutVisibility.sidebar && !sidebarCollapsed` i prosleđuje ga `TopBar.tsx`, koji bira između 255px (puna traka) i 31px (43px ActivityBar − 12px header padding, ista formula kao v1.65/v1.94) — logo ikonica se smanjuje i tekst se sakriva u uskom stanju (nema mesta za naziv). **Poznato pojednostavljenje** — ne prati uživo ručno prevučenu širinu unutar 180-420px opsega, samo razlikuje "puna/podrazumevana" naspram "kolabovana/sakrivena" (isti kompromis kao v1.65), dovoljno za prijavljen slučaj.
2. **Kolabovana leva traka sad NESTAJE POTPUNO**, ne skuplja se na tanku ikonsku traku (poništava 19.8.2026 obrazac). `ResizablePane` (Shell.tsx) kolabuje na `collapsedWidth={0}` umesto `40`; `Sidebar.tsx`-ov kolabovan prikaz (traka sa ikonicama trenutne sekcije + strelica za širenje) je uklonjen u potpunosti (bio bi nedostupan mrtav kod na 0px). Pošto je strelica za ponovno širenje time izgubljena, `ActivityBar.tsx` dobija zamenu — VS Code obrazac: klik na VEĆ AKTIVNU grupu (koja bi inače bila no-op) sad prebacuje skupi/proširi (`onToggleCollapse`, i za `<button>` grupe i za `<Link>` grupe sa jednom stavkom, `e.preventDefault()` pre toggle-a kod potonjih).

**Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju (curl + dev-login) — podrazumevano (proširena) stanje potvrđeno kroz server-render (`width:255px`, "Terminal Travel" tekst prisutan); kolabovano stanje je čisto klijentsko (`useState`/`localStorage`, isto ograničenje kao ostatak ove faze) — logika potvrđena čitanjem koda (`sidebarWide` bulean, `collapsedWidth={0}`, `Sidebar` vraća `null` kad je `collapsed`), stvaran klik/vizuelni ishod u browseru nije uživo potvrđen.
**Verzija:** 1.94 — Nevidljiva scroll traka, logo premešten u gornju traku, novi prazan tab, više tabova istog puta (23.8.2026, na zahtev vlasnika, četiri odvojene stavke istog upita).
1. **Nevidljiva scroll traka svuda** ("uklonite bocnu traku za scrolovanje... deluje ruzno") — `globals.css`, globalno `scrollbar-width: none` (Firefox) + `*::-webkit-scrollbar { display: none }` (Chrome/Edge/Safari), primenjeno na `*` umesto pojedinačno po 13 poznatih `overflow-y-auto` mesta, da nijedno buduće mesto ne bude propušteno. Scrolovanje ostaje potpuno funkcionalno, samo se traka ne iscrtava.
2. **Logo premešten sa dna Sidebar-a u gornju traku** ("Uklonite logo iz levog panela onemogucava da se panel skroz zatvori... stavite na gornju traku iznad levog panela") — poništava v1.71/v1.72 obrazac. Uzrok bloka: `<img>` bez eksplicitne širine (samo `w-auto max-w-[70%]`) unutar `flex-shrink-0` kolone ne skuplja flex kontejner ispod svoje prirodne veličine, pa je sprečavao potpuno kolabovanje bočne trake na uzak rail. Oba `<img>` uklonjena iz `Sidebar.tsx` (kolabovano i prošireno stanje). `TopBar.tsx`: prethodno prazan `w-[255px]` razmak (v1.72, poravnat sa ActivityBar+Sidebar kolonom ispod) sad sadrži ikonicu (`terminal-travel-icon.png`, 20×20px) + tekst "Terminal Travel" — ISTA širina razmaka znači da se automatski zadovoljava i "logo iznad levog panela" (već poravnat) i "prvi tab počinje od leve ivice centralnog panela" (pozicija tabova nepromenjena).
3. **"+" dugme sad stvarno otvara nov, prazan tab** ("kada se klikne na + ... nista se ne dogadja") — ranije je bio čist `<Link href="/">` (uvek Početna, bez ikakve logike); ako je Početna već otvorena, jednostavno bi je fokusirao, ne bi napravio ništa novo. Nov, statičan `/blank` ekran (`app/(app)/blank/page.tsx`, bez sopstvenog stanja) + `TabsContext.openTab(..., { forceNew: true })` (dopuna, vidi tačku 4) — svaki klik pravi zaseban zapis u traci tabova, bez obzira što svi vode na istu putanju.
4. **Više tabova iste putanje** ("omogucite otvaranje vise tabova za isti modul na primer pretrage") — `TabsContext.tsx` prošao kroz identitet zasnovan na `id` polju (novo, uz postojeće `path`/`label`) umesto isključivo na `path`-u; dodat `activeTabId` (koji ZAPIS je istaknut, ne koja putanja) i `setActiveTab(id)` (menja istaknut zapis BEZ navigacije — potrebno kad dva zapisa dele istu putanju, klik ne menja URL). `navigateInTab` (drill-down) sad prati `activeTabId` umesto pronalaženja po putanji — robusnije, više ne zavisi od jedinstvenosti putanje. `closeTab` menjan sa `path` na `id` argument (jedini pozivalac, `TabBar.tsx`, ažuriran). Podrazumevano ponašanje (`openTab` bez `forceNew`) OSTAJE nepromenjeno — pronađi po putanji, nikad ne dupliraj — samo "+" dugme trenutno koristi `forceNew: true`; pojedinačni ekrani (npr. sopstveno "Nova pretraga" dugme unutar `/rezervacije/pretraga`) mogu isto da zatraže kad im zatreba, ostaje sledeći korak po ekranu, ne urađeno ovim prolazom za sve module odjednom.

**Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravu VLASNIK sesiju (curl + dev-login kolačić, pravi browser nedostupan u ovom okruženju) — kompajlirani CSS (`layout.css`) sadrži `scrollbar-width: none`; `/` HTML potvrđuje da `terminal-travel-logo.png` više NIGDE ne postoji (samo `terminal-travel-icon.png`, u `TopBar.tsx` bloku), "Terminal Travel" tekst prisutan u `w-[255px]` bloku; `/blank` vraća 200 i renderuje "Prazan tab" placeholder. Vizuelni izgled (stvaran raspored/klik u browseru) nije potvrđen — isto ograničenje kao ostatak ove faze.
**Verzija:** 1.93 — Terminal panel: dugme "Kopiraj ceo razgovor" (23.8.2026, na zahtev vlasnika: "omoguci kopiranja svih poruka ne samo pojedinacnih iz jednog chata"). `CopyButton.tsx` dobio opcioni `alwaysVisible`/`title` prop (dopuna, ne lom postojećeg API-ja — podrazumevano ponašanje nepromenjeno) da isti obrazac radi i van hover-`group` konteksta pojedinačne poruke. `TerminalPanel.tsx` zaglavlje dobija dugme (vidljivo samo kad postoji bar jedna tura) koje kopira ceo razgovor kao običan tekst (`$ pitanje` / odgovor, tura po turu, odvojeno praznim redom) — dopuna uz postojeći `CopyButton` po poruci, ne zamena. **Provera:** `tsc --noEmit` čist za `apps/panel`; uživo potvrđeno da API iza (M15 spec §6.9.6 v1.33 — `bookings` pogled dobio `groupBy=status`) sad odgovara na pitanja koja su ranije bila bez pokrića.
**Verzija:** 1.92 — Terminal panel: kopiranje poruka, oznaka pozvanog alata, kartica odobrenja za web fetch (23.8.2026, M15 spec §6.9.6/§6.9.7 dopuna v1.31 — "vizuelno pitanja i odgovori... kao u VS Code. Omogucite kopiranje poruka" + "izadje na internet... uz pregled sadrzaja od agenta zaduzenog za kontrolu opasnosti"). `TerminalPanel.tsx`: (1) `CopyButton` (izdvojen iz `AiChatBox.tsx` u deljeni `apps/panel/src/components/CopyButton.tsx`, DRY — oba mesta sad koriste isti kod) uz svako pitanje i odgovor, hover-reveal, isti obrazac kao AI chat; (2) svaka tura odvojena `border-t` linijom (jasnija segmentacija po turi, Claude-Code-CLI referenca koju je vlasnik poslao); (3) diskretna oznaka `⟶ {alat}` iznad odgovora kad je pozvan alat (transparentnost — "agent nikad ne izmišlja podatke", `toolsCalled[]` iz odgovora); (4) nova `WebFetchApprovalCard` — kad odgovor nosi `pendingWebFetch`, prikazuje predloženi URL+razlog u `warn` okviru sa dugmadima "Odobri"/"Odbij" (nove BFF rute `/api/bi-terminal/web-fetch/approve`/`.../deny`), ništa se ne preuzima dok Vlasnik ne klikne; posle odluke kartica prikazuje status (odobreno → sledi stvaran odgovor sa izvorom, odbijeno → kratka potvrda). **Provera:** `tsc --noEmit` čist za `apps/panel` i `apps/api`; uživo kroz pravu VLASNIK sesiju preko curl-a (pravi browser nedostupan u ovom okruženju, isto ograničenje kao ostatak M17 faze) — `query_view` (§6.9.6) potvrđen na tri stvarna pitanja ("koliko rezervacija ukupno", "ko od zaposlenih...", "najpovoljnija ponuda u Budvi"), `propose_web_fetch` potvrđeno da vraća `pendingWebFetch` bez ijednog spoljnog HTTP poziva, `approveWebFetch` potvrđeno da ispravno odbija dok `M15_WEB_RESEARCH` nije `ACTIVATED` (fail-closed poruka), `safeFetchText` SSRF zaštita potvrđena direktno (localhost/127.0.0.1/169.254.169.254 blokirani, `example.com` uspešno preuzet), `WebContentSafetyService.review()` potvrđeno razlikuje benigni tekst (`SAFE`) od prompt-injection pokušaja (`BLOCKED`, tačno prepoznat razlog). Vizuelni izgled (stvaran raspored/boje u browseru) nije potvrđen — isto ograničenje kao ostatak ove faze.
**Verzija:** 1.91 — Terminal panel: kartica generisanog izveštaja (23.8.2026, M15 spec §6.9.3 dopuna v1.30 — "omogucite kreiranje excel/pdf/html izvestaja... slanje putem internog chata"). Kad odgovor nosi `report` (fajl je spreman), `TerminalPanel.tsx` prikazuje malu karticu ispod odgovora: naziv fajla, dugme "Preuzmi" (`/api/bi-terminal/reports/:id/download`, novi BFF, isti obrazac kao postojeći M19 prilog-download) i dugme "Pošalji u chat" (padajući spisak razgovora preko postojeće `/api/chat/conversations` rute, klik šalje `POST /api/bi-terminal/reports/:id/send-chat`, novi BFF). Slanje je EKSPLICITAN klik, nikad automatsko. **Provera:** `tsc --noEmit` čist; uživo kroz pravu VLASNIK sesiju — sva tri formata preuzeta uspešno, izveštaj poslat u pravi razgovor, prilog potvrđeno preuzet nazad kroz postojeći M19 mehanizam.

**Verzija:** 1.90 — Terminal panel: promenljiva visina + prikaz linkova (23.8.2026, na zahtev vlasnika: "omogucute povecavanje visine terminala" + "dodajte linkovanje prema onome sto se moze otvoriti putem linka"). `TerminalPanel.tsx`: gornja ivica prevlačiva (isti obrazac kao `ResizablePane.tsx`, samo vertikalno — `120`–`640px`, dvoklik vraća podrazumevanih `220px`, pamti se u `localStorage`); odgovori sad prikazuju klikabilne linkove (M15 spec §6.9.3 v1.29 `links[]`) kao pilule ispod teksta, otvaraju zapis u novom tabu — potvrđeno kroz `AskUserQuestion` da se prikazuju ODMAH, ne tek posle potvrde. **Provera:** `tsc --noEmit` čist; uživo — link ka `/b2b/<id>` potvrđen tačan za stvaran subagent u bazi.

**Verzija:** 1.89 — "Customize Layout" dugme + terminal panel implementirani (23.8.2026). `CustomizeLayoutButton.tsx` (nov, padajući meni u `TopBar.tsx`) i `TerminalPanel.tsx` (nov, VS Code pozicija — u centralnoj koloni, ispod `<main>`, iznad statusne trake, span samo te kolone). `Shell.tsx`: `layoutVisibility` state (sidebar/statusBar/terminal, isti bezbedan server/klijent hidratacioni obrazac kao `sidebarCollapsed`), čuva se u `localStorage` (`tt-panel-layout-visibility`) — pravi `UserPreference` backend (M1 §3.9) i dalje ne postoji u kodu, isti privremeni obrazac. Terminal stavka/panel se montiraju SAMO uz `showBiTerminal` prop (izračunat u `layout.tsx` preko `hasPermission(me, 'M15','bi-terminal','VIEW')`, stvarna efektivna dozvola sa servera, ne pretpostavka po imenu uloge) — bez dozvole, ni stavka u meniju ni panel ne postoje u DOM-u. Nov BFF `apps/panel/src/app/api/bi-terminal/query/route.ts`. **Provera:** `tsc --noEmit` čist; uživo kroz pravu VLASNIK sesiju — Customize Layout ikonica prisutna u iskompajliranom HTML-u, terminal panel odsutan pri prvom učitavanju (podrazumevano zatvoren), stvaran upit kroz `/api/bi-terminal/query` vraća pravi AI odgovor; smoke-test `/`, `/crm`, `/katalog` i dalje vraćaju 200 (nema regresije od izmena u `Shell.tsx`). Vizuelni izgled i dalje nije potvrđen u pravom browser-u — čeka potvrdu vlasnika.

**Verzija:** 1.88 — Spec pre koda: "Customize Layout" dugme + terminal panel (23.8.2026, na zahtev vlasnika: "dugme iz VS Code Customize Layout" + "otvaranje terminala isto kao u VS Code"). Vlasnik je prvo tražio pravi shell/terminal sa CLI AI alatima (Claude Code, Gemini CLI) — objašnjen bezbednosni rizik (pravi shell zaobilazi ceo M15 model upravljanja agentima), vlasnik kroz `AskUserQuestion` potvrdio alternativu: terminal IZGLED, kontrolisan read-only `BiTerminalAgent` ISPOD (nov M15 `agent_role`, poglavlje 6.9 M15 spec). UI mehanika (pozicija, izgled, "Customize Layout" meni, vidljivost, "obriši red" ponašanje) specirana u `docs/analize/29-DIZAJN-SISTEM-UI.md` poglavlje 5f. RBAC isključivo VLASNIK (M15 spec §6.9.2, `M15/bi-terminal/VIEW`) — M17 samo poštuje tu dozvolu pri iscrtavanju (poglavlje 6, nepromenjeno pravilo). **Nije još implementirano** — ovaj upis je spec pre koda po CLAUDE.md, kod sledi u narednom prolazu.

**Verzija:** 1.87 — v1.86 popravka nedovoljna — trka pri osvežavanju tokena (23.8.2026, isti dan, vlasnik uživo potvrdio: greška se ponavlja u pravom browser-u i posle v1.86 i posle tvrdog osvežavanja, iako izolovan test uvek uspeva). Pravi uzrok drugog kruga: panel ima nekoliko komponenti koje se osvežavaju u pozadini na 30 sekundi i dele istu sesiju/kolačić — `TopBar.tsx` (Agent Inbox broj), `StatusBar.tsx` (AI status), `NotificationBell.tsx`. Kad access token istekne, VIŠE njih istovremeno dobije 401 i svako pokuša osvežavanje nezavisno — refresh token je jednokratan (rotira se pri svakoj upotrebi, §3.7), pa prvi zahtev koji stigne uspe i rotira token, a svi OSTALI koji su krenuli sa istim (u međuvremenu već iskorišćenim) refresh tokenom dobiju grešku — "izgube trku" iako je sesija stvarno validna. Izolovan `curl` test (jedan zahtev odjednom) nikad nije nailazio na ovo, otud "kod meni radi" razmimoilaženje sa stvarnim iskustvom u browser-u. Ispravljeno "single-flight" obrascem u `api-client.ts`: `inFlightRefreshes` mapa (u memoriji procesa) pamti Promise po tačnoj vrednosti refresh tokena koji se trenutno osvežava — svaki naredni poziv sa ISTIM tokenom, dok je prvi još u toku, čeka isti Promise umesto da pokrene sopstveni gubitnički poziv. **Provera:** `tsc --noEmit` čist; uživo — 5 NAMERNO paralelnih zahteva sa istim pokvarenim access tokenom (`Promise.allSettled`, jednokratna dijagnostička ruta) svih 5 uspešno, 0 neuspešno (bez ove popravke bi uspeo samo prvi, preostala 4 bi propala zbog rotacije) — obrisana posle provere. Vizuelni izgled i dalje nije potvrđen u pravom browser-u — čeka treću potvrdu vlasnika, ovog puta posle stvarne trke koja je izazivala problem.

**Verzija:** 1.86 — AI chat je tiho javljao "AI pretraga još nije uključena" i kad je stvarno bio aktiviran — pravi uzrok je bio istekao access token, ne aktivacija (23.8.2026, prijavio vlasnik uživo, posle probe da ponovo pošalje pitanje). M1 spec §3.7 access token TTL je 15 minuta; `apps/panel/src/lib/api-client.ts` je čuvao `refreshToken` u sesiji (kolačić traje 7 dana, isti rok) ali ga NIKAD nije koristio — `POST /iam/auth/refresh` (M1 spec poglavlje 6, već implementiran i testiran otkad M1 postoji) nikad nije bio ožičen na panel stranu. Posledica: svaki zahtev posle 15 minuta je tiho dobijao 401, a `AiChatBox.tsx` nije proveravao `res.status` — svaki auth-neuspeh se renderovao identično kao "omnisearch nije aktivan" (oba slučaja `active` falsy), dva različita uzroka, ista zbunjujuća poruka. Ispravljeno u dva sloja:
1. `apiFetch`/`apiFetchMultipart` (`api-client.ts`) sad na 401 pokušavaju TAČNO JEDNOM `POST /iam/auth/refresh` pre nego što odustanu — sprečava beskonačnu petlju. Osvežena sesija se pokušava upisati nazad u kolačić (`setSession`, best-effort `try/catch` — u Server Component read-only kontekstu upis nije dozvoljen, tad se osvežen token koristi samo za taj jedan zahtev, sledeći će ponovo osvežiti).
2. `AiChatBox.tsx` `send()` sad proverava `res.status === 401` posebno — ako i osvežavanje ne uspe (refresh token takođe istekao posle 7 dana), korisnik dobija tačnu poruku ("Sesija je istekla — osveži stranicu i prijavi se ponovo"), ne pogrešnu "AI još nije aktiviran".

**Provera:** `tsc --noEmit` čist; uživo kroz pravi login — normalan tok i dalje radi (`active: true`, stvaran AI odgovor); END-TO-END test osvežavanja: jednokratna dijagnostička ruta je namerno pokvarila access token u sesiji (validan refresh token netaknut), pozvala `apiFetch`, i potvrdila da se zahtev SAM oporavio (401 → automatski refresh → uspešan odgovor) bez ikakve akcije korisnika — ne samo da kod prolazi kompajliranje, stvaran tok proveren uživo. Sve dijagnostičke rute obrisane posle provere (isti "nikad se ne commit-uje" obrazac kao `dev-login`).

**Verzija:** 1.85 — Ispravljena "Hydration failed" greška u AI chatu (23.8.2026, prijavio vlasnik uživo pun stek trag — "Expected server HTML to contain a matching `<button>` in `<div>`"). Uzrok: `AiChatBox.tsx` je računao `speechSupported` (mikrofon dugme, uvedeno v1.7x glasovni unos) direktno u telu komponente preko `typeof window !== 'undefined' && !!(window.SpeechRecognition || window.webkitSpeechRecognition)` — na serveru uvek `false` (nema `window`, dugme se ne renderuje), ali na klijentu `window` već postoji U TRENUTKU HIDRATACIJE, pa prvi klijentski render odmah uključuje dugme koje server nije poslao — pravo mimoilaženje, ne lažna uzbuna. Ispravljeno istim bezbednim obrascem koji već postoji u `Shell.tsx` (`sidebarCollapsed`): `speechSupported` počinje kao `useState(false)` (identično na serveru i prvom klijentskom renderu), stvarna provera podrške ide u `useEffect` POSLE hidratacije — mikrofon dugme se pojavi tek na sledećem, običnom re-renderu, ne izaziva mimoilaženje. **Provera:** `tsc --noEmit` čist; uživo kroz pravi login — `/` vraća HTTP 200; SSR HTML potvrđeno NE sadrži `codicon-mic` (isto stanje kao inicijalni klijentski render pre `useEffect`-a). Vizuelni izgled i dalje nije potvrđen u pravom browser-u — čeka potvrdu vlasnika da je "Unhandled Runtime Error" ekran nestao.

**Verzija:** 1.84 — "+" meni i dalje sečen posle v1.83 pokušaja ("otvara nadole") — pravi uzrok i pravo rešenje (23.8.2026, vlasnik poslao snimak ekrana koji je pokazao meni odsečen na donjoj ivici plutajućeg prozora). Stvaran uzrok NIJE bio smer otvaranja (gore/dole) — `overflow-hidden` na plutajućem prozoru (`Shell.tsx`) seče SVAKI apsolutno pozicioniran element koji izađe van NJEGOVIH stvarnih, sadržajem određenih granica, bez obzira na smer, jer panel nema fiksnu visinu. v1.83 izmena je promenila smer, ali ne i taj osnovni problem — otud je meni i dalje bio odsečen, sad na donjoj umesto gornje ivice. Pravo rešenje: meni (`AiChatBox.tsx`, "+" dugme) sad izlazi kroz `createPortal` (react-dom) direktno u `document.body`, pozicioniran preko `position: fixed` i piksela izračunatih iz `getBoundingClientRect()` dugmeta u trenutku otvaranja — potpuno van domašaja bilo kog roditeljskog `overflow-hidden`, jer više nije njegovo DOM dete. Isti obrazac je opšte rešenje za svaki budući dropdown unutar ovog plutajućeg prozora, ne zakrpa specifična za ovaj meni.

**Provera (v1.84):** `tsc --noEmit` čist za `apps/panel`. Uživo — pravim login+MFA i pravom bazom (privremeni VLASNIK nalog, obrisan posle provere): `/` HTML posle prijave potvrđuje da se stranica renderuje bez "Hydration failed"/"Application error" (portal se ne poziva na serveru jer je meni podrazumevano zatvoren pri SSR-u — `document` tada ne postoji, `plusOpen && plusMenuPos && createPortal(...)` se kratko-spaja pre poziva), dugme "+" (`title="Priloži kontekst"`) prisutno. Samo OTVARANJE menija (klik, pa provera da li se stvarno vidi nesečen na ekranu) NIJE kliknuto u pravom pregledaču — nema headless browser alata u repozitorijumu (isto ograničenje kao v1.82/v1.83); potvrđeno čitanjem koda (računica pozicije, `position: fixed`, portal cilj `document.body`) i logikom zašto CSS `overflow-hidden` više fizički ne može da dohvati element van sopstvenog DOM podstabla.

**Verzija:** 1.83 — Chat postaje prevlačiv, zamenjuje "klik van chata zatvara" (23.8.2026, na zahtev vlasnika: "omogućite pomeranje chata gde god želimo da bude u centralnom panelu... ovim pomeranjem ćemo rešiti problem" — eksplicitna zamena za v1.82 ponašanje, ne dopuna) + ispravka "+" menija koji se sekao (podvlačio pod plutajući prozor).

- **Prevlačenje** (`Shell.tsx`) — zaglavlje plutajućeg chat prozora (`cursor-move`) sad pokreće `mousedown`→`mousemove`→`mouseup` tok preko `window` event listener-a (ne biblioteka za prevlačenje — obična koordinatna razlika u odnosu na početak prevlačenja, isti nivo složenosti kao `ResizablePane.tsx`). `chatPos` je `null` dok se ne prevuče bar jednom (podrazumevana pozicija ostaje CSS `bottom-[38px] right-4`, nepromenjeno); posle prvog prevlačenja prelazi na `style={{ top, left }}` u pikselima, sa granicom da zaglavlje nikad potpuno ne izađe van ekrana (uvek bar 40px dohvatljivo za vraćanje nazad).
- **"Klik van chata ga zatvara" (v1.82) UKLONJENO** — vlasnikova eksplicitna odluka istog dana: sad kad korisnik može sam da odvuče chat u stranu kad mu nešto zaklanja, nestajanje na slučajan klik postaje nepotrebno (i nezgodno — gasilo je razgovor usred rada). `chatOpen` se od sada menja ISKLJUČIVO preko ikonice u donjoj traci ili X dugmeta u zaglavlju chata, ne i preko klika bilo gde drugde.
- **"+" meni (`AiChatBox.tsx`) sečen/nevidljiv — ispravljeno.** Nalaz istog dana (vlasnik: "kada se klikne na + u chatu taj modul koji iskače se podvlači i ne vidi se") — meni se otvarao NAGORE (`bottom-full`), a dugme "+" je pri DNU chat-a (red za unos); kad je razgovor prazan, red za unos je odmah ispod zaglavlja pa meni nije imao prostora da se otvori nagore i bivao je odsečen `overflow-hidden` plutajućeg prozora (Shell.tsx). Promenjeno da se otvara NADOLE (`top-full`, `z-50`) — dole uvek ima prostora (red brzih prečica pa dalje do dna panela), isti odbrambeni `z-50` razlog kao meni "Poruke" u `StatusBar.tsx` (v1.82).

**Provera (v1.83):** `tsc --noEmit` čist za `apps/panel`. Prevlačenje i otvaranje "+" menija su klijentske interakcije (mouse pokreti/klikovi) — kao i u v1.82, nema headless browser alata u repozitorijumu (Playwright/Puppeteer nisu instalirani) da se stvarno KLIKNE u pravom pregledaču; potvrđeno čitanjem koda (koordinatna matematika prevlačenja, `top-full`/`z-50` klase menija) i typecheck-om, ne uživo interakcijom. Ostatak M17/M19 promena iz istog prolaza (prilozi fajlova, poglavlje niže) JESU uživo provereni protiv prave baze/servera, pošto su to server-side/HTTP tokovi dostupni preko curl-a, za razliku od miš-pokreta u pregledaču.

**Prilozi fajlova u M19 chat-u (23.8.2026)** — implementirano u `apps/panel/src/app/(app)/chat/[conversationId]/ChatPanel.tsx` (dugme za prilaganje, prikaz izabranog fajla, prikaz priloga na porukama) i novoj panel BFF ruti `apps/panel/src/app/api/chat/attachments/[attachmentId]/route.ts` (provlači preuzimanje kroz panel server, ne otkriva M1 pristupni token klijentu). Backend deo (`MessageAttachment`, upload/download endpoint-i, bezbednosna higijena) je M19 nadležnost — pun detalj, uključujući vlasnikove odluke o skladištenju/obimu preko `AskUserQuestion`, u `docs/moduli/M19-komunikaciona-platforma/20-SPECIFIKACIJA-M19-KOMUNIKACIONA-PLATFORMA.md` verzija 1.6. Uživo provereno kroz panel (ne samo direktno na API): poruka sa prilogom poslata, prikazana u panel HTML-u sa linkom, preuzimanje kroz panel BFF rutu vraća ispravan `Content-Disposition`/sadržaj.

**Verzija:** 1.82 — Dorada plutajućeg AI chat-a (22.8.2026, treći krug istog dana, na zahtev vlasnika posle pregleda snimka ekrana v1.81) + ispravka starog bag-a u `TabsContext.tsx` otkrivenog u istom prolazu.

- **Prečice ispod polja za unos** (`AiChatBox.tsx`) — tekst uklonjen, ostaje samo ikonica; naziv se sad prikazuje kao `title` (hover tooltip), na zahtev vlasnika ("uklonite tekst neka bude tekst koji se pojavljuje kada se miš pređe preko ikone").
- **Širina plutajućeg prozora** promenjena sa proizvoljnih `400px` na `560px` — na zahtev vlasnika ("širina chata neka bude kao max širina desnog panela"), usklađeno sa `RightPanel.tsx` koji koristi `ResizablePane maxWidth={560}`.
- **Spoljni okvir uklonjen** (`border border-border` skinut sa plutajućeg `<div>`-a u `Shell.tsx`) — na zahtev vlasnika ("uklonite okvir celog chata, nepotreban je"). `shadow-lg` i tonska razlika pozadine ostaju dovoljni za vizuelno odvajanje.
- **Klik van chata ga zatvara** (novi `useEffect` u `Shell.tsx`, `chatPanelRef` + `document.addEventListener('mousedown', ...)`, isti obrazac kao `messagesRef` u `StatusBar.tsx`) — na zahtev vlasnika ("kada se klikne na bilo šta što ga zaklanja chat da se zatvori u stanju u kom je zatečen"). Dugme za otvaranje/zatvaranje (`data-chat-toggle` atribut, dodat u `StatusBar.tsx`) je namerno izuzeto iz provere "klik van chata" — bez izuzetka bi `mousedown` prvo zatvorio chat, a odmah zatim `onClick` (`onToggleChat`) ga ponovo otvorio, umesto da dugme radi kao pravi prekidač. Ovo rešava i prijavljeni problem "kad se klikne na ikonu za poruke, ne vidi se taj modul od AI chata" — klik na ikonicu "Poruke" (donja traka) se sad tretira kao "van chata" i zatvara ga PRE nego što se meni otvori (dodatno, `z-50` na `StatusBar.tsx` meniju poruka, iznad chatovog `z-40`, kao odbrana u dubinu).
- **Zatečeno stanje se čuva** — zatvaranje ovim putem i dalje koristi `chatOpen` CSS `hidden`/`flex` prekidač uveden u v1.81, ne uslovan JSX render — `turns` (istorija razgovora) se ne gubi.

**Nezavisno otkriven bag u `TabsContext.tsx` (prijavio vlasnik posle pregleda uživo — "kada se klikne na neku ikonu čime se otvara novi tab, ne otvara se taj tab već se ostaje u prethodnom"):** `openTab(path, label)` je od uvek samo upisivao zapis u niz tabova, NIKAD stvarno ne menjajući rutu — radilo je slučajno svuda gde poziv dolazi sa pravog `<Link href>` elementa (bočna traka — sam `<Link>` navigira, `openTab` samo registruje tab preko `useRegisterTab` na odredišnoj stranici), ali ne i svuda gde se `openTab` poziva direktno iz `onClick` bez `<Link>`-a (AI chat prečice, meni "Poruke", "Agent Inbox" dugme u `TopBar.tsx`, klik na obaveštenje u `NotificationStack.tsx`/`NotificationBell.tsx`) — ti klikovi su tiho ostajali na prethodnoj stranici. Ispravljeno dodavanjem `if (path !== pathname) router.push(path)` na kraju `openTab`-a; `pathname !== path` provera sprečava suvišan `router.push` kad `useRegisterTab` sam sebe poziva sa VEĆ aktivnom rutom (ne bi trebalo da dodaje zapis u istoriju pregledača za rutu na kojoj se već nalazimo).

**Provera (v1.82):** `tsc --noEmit` čist za `apps/panel`. Uživo kroz pravu prijavu i pravu bazu (nov privremeni VLASNIK nalog, obrisan posle provere) — `/` HTML posle prijave potvrđuje: `w-[560px]` na plutajućem `<div>`-u (stari `w-[400px]` više ne postoji nigde u HTML-u), isti `<div>` bez `border-border` klase, `data-chat-toggle` atribut prisutan na dugmetu u donjoj traci, prečice renderovane kao `title="Katalog proizvoda"` (i ostale tri) BEZ vidljivog teksta kao deteta elementa. Klik-van-chata i "zatvori pa se ponovo ne otvori sam" ponašanje (klijentska interakcija) NIJE kliknuto u pravom browseru — nema dostupnog headless browser alata u repozitorijumu (proveren `package.json` i `node_modules/.bin` — ni Playwright ni Puppeteer instalirani) — potvrđeno samo čitanjem koda i logike efekta, ne uživo klikom. `openTab` ispravka je klijentska navigacija — istom ograničenju podleže, potvrđena čitanjem koda (`router.push` poziv, `pathname !== path` uslov) i `tsc --noEmit`, ne uživo klikom.

**Ranije (v1.81)** — AI chat postaje plutajući prozor umesto trajno usidrenog dela centralnog panela (22.8.2026, na zahtev vlasnika: "chat treba da se pojavljuje na klik na ikonu koju treba staviti na sredinu donje trake... drugi klik se uklanja iz vidokruga, ne briše ono što je chatovano"). **Svesno poništava** raniji princip "uvek deo centralnog panela bez obzira koji modul je aktivan" (dizajn dok. §6c, 19.8.2026) — nije previd, nego direktna vlasnikova odluka.

- **Okidač:** nova ikonica na SREDINI `StatusBar.tsx` (apsolutno centrirana u odnosu na celu traku — jedino mesto koje odstupa od levo/desno poravnanja ostatka trake, koja inače nema prirodnu "sredinu"). Klik prebacuje `chatOpen` stanje (podignuto u `Shell.tsx`, prosleđeno kao prop, isti obrazac kao `rightPanelOpen`).
- **"Ne briše, samo uklanja iz vidokruga":** `AiChatBox` ostaje MONTIRAN uvek — vidljivost se kontroliše CSS `hidden`/`flex` klasom, ne uslovnim JSX renderom. Da je uslovan render korišćen, `turns` stanje (istorija razgovora, `useState` unutar `AiChatBox`) bi se brisalo pri svakom zatvaranju — ovo je bila namerna tehnička odluka, ne slučajno rešenje.
- **Stil overlay-a — vlasnikov izbor preko `AskUserQuestion`:** plutajući prozor u donjem desnom uglu (`fixed bottom-[38px] right-4`, iznad `StatusBar`-a), BEZ zatamnjenja pozadine — razmatrana alternativa (pun modal sa zatamnjenom pozadinom, isti stil kao popup pretrage) je odbačena jer bi blokirala baš onaj sadržaj koji AI automatski čita i o kom se razgovara (M15 spec §6.5.1 dopuna, "sadržaj trenutnog ekrana") — zatamnjena/blokirana pozadina bi značila da korisnik ne može da gleda ekran i razgovara o njemu istovremeno, što bi obesmislilo tu prethodnu dopunu.
- **`<main>`** (centralni sadržaj) sad UVEK zauzima punu visinu centralne kolone, bez obzira da li je chat otvoren — ranije je dokovan `AiChatBox` trajno oduzimao prostor od `<main>` (flex sibling); sad plutajući prozor SAMO se nadovezuje preko sadržaja, ne gura ga.
- Mali zaglavlje red u plutajućem prozoru ("AI asistent" + X dugme) dodat u `Shell.tsx` (ne u `AiChatBox.tsx` samom) — isti obrazac kao zaglavlje `RightPanel.tsx`.

**Provera:** `tsc --noEmit` čist; uživo kroz pravu prijavu i pravu bazu — `/` HTML potvrđuje plutajući `<div>` sa klasom `flex` (podrazumevano otvoren) i tekstom "AI asistent"/"Sakrij AI chat (istorija se čuva)"; stari dokovan `w-[50%]` omotač više ne postoji u HTML-u (potvrđeno odsustvo). Sam klik/otvaranje-zatvaranje (klijentsko stanje) nije uživo kliknuto u pravom browseru — potvrđeno čitanjem koda i podrazumevanog stanja u markup-u. Test nalog obrisan posle provere. v1.80 — popup dobio 4 tip-specifična polja, prikazana samo za odgovarajući tip (22.8.2026, sledeći korak posle v1.79, na zahtev vlasnika) — FLIGHT: klasa (cabin class); TRANSPORT (rent-a-car): starost vozača; CRUISE: broj noćenja + tip kabine. Pun detalj (backend ožičenje, koji su parametri namerno izostali i zašto) u M5 spec §11 v1.41. v1.79 — popup pretraga po tipu proizvoda + chip sa "izmeni" (22.8.2026, na zahtev vlasnika — konkretan predlog rasporeda, prihvaćen posle rasprave o gde treba da žive okidačke ikonice, vidi ispod). Zamenjuje raniji "Pretraga" formular u levom panelu (uvek otvoren, generičan) sledećim tokom: klik na ikonicu tipa proizvoda (levi panel, nepromenjeno mesto) → otvara `SearchCriteriaPopup.tsx` (modal sa poljima destinacija/datumi/broj osoba) → "pretraži" zatvara popup i navigira → `SearchCriteriaChip.tsx` (nov, vrh centralnog panela) prikazuje sažetak aktivne pretrage (tip + destinacija + datumi + broj osoba) sa dugmetom "izmeni" koje ponovo otvara ISTI popup, predpopunjen trenutnim vrednostima. Oba mesta dele jednu popup komponentu i jedan izvor liste tipova (`lib/search-product-types.ts`, izvučeno iz ranijeg `SearchSidebarPanel.tsx` da se lista ne razilazi između dva mesta).

**Odbačena alternativa (vlasnikov prvobitni predlog):** okidačke ikonice ispod AI chat-a, ne u levom panelu. Odbijeno jer se sudara sa postojećim pravilom da je chat "uvek deo centralnog panela bez obzira koji modul je aktivan" (dizajn dok. §6c) — ikonice specifične za pretragu bi ili bile vidljive na SVAKOM ekranu (besmisleno na CRM/Finansije) ili zahtevale uslovno renderovanje po ruti (razbija princip da chat izgleda isto svuda), PLUS bi duplirale ikonice koje već postoje i rade u levom panelu. Dogovorena alternativa (ikonice ostaju u levom panelu, chip na vrhu centralnog panela umesto vezano za chat) zadržava traženi tok (popup → pretraži → chip → izmeni) bez sudara sa tim pravilima.

**"Filteri" sekcija levog panela** dobila sopstveno dugme "primeni filtere" i `onSubmit` koji SPAJA (ne zamenjuje) sa postojećim URL parametrima — ranije je jedna zajednička `<form>` (Pretraga+Filteri) delila jedno dugme; pošto Pretraga polja sad žive u popup-u, Filteri mora sam da nosi svoj submit, a native GET submit bi obrisao `type`/destinaciju/datume iz URL-a (native forma zamenjuje ceo query string, ne spaja) — otkriveno i ispravljeno u istom prolazu, pre nego što je postalo bag u produkciji.

**Selekcija → desni panel** (drugi deo zahteva, "omogućite odabir koji ide u desni panel") — VEĆ POSTOJALA, nedirana: `QuoteButton.tsx`/`SelectionContext.tsx`/`RightPanel.tsx` (M5 spec §3.0e.3, implementirano 21.8.2026) — dugme "dodaj" na svakoj kartici/redu rezultata već dodaje u selekciju i automatski otvara desni panel na prvi dodatak. Provereno da i dalje radi kroz nove grid/red rezultate.

**Provera:** `tsc --noEmit` čist; uživo kroz pravu prijavu i pravu bazu — `/rezervacije/pretraga` bez `type` parametra ne prikazuje chip (nema aktivne pretrage); sa `?type=ACCOMMODATION` chip prikazuje "Smeštaj" i grid kartice sa dugmetom "dodaj" (19 stvarnih `ACTIVE` proizvoda u dev bazi); sa `?type=TRANSFER` chip prikazuje "Transferi" i red-raspored (isti podatak, `CARD_TYPES` razdvajanje nepromenjeno). Sam klik na ikonicu/popup/izmeni dugme (klijentsko stanje) nije uživo kliknut u pravom browseru — potvrđeno čitanjem koda i URL-parametara koje popup generiše. Test nalog obrisan posle provere. v1.78 — dugme mikrofona u AI chat-u (22.8.2026, na zahtev vlasnika — pun detalj u M15 spec v1.25, poglavlje 6.6). `AiChatBox.tsx`: `speechSupported` provera (`window.SpeechRecognition`/`webkitSpeechRecognition`), dugme se prikazuje SAMO kad browser podržava (Chrome/Edge; ne u Firefox/starijem Safari-ju). Klik pokreće snimanje (pulsirajuća crvena ikonica dok traje), prepoznat tekst se automatski šalje čim korisnik prestane da govori (vlasnikova odluka preko `AskUserQuestion`) — `send()` dobio opcioni parametar da izbegne zastarelo React stanje pri pozivu iz callback-a prepoznavanja. **Provera:** `tsc --noEmit` čist; klijentska Web API interakcija, nije proverljiva preko curl-a — potvrđeno čitanjem koda, stvaran mikrofon u browseru nije uživo potvrđen (isto ograničenje kao ostatak ove faze). v1.77 — kopiranje pojedinačne poruke u AI chat-u (22.8.2026, na zahtev vlasnika: "omogućite kopiranje svake poruke") — nov `CopyButton` u `AiChatBox.tsx`, ikonica se pojavljuje na hover (grupa/`group-hover`) pored pitanja i iznad odgovora, `navigator.clipboard.writeText`, kratka vizuelna potvrda (ikonica se na 1.5s menja u kvačicu). Bez servera/persistencije — čisto klijentska radnja nad tekstom koji je već u `turns` stanju. **Provera:** `tsc --noEmit` čist, dev server kompajlira bez greške (`✓ Compiled /api/omnisearch`); stvaran klik/kopiranje u browseru nije uživo potvrđeno (isto ograničenje kao ostatak ove faze — klijentska UI radnja, ne proverljiva preko curl-a). v1.76 — `Shell.tsx` `<main>` omotač sadržaja taba dobio `id="tt-main-content"` (22.8.2026) — jedino mesto izmene u M17 za veću M15 dopunu (v1.24, pun detalj tamo): `AiChatBox.tsx` sad čita vidljiv tekst tog elementa i šalje ga automatski uz svako pitanje u AI chat-u, da bi agent mogao stvarno da analizira sadržaj otvorenog ekrana, ne samo naziv taba. `id` namerno NIJE na omotaču koji sadrži i AiChatBox (odvojen sibling `<div>` ispod `<main>`) — nema rizika da se sopstvena istorija razgovora pročita kao "sadržaj ekrana". v1.75 — ispravka v1.74 nalaza (22.8.2026, uživo — vlasnik otvorio "Agent Inbox" tab i pitao "šta vidiš u ovom tabu", AI je i dalje odgovorio da ne vidi ekran i uputio na "+", iako je automatski kontekst trebalo da već radi). Uzrok: `autoContext` je isključivao BILO KOJI tab na ruti `/` (`activePath !== '/'`), pod pretpostavkom da je to uvek prazna podrazumevana Početna — ali "Agent Inbox" nema sopstvenu rutu (poglavlje 6, InboxButton otvara Početnu kao nov tab preimenovan preko `openTab('/', 'Agent Inbox')`, M15 spec §6), pa je baš taj tab tiho ostajao bez konteksta. Ispravljeno da proverava NAZIV taba (`activeTab.label === homeLabel`, iz `NAV_ITEMS`), ne rutu — Agent Inbox (i svaki budući tab koji deli rutu `/` sa drugačijim nazivom) sad dobija automatski kontekst, dok prava neizmenjena Početna ostaje isključena (isti razlog kao pre — prazna kontrolna tabla nema šta da doda). Ista ispravka primenjena i na "+" meni dugme (isti bag, `activeTab.path === '/'` zamenjeno sa `isUnlabeledHome`). **Provera:** `tsc --noEmit` čist; ponovo je klijentsko React stanje, ne proverljivo preko curl-a — potvrđeno čitanjem ispravljene logike i uslovnog izraza, stvaran tok u browseru (da čip stvarno postane vidljiv na "Agent Inbox" tabu) nije uživo potvrđen kroz pravi klik (isto ograničenje kao v1.74). v1.74 — naziv otvorenog taba se automatski prilaže AI chatu kao kontekst (22.8.2026, na zahtev vlasnika, posle v1.73 M15 ispravke istog dana — "da li ai agent odmah moze da zna koji je tab otvoren"). Ranije je isti podatak (naziv trenutnog zapisa) zahtevao ručan klik na dugme "+" (poglavlje 6c) — sad se automatski vezuje za aktivan tab i šalje na svaku poruku, dok se korisnik eksplicitno ne postavljen na Početnu ili ne obriše čip (X) za taj konkretan tab (`dismissedForPath`, pamti se dok se tab ne promeni). Namerno OSTAJE isti obim podatka kao pre (samo naziv/label taba, ne stvaran sadržaj ekrana — dokument/kontakt/istorija) — vlasnik je preko `AskUserQuestion` eksplicitno birao između lakše (samo naziv, automatski) i teže varijante (pun sadržaj ekrana automatski, veći trošak po poruci i pitanje privatnosti ličnih podataka gosta) i potvrdio lakšu. `AiChatBox.tsx`: nov `autoContext`/`effectiveContext` izvod iz `activeTab`/`dismissedForPath`, `useEffect` resetuje oboje pri promeni taba. **Provera:** `tsc --noEmit` čist; uživo kroz pravi login+MFA protiv stvarne baze — `/`, `/mcp`, `/crm` vraćaju 200 posle čišćenja pokvarenog `.next` keša (isti uzrok kao raniji nalaz istog dana, vidi v1.73 provera-napomenu). Sama automatska logika je klijentsko React stanje (`useState`/`useEffect`) — ne proverljivo preko curl-a, potvrđeno čitanjem koda i tipskom proverom, stvaran vizuelni tok (čip se pojavljuje/menja pri promeni taba) nije potvrđen u pravom browseru. v1.73 — nov ekran `/mcp` za M16 (MCP klijenti, 22.8.2026) — jedini backend modul koji do sad nije imao nijedan panel ekran (uočeno posle analize Phocuswright izveštaja 2026, provereno grep-om kroz `apps/panel/src`). Poglavlje 4 tabela dopunjena (M16 → Faza 6, red je nedostajao). Novi fajlovi: `apps/panel/src/app/(app)/mcp/page.tsx` (lista klijenata sa statusom/nivoom pristupa/rate limitom), `CreateClientForm.tsx` (klijentska komponenta — plaintext kredencijal iz M16 spec §3.1 se prikazuje TAČNO JEDNOM, u lokalnom React stanju, nikad ponovo dohvatljiv), `ClientActionButtons.tsx` (Aktiviraj/Odobri READ_WRITE/Suspenduj — tri odvojene dozvole, `MANAGE`/`APPROVE_READ_WRITE`/`MANAGE`, isti obrazac kao `RetryRegistrationButton` u M11 ekranu), `actions.ts` (Server Action-i nad postojećim `/mcp-admin/clients*` endpoint-ima, M16 spec §8 — nijedan nov API endpoint, samo prikaz). `nav.ts` dobio stavku `mcp` (grupa "Administracija", isto mesto kao Korisnici/Audit log — upravljanje agentskim pristupom je administrativna funkcija Vlasnika/Direktora, ne prodajni kanal). **Provera:** `tsc --noEmit` čist za `apps/panel`; uživo kroz pravi login+MFA (privremen VLASNIK nalog, obrisan posle provere) protiv stvarne baze — `GET /mcp` vraća 200, prazna lista prikazuje "Nema registrovanih MCP klijenata."; pun ciklus (`POST /mcp-admin/clients` → kredencijal vraćen tačno jednom → `activate` → `approve-read-write` → `suspend`) izvršen direktno nad pravim API-jem i potvrđeno da se svaki korak odmah odražava na `/mcp` ekranu (status PENDING→ACTIVE→SUSPENDED, nivo READ_ONLY→READ_WRITE, dugmad se ispravno pojavljuju/nestaju po stanju). Test klijent i privremeni nalog obrisani posle provere. v1.72 — "TT Terminal Travel" naziv UKLONJEN iz gornje trake (22.8.2026, na zahtev vlasnika: "uklonite logo u gornjem redu") — posle v1.71 (pun logo na dnu Sidebar-a), tekstualni naziv u `TopBar.tsx` je postao suvišan/dupliran, uklonjen u potpunosti. Čišćenje pratećih delova koji su postojali isključivo zbog njega: `leftRailWidth` prop i računica (`TopBar.tsx`, `Shell.tsx` — `sidebarWidth` state, `ResizablePane.onWidthChange` poziv) uklonjeni, tabovi sad prosto prvi element u traci bez posebnog poravnanja. `ResizablePane.tsx` `onWidthChange` prop u potpunosti uklonjen (jedini potrošač bio je `leftRailWidth`). `--wordmark` CSS token (sva 4 bloka u globals.css), `wordmark` Tailwind boja i `font-tech`/JetBrains Mono (`layout.tsx`, `next/font/google` uvoz, `tech` Tailwind font porodica) takođe uklonjeni — nijedan više nema potrošača u kodu (grep potvrđen). **Provera:** `tsc --noEmit` čist; uživo kroz pravi login+MFA — `/` HTML ne sadrži ni "TT", ni "Terminal Travel", ni `font-tech`, ni `text-wordmark` ni na jednom mestu. **ISPRAVKA istog dana, odmah zatim** ("tabove ostavite gde su i bili ne treba da idu skroz u levo") — uklanjanje naziva je pomerilo tabove skroz do leve ivice trake, što nije bilo traženo; dodat prazan razmak fiksne širine (`w-[255px]`, ista vrednost koju je naziv zauzimao u podrazumevanom stanju Sidebar-a, v1.65) da tabovi ostanu vizuelno na istoj poziciji — bez teksta/sadržaja, i namerno bez dinamičkog praćenja širine Sidebar-a (ta računica je uklonjena zajedno sa nazivom, pojednostavljeno na fiksnu vrednost — ne prati više prevlačenje/kolaps Sidebar-a kao što je `leftRailWidth` nekad radio). Uživo potvrđeno: HTML pokazuje prazan `<div class="w-[255px] flex-shrink-0">` pre trake tabova. v1.71 — logo agencije na dnu levog panela (22.8.2026, na zahtev vlasnika: "dajte mi predlog kako da ovaj logo stavimo u donji deo levog panela") — `Sidebar.tsx`, oba stanja (prošireno/kolabovano). Vlasnik je dostavio izvornu sliku loga (avion/circuit ikonica + "TERMINAL TRAVEL" wordmark, plavo→teal gradijent) — **ni JPG ni prvi PNG izvoz nisu imali stvarnu providnost** (šahovska šara koju su alati za generisanje prikazali kao "ovde će biti providno" ispala je trajno naslikana u pikselima, alfa kanal — kad je postojao — uvek 255/neproviden, provereno programski čitanjem PNG bajtova/`pngjs` histogramom, ne samo vizuelnim pregledom). Providnost uklonjena programski (`pngjs`, jednokratna Node skripta, nije trajni deo koda): piksel se tretira kao pozadina i postavlja na `alpha=0` kad je svetao i malo zasićen (`max(R,G,B) ≥ 195` i `max−min < 25`) — razdvaja pouzdano svetlu/sivu šahovnicu (i beli isečak aviona unutar strelice, namerno takođe providan) od zasićenih boja loga (plavo/teal gradijent, tamnonavy kontura). Rezultat isečen na stvarnu granicu sadržaja (`apps/panel/public/brand/terminal-travel-logo.png`, pun logo) i zaseban isečak samo ikonice bez teksta (`terminal-travel-icon.png`, kolabovano stanje, 40px trake nema mesta za pun naziv) — pronađen prazan providan red između ikonice i teksta kao granica reza. Oba `<img>` sa `mt-auto` (isti trik kao "Administracija" ikonica na dnu `ActivityBar.tsx`) — logo se gura na dno panela kad ima slobodnog prostora; ako sadržaj sekcije preraste visinu, logo ide posle liste u scroll-u umesto da ostane zalepljen za dno viewport-a (svestan kompromis, isto ponašanje kao footer u većini scroll kontejnera, nije zahtevana potpuna fiksacija). **Provera:** `tsc --noEmit` čist; uživo kroz pravi login+MFA — `/` HTML potvrđuje `<img src="/brand/terminal-travel-logo.png">`, oba fajla vraćaju HTTP 200. Vizuelni izgled (stvaran raspored/veličina u panelu) nije potvrđen u pravom browser-u (isto ograničenje kao ostatak ove faze). **Otvoreno:** fajlovi su relativno veliki za dekorativni element bočnog panela (1.5MB pun logo, 444KB ikonica, raster ne vektor) — kompresija/SVG rekonstrukcija ostaje mogući sledeći korak ako veličina učitavanja postane bitna. v1.70 — iskošene ivice POVUČENE, vraćen pravougaon tag/pilula oblik (22.8.2026, na zahtev vlasnika: "ne svidja mi se vratite kako je bilo samo neka tabovi budu iste visine kao i polje pretrage") — poništava ceo v1.68/v1.69 eksperiment sa `skewX` paralelogram oblikom. `TabBar.tsx` tabovi i `AiChatBox.tsx` dugmad brzih prečica vraćeni na `rounded border` bez `transform` (isti oblik kao v1.66, pre iskošenja). Jedina trajna izmena iz ovog kruga eksperimenata: tabovi i polje za pretragu (`TopBar.tsx`) ostaju izjednačeni na `h-[29px]` (v1.69) — to je vlasnik izričito zadržao. **Provera:** `tsc --noEmit` čist; uživo kroz pravi login+MFA — `/` HTML potvrđuje da `skewX` više nigde ne postoji, `rounded border` (ne `rounded-sm`) na tabovima/tagovima, `h-[29px]` i dalje prisutno na oba. Vizuelni izgled nije potvrđen u pravom browser-u (isto ograničenje kao ostatak ove faze). v1.69 — ispravka iskošenih tabova/tagova posle snimka ekrana (22.8.2026, na zahtev vlasnika: "nije bas najbolje stao tekst u tabovima... tabovi neka budu visine kao i polje pretrage... Tekst u tabovima i tagovima da prati kosi ugao. i ostre ivice zameniti vrlo blagim zaokruzenjem") — tri odvojene ispravke v1.68 pokušaja:
  1. **Kontra-transform uklonjen.** v1.68 je kontra-transformisao SADRŽAJ (`skewX(20deg)` na unutrašnjem `<span>`) da tekst ostane uspravan unutar kose kutije — na snimku se to pokazalo kao tekst koji vizuelno "izlazi" iz kosih ivica u uglovima (ono što je vlasnik video kao "tekst nije stao"). `TabBar.tsx` i `AiChatBox.tsx`: unutrašnji `style={{transform: 'skewX(20deg)'}}` span uklonjen — tekst/ikonice sad nasleđuju isti `skewX(-20deg)` kao kutija, prati ugao ivica (bukvalno "tekst da prati kosi ugao").
  2. **Visina.** Tabovi (`TabBar.tsx`) dobijaju eksplicitno `h-[29px]` (ranije nasleđivali visinu od `<header>` reda preko padding-a) — izjednačeno sa poljem za pretragu (`TopBar.tsx`, i ono dobija `h-[29px]`, ranije `py-1` bez eksplicitne visine).
  3. **Zaobljenje.** `rounded` (potpuno uklonjen u v1.68, oštri dijagonalni uglovi) → `rounded-sm` (2px, "vrlo blago zaokruženje") na tabovima i tagovima.
  Razmak između tabova/tagova povećan `gap-1.5`→`gap-3` (skewX vizuelno "širi" kutiju van njenih layout granica na uglovima — veći razmak sprečava da se susedni kosi tabovi vizuelno dodiruju/preklapaju).
  **Provera:** `tsc --noEmit` čist; uživo kroz pravi login+MFA — `/` HTML potvrđuje `h-[29px]` na tabovima i polju za pretragu, `rounded-sm border` na tabovima/tagovima, bez `skewX(20deg)` kontra-transforma. Vizuelni izgled (stvaran raspored teksta unutar kose kutije) nije potvrđen u pravom browser-u (isto ograničenje kao ostatak ove faze). v1.68 — tabovi i tagovi dobijaju iskošene ivice/paralelogram oblik (22.8.2026, na zahtev vlasnika: "hocu da leva i desna strana tabova i tagova budu iskošene pod 20%", pojašnjeno preko `AskUserQuestion` na "iskošene ivice, paralelogram") — `TabBar.tsx` tabovi i `AiChatBox.tsx` dugmad brzih prečica: `rounded` klasa uklonjena (oštri dijagonalni uglovi, ne zaobljeni), spoljašnji element `style={{ transform: 'skewX(-20deg)' }}`, sadržaj (tekst/ikonice/dugme za zatvaranje) omotan u unutrašnji `<span>` sa kontra-transformacijom `skewX(20deg)` da ostane uspravan — standardna tehnika za koso-obeležene tabove (npr. Chrome), izbegava krivljenje samog teksta. **Provera:** `tsc --noEmit` čist; uživo kroz pravi login+MFA — `/` HTML potvrđuje 5 parova `skewX(-20deg)`/`skewX(20deg)` (1 tab "Početna" + 4 taga brzih prečica). Vizuelni izgled (stvaran ugao, poravnanje ivica) nije potvrđen u pravom browser-u (isto ograničenje kao ostatak ove faze). v1.67 — "TT" boja postala theme-aware (`--wordmark`), istražen "nestalo zvono" nalaz (21.8.2026, na zahtev vlasnika: "TT neka bude u beloj boji" → posle upozorenja o kontrastu na skoro-beloj pozadini svetlog moda, izabrao "belo u dark a navy teget u light"). Nov CSS token `--wordmark` (globals.css, sva četiri bloka: `:root`, `prefers-color-scheme: dark` media, `[data-theme='dark']`, `[data-theme='light']`) — `#1f3a5f` (isti "navy teget" koji već postoji za `--icon-line`) u svetlom modu, `#ffffff` u tamnom; nov Tailwind token `wordmark` (`tailwind.config.ts`). `TopBar.tsx` "TT" span: `text-accent` → `text-wordmark` (sjaj/text-shadow ostaje u akcentnoj boji, nepromenjen). **Provera:** `tsc --noEmit` čist; uživo kroz pravi login+MFA — HTML potvrđuje klasu `text-wordmark` na "TT". Stvaran vizuelni izgled (belo/navy u odgovarajućem modu) nije potvrđen u pravom browser-u.

  **Odvojen nalaz, bez izmene koda ovog prolaza** — vlasnik je ranije prijavio da zvono za obaveštenja "i dalje" nije vidljivo u gornjoj traci. Istraženo: `NotificationBell.tsx` je klijentska komponenta koja podatke učitava u `useEffect` POSLE hidratacije (`conversationId` počinje kao `null`, komponenta vraća `null` dok fetch ne uspe) — zato se nikad ne vidi u sirovom HTML-u koji `curl` dobija, nezavisno od stvarnog stanja podataka; ovo ograničenje važi za svaku proveru ove faze koja se oslanja na `curl`, ne samo za zvono. Pravi uzrok odsustva: zvono se oslanja na M19 "Obaveštenja" `DIRECT` konverzaciju koja se (`InAppNotificationsService.ensureNotificationsConversation`, M19 spec §5) kreira LENJO — tek kad prvi KRITIČAN M18 signal stigne korisniku sa ulogom VLASNIK/DIREKTOR — nije greška u kodu, taj uslov se prosto još nije desio za dev-login nalog napravljen direktno preko Prisma (van normalnog registracionog toka). Konverzacija je ručno napravljena za taj nalog (ista šema koju bi servis sam napravio: `Conversation{type:DIRECT, name:'Obaveštenja'}` + dva `ConversationParticipant` reda + jedna dobrodošla `Message`) radi lokalnog testiranja — `GET /api/chat/conversations` potvrđeno vraća je uživo (pravi login+MFA). Nema izmene koda — ovo je isključivo dev-podatak za jedan test nalog, ne popravka mehanizma (mehanizam radi po spec-u).

v1.66 — uklonjena srednja linija chata (21.8.2026, na zahtev vlasnika: "uklonite srednju liniju chata") — poništava v1.60 "ostavite samo gornju liniju donjeg dela" — `border-t border-ink-faint` na redu sa brzim prečicama (`AiChatBox.tsx`, razdvajao ga je od reda za unos iznad) je uklonjen, oba reda sad bez razdelne linije između sebe. **Provera:** `tsc --noEmit` čist; uživo kroz pravi login+MFA — `/` HTML potvrđuje da red sa prečicama više nema `border-t` klasu. Vizuelni izgled nije potvrđen u pravom browser-u (isto ograničenje kao ostatak ove faze). v1.65 — ispravka poravnanja tabova (pravi uzrok pronađen), "Terminal Travel" -20% (21.8.2026, peti krug istog dana, na zahtev vlasnika: "i dalje tabovi ne pocinju odakle treba... smanjite Terminal Travel za 20%") — v1.64 računica (`leftRailWidth`) je zaboravila da `<header>` ima sopstveni `px-2` (8px) i `gap-1` (4px) pre `TabBar` omotača, pa je naziv-omotač zauzimao PUNO `leftRailWidth`, gurajući tabove 12px udesno od stvarne ivice centralnog panela — ispravljeno na `Math.max(leftRailWidth - 12, 140)` (8px padding + 4px gap = 12px, poništava tačno tu razliku). "Terminal Travel" tekst smanjen sa 33px na 26px (20% manje od "TT", koje ostaje 33px). **Provera:** `tsc --noEmit` čist; uživo kroz pravi login+MFA — `/` HTML potvrđuje `style="width:255px"` na omotaču naziva (leftRailWidth=267 za podrazumevanu Sidebar širinu 224, 267-12=255, aritmetika se poklapa) i `text-[26px]` na "Terminal Travel". Vizuelni izgled (stvarno piksel poravnanje u browser-u) nije potvrđen (isto ograničenje kao ostatak ove faze). v1.64 — tabovi poravnati sa levom ivicom centralnog panela, naziv restilizovan (sjaj/velika slova), tabovi kao tagovi (21.8.2026, četvrti krug istog dana, na zahtev vlasnika: "tabovi ostaju u gornjem redu ali se pojavljuju od levog pocetka centralnog panela. TT napravite kao da svetli... TERMINAL TRAVEL iste visine kao i TT. TT slova treba da budu boldirana" + naknadno "tabovi treba da izgledaju kao tagovi u donjem redu chata"). Tri odvojene izmene:
  1. **Poravnanje tabova**: `ResizablePane.tsx` dobija opcioni `onWidthChange` (jedan `useEffect` na `[width, collapsed, collapsedWidth]`, pokriva učitavanje iz localStorage/prevlačenje/reset/kolaps jednim mestom) — `Shell.tsx` prati `sidebarWidth` state (ignoriše izveštaje dok je Sidebar kolabovan, da ne prepiše "pravu" širinu sa `collapsedWidth`), računa `leftRailWidth = 43 (ActivityBar) + (kolabovano ? 40 : sidebarWidth)`, prosleđuje ga `TopBar`-u. `TopBar.tsx` omotač oko naziva dobija `style={{ width: Math.max(leftRailWidth, 140) }}` — tabovi posle njega sad počinju tačno iznad leve ivice centralnog panela, prati se i kod prevlačenja granice uživo. `Math.max(..., 140)` je svesna zaštita: kad je Sidebar kolabovan (`leftRailWidth` pada na 83px), sam naziv je širi od toga — bez ove donje granice bi se naziv i prvi tab preklapali; posledica je da poravnanje nije piksel-tačno u tom jednom stanju (kolabovan Sidebar), povučen je poznat, dokumentovan kompromis, ne previd.
  2. **Naziv**: "TT" boldirano (`font-bold`) + efekat sjaja (`text-shadow: 0 0 6px var(--accent), 0 0 16px var(--accent)`, inline style, akcentna boja); "Terminal Travel" promenjeno iz manjeg/tišeg dodatka (v1.62: `text-xs text-ink-faint`) u VELIKA SLOVA (`uppercase`) iste veličine kao TT (`text-[33px]`), obično (ne boldirano); razmak između njih tačno "3 slovna mesta" preko `ml-[3ch]` (CSS `ch` jedinica = širina jednog karaktera trenutnog fonta — radi precizno jer je `font-tech` monospace).
  3. **Tabovi kao tagovi**: poništava v1.60 "pravi VS Code" pravougaoni oblik (`border-r`/`border-t-2`, pripijeni, bez zaobljenosti) — `TabBar.tsx` tabovi sad isti tag/pilula oblik kao dugmad brzih prečica u `AiChatBox.tsx` (`rounded border px-2 py-0.5 text-[11px]`, razmak `gap-1.5` između), aktivan tab `border-accent bg-accent-soft text-ink`, neaktivan `border-ink-faint text-ink-faint hover:border-accent hover:text-ink`. Dugme za nov tab i "zatvori sve" smanjeni na `23×23px` da prate manju visinu tag-oblika.
  **Provera:** `tsc --noEmit` čist; uživo kroz pravi login+MFA — `/` HTML potvrđuje `style="width:267px"` na omotaču naziva (43+224 podrazumevana Sidebar širina), `font-bold`+`text-shadow` na "TT", `uppercase text-[33px]` na "Terminal Travel", `ml-[3ch]` razmak, i `rounded border ... text-[11px]` tag-oblik na tabu "Početna". Vizuelni izgled (stvaran sjaj, poravnanje pri prevlačenju) nije potvrđen u pravom browser-u (isto ograničenje kao ostatak ove faze). **Napomena bez izmene koda:** vlasnik je prijavio da je zvono za obaveštenja (`NotificationBell.tsx`) "nestalo" iz gornje trake — provereno uživo (`GET /api/chat/conversations` za dev-login nalog vraća prazan niz): zvono se ćutljivo ne prikazuje kad ne postoji M19 "Obaveštenja" sistemska konverzacija za tog korisnika (isti obrazac kao Agent Inbox ikonica), a taj dev-login nalog (napravljen direktno preko Prisma, ne kroz normalan `seed.ts`/registraciju tok) nema nijednu M19 konverzaciju — nije regresija ovog prolaza, kod je nepromenjen. v1.63 — traka tabova VRAĆENA u gornji red, poništava v1.62 (21.8.2026, treći krug istog dana, na zahtev vlasnika: "vratite tabove u gornji red") — `TabBar` ponovo unutar `TopBar.tsx` header-a (odmah posle "TT Terminal Travel" naziva), uklonjena iz vrha centralne kolone u `Shell.tsx`; `TabBar.tsx` sopstveni stil vraćen na `h-full flex-1` (nasleđuje visinu `<header>` reda) sa `h-[37px] bg-panel-2` samostalnog bloka. Naziv "TT Terminal Travel" (v1.62) ostaje nepromenjen ovim prolazom. **Provera:** `tsc --noEmit` čist; uživo kroz pravi login+MFA — `/` HTML potvrđuje da je `TabBar` (tab "Početna") unutar `<header>`, odmah posle naziva. Vizuelni izgled nije potvrđen u pravom browser-u (isto ograničenje kao ostatak ove faze). v1.62 — traka tabova vraćena na početak centralnog panela, naziv smanjen i prepisan kao "TT Terminal Travel" (21.8.2026, drugi krug istog dana, na zahtev vlasnika: "prvi tab postavite na pocetku centralnog panela. Terminal smanjite za 20% i naisite ovako TT Terminal Travel") — `TabBar.tsx` uklonjen iz `TopBar.tsx` (v1.61 ga je stavila između imena i pretrage), sad prvi red centralne kolone u `Shell.tsx`, iznad `<main>`/AI chat-a, levo poravnat, sopstvena `h-[37px] bg-panel-2` traka (ranije nasleđivala `h-full` od `TopBar` reda, sad samostalan blok, treba sopstvenu visinu/pozadinu). Naziv: 41px→33px (20% manje), tekst promenjen sa "TERMINAL" na dvodelni "TT" (istaknuto, `text-[33px]`, akcentna boja) + "Terminal Travel" (manje, `text-xs`, `text-ink-faint`), oba i dalje `font-tech`/kurziv. **Provera:** `tsc --noEmit` čist; uživo kroz pravi login+MFA — `/` HTML potvrđuje `text-[33px]`, ">TT<" i "Terminal Travel<" u gornjoj traci, `TabBar` više nije unutar `<header>`. Vizuelni izgled nije potvrđen u pravom browser-u (isto ograničenje kao ostatak ove faze). v1.61 — grupne ikonice premeštene u novu vertikalnu `ActivityBar.tsx` (21.8.2026, na zahtev vlasnika: "premestite ikone u vertikalnu levu traku") — pravi VS Code Activity Bar obrazac (uska vertikalna traka uz levu ivicu, odvojena od Sidebar sadržaja, ispred njega u `Shell.tsx`), ista logika/podaci kao ranije u `TopBar.tsx` (samo vertikalno složeni, admin grupa `mt-auto` umesto `ml-auto`). `TopBar.tsx` sad nosi samo naziv, tabove, pretragu i desni klaster dugmadi. Naziv "TERMINAL" uvećan na 95% visine trake (`text-[41px]`, 43px×0.95≈41px), kurziv, nov `font-tech` (JetBrains Mono preko `next/font/google`, self-hosted u build-u — `layout.tsx`, `--font-tech` CSS varijabla, `tailwind.config.ts`). **Provera:** `tsc --noEmit` čist; uživo kroz pravi login+MFA — `/` HTML potvrđuje `ActivityBar` (`w-[43px]` vertikalna traka, `bg-panel-2`) i `font-tech text-[41px]` na "TERMINAL". Vizuelni izgled nije potvrđen u pravom browser-u (isto ograničenje kao ostatak ove faze). v1.60 — uklonjen okvir (border) sa tri dugmeta u gornjoj traci (21.8.2026, na zahtev vlasnika, uočeno na snimku ekrana) — `ThemeToggle.tsx` i `TopBar.tsx` (dugmad za desni panel i odjavu) su imali `border border-border`, jedini elementi u traci sa vidljivim okvirom pored inače bezokvirne ikonice-dugmadi (Agent Inbox, grupne ikonice) — uklonjeno, hover sad koristi pozadinu (`hover:bg-panel`) umesto okvira, isti obrazac kao ostatak trake; aktivno stanje desnog panela sad `bg-accent-soft text-accent-strong` (isti par kao aktivna grupna ikonica) umesto `border-accent`. **Provera:** `tsc --noEmit` čist u `apps/panel`. v1.59 — Chat blok sužen za 30% (21.8.2026, na zahtev vlasnika: "sada suzite ceo chat za 30%"). `Shell.tsx` omotač oko `AiChatBox` — bilo `w-[72%]`, sad `w-[50%]` (72%×0.7≈50%). **Provera:** `tsc --noEmit` čist; uživo kroz pravi login — `/` vraća HTTP 200; HTML potvrđuje `w-[50%]`. Vizuelni izgled i dalje nije potvrđen u pravom browser-u.

**Verzija:** 1.58 — Tag-pilule minimalno zaobljene (21.8.2026, na zahtev vlasnika: "tagovi u donjem delu - treba da imaju manje zaobljen tag - minimalno zaobljen"). `AiChatBox.tsx` brze prečice — `rounded-full` (potpuno ovalan/pilula oblik) zamenjen sa `rounded` (standardni minimalan radijus, isti kao ostatak interfejsa, npr. dugmad u trakama). **Provera:** `tsc --noEmit` čist; uživo kroz pravi login — `/` vraća HTTP 200; HTML potvrđuje `rounded` klasu (bez `-full`). Vizuelni izgled i dalje nije potvrđen u pravom browser-u.

**Verzija:** 1.57 — Okvir donjeg reda sveden na samo gornju liniju (21.8.2026, četvrti zahtev o chat okviru istog dana: "ostavite samo gornju liniju donjeg dela ostale linuije uklonite"). v1.56 pun okvir (`border`, sve četiri strane) oko reda brzih prečica zamenjen sa `border-t` (samo linija koja razdvaja red prečica od reda za unos iznad). **Provera:** `tsc --noEmit` čist; uživo kroz pravi login — `/` vraća HTTP 200; HTML potvrđuje `border-t border-ink-faint` (bez `border` na sve četiri strane). Vizuelni izgled i dalje nije potvrđen u pravom browser-u.

**Verzija:** 1.56 — Okvir chata suzen na donji red (21.8.2026, treći zahtev o chat okviru istog dana: "Uklonite linije gornjeg dela chata ostaje uokviren samo donji deo"). v1.50 je crtala zajednički pun okvir oko OBA reda (unos + prečice) — i dalje je pokrivala gornji (unos) red, što nije bilo traženo. `AiChatBox.tsx`: red za unos sad je BEZ ikakvog okvira; pun okvir (`border border-ink-faint`, sve četiri strane) ostaje SAMO oko donjeg reda (brze prečice). **Provera:** `tsc --noEmit` čist; uživo kroz pravi login — `/` vraća HTTP 200; HTML potvrđuje red za unos bez `border` klase i red prečica sa `border border-ink-faint`. Vizuelni izgled i dalje nije potvrđen u pravom browser-u.

**Verzija:** 1.55 — `--border` potamnjen, bio praktično nevidljiv (21.8.2026, na zahtev vlasnika uz snimak: "Jedva se vide okvirne linije sadrzaja u centralno delu. Potamnite ih za 15%. Ovo vazi za sve module"). Provera je pokazala stvaran propust, ne samo suptilnu pritužbu — `--border` (`#f0f1f2` svetli mod) je davao samo 1.13:1 na belu pozadinu, daleko ispod 3:1 praga (§2a). Doslovnih "-15%" na vrednost ovoliko blizu bele bi ostalo praktično nepromenjeno (1.57:1) — umesto toga izabrana vrednost koja stvarno prolazi prag: svetli `#858c92` (3.41:1), tamni `#748088` (3.25:1, tamni mod nije bio deo pritužbe ali je imao isti propust, ispravljen u istom prolazu). Puna tabela i obrazloženje: `docs/analize/29-DIZAJN-SISTEM-UI.md` §2.0d. Deljen token — primenjeno svuda gde se `--border` koristi (kartice, forme, padajući meniji), ne samo dashboard kartice sa snimka. **Provera:** `tsc --noEmit` čist; uživo kroz pravi login — `/` vraća HTTP 200; iskompajliran CSS potvrđuje nove vrednosti u sva četiri palete-bloka. Vizuelni izgled i dalje nije potvrđen u pravom browser-u.

**Verzija:** 1.54 — Dve dopune (21.8.2026, na zahtev vlasnika: "centrirajte tagove na sredinu donje polovine chata" + "sta god da se pojavljuje u centralnom delu treba da zauzima 90% sirine centralnog dela. koju god ikonu da kliknemo i koji god modul da pozovemo"):
1. **Tag-pilule brzih prečica centrirane.** `AiChatBox.tsx`, red ispod polja za unos — `justify-center` dodat na `flex flex-wrap` red (ranije levo poravnato).
2. **90% pravilo stvarno univerzalno primenjeno na sve module.** Otkriveno da je 57 `page.tsx` ekrana (59 pojavljivanja — dva fajla imaju po dva mesta) imalo SOPSTVENI `mx-auto max-w-{lg,2xl,3xl,4xl,5xl} p-6` omotač oko svog sadržaja — ovo je bilo UGNJEŽĐENO unutar `main` (već `w-[90%]`, Shell.tsx v1.44/v1.45), pa je stvaran prikazan sadržaj bio uži od 90%, dosledno manji na svakom ekranu osim Početne (koja slučajno koristi `max-w-5xl`, blisko vizuelno 90%-u na uobičajenim rezolucijama, zato ranije neupadljivo). Mehanički uklonjen `mx-auto max-w-*` prefiks sa svih 57 fajlova (Node skripta, regex zamena `mx-auto max-w-\w+ p-6` → `p-6`), sadržaj svakog ekrana sad direktno popunjava `main` (90%) bez sopstvenog ograničenja. **Provera:** `tsc --noEmit` čist; uživo kroz pravi login — `/crm`, `/katalog`, `/finansije`, `/pomoc`, `/email`, `/nadzor` svi vraćaju HTTP 200; HTML potvrđuje da `main` (klasa `w-[90%]`) sad direktno sadrži `<div class="p-6">` bez `max-w-*` (jedini preostali `max-w-` pogodak na stranici je nepovezan `max-w-[200px]` na tabu u traci tabova). Vizuelni izgled i dalje nije potvrđen u pravom browser-u.

**Verzija:** 1.53 — Semantička "upozorenje" boja ispravljena DRUGI PUT, promenjena porodica (21.8.2026, na zahtev vlasnika: "nista se nije promenilo", uz nova dva snimka posle osvežavanja — v1.52 prigušeni zlatno-braon amber je i dalje vizuelno čitao kao "žuto"). Umesto dalje kalibracije unutar iste amber porodice, promenjena porodica boje — ponuđene tri opcije kroz `AskUserQuestion` (narandžasto-riđa, maslinasto-zlatna bliža `--accent`, potpuno neutralna bez boje pozadine), vlasnik izabrao **maslinasto-zlatnu**: ista porodica kao jedinstvena akcentna boja (`#8A8A5E`), samo tamnija/zasićenija nijansa. Finalne vrednosti u `globals.css`, sve četiri palete-bloka: svetli `--warn: #6b6b1f` / `--warn-bg: #eeeedc`, tamni `--warn: #c2c26a` / `--warn-bg: #2a2a12` — PROVERENE WCAG 2.1 formulom (4.78:1–7.80:1 zavisno od para, sve iznad 4.5:1 praga, §2a). Puno obrazloženje i tabela: `docs/analize/29-DIZAJN-SISTEM-UI.md` §2.0c. **Provera:** `tsc --noEmit` čist; uživo kroz pravi login — `/` vraća HTTP 200; iskompajliran CSS potvrđuje nove vrednosti u sva četiri bloka (`:root`, tamni media-query, `[data-theme='dark']`, `[data-theme='light']`). Vizuelni izgled i dalje nije potvrđen u pravom browser-u — čeka treću potvrdu vlasnika.

**Verzija:** 1.52 — Semantička "upozorenje" boja ispravljena (21.8.2026, na zahtev vlasnika uz snimak dashboard kartica: "ukloniti ove zute boje koje sam oznacio, ne uklapaju se ni u jedan mod. Dajte odgovarajuce boje"). `--warn`/`--warn-bg` (`globals.css`, deljen token, 51 mesto kroz 34 ekrana panela) zamenjeni prigušenijim zlatno-braon tonom u sve četiri palete-blokove; usput otkriven i ispravljen stvaran WCAG propust (tekst na sopstvenoj pill pozadini u svetlom modu bio je `3.82:1`, ispod 4.5:1 praga, §2a — nije uočeno ranije jer se merilo samo protiv `bg`/`panel`, ne i protiv `warn-bg`). Pune izmerene vrednosti i obrazloženje: `docs/analize/29-DIZAJN-SISTEM-UI.md` §2.0c. **Provera:** `tsc --noEmit` čist; uživo kroz pravi login — `/` vraća HTTP 200; iskompajliran CSS potvrđuje nove `--warn`/`--warn-bg` vrednosti u sva četiri bloka (`:root`, tamni media-query, `[data-theme='dark']`, `[data-theme='light']`), stare vrednosti se više ne pojavljuju kao stvarna CSS deklaracija (samo u komentaru koji ih pominje kao istoriju). Vizuelni izgled i dalje nije potvrđen u pravom browser-u.

**Verzija:** 1.51 — Ime/uloga uklonjeni iz gornje trake (21.8.2026, na zahtev vlasnika: "uklonio bih iz gornje trake naziv naloga i ulogu jer to opise u donjoj traci"). `TopBar.tsx` je prikazivao `{fullName} · {roles}` — dupliralo je ono što `StatusBar.tsx` (donja traka, §5d) već prikazuje na levom kraju. Uklonjeno iz `TopBar`, `fullName`/`roles` props u potpunosti uklonjeni iz njegovog interfejsa (nisu ostavljeni neiskorišćeni); `Shell.tsx` i dalje prosleđuje `fullName`/`roles` samo `StatusBar`-u, koji ostaje jedini izvor te informacije u školjci. **Provera:** `tsc --noEmit` čist; uživo kroz pravi login — `/` vraća HTTP 200; iskompajliran HTML potvrđuje da `<header>` više NE sadrži "Nenad Tomić"/"VLASNIK", dok `<footer>` i dalje sadrži oboje. Vizuelni izgled i dalje nije potvrđen u pravom browser-u.

**Verzija:** 1.50 — Okvir chata ispravljen posle snimka ekrana (21.8.2026, na zahtev vlasnika: "Pogledajte sliku ovako treba da bude oivicen chat. Linija ne treba da ide u unutrasnjost panela"). v1.49 (`border-x` na `Shell.tsx` omotaču oko celog `AiChatBox`) je razvlačio bočne linije kroz CEO blok, uključujući istoriju razgovora iznad polja za unos — nije bilo traženo, snimak jasno pokazuje okvir SAMO oko reda za unos + reda brzih prečica (donja dva reda). POVUČENO: `border-x` uklonjen sa `Shell.tsx` omotača. `AiChatBox.tsx` sad sam crta pun okvir (`border border-ink-faint`) oko novog omotača koji sadrži TAČNO ta dva reda (istorija razgovora i kontekst-čip ostaju van tog omotača, bez okvira). Unutrašnja linija između reda za unos i reda prečica ostaje `border-t border-ink-faint` (razdvaja ih iznutra, ne dupli spoljni okvir). **Provera:** `tsc --noEmit` čist; uživo kroz pravi login — `/` vraća HTTP 200; iskompajliran HTML potvrđuje `<div class="flex-shrink-0 border border-ink-faint">` koji sadrži tačno red za unos i red prečica, istorija razgovora je van njega. Vizuelni izgled i dalje nije potvrđen u pravom browser-u.

**Verzija:** 1.49 — Bočne linije AI chat bloka (21.8.2026, na zahtev vlasnika: "nema bocnih linija chata", posle v1.47 koji je pojačao samo `border-t` linije unutar `AiChatBox.tsx`). `Shell.tsx` omotač oko `AiChatBox` (`w-[72%]` blok) dobija `border-x border-ink-faint` — isti ton kao gornje `border-t` linije, ceo blok sad vidljivo uokviren sa leve i desne strane. **Provera:** `tsc --noEmit` čist; uživo kroz pravi login — `/` vraća HTTP 200; HTML potvrđuje `border-x border-ink-faint` na omotaču. Vizuelni izgled i dalje nije potvrđen u pravom browser-u.

**Verzija:** 1.48 — Traka tabova preseljena u gornju traku (21.8.2026, na zahtev vlasnika, potvrđeno kroz `AskUserQuestion`: "Tabove stavite i gornju traku izmedju ikona i pretrage i neka budu sirine trake" → izabrana opcija "tabovi unutar gornje trake"). Ranije poseban red ispod `TopBar` (§5a, v1.6+) — sad `TabBar` živi UNUTAR `TopBar.tsx`, tačno na mestu gde je ranije bio prazan `flex-1` razmak između grupnih ikonica (levo) i dugmeta za pretragu (`Ctrl K`, desno); popunjava tu širinu (`min-w-0 flex-1`), horizontalno skroluje kad ima previše tabova (nepromenjeno, `overflow-x-auto`). Poseban `<TabBar />` red u `Shell.tsx` ukinut. `TabBar.tsx` unutrašnji omotač izgubio sopstvenu visinu/pozadinu (`h-[43px] bg-panel-2` → `h-full`) jer sad nasleđuje oboje od `TopBar` header-a u kom je ugnježden. Tabovi (flush oblik bez razmaka/zaobljenosti, v1.45 deo 2) i linija ikonica ostaju nepromenjeni. **Provera:** `tsc --noEmit` čist; uživo kroz pravi login — `/` vraća HTTP 200; sadržaj `<header>...</header>` elementa (jedan DOM element, potvrđeno parsiranjem HTML-a) sadrži i tab labelu ("Agent Inbox") i dugme za pretragu ("Ctrl K") — tabovi su stvarno unutar gornje trake, ne pretpostavka. Vizuelni izgled i dalje nije potvrđen u pravom browser-u.

**Verzija:** 1.47 — Linije AI chat trake pojačane (21.8.2026, na zahtev vlasnika uz snimak: "pojacajte boji linija chata jedva se vide u light modu, u dark modu neka budu jos svetlije"). `AiChatBox.tsx` — dve `border-t` linije (iznad reda za unos, iznad reda brzih prečica) i okvir pilula brzih prečica su koristili `border-border` (`--border`, namenjen blagim unutrašnjim razdelnicima — `#f0f1f2` svetli, praktično nevidljivo na belu pozadinu). Zamenjeno postojećim `ink.faint` tokenom (`border-ink-faint`, `--text-faint`) — bez novog CSS tokena: u svetlom modu `#606060` (već prolazi 4.5:1 tekst-kontrast, daleko iznad 3:1 praga za granice, §2a), u tamnom modu `#9bb0bd`, SVETLIJI od dotadašnjeg `--border` (`#3b4a51`) — tačan smer koji je vlasnik tražio u oba moda. **Provera:** `tsc --noEmit` čist; uživo kroz pravi login — `/` vraća HTTP 200; iskompajliran CSS sadrži `.border-ink-faint { border-color: var(--text-faint); }` (potvrđeno da Tailwind stvarno generisao klasu, ne pretpostavka — poznat rizik iz gotche 1.5/ranijih `bg-panel/60` bagova u ovom modulu). Vizuelni izgled i dalje nije potvrđen u pravom browser-u.

**Verzija:** 1.46 — Povučen deo v1.45 (21.8.2026, drugi krug istog dana, na zahtev vlasnika uz nov snimak ekrana: "u centralnom delu sve treba da bude jedna boja"). v1.45 je dodao `bg-panel-2` na omotač oko `TabBar`+sadržaja da margina oko `w-[90%]` postane vidljiva — na snimku je to ispalo kao vidljiva "kutija"/zakrpa dva tona oko sadržaja, suprotno od traženog. Uklonjen `bg-panel-2` sa tog omotača — nasleđuje `bg-bg` (namerno ista vrednost kao `bg-panel`), cela centralna kolona je opet jedna boja. `w-[90%]` na `main` ostaje u layout-u nepromenjen (i dalje stvarno 90%, samo se više ne ističe bojom — potvrđeno grep-om HTML-a da klasa i dalje postoji). Tabovi (v1.45 deo 2, flush oblik bez razmaka/zaobljenosti) ostaju nepromenjeni, nisu deo ove ispravke. **Provera:** `tsc --noEmit` čist; uživo kroz pravi login — `/` vraća HTTP 200; iskompajliran HTML potvrđuje da omotač više nema `bg-panel-2` (`class="flex flex-1 flex-col overflow-hidden"` bez dodatka). Vizuelni izgled i dalje nije potvrđen u pravom browser-u.

**Verzija:** 1.45 — Dve ispravke posle uživo pregleda snimka ekrana (21.8.2026, vlasnik: "Prikz u centralnom panelu nije na 90%... Tabovi rekao sam treba da izgledau kao u Vs Code", uz priložen snimak i ponovljen VS Code referentni snimak):
1. **90% širina sadržaja bila je nevidljiva, ne neprimenjena.** `--bg` i `--panel` su NAMERNO ista boja u oba moda (v1.38 paleta) — omotač oko `main` (`Shell.tsx`) je nasleđivao `bg-bg` sa spoljnog wrapper-a, pa je 5%-margina oko `w-[90%]` sadržaja bila ista boja kao sam sadržaj, dakle nevidljiva iako je CSS pravilo ispravno primenjeno (potvrđeno grep-om iskompajliranog HTML-a: `w-[90%]` je uvek bilo prisutno, i u v1.44). Ispravljeno dodavanjem `bg-panel-2` na omotač koji drži `TabBar`+sadržaj — margine se sad vide kao traka drugog tona, ista `panel-2` porodica kao `TabBar` iznad.
2. **Tabovi vraćeni sa "tag"/pilula oblika (v1.43) na pravi VS Code oblik.** v1.43 ("tabovi kao tagovi", `rounded-t-md` + `gap-0.5` razmak) je, uz stvaran VS Code snimak kao referencu, i dalje ličilo na plutajuće pilule, ne na flush VS Code trake. Uklonjeno: `gap-0.5` (nema razmaka između tabova), `rounded-t-md` (bez zaobljenosti). Dodato: `border-r border-border` kao tanak vertikalni razdvajač između susednih tabova (jedina linija koju pravi VS Code ima unutar trake tabova). Aktivan tab i dalje razlikovan isključivo `bg-panel` pozadinom (stapa se sa sadržajem ispod) + `border-t-2 border-t-accent` trakom na vrhu — princip "blago osenčen" iz v1.43 ostaje, samo bez tag-oblika. `+`-dugme dobija `ml-1` da ne bude zalepljeno za poslednji tab pošto je razmak trake uklonjen. **Provera:** `tsc --noEmit` čist; uživo kroz pravi login — `/katalog` vraća HTTP 200; iskompajliran HTML potvrđuje omotač sa `bg-panel-2`, tab klase bez `rounded-t-md`/`gap-0.5` i sa `border-r border-t-2 border-border`. Vizuelni izgled i dalje nije potvrđen u pravom browser-u — nema pravog browser-a u ovom okruženju, čeka potvrdu vlasnika.

**Verzija:** 1.44 — v1.42 ("svaka traka svoju liniju") POVUČENO, zamenjeno tonskim razdvajanjem (21.8.2026, na zahtev vlasnika, uz stvaran VS Code snimak ekrana kao referencu: "ne sviđa mi se [prethodni prolaz]... uklonite linije oko traka i panela, neka razdvajanje bude različitim tonovima boje"). Pravi VS Code nema linije NI razmak između susednih zona (aktivnosna traka/bočna traka/traka tabova/statusna traka se dodiruju bez ijedne granice) — razdvajanje ide isključivo preko dva ponovljena tona: `bg-panel-2` (hrom — TopBar/Sidebar/TabBar/StatusBar/RightPanel) naspram `bg-panel` (sadržaj — glavni panel, aktivan tab, AiChatBox). `border-frame`/`gap-1.5`/`p-1.5`/`rounded-lg` iz v1.42 uklonjeni sa svih sedam zona; `--frame-border` token (globals.css) i `frame` Tailwind boja (tailwind.config.ts) obrisani jer više nemaju upotrebu — nema mrtvog koda. Interni razdelnici UNUTAR jednog panela (npr. RightPanel header/footer linija, AiChatBox razdelnik pre input reda) vraćeni na generički `border-border` (nisu "linija između dva panela", nego strukturni razdelnik unutar jednog) — ostaju, nisu deo pritužbe. `ResizablePane` ručka i dalje bez trajno vidljive linije (nepromenjeno iz v1.42, već je bila tonska/hover-only, ne kosi se sa ovom ispravkom). **Provera:** `tsc --noEmit` čist; uživo kroz pravi login — `/`, `/crm`, `/znanje`, `/chat` vraćaju HTTP 200; kompajliran CSS (`/_next/static/css/app/layout.css`) sadrži NULA pojavljivanja `frame-border`/`border-frame` (potvrđeno grep-om nad isporučenim bundle-om, ne pretpostavkom da je uklanjanje uspelo), `--icon-line` i dalje prisutan (nepromenjeno, to nije bila pritužba). Vizuelni izgled i dalje nije potvrđen — nema pravog browser-a u ovom okruženju.

**Verzija:** 1.43 — Tabovi kao tagovi, aktivan blago osenčen (21.8.2026, na zahtev vlasnika: "tabovi treba da izgledaju kao tagovi isto kao u VS Code i onaj aktivan da bude blago osenčen"). Ranija v1.39 verzija je davala aktivnom tabu pun okvir sa tri strane (`border border-b-0 border-frame`) — izgledalo je previše "uokvireno" za VS Code stil. Uklonjen taj okvir; aktivan tab se sad razlikuje samo blagom senkom pozadine (`bg-panel` naspram `bg-panel-2` trake ispod) + tanka obojena traka na vrhu (`border-t-2 border-t-accent`, zadržano iz v1.39). Zaobljenost smanjena na `rounded-t-md` (manji radijus, "tag" oblik, ne `rounded-t` kao ranije). Neaktivni tabovi dobijaju istu blagu senku na hover (`hover:bg-panel`). **Napomena o promašenom pokušaju**: prvo je isprobano `hover:bg-panel/60` (providna verzija iste senke) — Tailwind tiho NIJE generisao tu klasu jer ne ume da izračuna opacity modifikator nad CSS promenljivom bojom (`var(--panel)` nije u `rgb(var(...) / <alpha>)` obliku koji to zahteva); otkriveno tek čitanjem iskompajliranog CSS-a, ne pretpostavkom da radi. Rešeno punom `bg-panel` bojom (ista kao aktivan tab) — accent traka na vrhu ostaje jedini pouzdan signal koji tab je STVARNO aktivan nasuprot samo-hoverovanog. **Provera:** `tsc --noEmit` čist; uživo kroz pravi login — `/`, `/crm` vraćaju HTTP 200; kompajliran CSS sadrži `.rounded-t-md` i `.hover\:bg-panel:hover`. Vizuelni izgled nije potvrđen — nema pravog browser-a u ovom okruženju.

**Verzija:** 1.42 — Svaka traka/panel dobija sopstvenu ivicu, ne deljenu (21.8.2026, na zahtev vlasnika: "svaka traka i svaki panel imaju svoje linije, ne dele je... postoji milimetarski prazan prostor između, ivice su blago zaobljene" — referenca VS Code). Ranije su susedne zone delile jednu liniju (npr. `TopBar` `border-b`, `TabBar` odmah ispod bez sopstvene gornje ivice) — sad je svaka od sedam strukturnih zona (`TopBar`, `Sidebar`, `TabBar`, sadržaj/`main`, `AiChatBox`, `RightPanel`, `StatusBar`) sopstvena `rounded-lg border border-frame` kutija. Razmak između njih: `gap-1.5` na sva tri nivoa ugnježdenih flex kontejnera u `Shell.tsx` + `p-1.5` na spoljnom omotaču (i uz ivicu prozora, ne samo između zona) — `bg-bg` pozadina se vidi kroz taj razmak. `ResizablePane.tsx` ručka za prevlačenje (bočna traka + desni panel) više nema trajno vidljivu liniju (`border-frame` uklonjeno iz mirnog stanja) — sad je providna dok se ne pređe mišem/prevlači (`hover:border-accent`/`border-accent` pri prevlačenju), jer stalna linija tu više nije potrebna (razmak je sam po sebi granica) i bila bi u sukobu sa "ne dele je" pravilom. `Sidebar`/`RightPanel` dobijaju `overflow-y-auto`/`overflow-hidden` da sadržaj ne probije zaobljene ivice. **Provera:** `tsc --noEmit` čist; uživo kroz pravi login — `/`, `/crm`, `/znanje`, `/chat` vraćaju HTTP 200; `GET /` HTML sadrži 5 pojavljivanja `rounded-lg border border-frame` (TopBar/Sidebar/TabBar/main/StatusBar — RightPanel nije prisutan dok nije otvoren) i 6 pojavljivanja `gap-1.5`. Vizuelni izgled (da li razmak/zaobljenost stvarno izgledaju kao VS Code referenca) nije potvrđen — nema pravog browser-a u ovom okruženju.

**Verzija:** 1.41 — Šest povezanih vizuelnih dopuna (21.8.2026, na zahtev vlasnika, jedan prolaz):
1. **Debljina linije ikonica -10% dodatno** (0.3px→0.27px `text-stroke`, treći uzastopni korak istog dana).
2. **Boja ikonica "navy teget" u svetlom modu** — nov `--icon-line` token (`#1f3a5f` u svetlom, `currentColor`→`inherit` u tamnom, bez promene tamo). `.codicon` sad ima eksplicitnu `color`, što bi bez dodatne mere prekinulo postojeće hover/aktivno/semantičko bojenje ikonica (ikonica deteta sa sopstvenom `color` vrednošću ne nasleđuje više roditeljevu boju) — dodato 10 kompenzacionih selektora (`.text-accent .codicon`, `.hover\:text-danger:hover .codicon`, itd., popisano grep-om kroz sve trake) koji vraćaju `inherit` tačno tamo gde je bojenje već postojalo (aktivna grupa u TopBar-u, hover brisanja taba, otvoreno zvono...). Podrazumevano (`text-ink-faint`) stanje ikonice postaje navy, namerne promene ostaju netaknute.
3. **Sadržaj centralnog panela na 90% širine** (`Shell.tsx`, ranije `w-[56%]` deljeno sa chat poljem).
4. **AI chat polje 20% uže od sadržaja** (72% = 90%×0.8) — sadržaj i chat više NISU u istom omotaču (bili su namerno spojeni u ranijem prolazu, 19.8.2026) — sad se svaki centrira nezavisno (`mx-auto`).
5. **"Navy teget" linije koje uokviruju panele/trake u svetlom modu** — nov `--frame-border` token (isti `#1f3a5f`, u tamnom = `var(--border)`, bez promene), nova Tailwind klasa `border-frame`, primenjena na TopBar/StatusBar/TabBar donje-gornje ivice, `ResizablePane` ručku (leva traka + desni panel), `RightPanel` header/footer, AiChatBox okvir i unutrašnje razdelnike. Namerno odvojeno od generičkog `border-border` — taj token ostaje nepromenjen za tabele/kartice/liste u sadržaju (~40+ ekrana), da se ne zatamni ceo interfejs, samo strukturne trake.
6. **Aktivan tab obeležen kao u VS Code** — obojena traka na vrhu aktivnog taba (`border-t-2 border-t-accent`), pozadina se stapa sa sadržajem (`bg-panel`); neaktivni tabovi zadržavaju `border-t-2 border-t-transparent` da se visina reda ne pomera pri promeni aktivnog taba.

**Provera:** `tsc --noEmit` čist; uživo kroz pravi login — `/`, `/crm`, `/znanje`, `/chat` vraćaju HTTP 200; kompajliran CSS (`/_next/static/css/app/layout.css`) pročitan direktno i potvrđeno: `--icon-line`/`--frame-border` prisutni u sva tri bloka palete sa očekivanim vrednostima, `.codicon { color: var(--icon-line); ... }` i svih 10 kompenzacionih selektora prisutni, `.border-frame { border-color: var(--frame-border); }` prisutna; `GET /` HTML sadrži `w-[90%]` i `w-[72%]`. Vizuelni izgled (da li kompenzacija boje stvarno izgleda skladno na svakoj ikonici, da li je 90%/72% odnos vizuelno prijatan, izgled VS Code trake aktivnog taba) nije potvrđen — nema pravog browser-a u ovom okruženju.

**Verzija:** 1.40 — Ikonice -15%, debljina linije -15% (21.8.2026, na zahtev vlasnika, posle v1.39: "sada je ovo preveliko, smanjte za 15%, i stanjite za 15% širinu linija"). `.codicon` font-size 25px→21px, `text-stroke` 0.35px→0.3px (jedini izvor istine, `globals.css`); `TabBar.tsx` tri manja override-a skalirana istim korakom (16px→14px, 19px→16px, oba puta ×2 pojave). Kontejneri (dugmad/trake) **namerno nisu umanjeni** u ovom prolazu — traženo je manje ikonice, ne manje trake; ako se pokaže neusklađeno, sledeći odvojen zahtev. **Provera:** `tsc --noEmit` čist; uživo kroz pravi login — kompajliran CSS (`/_next/static/css/app/layout.css`, sveža verzija posle izmene) pročitan direktno i potvrđeno `font-size: 21px !important`, `text-stroke: 0.3px`, `.text-\[14px\]{font-size:14px !important}`, `.text-\[16px\]{font-size:16px !important}` — isti nivo provere kao ispravka bag-a u v1.39, ne samo da je izvor izmenjen.

**Verzija:** 1.39 — BAG ispravljen: v1.37 (ikonice +20%) faktički NIJE radila (21.8.2026, prijavio vlasnik uživo: "ne vidim da se veličina ikona uvećala, vidim da se uvećao prazan prostor između njih"). Uzrok: `@vscode/codicons/dist/codicon.css` postavlja veličinu na `.codicon[class*='codicon-']` (specifičnost 0,2,0), specifičnije od običnog `.codicon` pravila u `globals.css` (0,1,0) — vendor pravilo je pobeđivalo bez obzira na redosled u fajlu, ikonice su se cele sesije renderovale na vendor-ovih 16px dok su kontejneri oko njih (v1.37) stvarno rasli, otud utisak "veći razmak, ista veličina". Ovo znači da ni raniji "+30%" prolaz (19.8.2026) nikad nije stvarno radio — nije bio primećen do sad jer je ova +20% izmena bila dovoljno velika (16px→25px, ne 16px→21px) da razlika u praznom prostoru postane vidljiva. Ispravljeno sa `!important` na `.codicon` (pragmatičan override tuđe biblioteke); tri postojeća manja override-a u `TabBar.tsx` (dugme zatvaranja taba i +/zatvori-sve, namerno manje ikonice) prebačena na Tailwind `!` important modifikator (`!text-[16px]`, `!text-[19px]`) da i dalje pobede novi `!important` na `.codicon`. **Provera:** `tsc --noEmit` čist; uživo kroz pravi login — kompajliran CSS (`/_next/static/css/app/layout.css`) sadrži `.codicon { font-size: 25px !important; ... }` i `.\!text-\[16px\]`/`.\!text-\[19px\]` sa `!important`, potvrđeno direktnim čitanjem isporučenog fajla (ne samo pretpostavka). Stvaran vizuelni izgled u browseru i dalje nije potvrđen (nema pravog browser-a u ovom okruženju) — ali sada je barem dokazano da CSS pravilo koje bi trebalo da primeni veličinu stvarno postoji u isporučenom kodu, za razliku od prethodnog prolaza gde to nikad nije provereno na ovom nivou.

**Verzija:** 1.38 — `TabLink` na preostalim drill-down mestima (21.8.2026, zatvara stavku iz v1.36): `crm/ankete` (dva linka po redu — rezervacija i nalogodavac), `b2b/rabati` (subagent), `compliance` (rezervacija), `chat/dobavljaci` (`SupplierConversationRow.tsx` — otvara isti `ChatPanel` kao interni razgovori). Provereno da `ugovori`, `dobavljaci`, `izvestaji`, `nadzor`, `email/sanducad` **nemaju** [id] drill-down red (samo nav linkovi ka susednim sekcijama ili query-param filteri koji ne menjaju `pathname`) — nema šta da se menja na tim ekranima, ne propust. Ovim je pokriven svaki poznati drill-down red u panelu. **Provera:** `tsc --noEmit` čist; uživo kroz pravi login — sve 4 izmenjene stranice vraćaju HTTP 200.

**Verzija:** 1.37 — Ikonice +20% (na 21px iz prethodnog +30% prolaza, 19.8.2026 → sad 25px), debljina linije +10% (21.8.2026, na zahtev vlasnika). Jedini izvor istine ostaje `.codicon` u `apps/panel/src/app/globals.css` (font-size 21px→25px); debljina je aproksimirana preko `text-stroke: 0.35px` jer je Codicon jednotežinski font bez prave promenljive debljine linije (dokumentovano kao ograničenje direktno u CSS komentaru — zamena celog ikonskog seta bi bila prava alternativa, van obima ove izmene, novi element steka). Svi fiksni kontejneri ikonica u trakama skalirani +20% na istom mestu (TopBar/ThemeToggle/NotificationBell/InboxButton: 36px→43px; StatusBar/AiChatBox dugmad: 26px→31px; StatusBar traka: 24px→29px; TabBar traka: 36px→43px, dugme za zatvaranje taba: 21px→25px sa 13px→16px ikonicom, +/zatvori-sve: 31px→37px sa 16px→19px ikonicom; Sidebar dugmad: 24px→29px i 28px→34px, poravnavajuća kolona ikonica sekcije: 24px→29px; RightPanel traka: 36px→43px, dugme zatvaranja: 24px→29px). **Provera:** `tsc --noEmit` čist; uživo kroz pravi login — `/`, `/crm`, `/znanje`, `/chat` vraćaju HTTP 200. Vizuelni izgled (da li +20%/+10% stvarno izgleda skladno, da li `text-stroke` na malim veličinama izgleda zamućeno) nije potvrđen — nema pravog browser-a u ovom okruženju; ako rezultat ne odgovara očekivanju, vrednosti su na jednom mestu (globals.css + navedeni Tailwind arbitrary brojevi) za brzu doradu.

**Verzija:** 1.36 — `TabLink` primenjen na 10 dodatnih lista (21.8.2026, nastavak M17 školjke posle v1.35): `pomoc`, `podrska`, `marketing`, `znanje`, `crm/gosti`, `finansije` (dva mesta — rekonsilijacija i probijeni rokovi naplate, oba vode na `/rezervacije/[id]`), `ugovori-klijenti`, `chat`, `email`, `rezervacije/kalendar` — svaki drill-down red liste sad ostaje u istom tabu umesto da otvara nov (dizajn dok. §5a), isti obrazac kao Faza 1 (`crm`, `b2b`, `katalog`, `rezervacije/pretraga`). Namerno preskočeno u ovom prolazu: filter/paginacione veze koje ne menjaju `pathname` (npr. status taster u `ugovori-klijenti`, mesec/dan u `rezervacije/kalendar`) — `TabsContext.useRegisterTab` prati samo `pathname`, ne query string, pa te veze već ne otvaraju nov tab bez izmene. Preostalo (§8 dole): `email/sanducad` (nema drill-down reda, samo upravljačke kartice), `crm/ankete`, `b2b/rabati`, `compliance`, `ugovori`, `dobavljaci`, `izvestaji`, `nadzor` — svaki zahteva pojedinačnu proveru da li uopšte ima [id] drill-down pre nego što se `TabLink` primeni. **Provera:** `tsc --noEmit` čist; uživo kroz pravi login — svih 10 izmenjenih lista vraća HTTP 200 (nijedna nije pokvarena izmenom).

**Verzija:** 1.35 — "Zvono za obaveštenja" u gornjoj traci (21.8.2026, na zahtev vlasnika — nastavak iste stavke plana kao v1.34: dizajn dok. §5d, "Gornji desni deo gornje trake"). Nov `NotificationBell.tsx`, dva nova BFF-a (`apps/panel/src/app/api/chat/conversations/[id]/messages/route.ts`, `.../[id]/read/route.ts`, isti posrednik obrazac kao `/api/chat/conversations`). Prikazuje istoriju "Obaveštenja" M19 razgovora (isti izvor kao `NotificationStack.tsx` §5e toast kartice — bez novog backend mehanizma), tačka nepročitanog stanja izvedena poređenjem `lastMessage.sentAt`/`lastReadAt` (isti podatak koji `GET /chat/conversations` već vraća). Otvaranje gomile označava razgovor pročitanim. **Namerno uže od pune spec formulacije** (§5d pominje i "nova poruka u M19 chat-u" bilo kog razgovora i "M18 upozorenje" opštije) — ovaj prolaz pokriva isključivo sistemski "Obaveštenja" razgovor, isti obim kao već postojeće toast kartice; proširenje na svaki M19 razgovor ostaje otvoreno (poglavlje 8, nova stavka). **Provera:** `tsc --noEmit` čist; uživo kroz pravi login (`dev-login`) — `GET /api/chat/conversations` vraća "Obaveštenja" razgovor sa `lastReadAt: null` i postojećom (ranijom demo) porukom, `GET .../messages` vraća listu, `POST .../read` ažurira `lastReadAt` (potvrđeno ponovnim `GET /chat/conversations`). Vizuelni izgled (dropdown, tačka nepročitanog) nije potvrđen — nema pravog browser-a u ovom okruženju.

**Verzija:** 1.34 — Ikonica "Agent Inbox" sa brojem u gornjoj traci (21.8.2026, na zahtev vlasnika — nastavak M17 školjke Faze 1, dizajn dok. §5c, M15 spec poglavlje 6). Nov BFF `/api/ai-orchestration/inbox` (`TopBar.tsx` → `InboxButton`), poziva `GET /ai-orchestration/inbox` na 30s interval (isti obrazac kao `StatusBar.tsx` AI status), zbir `count` polja svih izvora prikazan kao bedž (99+ iznad tog broja). Bez `M15/agent-inbox/VIEW` dozvole (403) ikonica se ćutke ne prikazuje, isti princip kao AI status u donjoj traci. Agent Inbox nema sopstvenu rutu — isti agregovan prikaz kao kartica "Agent Inbox — čeka odobrenje" na Početnoj (M17 spec §5) — klik zato otvara Početnu kao nov tab, ne novu stranicu. Zatvara raniju otvorenu stavku iz "Van obima" (M17 školjka plan, avgust 2026): "M15 Agent Inbox još nema UI potrošača u panelu" — sad ima, kroz postojeću kartica na Početnoj + ovu ikonicu. **Provera:** `tsc --noEmit` čist; uživo kroz pravi login (`dev-login`) — `GET /api/ai-orchestration/inbox` vraća HTTP 200 sa stvarnim podacima (5 izvora, zbir 211 u trenutnom stanju baze, uglavnom M12 sadržaj na čekanju), `GET /` HTML sadrži `codicon-inbox` unutar novog dugmeta. Vizuelni izgled bedža (pozicija, boja) nije potvrđen — nema pravog browser-a u ovom okruženju.

**Verzija:** 1.33 — Bag ispravljen: iskačuća obaveštenja (§5e, v1.31) tiho nisu radila za baš PRVU ikad notifikaciju korisnika (21.8.2026, uživo potvrđeno od vlasnika u pravom browser-u posle ispravke — "Pojavilo se."). Uzrok: `NotificationStack.tsx` je tražio id "Obaveštenja" razgovora PRE otvaranja WebSocket konekcije i, ako razgovor još ne postoji (najčešći slučaj — nastaje lenjo tek pri prvom CRITICAL signalu za tog korisnika, M19 spec §5), tiho je odustajao od konekcije zauvek, bez greške vidljive korisniku. Ispravljeno: soket se sad UVEK otvara; ako id razgovora nije poznat pri konekciji, svaki dolazni `message.new` okida ponovnu proveru (razgovor je možda upravo nastao TOM porukom). **Provera:** `tsc --noEmit` čist; uživo, dva puta zaredom pravim `pg_notify` signalom (prvi put je otkrio problem — ništa se nije pojavilo u pravom browser-u iako je server obradio signal ispravno; drugi put, posle ispravke, potvrđeno od vlasnika da se kartica pojavila). Test podaci obrisani posle provere.

**Napomena (isti nalaz kao gore, ali backend infrastruktura, ne kod ove funkcije):** tokom istrage otkriven treći put u ovoj sesiji da zastareo `node dist/src/main` proces (produkcioni build, ne `nest start --watch`) drži port 3000 i sprečava da API server pokupi nove izmene — dodato kao zamka 5.9 (`33-ZAMKE-I-OBAVEZNE-PROVERE.md`), uzrok nije razjašnjen (mogući paralelni proces koji ga ponovo pokreće), samo simptom i ispravka.

**Verzija:** 1.32 — Bag ispravljen: "Hydration failed" greška prijavljena od vlasnika (21.8.2026) u pravom browser-u. Uzrok: `Shell.tsx` `sidebarCollapsed` state je čitao `localStorage` direktno u `useState(() => ...)` inicijalizatoru sa `typeof window === 'undefined'` granom — na SERVERU vraća `false` (nema `window`), ali na KLIJENTOVOM prvom renderu (koji React poredi sa server HTML-om pri hidrataciji) `window` postoji pa odmah čita sačuvanu vrednost — ako je korisnik ranije skupio traku (`localStorage` = "1"), prvi klijentski render (`true`) se ne slaže sa server HTML-om (`false`), React baca "Hydration failed". `ResizablePane.tsx` u ISTOM fajlu je već imao ispravan obrazac (fiksan podrazumevani `useState`, čitanje `localStorage` tek u `useEffect` POSLE hidratacije) — `sidebarCollapsed` prepravljen da prati isti obrazac. Pretražen ceo `apps/panel/src` za isti antipattern (`typeof window === 'undefined'` unutar `useState` inicijalizatora) — ovo je bio jedini slučaj. **Provera:** `tsc --noEmit` čist; uživo kroz pravi login — početna stranica i dalje vraća HTTP 200. Sama hidratacija (da greška stvarno nestaje u browser-u) nije potvrđena uživo — nema pravog browser-a u ovom okruženju, ispravka je zasnovana na tačnom poznavanju React hidratacionog pravila (server i prvi klijentski render moraju biti identični) i već postojećem, ispravnom obrascu u istom fajlu.

**Verzija:** 1.31 — Iskačuća obaveštenja implementirana (§5e, 21.8.2026, na zahtev vlasnika). Novi `NotificationStack.tsx`, uvek montiran u `Shell.tsx` (fiksna pozicija donji desni ugao, nezavisno od otvorenog taba): otvara istu `/ws/chat` WebSocket konekciju koju već koristi `ChatPanel.tsx` (nov BFF `/api/chat/conversations` da se pronađe id "Obaveštenja" razgovora, postojeći `/api/chat/ws-token` za token), sluša `message.new` filtrirano na taj razgovor. Ne nestaju same (samo ručni × ili "Zatvori sve" kad ima >2), gomila raste naviše, sažimanje u "+N još" posle 5, tanka traka boje po ozbiljnosti uz levu ivicu, link "Otvori razgovor" u novom tabu. Izvor podataka je **postojeći** mehanizam bez novog backend-a na M17 strani, tačno kako spec traži — ali je pri gradnji otkriven i ispravljen pravi bag u M19 koji je to sprečavao (vidi M19 spec v1.4, `InAppNotificationsService` nikad nije emitovao WS događaj, samo upisivao u bazu). **Provera:** `tsc --noEmit` čist u `apps/panel`; jedinični testovi M19 (5 test suite-ova, 40 testova) prolaze; pun uživo test kroz pravi `pg_notify` (simulira stvaran M18 CRITICAL signal) potvrđuje da poruka stigne do stvarnog `socket.io-client`-a u par sekundi, i da se ista konverzacija vidi preko `/api/chat/conversations` sa `name: "Obaveštenja"` — najjača živa provera u ovoj sesiji, ide do pravog WebSocket sloja, ne samo HTTP/SSR. Vizuelni izgled kartice (boje po ozbiljnosti, razmak, animacija) nije potvrđen — nema pravog browser-a u ovom okruženju. **Namerno van obima:** WARNING severity danas nikad ne stiže ovim putem (samo CRITICAL ima IN_APP dispatch, M19 spec §5) — kad WARNING dobije IN_APP isporuku, `NotificationStack.tsx` je već spreman (severity se čita iz teksta, `[CRITICAL]` prefiks).

**Verzija:** 1.30 — Desni panel dobija prvi stvaran sadržaj (21.8.2026, na zahtev vlasnika, uz razjašnjenje ranijeg nalaza iz v1.26): M5 selekcija pretrage (M5 spec §3.0e.3, v1.40) — `RightPanel.tsx` prikazuje izabrane stavke sa cenom/istekom/zbirom umesto opšteg placeholder-a, otvara se automatski čim prva stavka uđe u selekciju (jedini namerni auto-otvarač, `SelectionProvider.onFirstAdd`, Shell.tsx). Razjašnjava raniji nalaz "desni panel se sam otvara na katalogu" — ovo NIJE taj slučaj (katalog i dalje ne šalje ništa u desni panel), ali potvrđuje da je koncept "kontekstno izdvajanje po ekranu" (dizajn dok. §5b) stvarno nameravano ponašanje, samo do sada nigde ožičeno. Ostalih ~18 sekcija (uklj. katalog, CRM) i dalje vide samo prazan placeholder dok ne dobiju sopstven izvor sadržaja — ne prećutan propust, isti obrazac kao ranije "nijedan ekran još ne šalje sadržaj ovamo". **Provera:** `tsc --noEmit` čist; uživo kroz pravi login — pretraga vraća HTTP 200 sa "dodaj" dugmetom na stvarnim rezultatima. Klik/interakcija nije potvrđena (nema browser-a u ovom okruženju).

**Verzija:** 1.29 — Ikonica "Krstarenja" otključana (21.8.2026, na zahtev vlasnika) — CRUISE dodat u `ProductType` šemu (M5 spec v1.39), `SearchSidebarPanel.tsx` više ne prikazuje ovu ikonicu kao zaključanu, `types: ['CRUISE']` šalje pravi upit. **Provera:** `tsc --noEmit` čist; uživo kroz pravi login — `GET /rezervacije/pretraga?type=CRUISE` vraća HTTP 200, pošteno prazno stanje (nema seed CRUISE proizvoda), ne validacionu grešku.

**Verzija:** 1.28 — Deljena komponenta `ContentCard.tsx` (20.8.2026, na zahtev vlasnika, uz dva snimka ekrana VS Code MSSQL ekstenzije "What's new" panela) — konkretizuje dizajn dok. §6e (naslov + opcion bedž + opis + red akcija-sa-strelicom ILI vertikalna lista veza-sa-ikonicom). Vlasnik je izričito potvrdio (preko pitanja) da želi samo vizuelni obrazac kartice za opštu upotrebu kroz module — sama "šta je novo" funkcija (panel koji se sam otvara posle ažuriranja aplikacije) namerno NIJE uvedena u ovom prolazu, nema još mesta u specifikaciji niti odluke ko piše sadržaj/kada se okida. Primenjeno uživo na jedno stvarno mesto radi provere, ne kao izmišljen placeholder: "Audit log" kartica na Početnoj (`(app)/page.tsx`) zamenjena sa `ContentCard` (naslov + opis + akcija "Otvori audit log"). **Provera:** `tsc --noEmit` čist; uživo kroz pravi login (`dev-login` privremena ruta) — `GET /` HTML sadrži tekst akcije i `codicon-arrow-right` ikonicu unutar `rounded-lg border border-border bg-panel p-4 shadow-sm` kartice. Vizuelni izgled (razmak, bedž boje, hover) nije potvrđen (isto ograničenje kao ostatak ove faze — nema pravog browser-a u ovom okruženju).
v1.27 — Devet ikonica po tipu turističkog proizvoda u levom panelu (19.8.2026, na zahtev vlasnika — dizajn dok. §5b tabela, prethodno namerno odloženo u v1.22 "uzak prvi rez"). `SearchSidebarPanel.tsx` dobija red od 9 ikonica iznad postojećih Pretraga/Filteri sekcija — klik direktno navigira sa odgovarajućim `type` parametrom(ima), preko postojećih polja destinacije/datuma/okupacije iz trenutnog URL-a. **Otkriven pri implementaciji: API već podržava `type` kao NIZ** (`SearchQueryDto.type: ProductType[]`, M5 spec §11, "dopuna avgust 2026") — "Things to do" (EXCURSION+EVENT+TICKET) šalje sve tri vrednosti u JEDNOM pozivu (`type=EXCURSION&type=EVENT&type=TICKET`), bez tri odvojena poziva ili post-filtera na klijentu. `page.tsx` ažuriran da čita `searchParams.type` kao `string | string[]`. **Dva od devet ikonica su zaključana, sa objašnjenjem u naslovu (isti princip kao zaključane sekcije, poglavlje 3):** "Individualni paketi" (čeka Itinerary tok, M5 spec §3.0d.5, nije još izgrađen) i **"Krstarenja" — otkriven nesklad pri implementaciji: `CRUISE` ne postoji u `ProductType` enumu (`schema.prisma`)**, iako dizajn dokument §5b tvrdi da je "dodat 17.8.2026, M2 poglavlje 2.1/2.3" — ta šema izmena se nikad nije stvarno desila. Nije ispravljeno u ovom prolazu (dodavanje enum vrednosti je Prisma migracija, traži potvrdu vlasnika po CLAUDE.md pravilu o šemi) — upisano ovde i u M5 "Otvoreno za dalje" (ispod) da se ne izgubi. "Rent-a-car" (TRANSPORT) nema podfilter po `transport_mode` na API-ju — ikonica šalje ceo `TRANSPORT` tip, može vratiti i ne-rent-a-car prevoz dok M5 ne dobije taj filter. **Provera:** `tsc --noEmit` čist; uživo kroz pravi login+MFA tok — `/rezervacije/pretraga` HTML sadrži red ikonica i naslov objašnjenja za zaključanu stavku; `?type=EXCURSION&type=EVENT&type=TICKET` vraća HTTP 200 bez greške kroz pravu API rutu. Vizuelni izgled/klik nisu potvrđeni (isto ograničenje kao ostatak ove faze).

**Verzija:** 1.26 — Tri dalje ispravke (19.8.2026, na zahtev vlasnika): (1) Deljena širina sadržaja/chat-a smanjena za 20% (`w-[56%]`, bilo `w-[70%]`). (2) Kolabovana leva traka sad prikazuje ikonice trenutno aktivne sekcije (ili cele grupe ako ništa nije izabrano) umesto prazne trake — `ResizablePane.tsx` dobio `collapsed`/`collapsedWidth` propove (uvek montiran, širina se forsira na 40px umesto uslovnog swap-a između bare dugmeta i punog panela), `Sidebar.tsx` dobio sopstveni `collapsed` prikaz koji ponovo koristi već izračunat `sectionItems`/`selected`. (3) Odgovor AI chat-a se ispisuje reč-po-reč (nov `TypewriterText` u `AiChatBox.tsx`, na zahtev vlasnika — "kao u AI pretrazi u Chrome ili VS Code-u") — **čisto vizuelna animacija otkrivanja već primljenog teksta**, ne prava postepena generacija (M15 omnisearch i dalje vraća ceo odgovor u jednom pozivu, pravi streaming ostaje van obima, §6c.3); poštuje `prefers-reduced-motion`. **Neistraženo/otvoreno:** vlasnik je prijavio da se desni panel sam otvara i ne može da se zatvori na `/katalog` — pregledan kompletan kod (`Shell.tsx`/`TopBar.tsx`/`RightPanel.tsx`), `rightPanelOpen` je obično `useState(false)` menjano isključivo klikom na dugme, nije nađen nijedan automatski okidač niti druga referenca na taj state u kodu. Nije reprodukovano niti ispravljeno u ovom prolazu — čeka dalji opis/snimak da se locira uzrok, ne prećutan kao rešen. **Namerno van obima ovog prolaza** (vlasnik je tražio "trakasti prikaz pored kartičnog za sve module u centralnom panelu") — ovo bi zahtevalo prekidač kartice/redovi i po jedan red-prikaz dizajniran za SVAKI od ~15 modularnih ekrana (svaki ima drugačiji oblik podataka), prevelik i heterogen zahvat za jedan prolaz bez prioritizacije; trenutno kartice/redovi postoje samo na `/rezervacije/pretraga` (v1.22, po tipu proizvoda). Čeka listu prioritetnih ekrana od vlasnika. **Provera:** `tsc --noEmit` čist; uživo kroz pravi login+MFA tok — `/katalog` HTML potvrđuje `w-[56%]` i dugme za skupljanje trake. Vizuelni izgled/klik (kolaps, ispisivanje teksta, desni panel bug) nisu potvrđeni (isto ograničenje kao ostatak ove faze).

**Verzija:** 1.25 — Pet odvojenih izmena na zahtev vlasnika (19.8.2026): (1) Sadržaj i AI chat traka sad dele JEDAN zajednički `w-[70%] mx-auto` omotač u `Shell.tsx` umesto dva odvojena — garantovano ista širina ("prikaz na širinu chata"), ne dva mesta koja bi mogla vremenom da se razjednače. (2) Chat traka dobija pun okvir (`border-2 rounded-lg shadow-sm`) umesto same gornje linije koja se nije videla. (3) Desni panel **vraćen** (dugme u `TopBar.tsx`, `RightPanel.tsx` ponovo postoji) — pojavljuje se prema potrebi (podrazumevano zatvoren), NE nosi više AI chat (taj je od v1.23 trajno u centralnom panelu) — sadržaj je iskren placeholder ("klikni na red liste... ili otvori pun zapis") jer nijedan ekran još ne šalje sažetak/"Povezano" u ovaj panel (dizajn dok. §5b), to ostaje sledeći korak. (4) Leva traka se sad može skupiti na tanku traku od 24px (VS Code obrazac) — dugme u vrhu `Sidebar.tsx`, stanje u `Shell.tsx` (`tt-panel-sidebar-collapsed` u `localStorage`, isti ad-hoc obrazac kao širina panela); kolabovana traka je samostalno dugme za proširivanje, ResizablePane se u tom stanju uopšte ne renderuje (ne skriveni sadržaj, stvarno drugi element). (5) `TabsContext.tsx` dobija `closeAllTabs()` — dugme u `TabBar.tsx` vidljivo tek kad ima više od 3 otvorena taba ("kad se otvori previše njih"), zatvara sve osim Početne. **Provera:** `tsc --noEmit` čist; uživo kroz pravi login+MFA tok — `/katalog` HTML sadrži dugme za skupljanje trake, dugme za desni panel i `border-2` na chat traci. Vizuelni izgled/klik (kolaps trake, otvaranje desnog panela, zatvaranje svih tabova) nisu potvrđeni (isto ograničenje kao ostatak ove faze).

**Verzija:** 1.24 — `w-[70%]` centriran (`mx-auto`), ne uz levu ivicu (19.8.2026, na zahtev vlasnika, ispravka istog dana kao v1.23) — vlasnik je posle live pogleda primetio da sadržaj uz levu ivicu ostavlja praznu trećinu ekrana bez ikakve svrhe otkad desni panel ne postoji (v1.23 ga je uklonio), pa izgleda polomljeno umesto namerno. `Shell.tsx` sad centrira i centralni sadržaj i chat traku istom `mx-auto`.

**Verzija:** 1.23 — Chat premešten iz desnog panela u centralni; 70% širina globalna; proširen `+` meni (19.8.2026, na zahtev vlasnika, ispravka posle v1.21/v1.22): (1) Prvi pokušaj (v1.21) je AI razgovor stavio i u desni panel (podrazumevano otvoren preko novog dugmeta) — vlasnik je posle probe eksplicitno rekao "u centralnom panelu, ne u desnom". `RightPanel.tsx` obrisan (nije imao drugu upotrebu, prazan bez chat-a), dugme "podeli ekran" uklonjeno iz `TopBar.tsx`, `rightPanelOpen` state uklonjen iz `Shell.tsx`. `AiChatBox.tsx` sad je TRAJAN deo centralnog panela — renderuje ga `Shell.tsx` kao poslednji red centralne kolone (ispod `<main>`, iznad `StatusBar`), ne pojedinačna stranica — vidljiv na svakom ekranu bez obzira koji modul je aktivan (nekadašnji `variant="inline"|"panel"` prop uklonjen, ostao je samo jedan kontekst upotrebe). Početna (`page.tsx`) vraćena na jednostavniju strukturu bez sopstvenog chat bloka. (2) `w-[70%]` sad postavljen na JEDNOM mestu — `Shell.tsx` omotava `{children}` unutar `<main>` — umesto po ekranu (v1.22 ga je stavio samo na `/rezervacije/pretraga`, sad uklonjeno odatle, dosledno za sve module). (3) `+` meni u `AiChatBox.tsx` proširen (dizajn dok. §6c.1, "razmislite šta još"): "Trenutno otvoren zapis" (naslov aktivnog taba, `useTabs()`) i "Rezultati trenutne pretrage" (kad je aktivan tab pretraga sa upitom) kao prilažljiv kontekst-čip; traka brzih linkova (Pretraga/CRM/Katalog/Podrška, `openTab`) ispod polja za unos. **Provera:** `tsc --noEmit` čist (uhvaćena i ispravljena tranzitorna sintaksna greška iz sopstvene izmene pre commit-a — Fast Refresh log to je pokazao, ne tiha greška); uživo kroz pravi login+MFA tok — tri različita modula (`/`, `/crm`, `/rezervacije/pretraga`) potvrđuju `w-[70%]`, prisustvo chat polja, odsustvo dugmeta za desni panel, prisustvo brzih linkova. Vizuelni izgled/klik nisu potvrđeni (isto ograničenje kao ostatak ove faze).

**Verzija:** 1.22 — Tri ispravke posle prve uživo probe vlasnika (19.8.2026): (1) Početna AI chat traka jače uokvirena (`rounded-xl border` umesto same `border-t`) i stvarno pribijena za dno VIDLJIVOG panela — `page.tsx` sad ima sopstveni `flex h-full flex-col` sa unutrašnjim scroll-om za kartice, ne oslanja se na `main` iz `Shell.tsx` (ranije: `sticky bottom-0` nije radio kad ima malo kartica, traka je jednostavno stajala posle njih sa praznim prostorom ispod). (2) Klaster od pet ikonica-pokretača u donjoj traci (v1.20) zamenjen jednom ikonicom "Poruke" sa iskačućim mini-panelom od pet stavki — vlasnikova ocena posle uživo pogleda da pet odvojenih ikonica "nije optimalno"; funkcionalnost identična (isti pet odredišta), samo manje zauzeća u traci. (3) `/rezervacije/pretraga` (M5) — otkriveno da je ekran ostao iz prvog prolaza (v1.6, pre nego što je dizajn dok. §5b/§6d napisan) sa CELOM formom direktno u centralnom panelu, bez ičega od filtera/kartica koje spec od 17-18.8.2026 traži. **Uzak prvi rez** (dogovoreno sa vlasnikom da pun §5b/§6d obim — 9 ikonica po tipu proizvoda, itinerar builder, sačuvani prikazi, grupisani filteri sa potpragovima — ostaje sledeći, odvojeni prolaz): forma za pretragu premeštena u levi panel (nov `SearchSidebarPanel.tsx`, prikazan u `Sidebar.tsx` samo kad je izabrana sekcija "pretraga"), uz jednostavan — ne grupisan — blok "Filteri" (cena od/do, dostupnost Odmah/Upit) u istom `<form>`-u. Centralni panel sad širine `w-[70%]` (na zahtev vlasnika), isključivo prikaz rezultata: kartice (slika/naziv/ponude/dugme "Dodaj") za ACCOMMODATION/PACKAGE/EXCURSION/EVENT/TICKET, kompaktni redovi za ostatak (FLIGHT/TRANSFER/TRANSPORT/INSURANCE) — tačno podela iz §6d. Filteri cena/dostupnost primenjeni **posle** poziva `GET /search` (M5 spec §11 ne podržava ta dva kao upitne parametre) — "Refundabilno/Nerefundabilno" brzi filter iz §6d namerno izostavljen, `cancellationPolicySummary` je slobodan tekst, nema strukturno polje da se pouzdano filtrira. `ResizablePane.tsx` (v1.21) sad nosi i ovu izmenu neproblematično jer je već bio proširen na `storageKey`/`handleSide`. **Provera:** `tsc --noEmit` čist; uživo kroz pravi login+MFA tok — `/rezervacije/pretraga` HTML sadrži `w-[70%]`, polje `destinationCountry` premešteno (potvrđeno odsustvo stare inline forme), sekcija "Filteri" prisutna, pretraga sa `type=ACCOMMODATION` vraća HTTP 200 bez greške; dev log bez upozorenja/grešaka. Vizuelni izgled kartica/redova i klik na filtere u pregledaču nisu potvrđeni (isto ograničenje kao ostatak ove faze).

**Verzija:** 1.21 — AI razgovor na dnu centralnog i desnog panela (19.8.2026, na zahtev vlasnika, treći nastavak "Faze 2"): dizajn dok. poglavlje 6c je opisivalo ovo od 18.8.2026 (v1.18 changelog ovde je najavio "Početna" AI chat polje), ali kod nikad nije napisan — provereno pre ovog prolaza, `page.tsx` nije imao ni traga od toga. Sada: nov `AiChatBox.tsx` (deljen, `variant="inline"|"panel"`), koristi postojeću `POST /api/omnisearch` rutu (M15 spec §9, ista koju koristi CommandPalette) — **jednokratan poziv po pitanju, bez memorije prethodnih poruka na serveru** (endpoint prima samo `query`, ne istoriju razgovora), prikazana "istorija" u komponenti je čisto vizuelna, ne pravi kontekstualni razgovor — pravi multi-turn zahteva izmenu M15 API-ja, van obima ovog prolaza. Postavljeno na dva mesta: (1) `(app)/page.tsx` (Početna) — `variant="inline"`, `sticky bottom-0` u okviru centralnog panela (§6c: "razgovor glavni sadržaj ekrana"); (2) nov `RightPanel.tsx` — `variant="panel"`, prvi put desni panel uopšte postoji u kodu (do sad potpuno van obima), otvara se/zatvara preko novog dugmeta u `TopBar.tsx` (§5d "dugme za podelu ekrana", ikonica `layout-sidebar-right`/`-off`). `ResizablePane.tsx` proširen sa `storageKey`/`handleSide` propovima (ranije hardkodovan na jedan `localStorage` ključ i ivicu — sad leva traka i desni panel imaju nezavisnu širinu i ručku na suprotnim ivicama). **Namerno van obima** (upisano da se ne izgubi): §6c.1 (`+`/`@` prilaganje konteksta), §6c.2 (slash komande, dugme "Zaustavi", istorija po zapisu, traka mode/dozvola), §6c.3 (pravi streaming — trenutno odgovor se pojavljuje odjednom kad stigne, ne postepeno; izvori kao pilule postoje u pojednostavljenom obliku — `matchedRoutes` kao klikabilni čipovi — ali bez tipa izvora/ikonice; predložena sledeća pitanja), sažetak zapisa/"Povezano" traka u desnom panelu (§5b, čeka ekran koji bi ga popunio). **Provera:** `tsc --noEmit` čist; uživo kroz pravi login+MFA tok (skripta van `apps/api`, zamka 5.6) — server-render Početne sadrži placeholder chat polja i naslov dugmeta za podelu ekrana; `POST /api/omnisearch` sa pravim pitanjem vraća HTTP 200 `{"active":true,...}` kroz istu rutu koju komponenta poziva. Vizuelni izgled, prevlačenje desnog panela i klik na dugme za podelu ekrana nisu potvrđeni (nema pravog pregledača u ovom okruženju, isto ograničenje kao ostatak Faze 1/2).

**Verzija:** 1.20 — Donja statusna traka i klaster pokretača (19.8.2026, na zahtev vlasnika, "Faza 2" nastavak posle Faze 1): nov `StatusBar.tsx`, prikačen ispod glavnog sadržaja u `Shell.tsx` (dizajn dok. §5d). Sadrži: nalog (ime+uloga, bez klika ka profilu — ekran profila ne postoji, van obima), status veze (izveden iz istog poziva kao AI status, bez posebnog `/health` endpoint-a), AI status po trenutnom modulu, sat sa vremenskom zonom, oznaka okruženja (`process.env.NODE_ENV`, inline u klijentskom bandlu), i klaster od pet ikonica-pokretača: Mejl/Interni chat (`openTab` ka `/email`/`/chat`, isti mehanizam kao levi meni) i WhatsApp/Viber/Telegram (spoljni `<a>` linkovi — `web.whatsapp.com`, `viber://`, `web.telegram.org` — bez ikakve veze sa Terminal bazom, čist prečac kao što dizajn dok. §5d traži). WhatsApp/Viber/Telegram dobijaju inline SVG brend-ikonice (nov `BrandIcon.tsx`, jednobojne, `currentColor`) umesto Codicon-a, koji nemaju logotipe spoljnih aplikacija — nije nova biblioteka, samo statičan markup, ne traži potvrdu vlasnika po pravilu o steku. Nova BFF ruta `apps/panel/src/app/api/module-activation/[code]/route.ts`, posrednik ka `GET /ai-orchestration/modules/:code/activation` (M15 spec §9), isti obrazac kao `api/omnisearch/route.ts`. **Bitan nalaz otkriven pri live-proveri:** M15 `ModuleAgentActivation` trenutno ima samo dva imenovana gate-a u bazi (`M15_OMNISEARCH`, `M15_WEB_RESEARCH`, M15 spec §3/§4) — **ne** poseban red po M-modulu (M2/M3/M5/...), jer pravi domenski agenti po modulu još nisu izgrađeni (M15 spec v1.10 napomena: "gradi ogradu za kad taj dan dođe, ne uvodi izvršioce"). Posledica: AI-status segment u donjoj traci je ispravno povezan na pravi endpoint i ispravno se sam sakriva (API vraća 404 "Nepoznat module_code", `StatusBar.tsx` to tretira kao "nema dozvolu/nema podatak" i ništa ne prikazuje — isti princip kao poglavlje 3, sekcija bez oslonca se izostavlja, ne prikazuje grešku) — ali **trenutno nikad neće ništa prikazati ni za jedan ekran**, dok M15 ne dobije prave redove po modulu. Nije bag, ne treba ispravljati sada — samo dokumentovan, poznat, privremen jaz između dizajna (§5d pretpostavlja da AI status po ekranu već postoji) i stvarnog stanja M15 podataka. **Provera:** `npm run build` čist; uživo kroz pravi M1 login+MFA tok (skripta van `apps/api`, da ne pokrene nodemon rebuild usred provere — nalaz iz ovog prolaza, upisan i u zamke) — server-render Početne potvrđuje `<footer>`, oznaku okruženja, WhatsApp link; `/katalog` HTTP 200; `GET /api/module-activation/M2` vraća 404 kroz pravu BFF rutu (očekivano ponašanje objašnjeno gore) — vizuelni izgled trake, hover stanja i klik na spoljne linkove nisu potvrđeni (nema pravog pregledača u ovom okruženju, isto ograničenje kao Faza 1). Test nalog ostaje aktivan na zahtev vlasnika (privremeni pregled bez kredencijala) — brisanje na naredni zahtev.

**Verzija:** 1.19 — "M17 školjka — Faza 1" implementirana (19.8.2026, na zahtev vlasnika): shell komponente (`Shell`/`TopBar`/`Sidebar`/`TabBar`/`TabsContext`/`CommandPalette`) usklađene sa dizajn dokumentom, koji je narastao mnogo dalje od shell-a pisanog uz v1.6 prvi prolaz. `Sidebar` prelazi sa ravne liste (20 stavki) na grupa→spisak→kolaps na sekciju (poglavlje 4a, dizajn dok. §5c) — nov `NAV_GROUPS` u `nav.ts`, ikonice usaglašene sa dizajn dok. §3a.1. Tabovi ispravljeni da prate §5a pravilo ("drill-down u zapis ostaje u istom tabu") — nov `navigateInTab`/`TabLink`, primenjeno na 4 reprezentativne liste (CRM/katalog/B2B/pretraga→ponuda) kao dokazan obrazac, ostatak sekcija čeka naredni prolaz (namerno, ne propust). Granica bočne trake postaje prevlačiva (`ResizablePane`, širina u `localStorage` — pravi `UserPreference`, M1 spec §3.9, još ne postoji u kodu, van obima ovog prolaza). Komandna paleta dobija "Nedavno otvoreno". Paleta (`globals.css`) prebačena sa stare amber/teal kombinacije na Horizont v2 (Material Theme tamni / Light 2026 svetli) sa jedinstvenom maslinastom akcentnom bojom (dizajn dok. §2.0a/§2.0b). Van obima (upisano u plan fajlu sesije, prenosi se u backlog): desni panel, donja statusna traka, obaveštenja, AI chat poboljšanja, filteri po sekciji, boja brenda/logo. **Provera:** `npm run build` čist; uživo (bez pravog pregledača u ovom okruženju — izričito nepotvrđeno vizuelno/interakciono, ne lažno tvrđeno) proveren pravi M1 login+MFA tok kroz panel BFF rute i server-render 7 stranica protiv prave baze — HTTP 200 svuda, 9 grupnih ikona i tačan grupa/sekcija raspored potvrđeni u stvarno vraćenom HTML-u, nula grešaka u oba dev log-a; test nalog obrisan posle provere. v1.18 — novo poglavlje 5a (18.8.2026, na zahtev vlasnika): "Početna"/prazan tab dobija istaknuto AI chat polje (dizajn dokument poglavlje 6c/6c.1/6c.2) kao glavni sadržaj centralnog panela u nultom stanju, uz postojeća agregirana upozorenja i nedavno otvoreno/sačuvani prikazi — isti sastav za M7 portal (identičan interfejs, poglavlje 2.0.6 M7 spec). v1.17 — poglavlje 4a (novo, 17.8.2026, na zahtev vlasnika): 17 sekcija grupisano u 8 kategorija + Početna za gornju traku (razrešava dizajn dokument poglavlje 5c dopunu); polja za pretragu/filtriranje po sekciji istražena direktno iz postojećeg koda (ne izmišljena) — mnoga API polja već postoje ali ekran ih ne koristi (M22/M23 čak filtriraju lokalno posle preuzimanja cele liste umesto pravog upita), neka ne postoje ni na API-ju. v1.16 — live-provera obeležavanja autora dovršena (17.8.2026): stavka izlaznog kriterijuma popunjena, uz dva nalaza ispravljena u istom prolazu — AI bedž je u svetlom modu padao WCAG AA (3.96:1, sad 5.98:1 preko `accent-strong` + ivica) i `/audit-log` je prikazivao "Invalid Date" jer je čitao `createdAt` umesto `timestamp`. Detalji u izlaznom kriterijumu ispod. v1.15 — dodato poglavlje 3.1 (obeležavanje autora radnje — čovek/AI agent/spoljni nalog) i prateća stavka izlaznog kriterijuma, na zahtev vlasnika (avgust 2026). Nalaz koji je do toga doveo: svaki od ekrana koji prikazuje autora do sad je izmišljao sopstveni način obeležavanja (M14 ima bedž "AI nacrt", M6/M12 svoje obrasce, audit log ispisuje sirovu enum vrednost `AI_AGENT`) — pravilo sada živi na jednom mestu, `docs/analize/29-DIZAJN-SISTEM-UI.md` poglavlje 6a, a ovo poglavlje samo obavezuje panel da ga poštuje. Prati ga dopuna M19 poglavlja 2.3/9.5 (`Message.drafted_by_ai`). Implementirano u istom prolazu (avgust 2026): `apps/panel/src/components/ActorLabel.tsx` povezan na osam ekrana, tri ranija lokalna bedža uklonjena; checkbox ostaje prazan do ručne live-provere u pretraživaču (kontrast/izgled u oba moda). v1.14 — zatvorena tri API nedostatka otkrivena u Faza 7 live-proveri (16.8.2026): M21 `GET /help/articles` sada prima opcioni `status` parametar (DRAFT/PENDING_APPROVAL/ARCHIVED za nosioca EDIT dozvole, ograničeno na segmente za koje je ima; bez parametra nepromenjeno ponašanje), `GET /help/articles/:id` vraća `translations` (pun niz) pored `translation` (rešen fallback); M22 `GET /email/threads` i `GET /email/threads/:id` uključuju `mailbox.address`/`mailbox.displayName` (isti scoping, prošireni payload); M23 dobio `POST /knowledge/articles/:id/research` za istraživanje nad postojećim člankom, sa opcionim `revisionId` da popuni postojeći `PENDING_REVIEW` placeholder (npr. `SCHEDULED_REFRESH`) umesto da pravi nov. Panel ekrani (`/pomoc`, `/pomoc/[id]`, `/email`, `/email/[threadId]`, `/znanje/[id]/revizije`) ažurirani da koriste nove parametre/polja umesto ranijih zaobilaženja. Detalji u poglavlju 7 ispod. v1.13 — Faza 7 live-provera dovršena (16.8.2026, vidi poglavlje 7 za pun nalaz): pravi M1 login+MFA tok, svih pet ekrana potvrđeno protiv prave baze (čitanje i pisanje). v1.12 — Faza 7 implementacija (avgust 2026, kod napisan i pušovan): ekrani za M18 (Operativni nadzor), M19 (Komunikaciona platforma), M21 (Centar za pomoć), M22 (Email/Inbox), M23 (Znanje) — `apps/panel/src/app/(app)/nadzor/`, `chat/`, `pomoc/`, `email/`, `znanje/`. M19 je prvi ekran u panelu koji koristi pravu WebSocket konekciju (`socket.io-client`, novi frontend paket, direktan par postojeće backend `socket.io` zavisnosti iz M19 backend prolaza — ne nov izbor steka) — token za WS rukovanje se ne izlaže sirov klijentskom JS-u, prosleđuje se preko `apps/panel/src/app/api/chat/ws-token/route.ts` (server-side BFF proxy, isti princip kao ostatak panel sesije). Detalji svakog ekrana i otkriveni API nedostaci (M21/M22/M23) navedeni u poglavlju 7 ispod. v1.11 — Faza 5/6 implementacija (avgust 2026): ekrani za M13 (Izveštavanje i BI) i M14 (Podrška/Helpdesk), zajedno kao Faza 5, i M12 (Marketing sadržaj), Faza 6 — `apps/panel/src/app/(app)/izvestaji/`, `podrska/`, `marketing/`. M13: `/izvestaji` je jedna stranica sa pet tabova (profitabilnost/prodaja/smeštaj/dinamički/marketing), svaki gejtovan sopstvenom `M13/report:*/VIEW` dozvolom (tab se u potpunosti uklanja bez dozvole, isti princip kao poglavlje 3) — zajednički filter perioda (`from`/`to`) plus filteri specifični za tab (destinacija/dobavljač/kanal/tip proizvoda/`group_by`), svaki izveštaj prikazuje `lastSyncedAt` (M13 spec §2); dugme "pokreni rekonsilijaciju" (`POST /bi/reconciliation/run`) gejtovano sa `report:profitability/VIEW` (isti krug uloga kao API kontroler, koji nema poseban ključ dozvole za tu akciju). M14: `/podrska` lista tiketa sa filterom po statusu/kategoriji, `/podrska/novi` unos tiketa u ime gosta/subagenta koji je zvao telefonom (`requesterType=STAFF_ON_BEHALF`, jedini put kreiranja sa ovog ekrana — M14 spec §2.1), `/podrska/[id]` detalj sa zakonskim rokom reklamacije (§3.1, vizuelno istaknut rok/eskalacija), nit poruka (nacrt AI_DRAFT sa dugmetom "pošalji", isti obrazac kao M6 CommunicationLogPanel iz Faze 3) i forma za status/prioritet/dodelu/`refundDecision` (§3.2 — okida M10 nacrt storno dokumenta, ne izvršava ga). M12: `/marketing` kalendar sadržaja (lista sortirana kao M12 API, §7) sa filterom tip/status, `/marketing/nov` ručno kreiranje (`generated_by=HUMAN` uvek kroz ovaj ekran), `/marketing/[id]` detalj sa prevodima (M12 §2.2, isti obrazac kao M2 ProductTranslation) i dugmetom "odobri" (`M12/content/APPROVE_PUBLISH`, nepovratna granica §3 korak 4 — sopstveno dugme, nikad deo druge forme, isti princip kao M7 RebateActions), vidljivo upozorenje kad `contains_ai_generated_media=true` (YUTA preporuka §3c); `/marketing/kanali` konfiguracija distribucionih kanala (§4, kredencijali se ne prikazuju/ne unose u čitljivom obliku preko ove forme). `nav.ts` sve tri stavke `implemented: true`. Sve provereno uživo protiv prave baze (ne samo `tsc`/`next build`): STAFF nalog sa VLASNIK ulogom kroz pravu M1 JWT/sesijsku šemu, sva tri ekrana i njihovi detalji učitani sa stvarnim podacima (profitabilnost/prodaja/smeštaj/dinamički/marketing izveštaji nad postojećom M13 projekcijom, tiket detalj sa porukama, sadržaj detalj sa prevodima); stvarne mutacije potvrđene protiv prave baze: `POST /bi/reconciliation/run`, `POST /helpdesk/tickets/:id/messages` (nova STAFF poruka), `POST /marketing/content/:id/approve` (PENDING_APPROVAL→PUBLISHED, `approved_by` popunjeno). v1.10 — Faza 4 implementacija (avgust 2026): ekrani za M7 (B2B modul/Subagenti), staff-facing deo — `apps/panel/src/app/(app)/b2b/`. `/b2b` lista `Subagent` (agencija vidi ceo lanac) sa nazivom firme dovučenim iz M6 (`Subagent` sam po sebi ne nosi naziv, samo `client_account_id`); `/b2b/novi` registruje novog Tier 1 subagenta — pošto M6 nema filter po `accountType`, ekran pretražuje nalogodavce po email/PIB (isti filter kao `/crm`) i suzi rezultat na `LEGAL_ENTITY` na nivou prikaza, ne novi M6 API; `/b2b/[id]` detalj — kreditni limit i trenutni dug (uživo izračunat), osnovna i efektivna provizija sa obimskim statusom, pragovi obima (kreiranje novog ranga; izmena/brisanje postojećeg namerno odloženo, nije bilo u izlaznom kriterijumu), mreža direktnih sub-subagenata **samo za uvid** (upravljanje njome je posao roditeljskog subagenta kroz sopstveni, još negrađeni portal — M7 spec §2.0.1/§3/§6 — van obima M17 po pravilu iz zadatka: "šta subagent radi sopstvenim identitetom ide u portal, šta zahteva ovlašćenje osoblja agencije ide u M17"), i rabati provizije te subagenta; `/b2b/rabati` agregira rabate svih subagenata (M7 API §11 nema globalnu listu, samo po subagentu — ovo je tanak BFF sloj dozvoljen spec poglavljem 2, ne nov M7 endpoint) filtrirane po statusu (DRAFT/APPROVED/APPLIED/REJECTED), sa eksplicitnim dugmetom "odobri rabat" kao sopstvenom, namernom radnjom (M15 registar klasifikuje `commission_rebate.apply` kao `PROPOSE_THEN_APPROVE`) — nikad deo druge forme. Odobrenje subagenta (`PENDING_APPROVAL → ACTIVE`, postavlja kreditni limit i, ako Tier 1, proviziju) je poseban ekran/forma na `/b2b/[id]`, isti princip "namerna kontrola rizika" kao M7 spec §9. `nav.ts` M7 stavka `implemented: true`. Otkriven i ispravljen pre-postojeći nalaz pri implementaciji: `apps/api/prisma/seed/seed.ts` nije dodeljivao Računovođa ulozi `M7/commission-rebate/VIEW`+`APPROVE` iako ih M7 spec §10 tabela eksplicitno navodi ("Vlasnik, Direktor, Računovođa") — dopunjeno i re-seedovano. Sve provereno uživo kroz pravu prijavu/MFA i pravu sesiju panela protiv stvarne baze (42 postojeća `Subagent` zapisa iz ranijih e2e prolaza): lista i detalj učitani sa stvarnim M6 nazivima; nov subagent registrovan preko stvarnog toka pretraga→registracija (novi `ClientAccount` kreiran preko prave `POST /crm/client-accounts`, zatim `POST /b2b/subagents`) i odmah vidljiv na listi/detalju; postojeći `PENDING_APPROVAL` subagent odobren kroz pravu Server Action formu (status→ACTIVE, kreditni limit/provizija upisani); izmena kreditnog limita/statusa potvrđena kroz pravu formu; prag obima kreiran; `DRAFT` rabat odobren kroz pravu Server Action formu (status DRAFT→APPROVED) i potvrđeno da je to sinhrono pokrenulo pravi M10 `KNJIZNO_ODOBRENJE` nacrt (M7 spec §3.2) — zapis pronađen u bazi sa ispravnim `relatedSubagentId`/`creditedRebateId`; odbijanje rabata potvrđeno protiv iste API rute koju forma poziva (status→REJECTED). Poglavlja 2.0.3 (omnisearch) i 2.0.4 (AI chat, `SubagentBookingRequest`/`SubagentChatMessage`) namerno izostavljena — M7 spec §12 ih eksplicitno označava kao "čeka M15", isti razlog kao ostatak modula. v1.9 — Faza 3 implementacija (avgust 2026): ekrani za M6 (Gosti i nalogodavci/CRM) — `apps/panel/src/app/(app)/crm/`. `/crm` lista `ClientAccount` (nalogodavci) sa pretragom po email/PIB i formom za novi zapis (matched na `CreateClientAccountDto`); `/crm/[id]` detalj — kontakt, status lojalnosti sa ručnom korekcijom, povezani gosti, prava istorija putovanja učitana iz M5, log komunikacije (AI nacrt poruke sme da postoji, ali "označi kao poslato" ostaje isključivo ljudska radnja preko `/mark-sent`, isti princip kao "Predloži pa čovek odobri" kroz ostatak specifikacije); `/crm/gosti` lista i `/crm/gosti/[id]` detalj `GuestProfile` (dokument, nacionalnost, datum rođenja, matched na `CreateGuestProfileDto`); `/crm/ankete` pregled anketa posle putovanja filtriranih po statusu. `rezervacije/[id]` dobija dodatnu M6 kompozicionu karticu (link ka nalogodavcu) pored postojećih M10/M11/M20 kartica iz Faze 2 — isti princip kompozicije, poglavlje 2. Nema ekrana za spajanje duplikata gostiju/nalogodavaca — provereno da M6 spec (poglavlja 1–11) nema takvu funkciju definisanu, nije bilo šta da se odloži. `nav.ts` M6 stavka `implemented: true`. Sve provereno uživo kroz pravu prijavu i sesiju panela (ne samo `tsc`/`next build`): stvaran gost kreiran preko `POST /crm/guest-profiles`, potvrđeno da se odmah pojavljuje na listi i sopstvenom detalju; lista nalogodavaca, detalj, i kompoziciona kartica na rezervaciji potvrđeni protiv prave baze (52 postojeća `ClientAccount` zapisa iz ranijih e2e prolaza). v1.8 — Faza 2 implementacija (avgust 2026): ekrani za M10 (Finansije), M11 (Compliance) i M20 (Ugovori sa klijentima) — `apps/panel/src/app/(app)/finansije/`, `compliance/`, `ugovori-klijenti/`. M10: pošto `FiscalDocumentsController` (spec §10) nema poseban "lista svih dokumenata" endpoint, "Finansije" dashboard sastavlja ono što M10 API stvarno izlaže kao liste (rekonsilijacija §5.3, obaveze prema dobavljačima §8, rokovi naplate §5.4), a konkretna radnja iz izlaznog kriterijuma ("Računovođa priprema i šalje fiskalni dokument") živi na stranici pojedinačne rezervacije (`rezervacije/[id]`, dugme "Pripremi/prikaži fiskalni dokument" → detalj dokumenta sa eksplicitnim dugmetom "Potvrdi i pošalji fakturu") — direktan primer kompozicije iz poglavlja 2 ovog dokumenta. M11: "Compliance" ekran (trenutna garancija, iskorišćenost, CIS registracije sa ručnim retry) — pri implementaciji otkriven i ispravljen pre-postojeći bag u M11 (vidi `08-SPECIFIKACIJA-M11-COMPLIANCE.md` promenu verzije): `GET /travel-guarantee/utilization` je vraćao 404 čim bilo koja ORGANIZATOR rezervacija u bazi ima valutu bez unetog `ExchangeRateSnapshot`-a. M20: "Ugovori sa klijentima" lista+detalj sa filterom po statusu i ručnim evidentiranjem prihvatanja/poništavanja (generisanje ostaje automatsko, §3.1 tog dokumenta — nema ručnog "generiši" dugmeta jer ga API ni ne izlaže). `nav.ts` sve tri stavke `implemented: true`; dashboard (`page.tsx`) usklađen sa stvarnim poljima `/travel-guarantee/utilization` odgovora (bio je `utilizedPercent`/`guaranteeExpiresAt`, ne postoje u odgovoru — ispravljeno na `utilizationPercent`/`guaranteeStatus`). Sve provereno uživo (prijava kroz stvaran M1 tok, kreiran/poslat pravi fiskalni dokument, zabeležena uplata, ponovljena CIS registracija) protiv stvarne baze, ne samo `tsc`/`next build`. v1.7 — omnisearch povezan na pravi M15 endpoint (avgust 2026): `POST /ai-orchestration/omnisearch` sada postoji u kodu (M15 prvi prolaz, `apps/api/src/modules/m15-ai-orkestracija/`), `CommandPalette.tsx` poziva ga preko `apps/panel/src/app/api/omnisearch/route.ts` kad upit sadrži tekst — prazan upit + Enter ostaje nepromenjen (čisto lokalna navigacija, §5.5). Ako `M15_OMNISEARCH` gate nije `ACTIVATED`, panel prikazuje smirenu poruku ("AI pretraga još nije uključena") umesto greške, uz i dalje ispravan lokalni navigacioni fallback. Glasovni unos (§5.5 dopuna) ostaje van obima ovog prolaza. v1.6 — prvi prolaz implementacije (avgust 2026): osnova panela + Faza 0/1 (`apps/panel/`) — prijava (M1, obavezna 2FA), komandna paleta i navigacija koje prate M1 model prava, dashboard sa agregiranim upozorenjima, M2 katalog, M3 dobavljači/ugovori, M5 pretraga/ponuda/potvrda/kalendar. Pri implementaciji otkriven i ispravljen pre-postojeći bag u M5 (vidi `06-SPECIFIKACIJA-M5-REZERVACIJE.md` promenu verzije) — `GET /search` nikad nije stvarno radio za `channel=INTERNAL_PANEL` jer `Product.visible_channels` strukturno ne može sadržati tu vrednost. Omnisearch (poglavlje 5.5) i glasovni unos ostaju bez prave AI pozadine dok M15 ne postoji — panel u ovom prolazu nudi samo lokalnu pretragu kroz navigaciju kao privremenu zamenu. PWA instalabilnost odložena za naredni prolaz; v1.5 — rešena stavka "izgled/UX van obima" (poglavlje 8) — sad upućuje na `docs/analize/29-DIZAJN-SISTEM-UI.md`, na zahtev vlasnika (avgust 2026); v1.4 — dodat glasovni unos za omnisearch (poglavlje 5.5), na zahtev vlasnika (avgust 2026) — dopunjuje M15 poglavlje 6.6, prvi kanal koji dobija ovu opciju; v1.3 ažurirana lista modula koje panel implicitno prikazuje (dodati M21, M22 — nalaz iz revizije Master dokumenta, avgust 2026, ova lista nije praćena kad su ta dva modula dodata); v1.2 dodato poglavlje 5.5 (univerzalna pretraga i AI razgovor — omnisearch), na zahtev vlasnika (avgust 2026), dopunjuje M15 poglavlje 6.5; v1.1 dodata stavka izlaznog kriterijuma za responsive prikaz (Master dokument poglavlje 5.1)
**Zavisi od:** M1, M15 (poglavlje 5.5, omnisearch; poglavlje 6.6, glasovni modalitet). Implicitno od API-ja svakog modula koji prikazuje (M2, M3, M5, M6, M7, M9, M10, M11, M12, M13, M14, M16, M18, M19, M20, M21, M22 do sad specificirani).

---

## 1. Svrha i obim modula

M17 je jedan Next.js aplikacija kroz koju **ceo tim** (Vlasnik, Direktor, HR, Sales Manager, Prodajni agent, Računovođa) radi svakodnevni posao — katalog, ugovori, rezervacije, finansije, CRM, korisnici. Isti princip kao M8: **nema sopstvenu bazu ni poslovnu logiku**, čisto poziva interne API-je drugih modula. Razlika u odnosu na M8 je publika (interni tim, ne gosti) i obim (prikazuje praktično sve module, ne samo katalog/rezervacije/nalog).

M17 nije "gotov" u jednom trenutku — raste postepeno, u istom tempu u kom se moduli specificiraju i grade (poglavlje 7 ovog dokumenta).

---

## 2. Arhitektura — kompozicija nad više modula, ne nova poslovna logika

Next.js server komponente pozivaju API-je više modula i sastavljaju jedan prikaz — npr. stranica detalja rezervacije prikazuje podatke iz M5 (status, stavke), M6 (ko je gost/nalogodavac), M10 (status fiskalnog dokumenta i plaćanja) i M11 (status garancije putovanja) na jednom ekranu. Ovo je **kompozicija na nivou prikaza**, ne nova poslovna logika — M17 ne piše direktno u bazu nijednog modula, ne zaobilazi njihova pravila, samo poziva njihove već postojeće API-je i slaže odgovore u jedan ekran (princip #2 iz poglavlja 3 Master dokumenta).

Ako kompozicija postane složena (npr. mnogo poziva po jednom ekranu), dozvoljeno je da M17 ima sopstveni tanak "backend for frontend" sloj (agregacione rute unutar iste Next.js aplikacije) — ali taj sloj i dalje **samo poziva zvanične API-je** drugih modula, nikad njihovu bazu direktno.

---

## 3. Prijava i pristup

Isključivo preko M1: `account_type = STAFF`, obavezna 2FA za sve interne uloge (već odlučeno u M1 specifikaciji). Navigacija i vidljivost akcija na svakom ekranu **potpuno prate M1 model prava** (uloga + pojedinačni izuzeci) — ako korisnik nema `M5/booking/VIEW`, taj deo menija se ne prikazuje, ne samo da je onemogućen. Ovo sprečava da interfejs "curi" informaciju o postojanju podataka kojima korisnik ne sme da pristupi.

### 3.1 Obeležavanje autora radnje (dopuna, avgust 2026, na zahtev vlasnika)

Prava pristupa (poglavlje 3) određuju **šta** korisnik vidi. Ovo poglavlje određuje da mora videti i **ko je to uradio** — čovek, AI agent, ili spoljni nalog.

Svaki ekran panela koji prikazuje autora radnje ili poruke poštuje pravilo iz `docs/analize/29-DIZAJN-SISTEM-UI.md`, poglavlje 6a ("Obeležavanje autora radnje"). Taj dokument je jedini izvor istine za tačan vizuelni oblik — ovde se namerno **ne prepisuje**, da se dva mesta ne raziđu.

Obaveza važi za svaki ekran, postojeći i budući: M6 (log komunikacije), M12 (marketinški sadržaj), M14 (nit tiketa), M19 (chat), M21 (predlozi članaka), M22 (email nit), M23 (revizije članaka), i **audit log** (`/audit-log`), koji `actor_type` prikazuje kao čitljiv tekst sa oznakom, ne kao sirovu enum vrednost.

Razlog nije estetski: M15 poglavlje 1 propisuje da je AI agent formalan M1 nalog sa sopstvenim pravima i sopstvenim tragom u `AuditLogEntry` (`actor_type = AI_AGENT`). Ako se ta razlika ne vidi u interfejsu, tim ne može da proceni koliko da veruje onome što čita — a od te procene zavisi da li će tekst pregledati pre slanja ili prihvatiti kakav jeste.

---

## 4. Struktura navigacije (raste sa fazama)

| Sekcija | Modul iza nje | Dostupno od |
| :---- | :---- | :---- |
| Korisnici i uloge | M1 | Faza 0 |
| Katalog proizvoda | M2 | Faza 1 |
| Dobavljači i ugovori | M3 | Faza 1 |
| Pretraga i rezervacije | M5 (+ M4 uživo) | Faza 1 |
| Kalendar rezervacija (dolasci/odlasci/u toku po datumu) | M5 | Faza 1 |
| Finansije (fakture, plaćanja) | M10 | Faza 2 |
| Compliance (garancija putovanja) | M11 | Faza 2 |
| Ugovori sa klijentima | M20 | Faza 2 |
| Gosti i nalogodavci (CRM) | M6 | Faza 3 |
| B2B partneri | M7 | Faza 4 |
| Izveštaji | M13 | Faza 5 |
| Podrška | M14 | Faza 5 |
| Marketing sadržaj | M12 | Faza 6 |
| Operativni nadzor | M18 | Faza 7 |
| Razgovori (tim/dobavljači) | M19 | Faza 7 |
| Centar za pomoć | M21 | Faza 7 |
| Email/Inbox | M22 | Faza 7 |
| Znanje (destinacije/proizvodi) | M23 | Faza 7 |
| MCP klijenti (agentski pristup) | M16 | Faza 6 |

Svaka sekcija se dodaje u M17 **kad odgovarajući modul dođe na red i dobije svoju Nivo 2 specifikaciju** — ovaj dokument se ne menja retroaktivno za svaki modul, sekcija se prosto doda u naviguju kad je modul spreman.

---

## 4a. Grupisanje sekcija u gornju traku i polja za pretragu/filtriranje po modulu (dopuna, 17.8.2026, na zahtev vlasnika)

**Raspored** (obrazac definisan u `29-DIZAJN-SISTEM-UI.md` poglavlje 5b/5c — gornja traka nosi grupe, levi panel njihov sadržaj): gornjih 17 sekcija iz tabele u poglavlju 4 grupisano je u 8 kategorija + samostalna Početna, da gornja traka nosi 9 ikona umesto 17:

| Grupa | Sekcije unutra |
| :---- | :---- |
| Prodaja | Pretraga i rezervacije, Kalendar rezervacija |
| Katalog i nabavka | Katalog proizvoda, Dobavljači i ugovori |
| Klijenti i partneri | Gosti i nalogodavci (CRM), B2B partneri |
| Finansije i pravno | Finansije, Compliance, Ugovori sa klijentima |
| Komunikacija i podrška | Podrška, Razgovori, Centar za pomoć, Email/Inbox |
| Sadržaj i znanje | Marketing sadržaj, Znanje |
| Analitika i nadzor | Izveštaji, Operativni nadzor |
| Administracija | Korisnici i uloge, Audit log — namerno na suprotnom kraju trake od ostalih (isti princip kao VS Code zupčanik za podešavanja, odvojen od radnih sekcija) |

Klik na grupu prikazuje njene sekcije u levom panelu; klik na jednu sekciju skuplja prikaz na samo tu sekciju (ostale iz grupe se sklanjaju), sa poljima za pretragu/filtriranje ispod naziva. Tačan vizuelni obrazac (skupljanje, povratak na spisak) definisan je u dizajn dokumentu, ne ovde.

**Polja po sekciji** — istraženo direktno iz postojećeg koda (avgust 2026), ne izmišljeno: mnoga polja već postoje na API-ju ali ekran ih ne koristi (**[povezati]**), neka ne postoje ni na API-ju (**[novo]**).

| Sekcija | Postojeća polja (API već podržava) | Dopuna |
| :---- | :---- | :---- |
| Korisnici i uloge | — | **[novo]** ime/email (tekst), uloga, status naloga |
| Audit log | `module`, `actor_id`, `from`/`to` — API ima, ekran ih ne šalje | **[povezati]** sva četiri u UI; **[novo]** `action` (tip radnje) |
| Katalog proizvoda | `type`, `destination_country`, `status`, `channel`, `lang` — API ima, ekran ih ne šalje | **[povezati]** sva u UI; **[novo]** tekstualna pretraga po nazivu |
| Dobavljači i ugovori | — | **[novo]** naziv/tip dobavljača (`Supplier.type`), status; za ugovore: `supplier_id`, status, datum isteka |
| Pretraga i rezervacije | Vođena pretraga proizvoda već definisana (poglavlje 3.0c/3.0d M5 spec). Lista rezervacija: `status`, `channel`, `client_account_id` — API ima, ekran ih ne šalje | **[povezati]** sva tri; **[novo]** opseg `stay_from`/`stay_to` |
| Kalendar rezervacija | `from`/`to` (mesec) — već povezano | bez izmene |
| Finansije | `booking_id`/`supplier_id`/`status` po pod-ekranu — API ima, ekran ih delimično ne šalje | **[povezati]** sve; **[novo]** opseg datuma za fiskalne dokumente |
| Compliance | `status`, `booking_id` — API ima, ekran šalje samo `status` | **[povezati]** `booking_id` |
| Ugovori sa klijentima | `booking_id`, `status` — API ima, ekran šalje samo `status` | **[povezati]** `booking_id` |
| Gosti i nalogodavci (CRM) | `email`/`tax_id` (nalozi) — povezano; `linked_client_account_id`/`client_account_id`/`booking_id`/`status` (gosti/komunikacija/ankete) — API ima, nema pod-ekrana koji ih koristi | **[novo]** filter po `account_type` (INDIVIDUAL/LEGAL_ENTITY); **[povezati]** ostalo kad pod-ekrani dođu na red |
| B2B partneri | — | **[novo]** naziv/status subagenta, nivo hijerarhije |
| Izveštaji | Bogat skup po tipu izveštaja (`from`/`to`/`destination_country`/`destination_city`/`supplier_id`/`provider_code`/`channel`/`product_type`/`group_by`) — sve već povezano | bez izmene, najpotpuniji ekran danas |
| Podrška | `status`/`category` — postoje samo kao **filter posle preuzimanja cele liste**, ne kao pravi API upit | **[novo]** prebaciti na stvaran `GET` query (efikasnije od učitavanja cele liste svaki put); **[novo]** opseg datuma |
| Marketing sadržaj | `type`/`status` — povezano; `channel`/`slug` — API ima, ekran ne šalje | **[povezati]** `channel` |
| Operativni nadzor | `module`/`type`/`severity` (signali) — povezano; `agent_id` (AI troškovi) — API ima, ne šalje se | **[povezati]** `agent_id` na AI-troškovi pod-ekranu |
| Razgovori | — | **[novo]** tip sagovornika (tim/dobavljač), nepročitano |
| Centar za pomoć | `related_module`/`is_critical_example`/`lang`/`status` — sve povezano | bez izmene |
| Email/Inbox | `mailbox_id`/`status`/`correspondent_type` — API podržava kao query, ekran filtrira **lokalno** posle preuzimanja cele liste | **[novo]** prebaciti na stvaran API upit (isti razlog kao Podrška) |
| Znanje | `subject_type`/`status` — filtriraju se **lokalno**, API nema query za njih | **[novo]** dodati kao prave API query parametre |

**Napomena:** stavke označene [novo] na API strani zahtevaju malu dopunu odgovarajućeg modula (dodavanje `@Query()` parametara postojećem `findAll`) — ne novu funkcionalnost, isti obrazac koji svaki od tih modula već koristi negde drugde. Redosled kojim se ovo radi je implementaciono pitanje (npr. uz sledeći prolaz kroz taj ekran), ne ovde.

---

## 5. Kontrolna tabla (dashboard) i objedinjena upozorenja

Nekoliko modula već proizvodi sopstvena upozorenja o rokovima:
- M3: `/contracts/expiring-releases` (alotman kom se bliži rok povrata)
- M11: istek garancije putovanja (YUTA)
- M1: neuspeli pokušaji prijave, zaključani nalozi

M17 početna stranica (dashboard) agregira ova upozorenja u jedan prikaz, filtriran prema ulozi (Računovođa vidi finansijske rokove, Vlasnik/Direktor vidi sve). Ovo je **čitanje iz postojećih endpoint-a više modula**, ne novi entitet ni nova baza. Ako broj ovakvih upozorenja poraste do te mere da agregacija na nivou prikaza postane nezgodna, razmotriti zaseban "Notification" modul kasnije (poglavlje 7).

### 5a. "Početna" kao radna polazna tačka, ne samo spisak upozorenja (dopuna, 18.8.2026, na zahtev vlasnika)

**Nulto stanje centralnog panela** — i "Početna" (kad je aktivan tab, poglavlje 4a) i svaki **nov, prazan tab** (dizajn dokument poglavlje 5a) pre nego što korisnik izabere konkretnu stavku iz levog navigatora — nije prazan ekran, nego radna polazna tačka:

1. **Istaknuto AI chat polje** kao glavni sadržaj (isti mehanizam kao dizajn dokument poglavlje 6c/6c.1/6c.2, ovde prikazan preko celog centralnog panela dok ništa drugo nije otvoreno, ne sveden na uzanu traku) — direktan ulaz za pitanje agentu, uključujući pretragu interneta uz odobrenje (M15 poglavlje 6.5.6b) preko istog `+` menija. Bez priloženog konteksta (poglavlje 6c.1 dizajn dokumenta) dok korisnik sam nešto ne priloži — nulto stanje ne pretpostavlja o čemu će pitanje biti.
2. **Agregirana upozorenja** (postojeći deo ovog poglavlja) — ostaju, ispod/pored chat polja, ne zamenjena njime.
3. **Nedavno otvoreno i sačuvani prikazi** (dizajn dokument poglavlje 4 i 5b) — brz nastavak rada bez ponovnog kucanja/klikanja kroz navigator.

Ovo ne uvodi novu logiku — svaki od tri elementa već postoji definisan negde drugde (dashboard iznad, dizajn dokument poglavlja 4/5b/6c) — ova dopuna samo određuje da se sastavljaju zajedno kao podrazumevan sadržaj praznog centralnog panela, umesto da svaki tab počinje potpuno prazan. Isto važi identično za M7 portal (poglavlje 2.0.6 tog spec-a, identičan interfejs) — subagentova "Početna"/prazan tab dobija isti sastav, filtriran na njegov obim podataka.

### 5.5 Univerzalna pretraga i AI razgovor — omnisearch (dopuna, avgust 2026, na zahtev vlasnika)

Trajno pretraživačko polje u zaglavlju panela (dostupno sa svakog ekrana), koristi zajednički mehanizam iz M15 poglavlja 6.5:

- **Prazan upit + Enter** — prikazuje sve stavke navigacije iz poglavlja 4 (tabela) koje su trenutnom korisniku vidljive po pravima iz poglavlja 3 — isti filter kao levi meni, samo u brzem, pretraživom obliku (korisno kad tim zna šta traži, ali ne želi da klika kroz podmenije).
- **Uneti tekst** — poziva `POST /ai-orchestration/omnisearch` (M15 poglavlje 9) sa `channel = INTERNAL_PANEL`. Obim je najširi od sva tri kanala: rezervacije i kalendar (M5), katalog (M2), dobavljači/ugovori (M3), finansije (M10), compliance (M11), CRM (M6), B2B partneri (M7) — sve u granicama prava trenutnog korisnika (npr. Prodajni agent i dalje vidi u rezultatima samo svoje klijente). **Implementirano (avgust 2026, prvi prolaz)** — endpoint sada stvarno postoji (M15), ali korak 1 (direktno poklapanje) trenutno pokriva samo M5 (rezervacije) i M2 (katalog); M3/M6/M7/M10/M11 direktno poklapanje i dalje čekaju naredni prolaz (isti obrazac postepenih faza kao ostatak M17) — jezički model (korak 2) i dalje može da odgovori opisno na širi upit, samo bez direktnog poklapanja entiteta iz tih modula.
- Primeri upita: "koje rezervacije čekaju fiskalni dokument" (poziva M10 poglavlje 6.2 alarm), "hotel Sunčani Breg leto 2027" (M2/M3), "Sunny Travel kreditni limit" (M7).
- Rezultat je uvek link/navigacija ka postojećem ekranu ili zapisu — omnisearch ovde ne uvodi nijedan novi ekran za prikaz podataka, samo brži put do već postojećih (M15 poglavlje 6.5.4, "nikad ne izvršava radnju sam").
- **Glasovni unos (dopuna, avgust 2026, na zahtev vlasnika)** — ikonica mikrofona pored polja; zaposleni drži/klikne i govori, transkribovan tekst automatski popunjava polje i šalje se identično kao da je otkucan (M15 poglavlje 6.6). Odgovor se, pored uobičajenog vizuelnog prikaza, i pročita naglas (uz mogućnost isključivanja zvuka). M17 je prvi kanal koji dobija ovu opciju — nema je (za sada) M7/M8/M9.

---

## 6. Dozvole

M17 nema sopstveni katalog dozvola u M1 — isto obrazloženje kao M8: svaki pozvani API sam sprovodi prava pristupa. M17 samo mora da **poštuje** ta prava pri iscrtavanju interfejsa (poglavlje 3).

---

## 7. Izlazni kriterijum

Pošto M17 raste sa fazama, izlazni kriterijum je vezan za svaku fazu, ne za jedan trenutak:

- [x] **Faza 0:** tim se prijavljuje, vidi svoju ulogu, Vlasnik/Direktor vidi audit log. *(prijava preko M1 `/iam/auth/login`+MFA, `GET /iam/auth/me` vraća ulogu/prava, `/audit-log` ekran čita M1 audit log — potvrđeno ručnim smoke testom kroz `apps/panel`)*
- [x] **Faza 1:** tim može ručno da unese proizvod (M2), ugovor (M3), i da pretraži/rezerviše (M5) — ovo je doslovan izlazni kriterijum Faze 1 iz poglavlja 8 Master dokumenta. *(M2 katalog lista+forma, M3 dobavljači+ugovori lista+forma, M5 pretraga→ponuda→podaci putnika/nalogodavca→potvrda→prikaz rezervacije, sve potvrđeno ručnim smoke testom uživo kroz API+panel; pri tome otkriven i ispravljen pre-postojeći M5 bag, vidi promenu verzije iznad)*
- [x] **Faza 2:** Računovođa može da pripremi i pošalje fiskalni dokument (M10), tim vidi status garancije putovanja (M11). *(avgust 2026 — potvrđeno uživo: `/rezervacije/[id]` → "Pripremi/prikaži fiskalni dokument" → `/finansije/fiskalni-dokumenti/[id]` → "Potvrdi i pošalji fakturu", stvaran nacrt kreiran i poslat protiv prave baze (SEF/ESIR mock gateway), status DRAFT→SUBMITTED sa `external_reference` popunjenim; `/compliance` prikazuje trenutnu garanciju, iskorišćenost i CIS registracije sa ručnim retry, potvrđeno uživo da FAILED registracija posle unosa važeće garancije uspešno pređe u REGISTERED. M20 (Ugovori sa klijentima), van eksplicitne rečenice ali deo istog reda tabele poglavlja 4 — lista/detalj/prihvatanje/poništavanje na `/ugovori-klijenti`, potvrđeno uživo nad stvarnim generisanim ugovorom.)*
- [x] **Faza 3:** tim može da pretraži/pregleda profile gostiju i nalogodavaca (M6), vidi njihovu istoriju putovanja i komunikacije na jednom mestu. *(avgust 2026 — potvrđeno uživo kroz pravu prijavu i pravu sesiju panela: `/crm` lista nalogodavaca sa pretragom po email/PIB i formom za novi zapis, `/crm/[id]` detalj — kontakt, status/ručna korekcija lojalnosti, povezani gosti, prava istorija putovanja iz M5, log komunikacije; `/crm/gosti` lista i `/crm/gosti/[id]` detalj profila gosta (dokument, nacionalnost, datum rođenja); `/crm/ankete` pregled anketa posle putovanja. Stvaran gost kreiran preko prave `POST /crm/guest-profiles` i potvrđeno da se odmah pojavljuje u listi i na sopstvenom detalju; `/rezervacije/[id]` sad nosi i M6 kompoziciju (link ka nalogodavcu), potvrđeno uživo nad stvarnom rezervacijom. Nema posebnog ekrana za spajanje duplikata — M6 spec nema takvu funkciju, nije bilo šta da se odloži.)*
- [x] **Faza 4:** tim vidi i upravlja B2B partnerskim (subagentskim) nalozima — kreditni limit, provizija/obimski status, odobrenje novog partnera — i odobrava/odbija rabate provizije. Izlazni kriterijum je izveden iz M7 sopstvenog izlaznog kriterijuma (`docs/moduli/M07-b2b-subagenti/12-SPECIFIKACIJA-M7-B2B-SUBAGENTI.md` poglavlje 12, stavke koje se tiču staff-facing prikaza — isti obrazac kao Faza 3 izvedena iz M6). *(avgust 2026 — potvrđeno uživo kroz pravu prijavu i sesiju panela protiv stvarne baze: `/b2b` lista subagenata sa M6 kompozicijom naziva; `/b2b/novi` registruje Tier 1 subagenta preko stvarnog toka M6 pretraga→M7 registracija; `/b2b/[id]` detalj sa kreditnim limitom/dugom, provizijom/obimskim statusom, pragovima obima, mrežom sub-subagenata (samo uvid — upravljanje je van obima, posao roditeljskog subagenta kroz sopstveni negrađeni portal, M7 spec §3/§6) i rabatima te subagenta; odobrenje subagenta (`PENDING_APPROVAL→ACTIVE`) i izmena kreditnog limita/statusa potvrđeni kroz prave Server Action forme; `/b2b/rabati` agregira rabate svih subagenata po statusu; odobrenje `DRAFT` rabata potvrđeno kroz pravu formu, uz potvrđeno sinhrono pokretanje pravog M10 `KNJIZNO_ODOBRENJE` nacrta u bazi (M7 spec §3.2); usput otkriven i ispravljen nedostatak dozvole M7/commission-rebate/VIEW+APPROVE za Računovođu u `apps/api/prisma/seed/seed.ts`, koji je spec §10 oduvek predviđao. Omnisearch (§2.0.3) i AI chat (§2.0.4) izostavljeni — M7 spec §12 ih eksplicitno vodi kao "čeka M15".)*
- [x] **Faza 5:** tim vidi upravljačke izveštaje (M13 — profitabilnost/prodaja/smeštaj/dinamički/marketing) i može da otvori/odgovori/reši tiket podrške (M14), uz zakonski rok reklamacije jasno istaknut. *(avgust 2026 — potvrđeno uživo protiv prave baze: `/izvestaji` pet tabova, svaki gejtovan sopstvenom `M13/report:*/VIEW` dozvolom, sa filterima perioda/dimenzija i `lastSyncedAt` prikazom; ručno pokretanje rekonsilijacije potvrđeno; `/podrska` lista+filter, `/podrska/novi` STAFF_ON_BEHALF unos, `/podrska/[id]` detalj sa ZZP rokom (M14 spec §3.1), nit poruka i formom za status/prioritet/`refundDecision` (§3.2) — nova STAFF poruka i status/refund izmena potvrđene kroz prave Server Action forme protiv stvarnog tiketa.)*
- [x] **Faza 6:** tim vidi kalendar marketinškog sadržaja (M12), ručno kreira/uređuje nacrt, i odobrava objavu — nikad AI agent. *(avgust 2026 — potvrđeno uživo protiv prave baze: `/marketing` kalendar sa filterom tip/status; `/marketing/nov` ručno kreiranje; `/marketing/[id]` detalj sa prevodima (§2.2) i dugmetom "odobri" (`M12/content/APPROVE_PUBLISH`, §3 korak 4) — odobrenje `PENDING_APPROVAL→PUBLISHED` potvrđeno kroz pravu formu, sa `approved_by` popunjenim; upozorenje za `contains_ai_generated_media=true` (§3c) prikazano; `/marketing/kanali` konfiguracija distribucionih kanala (§4).)*
- [x] **Faza 7:** tim vidi operativna upozorenja i AI troškove (M18), razgovara uživo sa timom i dobavljačima (M19), upravlja bazom znanja za korišćenje platforme (M21), radi u centralizovanom email inbox-u (M22), i upravlja bazom sadržaja o destinacijama/proizvodima sa javnim deljenim linkom (M23). *(avgust 2026 — kod napisan i pušovan za svih pet ekrana: `/nadzor` (signali/kanali/trendovi/ai-troskovi, `4e5c82c`), `/chat` (lista/uživo razgovor preko `/ws/chat`/dobavljači, `a0138ee`), `/pomoc` (članci/prevodi/predlozi/pitanja, `420f650`), `/email` (inbox/nit/sanduke+pristup, `da1a521`), `/znanje` (članci/izvori/revizije/deljeni link, `a90522d`); `nav.ts` svih pet stavki `implemented: true`; `tsc`/`next build` čist za sve. **Naknadno potvrđeno uživo protiv prave baze** (16.8.2026, posle privremenog prekida jer je Docker servis na razvojnoj mašini bio zaustavljen — ponovo pokrenut, pa nastavljena provera): pravi M17 login tok (registracija→MFA enroll preko pravog TOTP koda→unapređenje na VLASNIK ulogu→prijava lozinka+MFA verifikacija→BFF sesijski kolačić), sva 5 ekrana vraćaju HTTP 200 kroz pravu sesiju. `/nadzor` prikazao stvarnih 705 `HealthSignal` zapisa iz baze (rezidualni podaci ranijih e2e prolaza) i pravi nedeljni pregled; za svaki od preostala četiri ekrana kreiran je svež test zapis preko pravog API-ja i potvrđeno da se odmah pojavljuje na listi/detalju u panelu: M18 — nov `TrendSuggestion` odobren, `approvedBy` popunjen pravim korisnikom; M19 — nova GROUP `Conversation` sa porukom, vidljiva u listi i detalju; M21 — nov `HelpArticle` sa prevodom objavljen (`PUBLISHED`, `approvedBy` popunjen); M22 — nova `EmailThread` sa dolaznom porukom (simulirana direktno u bazi jer `receiveInboundMessage` namerno nema HTTP rutu u ovom prolazu — mock adapter, nema žive konekcije) i ručan STAFF odgovor poslat i prikazan; M23 — nov `Article` objavljen sa `shareToken`, javna ruta `/knowledge/public/:token` potvrđena bez autentikacije. Svi test zapisi i test nalog obrisani posle provere. Usput otkriveni (ne prećutno zaobiđeni) stvarni nedostaci u API-ju ovih pet modula, dokumentovani u kodu/commit porukama za budući potvrđeni prolaz — **rešeno 16.8.2026 (isti dan, potvrđen prolaz):** M21 `GET /help/articles` sada prima opcioni `status` parametar (DRAFT/PENDING_APPROVAL/ARCHIVED za EDIT nosioca, ograničeno na sopstvene segmente; bez parametra nepromenjeno ponašanje) i `GET /help/articles/:id` vraća `translations` (pun niz) pored rešenog `translation`; M22 `GET /email/threads` i `GET /email/threads/:id` uključuju `mailbox.address`/`mailbox.displayName` (isti scoping preko `MailboxAccess`, prošireni payload, bez potrebe za `M22/mailbox/VIEW`); M23 dobio `POST /knowledge/articles/:id/research` (opcioni `revisionId` popunjava postojeći `PENDING_REVIEW` placeholder, npr. `SCHEDULED_REFRESH`, umesto da pravi nov). Panel ekrani ažurirani da koriste nove parametre/polja umesto ranijih zaobilaženja (poziv po jeziku za M21 prevode, best-effort admin poziv za M22 naziv sandučeta, napomena bez forme za M23 istraživanje). Jedinični i e2e testovi dodati za sve tri izmene.)*
- [x] Svaka naredna faza dodaje svoju sekciju bez izmene already postojećih. *(nav.ts već sadrži kompletnu tabelu poglavlja 4 sa `implemented: false` za Faze 2-6 — dodavanje sekcije je izmena samo tog jednog polja + novi ekran, bez diranja Faze 0/1 koda)*
- [ ] Panel se instalira kao PWA i ostaje potpuno upotrebljiv na telefonu i tabletu (fluidan raspored, ne samo skalirana desktop verzija) — Master dokument poglavlje 5.1. **Odloženo za naredni prolaz** — svesna odluka da se PWA manifest/service worker doda kad bude više ekrana da opravda test na stvarnom uređaju.
- [x] Omnisearch polje je dostupno sa svakog ekrana; prazan upit + Enter prikazuje navigaciju filtriranu na ulogu; upit sa tekstom vraća rezultate/AI odgovor koji nikad prekoračuju prava trenutnog korisnika (poglavlje 5.5). *(avgust 2026 — M15 sada postoji u kodu; prazan upit + Enter i dalje čisto lokalna navigacija, upit sa tekstom poziva pravi `POST /ai-orchestration/omnisearch`; ako gate nije aktiviran, panel prikazuje smirenu poruku umesto greške i lokalna navigacija ostaje kao fallback)*
- [x] Na svakom ekranu koji prikazuje autora, poreklo radnje/poruke je vidljivo bez prelaska mišem i po jedinstvenom pravilu (`29-DIZAJN-SISTEM-UI.md` poglavlje 6a) — AI agent nosi bedž "AI", spoljni nalog nosi neutralnu oznaku sa firmom/ulogom, zaposleni ostaje bez dodatne oznake; audit log više ne ispisuje sirovu `actor_type` vrednost. Sprovedeno kroz **jednu deljenu komponentu**, ne kroz obrazac prepisan po ekranima (poglavlje 3.1). *(avgust 2026 — `apps/panel/src/components/ActorLabel.tsx`; povezano na osam mesta: `/audit-log` (više ne ispisuje sirov `actor_type`), `/chat` (M19, uz vidljivo upozorenje pre slanja AI nacrta), `/podrska/[id]` (M14), `/crm` log komunikacije (M6), `/email/[threadId]` (M22), `/marketing/[id]` (M12), `/znanje/[id]` (M23), `/pomoc/predlozi` (M21). Tri ranija lokalna "AI nacrt" bedža zamenjena zajedničkom komponentom. `tsc` i `next build` čisti.)*

  **Live-provera dovršena (17.8.2026)** — pravi M1 tok (registracija → MFA enroll stvarnim TOTP kodom → unapređenje na VLASNIK → prijava kroz panel BFF rute `/api/session/login` + `/api/session/mfa`), pa prava sesija panela protiv stvarne baze. Potvrđeno: M19 razgovor sa dobavljačem prikazuje "Vi" na ručno otkucanoj poruci i "Vi · nacrt: AI agent [AI]" na poruci poslatoj iz nacrta — oba vidljiva bez prelaska mišem; `/audit-log` prikazuje "AI agent [AI]" i "sistem" umesto sirovih enum vrednosti. Kontrast izmeren nad **stvarno iscrtanim** elementima u oba moda (`getComputedStyle`), ne samo računski: AI bedž 5.98:1 (svetli) / 8.86:1 (tamni), neutralan bedž 4.98:1 / 5.25:1 — sve iznad AA praga 4.5:1 iz poglavlja 2a. Test podaci obrisani posle provere (osim dva audit zapisa — `audit_log_entries` je append-only po M1 §3.8, DB trigger odbija DELETE, što je ispravno ponašanje).

  Dva nalaza iz same provere, oba ispravljena u istom prolazu: (1) AI bedž je u **svetlom modu padao na 3.96:1** sa `text-accent` na `bg-accent-soft` — prebačen na `text-accent-strong` (5.98:1) i dobio ivicu, jer se bez nje stapao sa sopstvenom porukom u chatu koja koristi istu pozadinsku boju; (2) `/audit-log` je svaki red prikazivao **"Invalid Date"** — ekran je čitao nepostojeće polje `createdAt` umesto `timestamp` (M1 `AuditLogEntry`), pre-postojeći bag iz Faze 0 koji je ova provera otkrila.
- [ ] Glasovni upit preko mikrofona daje identičan rezultat kao isti tekst otkucan ručno, sa naglas pročitanim odgovorom (poglavlje 5.5, M15 poglavlje 6.6). **Čeka M15** — isti razlog kao stavka iznad, nije implementirano u ovom prolazu.

---

## 8. Otvoreno za dalje

- Ako se agregacija upozorenja (poglavlje 5) pokaže nedovoljnom kad broj modula poraste (posebno posle M12/M13/M14), razmotriti zaseban modul za notifikacije/podsetnike sa sopstvenim pravilima prioriteta — trenutno namerno nije uveden dok ne postoji stvarna potreba.
- ~~Tačan izgled/UX (raspored menija, boje, itd.) — dizajnersko pitanje van obima ove specifikacije.~~ **Rešeno (avgust 2026)**: `docs/analize/29-DIZAJN-SISTEM-UI.md` — paleta boja, tipografija, komandna paleta (`Ctrl+K`) kao vizuelna realizacija omnisearch-a (poglavlje 5.5), raspored bočne trake. HEX vrednosti implementirane u "M17 školjka — Faza 1" (19.8.2026, vidi promenu verzije).
- **Nastavak "M17 školjke"** (dopuna, 19.8.2026, posle Faze 1; ažurirano 21.8.2026) — desni panel sažetak-reda/"Povezano" traka (AI razgovor deo desnog panela je urađen, v1.21; M5 selekcija urađena, v1.30 — ovo ostaje: izvedeni prikaz zapisa kad centar prikazuje listu za ostalih ~18 sekcija, poglavlje §5b), AI chat `+`/`@`/slash komande, pravi streaming, izvori-kao-pilule sa tipom/ikonicom, predložena sledeća pitanja (§6c.1/6c.2/6c.3 — v1.21 pokriva samo osnovno pitaj-odgovori polje), "nulto stanje" centralnog panela van Početne (poglavlje 5a ovog dokumenta), grupisani filteri po sekciji (poglavlje 4a tabela) — `/rezervacije/pretraga` dobio jednostavan, negrupisan filter blok u v1.22, ostatak sekcija i grupisanje-po-kategoriji (§6d) i dalje otvoreno, `TabLink` primenjen na preostalih ~8 sekcija (`crm/ankete`, `b2b/rabati`, `compliance`, `ugovori`, `dobavljaci`, `izvestaji`, `nadzor`, `email/sanducad` — poslednja možda nema pravi drill-down red, proveriti pre primene; ažurirano 21.8.2026, v1.36 je pokrila 10 od originalnih ~16), pravi backend `UserPreference` (M1 spec §3.9 — Faza 1 koristi privremen `localStorage` za širinu panela), boja brenda/logo u gornjoj traci (čeka vlasnikovu konkretnu odluku). *(Iskačuća obaveštenja §5e, Inbox ikonica i zvono za obaveštenja su urađeni — v1.31/v1.34/v1.35.)*
- ~~`TabLink` primenjen na preostalih ~8 sekcija~~ **Rešeno (21.8.2026, v1.38)** — svaki poznati drill-down red u panelu sad koristi `TabLink`; `ugovori`/`dobavljaci`/`izvestaji`/`nadzor`/`email/sanducad` nemaju [id] drill-down (provereno, ne propust).
- **"Zvono za obaveštenja" — proširenje na sve M19 razgovore** (novo, 21.8.2026, posle v1.35) — trenutna implementacija (`NotificationBell.tsx`) pokriva samo sistemski "Obaveštenja" razgovor (isti obim kao §5e toast kartice); puna spec formulacija (§5d) pominje i "novu poruku u M19 chat-u" uopšteno (bilo koji DIREKTAN/GROUP razgovor korisnika, ne samo sistemski) — trenutno svaki M19 razgovor ima sopstveno nepročitano stanje vidljivo na `/chat` listi, ali nema jedinstven zbirni prikaz u gornjoj traci. Odluka da li zvono treba da agregira i to, ili ostaje namerno uže (Inbox/zvono razlika bi se onda delom preklapala sa `/chat` listom), ostaje otvorena.
- **Ikonica naloga (avatar → meni: profil/lična podešavanja/odjava) i "Lična podešavanja" zupčanik** (§5d) nisu implementirani — danas gornja traka ima samo tekst imena + odvojeno dugme za odjavu (v1.6). Zupčanik za lična podešavanja (tema preko automatske, veličina fonta, zvuk AI odgovora, uključi/isključi obaveštenja, oblik forme Ponuda→Rezervacija) zahteva pravi `UserPreference` backend (već pomenut gore kao otvoreno) — nema smisla graditi UI za podešavanja koja se ne pamte.
- **M5 pretraga — pun §5b/§6d obim** (novo, 19.8.2026, posle uzanog prvog reza v1.22) — devet ikonica po tipu proizvoda u levom navigatoru (dizajn dok. §5b tabela), itinerar builder za individualne pakete, sačuvani prikazi (§5b), grupisani filteri po kategoriji sa potpragovima i sadržaji-tagovima (§6d), pinovani brzi filteri Refundabilno/Nerefundabilno (čeka strukturno polje na M5/API strani — `cancellationPolicySummary` je danas slobodan tekst, M5 spec §11) i Odmah potvrda/Upit (ovaj drugi je već implementiran, v1.22).
