.claude/settings.local.json
/Primeri cenovnika/

# Implementacija (avgust 2026 —)
node_modules/
dist/
.env
.env.local
apps/api/generated/
*.tsbuildinfo
coverage/
.nx/
apps/web/.next/
apps/panel/.next/
apps/api/uploads/
# Dnevnici lokalnog dev servera
*-dev.log

# Privremena ruta za ulazak jednim klikom u lokalnom pregledu (2.9.2026).
# Sadrzi lozinku i MFA tajnu test naloga i sama u sebi kaze da nikad ne sme
# da bude deo commit-a — zato stoji ovde, a ne u istoriji repozitorijuma.
apps/panel/src/app/api/session/dev-login/

# Podaci mape (Protomaps/PMTiles) — GIGABAJTI, nikad ne idu u git.
# Pravi se lokalno: npm run map:extract --workspace=apps/panel (vidi M5 spec §3.0h).
apps/panel/public/maps/
*.pmtiles
